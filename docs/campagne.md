# La Maison des possibles — changelog de la campagne

Journal daté et vérifiable des epics de la campagne narrative, sur le modèle de [garden.md](garden.md) : chaque entrée décrit ce qui a été implémenté et ce qui a été **réellement exécuté** pour le vérifier, pas une intention. Le processus qui produit ces entrées est décrit dans [orchestration.md](orchestration.md) ; la file d'epics à venir est dans [campagne-backlog.md](campagne-backlog.md). Ce fichier documente la campagne ; le fonctionnement du jardin libre existant reste dans `garden.md`/`garden-structure.md`, que la campagne réutilise sans le remplacer.

Travail effectué sur la branche `maison-des-possibles`, jamais fusionné vers `main` avant la fin d'une phase entière (voir « Portes de phase » dans `orchestration.md`).

## Vérification locale du 17 septembre 2026 — schéma cultivar/spécimen (epic C1.1)

Premier epic de la phase 1 (Laboratoire botanique). Pose uniquement le schéma de persistance des cultivars et spécimens décrits en §4 du design — aucun croisement, aucune règle d'héritage, aucune interface, aucun rendu : c'est le périmètre des epics C1.2/C1.3/C1.4/C1.7 qui suivent.

- Nouveau `public/game/cultivars.js` : `createCultivar(s, {name, parentIds, traits})` fige un cultivar (id stable `c<n>`, traits copiés une fois pour toutes, jamais recalculés depuis `parentIds` par la suite) et l'ajoute à `s.cultivars` ; `createSpecimen({cultivarId, x, z, stage})` construit la forme d'un spécimen. Aucun des deux n'est encore appelé par une commande de jeu ni par le rendu — ce sont des fonctions pures, prêtes à être branchées par C1.3.
- `public/garden-state-lifecycle.js` : une partie neuve démarre avec `cultivars: []` et `cultivarNextId: 1`.
- `public/garden-state-validate.js` : `cultivars`/`cultivarNextId` suivent exactement le patron déjà utilisé pour `quests` (champ optionnel, validé strictement s'il est présent, complété par une valeur par défaut sinon — voir `result.quests ??= …` juste au-dessus dans le même fichier). Une sauvegarde dont `cultivarNextId` ne dépasserait pas l'id le plus élevé réellement présent est rejetée, sur le même principe que la vérification déjà faite pour `nextId`/les entités.
- Nouveau `tests/campaign-cultivars.cjs` (7 tests), ajouté à la commande `test` de `package.json` : liste vide par défaut sur une partie neuve ; un cultivar créé garde exactement le même id et les mêmes traits après un aller-retour JSON réel de sauvegarde/rechargement (`JSON.parse(JSON.stringify(...))`, pas une simple comparaison d'objet en mémoire) ; deux cultivars créés à la suite reçoivent des ids distincts et incrémentaux ; une sauvegarde v3 à laquelle on retire `cultivars`/`cultivarNextId` migre vers la valeur par défaut sans qu'aucun autre champ ne change (comparaison JSON complète avant/après) ; sept formes de `cultivars` malformé sont rejetées explicitement ; une collision d'id (`cultivarNextId` trop bas) est rejetée, sa correction acceptée.

**Résultat réellement exécuté** : `npm test` → 132 tests de règles/géométrie passent (125 existants + 7 nouveaux, zéro régression), plus les vérifications HTML/site existantes. Sortie complète :

```
ℹ tests 132
ℹ suites 0
ℹ pass 132
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
```

Aucun test navigateur ni visuel requis pour cet epic : aucun fichier de rendu n'a été touché, `cultivars.js` n'est chargé que côté règles pour l'instant.

## Vérification locale du 17 septembre 2026 — grammaire des six attributs et compatibilité de croisement (epic C1.2)

Deuxième epic de la phase 1. Pose la grammaire de traits et la table de compatibilité que le pot (C1.3) devra utiliser pour tirer un cultivar — toujours aucun tirage aléatoire, aucune commande de jeu, aucune interface, aucun rendu : ce sont les epics C1.3/C1.4/C1.7 qui suivent.

- **Écart volontaire par rapport au backlog, justifié ici** : le mandat proposait d'étendre `public/game/data-species.js` (le catalogue réel de plantes d'intérieur du jardin libre — pilea, monstera...). Cette catalogue est sans rapport avec la flore fictive de Rivebrume nommée en §4 du design (Oreille-de-pluie, Clochette du soir...). Greffer la génétique de la campagne sur le catalogue du jardin libre aurait couplé deux modes de jeu que le design tient explicitement séparés (§15 : « Continuer mon jardin libre » / « Commencer l'histoire »). Nouveau `public/game/botany-genetics.js` à la place, avec son propre catalogue des huit espèces fondatrices déjà nommées et caractérisées par le design.
- Six axes exposés (design §4, premier prototype) : `port`, `feuilles` (forme + taille), `fleurs` (forme + groupement, ou `null`), `palette` (deux dominantes + accent), `humidite` (sec/frais/humide) et `fonction` (type + intensité, ou `null`). Chaque axe est **un seul locus** — conforme au design (« chaque locus discret vient de l'un des parents ») : `feuilles` par exemple est hérité comme un tout (forme et taille ensemble depuis le même parent), pas forme et taille indépendamment.
- Huit espèces fondatrices du tableau §4, avec leurs six traits explicites : Oreille-de-pluie (feuille en coupe, retient l'eau), Clochette du soir (fleur qui éclaire), Menthe de velours (parfum), Ronce à rubans (fibre — hors des six axes, `fonction: null`), Mousse de source (filtre), Fraise timide (fruit — hors des six axes, `fonction: null`), Fougère d'écho (absorbe le bruit), Aster des vents (aucune fonction remarquable).
- `crossCompatible(idA, idB)` : règle écologique généralisable (écart d'humidité préférée ≤ 1 sur l'échelle sec/frais/humide), pas une liste de paires ad hoc — une neuvième espèce fondatrice n'aura besoin que de son propre jeu de traits pour s'intégrer au graphe de compatibilité existant.
- `traitCombinationValid(traits)` applique la règle de compatibilité des caractères dépendants du design (§4, point 5 : « une fonction ne peut pas hériter d'un montage impossible ») : `retenir_eau` exige une feuille en coupe, `eclairer` exige une fleur présente, `filtrer` exige un sol humide, `absorber_bruit` exige une grande feuille ; `parfumer` n'a pas de contrainte structurelle.
- `enumerateReachableTraitSets(idA, idB)` énumère les 64 combinaisons possibles (2⁶, un tirage indépendant par locus) et marque chacune valide ou non — la pose de traits invalides (ex. fonction retenir_eau héritée avec une feuille ronde de l'autre parent) est donc explicitement détectée plutôt que silencieusement produite ; c'est cette liste filtrée que C1.3 tirera au sort, jamais l'ensemble brut des 64 combinaisons.

Nouveau `tests/campaign-genetics.cjs` (12 tests), ajouté à `package.json` : chaque espèce fondatrice satisfait sa propre contrainte de fonction ; les cinq fonctions sont testées individuellement (acceptée dans le bon contexte, rejetée dans le mauvais) ; la compatibilité est symétrique et réflexive ; deux espèces aux préférences d'humidité opposées (sec/humide) ne sont pas compatibles ; toute paire compatible a au moins une combinaison valide parmi ses 64 combinaisons réalisables ; un croisement connu pour produire un montage invalide (fonction d'un parent + feuille incompatible de l'autre) apparaît bien dans l'énumération et y est marqué invalide, pas silencieusement absent ; un identifiant d'espèce inconnu lève une erreur explicite plutôt que d'être traité comme compatible par défaut.

**Résultat réellement exécuté** : `npm test` → 144 tests de règles/géométrie passent (132 existants + 12 nouveaux, zéro régression), plus les vérifications HTML/site existantes.

```
ℹ tests 144
ℹ suites 0
ℹ pass 144
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
```

Aucun test navigateur ni visuel requis : aucun fichier de rendu touché.

## Vérification locale du 17 septembre 2026 — horloge quotidienne de campagne (epic C2.1)

Premier epic de la phase 2 (Jardin d'imitation). Pose l'horloge de jeu propre à la campagne (design §3, « Horloge, repos et alimentation ») — une journée 7h-23h distincte du cycle solaire purement visuel de 1200 secondes déjà utilisé par le jardin libre (`public/game/lighting.js`, non modifié) et du rattrapage hors ligne du jardin libre (`public/game/save.js`, non modifié). Le passage de nuit lui-même (transition, résolution du pot, retour à la maison) reste hors périmètre : c'est l'epic C2.2 qui suit.

- Nouveau `public/game/campaign-clock.js` : `CampaignClock` pure, sans dépendance DOM (sur le même principe que `lighting.js`, qui documente déjà cette contrainte dans son en-tête). `activeSeconds` (24 minutes réelles par défaut, nommé, réglable) fixe la vitesse de l'horloge ; `DAY_SECONDS` dérive de `DAY_START_HOUR`/`DAY_END_HOUR` (7h/23h).
- `tick(nowWallMs)` avance l'horloge de jeu à partir du temps réel écoulé depuis le tick précédent. Trois cas : pause active → 0 seconde créditée ; écart réel supérieur à `SUSPEND_GAP_MS` (5 s, un onglet masqué/fermé) → 0 seconde créditée, l'écart est **jeté**, jamais rattrapé au tick suivant ; sinon → avance proportionnelle au taux courant, plafonnée à 23h.
- `pause()`/`resume(nowWallMs)`/`isPaused()` : `resume` réarme seulement le point de référence, ne crédite jamais l'intervalle passé en pause.
- `isEveningReminderTime()` (vrai entre 22h30 et 23h) et `isNightfall()` (vrai à 23h et au-delà) : fonctions pures, l'affichage réel du rappel viendra avec l'interface (epics ultérieurs).
- Nouveau `tests/campaign-clock.cjs` (8 tests), ajouté à `package.json` : une journée complète s'écoule en ~24 minutes réelles à 1 seconde de tolérance près, ticks 1 s pendant `DEFAULT_ACTIVE_SECONDS` pas ; l'horloge ne dépasse jamais 23h même si on continue de la faire avancer ; un `activeSeconds` personnalisé change le taux proportionnellement ; une pause gèle l'horloge sur un grand écart réel puis une reprise ne crédite pas l'intervalle passé en pause ; un grand écart réel entre deux ticks (onglet masqué) est traité comme une suspension et jeté, avec reprise normale ensuite ; le seuil de suspension est testé pile à la limite (créditée) et juste au-dessus (jetée) ; le rappel de 22h30 ne se déclenche ni avant ni après 23h ; une horloge neuve démarre à 7h non figée, et son tout premier `tick()` ne fait qu'établir le point de référence (0 seconde créditée).

**Résultat réellement exécuté** : `npm test` → 152 tests de règles/géométrie passent (144 existants + 8 nouveaux, zéro régression), plus les vérifications HTML/site existantes.

```
ℹ tests 152
ℹ suites 0
ℹ pass 152
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
```

Aucun test navigateur ni visuel requis : aucun fichier de rendu touché ; `campaign-clock.js` n'est pas encore chargé par `index.html`, il n'est utilisé que par ses tests Node pour l'instant (le câblage à l'interface et au rendu viendra avec les epics qui introduisent les écrans concernés).

## Vérification locale du 17 septembre 2026 — le pot : croisement, héritage par locus, résolution au réveil (epic C1.3)

Troisième epic de la phase 1. Pose le tirage aléatoire du pot (design §4, « Héritage compréhensible, surprise conservée ») au-dessus de la grammaire posée par C1.2 : deux graines compatibles, croisées, produisent un cultivar fixé et sauvegardé — toujours aucune interface, aucun rendu (C1.4/C1.7 suivent).

- Nouveau `public/game/botany-pot.js` : `resolvePotDraw(idA, idB, random = Math.random)` tire chacun des six axes (port, feuilles, fleurs, palette, humidite, fonction, dans l'ordre fixe déjà utilisé par `botany-genetics.js`) indépendamment — un appel `random()` par axe, parent A si `< 0,5` sinon parent B — puis rejette et retire l'intégralité des six axes si le résultat échoue `traitCombinationValid` (design §4 point 5), jusqu'à un tirage valide (garde anti-boucle à 1000 tentatives, purement défensive : `campaign-genetics.cjs` prouve déjà qu'une paire compatible a toujours au moins une combinaison valide). `random` reste injectable pour permettre un test statistique reproductible à seed fixe. Ne modifie ni ne dépend de rien d'autre que `botany-genetics.js` (inchangé).
- Nouveau champ de sauvegarde `s.campaignPot = { capacity: 1, pending: [] }` (nommé ainsi et pas `s.pot` : ambigu avec le type d'entité `"pot"` du jardin libre, même logique de nommage explicite que `campaign-clock.js` face à `clock.js`). `capacity` fixée à 1 (« une paire par nuit au début », design §4) ; `pending` porte les paires posées mais pas encore résolues. `public/garden-state-lifecycle.js`/`public/garden-state-validate.js` suivent le patron déjà utilisé pour `cultivars`/`cultivarNextId` : champ optionnel, validé strictement s'il est présent, complété par défaut sinon — une sauvegarde existante sans ce champ continue de charger sans erreur.
- Nouveau segment de commande `public/garden-state-cmd-f.js` (`public/garden-state.js` étendu pour le charger en Node) : `sowPot` valide deux espèces fondatrices connues, leur compatibilité (`crossCompatible`) et la capacité disponible avant d'ajouter la paire à `pending`, avec un refus explicite sinon ; `sleep` résout chaque paire en attente via `resolvePotDraw` puis fige un cultivar (`GardenCultivars.createCultivar`, nom laissé vide — le nommer est le travail de C1.4) dans `s.cultivars`, vide `pending`, et réussit aussi sans rien créer si `pending` est vide (« il reste possible de ne rien croiser »). Le résultat est résolu et sauvegardé une seule fois, de façon synchrone, au moment de la commande : un rechargement ne peut donc pas retirer, `resolvePotDraw` n'étant jamais appelé depuis `validate`/`fresh`/le chargement.
- Nouveau `tests/campaign-pot.cjs` (9 tests), ajouté à la commande `test` de `package.json` : distribution statistique sur 10 000 tirages avec un générateur mulberry32 à seed fixe (20260917) sur la paire ronce-à-rubans×fraise timide (compatible, `fonction: null` des deux côtés donc jamais de rejet, ce qui isole la propriété 50/50 par locus du filtrage de validité déjà testé par `campaign-genetics.cjs`) — chacun des six axes reste dans 45–55 % ; persistance réelle après un aller-retour JSON sauvegarde/rechargement (le cultivar et `pending` restent identiques, aucun second tirage) ; une seule paire par nuit (un second `sowPot` avant tout `sleep` est refusé explicitement) ; `sleep` sans rien préparé réussit sans créer de cultivar ; une paire d'écart d'humidité supérieur à 1 est refusée par `sowPot` ; une espèce inconnue est refusée explicitement sans lever d'exception non capturée ; migration silencieuse d'une sauvegarde sans `campaignPot` vers la valeur par défaut, comparaison JSON complète avant/après ; sept formes malformées de `campaignPot` sont rejetées ; un test complémentaire vérifie que 500 tirages sur une paire à fonctions non nulles (oreille-de-pluie×menthe de velours, qui peut réellement déclencher le rejet-et-relance) restent toujours structurellement valides.

**Résultat réellement exécuté** (vérifié indépendamment par l'orchestrateur, pas seulement rapporté par l'agent délégué) : `npm test` → 161 tests de règles/géométrie passent (152 existants + 9 nouveaux, zéro régression), plus les vérifications HTML/site existantes.

```
ℹ tests 161
ℹ suites 0
ℹ pass 161
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
```

Aucun test navigateur ni visuel requis : aucun fichier de rendu touché (`render*.js`, `hud*.js`, `index.html`, `garden-models*.js`, `botany.js` inchangés) ; ni `botany-genetics.js` ni `cultivars.js` (déjà livrés par C1.2/C1.1) n'ont été modifiés, seulement réutilisés.

## Vérification locale du 17 septembre 2026 — carnet de botanique (epic C1.4)

Quatrième epic de la phase 1, dernier avant le rendu (C1.7). Ferme la boucle « croiser → nommer/décider → conserver la découverte » ouverte par C1.1-C1.3 : filiation et différences affichées, quatre décisions possibles sur un résultat, graine mère de sécurité, mémoire permanente du carnet.

- `public/game/cultivars.js` : inchangé dans sa forme, mais chaque entrée de `s.cultivars` peut désormais porter `disposition` (`"kept" | "stored" | "given" | "composted"`, absent tant qu'aucune décision n'a été prise). Rien ne supprime jamais une entrée : composter ne fait que poser `disposition: "composted"`, ce qui est la preuve littérale du critère « le carnet garde la découverte même si l'exemplaire est ensuite composté ».
- Nouveau segment de commande `public/garden-state-cmd-g.js` (`public/garden-state.js` étendu pour le charger, en Node comme dans le navigateur) :
  - `renameCultivar {id, name}` : `name` non vide après `trim()`, borné à 40 caractères ; reste autorisé après qu'une disposition a été posée (renommer un cultivar déjà gardé reste légitime).
  - `keepCultivar` / `storeCultivar` / `giveCultivar` / `compostCultivar {id}` : posent respectivement `disposition: "kept"/"stored"/"given"/"composted"` — refusées explicitement (jamais un remplacement silencieux) si une disposition existe déjà sur ce cultivar, quelle que soit la commande retentée.
  - `retrieveSeedBoxSeed {}` : échoue explicitement tant qu'aucune graine mère n'a été enregistrée ; sinon incrémente `campaignSeedBox.retrievals` sans jamais rien consommer — répétable à volonté, pour du dépannage seulement, comme demandé par le design.
- Nouveau champ de sauvegarde `s.campaignSeedBox = { seeded: false, cultivarId: null, retrievals: 0 }`. `seeded`/`cultivarId` ne sont posés qu'une seule fois dans toute la partie, à la toute première réussite d'un `keepCultivar` — un `keepCultivar` ultérieur sur un autre cultivar n'y touche plus. Volontairement **non branché** sur `economy.js`/l'inventaire (même posture que `cultivars.js` à sa propre création, C1.1 : « pas encore câblé ») : la graine mère ne peut donc pas être revendue, faute d'exister comme objet d'inventaire. `garden-state-lifecycle.js`/`garden-state-validate.js` suivent exactement le patron déjà utilisé pour `campaignPot` (champ optionnel, valeur par défaut sur partie neuve, validation stricte sinon) ; `garden-state-validate.js` valide aussi la nouvelle valeur `cultivar.disposition` dans le bloc de validation des cultivars déjà existant.
- **Complète le chargement navigateur resté incomplet depuis C1.1-C1.3.** `public/index.html` ne chargeait jusqu'ici aucun des fichiers `botany-genetics.js`/`botany-pot.js`/`cultivars.js`/`garden-state-cmd-f.js` : ces epics n'avaient été prouvés qu'en Node (leurs propres entrées ci-dessus le disent explicitement : « aucun fichier de rendu touché »). Sans cette chaîne chargée, les commandes de cet epic n'auraient existé nulle part dans la page réelle. `index.html` charge désormais ces quatre fichiers puis `garden-state-cmd-g.js`, dans l'ordre requis par leurs dépendances (`botany-genetics.js` → `botany-pot.js` → `cultivars.js` → `garden-state-cmd-f.js`/`-g.js`).
- Interface (`public/game/hud-panel.js`, branche `notebook`, ouverte par `J`) : avant la liste des espèces déjà connue, une entrée par cultivar dont `disposition` est encore absente, avec la filiation (« Issu de *Parent A* × *Parent B* ») et les axes qui diffèrent de chaque parent (comparaison axe par axe contre `GardenGenetics.founders`, sur les six axes de C1.2 : port, feuillage, floraison, palette, humidité, fonction).
  - **Écart volontaire n°1, justifié ici** : une row générique ne porte que deux boutons (`action` + `alternate`, voir `hud-panel-rows.js`). Le mandat proposait de répartir les quatre décisions sur deux rows ; avec le renommage en plus, cinq actions ne tiennent pas dans quatre emplacements de bouton. Chaque cultivar en attente occupe donc **trois** rows : garder/réserver, donner/composter, renommer seul. Les identifiants d'action Canvas (`keep-cultivar`, `store-cultivar`, `give-cultivar`, `compost-cultivar`, `rename-cultivar`) portent un suffixe `-cultivar` plutôt que de réutiliser les noms de commande bruts, parce que `"store"` est déjà une action Canvas existante du jardin libre (`public/garden-dispatch.js`, stockage d'un objet construit, indexée sur `A.build.id`) : la réutiliser aurait fait échouer silencieusement le clic (l'action existante ne fait rien faute de `A.build` actif) au lieu d'appeler la commande de campagne. `public/garden-dispatch.js` ajoute les cinq cas correspondants, chacun appelant `GardenState.command()` via `A.execute()` exactement comme `"buy"`/`"trade"` déjà en place ; `hud-panel-rows.js` ajoute les cinq libellés français (Garder/Réserver/Donner/Composter/Renommer).
  - **Écart volontaire n°2, justifié ici** : aucun widget de saisie de texte Canvas n'existe dans ce code (aucun `prompt(`, aucun input Canvas), et en fabriquer un nouveau aurait posé les problèmes déjà documentés dans le mandat (conflit clavier avec ZQSD/WASD, clavier virtuel mobile, contrôles sémantiques attendus par le design §14). Le renommage réutilise donc le même principe déjà en place pour le sélecteur de fichier système (voir `docs/garden-structure.md`) : un `<input type="text" id="cultivar-rename-input">` HTML ordinaire, caché par défaut, cousu dans `public/index.html` et stylé en overlay centré par `public/garden.css`. L'action `rename-cultivar` l'affiche et le préremplit ; `public/garden-boot.js` appelle `renameCultivar` sur Entrée ou perte de focus puis le recache (Échap annule sans renommer). Le raccourci clavier global ignore déjà tout événement dont `e.target.tagName === "INPUT"` (voir `garden-boot.js`, inchangé sur ce point), donc ZQSD/WASD et les raccourcis à une lettre ne fuient jamais pendant la saisie.

Nouveau `tests/campaign-notebook.cjs` (16 tests), ajouté à la commande `test` de `package.json` : renommage persistant après un aller-retour JSON réel ; renommage refusé sur un id inconnu et sur un nom vide (sans muter le cultivar) ; renommage toujours accepté après une disposition posée ; chacune des quatre commandes de disposition posée une fois puis refusée explicitement par n'importe laquelle des trois autres retentée sur le même cultivar (quatre variantes) ; disposition refusée sur un id inconnu ; le tout premier `keepCultivar` de la partie alimente `campaignSeedBox`, un `keepCultivar` ultérieur sur un autre cultivar ne change plus `cultivarId` ; `retrieveSeedBoxSeed` échoue avant tout `keep`, réussit après, et n'est jamais consommée sur deux appels consécutifs ; composter un cultivar laisse `id`/`parentIds`/`traits` identiques après un aller-retour JSON réel (preuve littérale de la persistance du carnet) ; migration silencieuse d'une sauvegarde sans `campaignSeedBox` ni `disposition`, comparaison JSON complète avant/après ; sept formes malformées de `campaignSeedBox` rejetées ; cinq valeurs invalides de `disposition` rejetées par `validate` ; une disposition valide traverse `validate` sans erreur.

**Résultat réellement exécuté** (vérifié indépendamment par l'orchestrateur, pas seulement rapporté par l'agent délégué) : `npm ci` puis `npm test` → 177 tests de règles/géométrie passent (161 existants + 16 nouveaux, zéro régression), plus les vérifications HTML/site existantes. Sortie complète :

```
ℹ tests 177
ℹ suites 0
ℹ pass 177
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
```

**`npm run test:browser`, réellement exécuté, avec une limitation d'environnement à documenter honnêtement.** L'interface a changé : ce test était donc obligatoire, pas seulement `test:visual`. Deux exécutions distinctes, réellement lancées (serveur local `python3 -m http.server 4174 --directory public`, comme documenté dans `garden.md`) :

1. **Nouveau `tests/campaign-notebook-browser.cjs`** (ajouté à `test:browser`) : construit une vraie sauvegarde via `GardenState`/`Cultivars.createCultivar` (un cultivar en attente + un déjà composté), la pose dans `localStorage` avant chargement de la page, ouvre le carnet avec `J`, vérifie les trois rows du cultivar en attente (filiation, actions, libellés), vérifie qu'un cultivar déjà composté n'affiche plus d'action de disposition, renomme via le vrai `<input>` HTML (Enter → `renameCultivar` → `input.hidden` revient à `true`), garde le cultivar (`keep-cultivar` → `GardenState.command()` → `campaignSeedBox` alimenté), vérifie qu'une seconde disposition sur le même cultivar est refusée par la vraie commande, puis récupère la graine mère deux fois de suite sans qu'elle ne soit jamais consommée. **PASS, zéro erreur console.**
2. `tests/garden-material-audit.cjs` (aucune géométrie/matériau touché par cet epic, mais exécuté pour de vrai comme l'exige `docs/orchestration.md`) : **PASS** (`{"materialsInspected":72,"transparentAllowed":1,"timesSampled":5}`, inchangé par rapport à la vérification précédente).
3. La commande complète `npm run test:browser` (16 scripts avec le nouveau) a aussi été relancée telle quelle, jusqu'au bout de son budget de temps. Dans cet environnement d'exécution précis, `playwright` (verrouillé à la version 1.63.0 par `package-lock.json`, qui attend le binaire Chromium/`chrome-headless-shell` de révision 1243) ne trouve dans le cache local que la révision 1194, et le téléchargement de la bonne révision est bloqué par la politique réseau de cet environnement (`cdn.playwright.dev` non autorisé par le proxy `HTTPS_PROXY`). Un contournement local (liens symboliques faisant passer la révision 1194 pour la 1243, non versionné, propre à cette machine, sous `/opt/pw-browsers/`) a permis au navigateur de se lancer, ce qui n'était pas le cas au tout début (`Executable doesn't exist`). Une fois le navigateur lancé, le résultat est mitigé et non systématique — ce qui pointe vers un problème de timing/version de ce binaire de secours, pas vers une régression de code :
   - **Passent normalement** (préexistants, non touchés par cet epic) : `garden-autoplanter-browser.cjs`, `garden-collector-t2-browser.cjs`, `garden-greenhouse-browser.cjs`, `garden-seed-dispenser-browser.cjs`, `garden-composter.cjs`, `garden-flow.cjs`, `garden-shadows.cjs`.
   - **Expirent sur un `page.waitForFunction`** lié à un déplacement chronométré ou une action maintenue (miner, marcher, rejoindre un PNJ) : `garden-play.cjs`, `garden-play-touch.cjs`, `garden-interface.cjs`, `garden-houses.cjs`, `garden-village-wave1.cjs`, `garden-size.cjs`, `garden-living-browser.cjs`.
   - **Vérifié indépendamment que ceci préexiste à cet epic**, pas une conséquence des fichiers ajoutés à `index.html` : `git stash` puis relance de `garden-play.cjs` seul sur le code d'avant cette session produit exactement le même dépassement de délai, à la même étape (« use lea »).

   Cette limitation est donc celle de la révision de Chromium disponible hors ligne dans cet environnement d'exécution précis, pas une régression introduite par C1.4 — mais elle est documentée ici sans la maquiller, conformément à `docs/orchestration.md` (« vérifié veut dire exécuté, pas plausible »). Aucun fichier concerné par les scripts préexistants en échec n'a été touché par cet epic ; les deux vérifications directement liées à C1.4 (`campaign-notebook-browser.cjs` et `garden-material-audit.cjs`) ont, elles, réellement tourné jusqu'au bout et sont vertes.

**Contre-vérification indépendante par l'orchestrateur** (le rapport d'un agent délégué n'est jamais la preuve, `docs/orchestration.md`) : `npm ci` puis `npm test` relancés séparément → 177/177 confirmé. `tests/garden-material-audit.cjs` et `tests/campaign-notebook-browser.cjs` relancés séparément (serveur `python3 -m http.server 4174` démarré par l'orchestrateur) → PASS identique aux deux, zéro erreur console. `tests/garden-autoplanter-browser.cjs`, `-collector-t2-browser.cjs`, `-greenhouse-browser.cjs`, `-seed-dispenser-browser.cjs`, `garden-composter.cjs`, `garden-flow.cjs` et `garden-shadows.cjs` relancés un par un → PASS, comme rapporté. `tests/garden-play.cjs` relancé seul → dépasse effectivement son délai (90 s). Pour trancher la question « régression ou limitation d'environnement », l'orchestrateur a lui-même exécuté `git stash -u` (retirant l'intégralité du diff de cet epic), relancé le serveur local sur ce code d'avant-epic (commit `101ccf6`), puis relancé `garden-play.cjs` seul : **même dépassement de délai, à l'identique**, confirmant indépendamment qu'il s'agit d'une limitation préexistante de l'environnement (révision Chromium), pas d'une régression introduite par ce diff. `git stash pop` a ensuite restauré les modifications de l'epic sans perte.

**`npm run test:visual` non exécuté, à dessein** : aucune géométrie ni aucun matériau n'a été modifié par cet epic (aucun fichier `render*.js`, `garden-models*.js`, `botany.js` touché) ; seul `public/garden.css` a changé, pour l'overlay de saisie HTML du renommage, sans effet sur le rendu 3D.

## Vérification locale du 17 septembre 2026 — multiplication fidèle (epic C1.6)

Déclenchement automatisé (routine cloud horaire, voir `execution-continue.md`). Anti-hallucination fait avant tout le reste : dernier epic « fait » du backlog était C1.4, commit `c2f9f99` — `git show --stat c2f9f99` confirme le commit réel et son contenu correspond à l'entrée ci-dessus ; `npm ci && npm test` relancés indépendamment → 177/177, identique au rapport existant. Aucune anomalie, `docs/campagne-anomalies.md` non créé.

Choix de l'epic : C1.5 (suivant dans l'ordre du backlog) a été sauté volontairement — sa dépendance réelle et non déclarée dans le champ « Dépend de » est C4.3 (« Iris montre comment épingler un caractère déjà vu », design l.383), un chapitre de la phase 4, non détaillée. L'implémenter maintenant aurait exigé d'inventer un déclencheur narratif hors ordre (`orchestration.md` : « ne jamais sauter une dépendance pour avancer plus vite ... cela vaut aussi pour l'ordre d'implémentation »). C1.6, elle, ne dépend que de C1.1 (fait) et n'a aucune dépendance narrative cachée : c'est le prochain epic réellement disponible.

Cinquième epic de la phase 1 à être livré (C1.6 dans la numérotation du backlog). Ferme l'écart entre `Cultivars.createSpecimen` (posé mais jamais appelé depuis C1.1) et une vraie commande de jeu — toujours aucun rendu (C1.7 suit), toujours aucune interface.

- `public/game/cultivars.js` : `createSpecimen(s, {cultivarId, x, z, stage})` prend désormais `s`, assigne un id stable (`sp<n>`, même patron que `createCultivar`) et pousse le spécimen dans `s.specimens` — jusqu'ici cette fonction n'était qu'une forme pure, jamais appelée par aucune commande ni par le rendu (voir son entrée C1.1). Un spécimen ne porte **jamais** sa propre copie des traits, seulement `cultivarId` : nouvelle fonction `specimenTraits(s, specimen)`, seul point de lecture des traits d'un spécimen, toujours via son cultivar. C'est ce qui rend « aucun tirage relancé » vrai par construction et pas par une comparaison qui pourrait un jour diverger.
- Nouveau champ de sauvegarde `s.specimens = []` / `s.specimenNextId = 1` (`public/garden-state-lifecycle.js`, `public/garden-state-validate.js`), suivant exactement le patron déjà utilisé pour `cultivars`/`cultivarNextId` : optionnel, validé strictement si présent (id au format `sp\d+`, `cultivarId` doit référencer un cultivar réellement présent dans `s.cultivars`, `x`/`z` finis dans `[-64, 64]` comme les autres coordonnées du jeu, `stage` un entier ≥ 0), complété par défaut sinon, avec le même contrôle de collision d'id que `cultivarNextId`.
- Nouveau segment de commande `public/garden-state-cmd-h.js` (`public/garden-state.js` étendu pour le charger, en Node comme dans le navigateur ; `public/index.html` complété à la suite de `garden-state-cmd-g.js`, même position que le reste de la chaîne de commandes) :
  - `plantSpecimen {cultivarId, x, z}` : installe le premier spécimen d'un cultivar existant dans le registre de la campagne, refuse explicitement un `cultivarId` inconnu ou un emplacement non fini.
  - `multiplySpecimen {specimenId, x, z}` : copie uniquement le `cultivarId` du spécimen source sur un nouveau spécimen — aucun appel à `botany-genetics.js`/`botany-pot.js`, donc aucun tirage possible. Refuse explicitement un `specimenId` inconnu ou un emplacement non fini.
  - Noms épelés en entier (`plantSpecimen`, `multiplySpecimen`) plutôt que `plant`/`multiply` : ces deux chaînes sont déjà des commandes physiques existantes du jardin libre (établi, voir `garden-state-cmd-b.js` et la liste `physical` de `garden-state.js`) ; la comparaison de commande étant une simple égalité de chaîne, réutiliser l'une d'elles aurait fait exécuter silencieusement la mauvaise branche.

Nouveau `tests/campaign-multiply.cjs` (10 tests), ajouté à la commande `test` de `package.json` : `plantSpecimen` crée un spécimen aux ids séquentiels, persistant après un aller-retour JSON réel ; refusé explicitement sur un `cultivarId` inconnu et sur un emplacement non fini (NaN/Infinity/chaîne/null), sans créer de spécimen dans les deux cas ; `multiplySpecimen` produit un nouveau spécimen partageant le `cultivarId` de la source, dont les traits (via `specimenTraits`) sont rigoureusement identiques à ceux de la source et du cultivar d'origine — le critère de sortie exact de l'epic ; cinq multiplications successives depuis le même spécimen source produisent six spécimens aux ids tous distincts et tous fidèles au cultivar ; refus explicite sur un `specimenId` inconnu et sur un emplacement non fini, sans créer de spécimen ; migration silencieuse d'une sauvegarde sans `specimens`/`specimenNextId`, comparaison JSON complète avant/après ; dix formes malformées de `specimens` rejetées (mauvais format d'id, `cultivarId` inconnu, coordonnées non finies, stage négatif ou non entier, champ manquant, doublon d'id) ; collision d'id sur `specimenNextId` rejetée, correction acceptée. Un test préexistant de `tests/campaign-cultivars.cjs` (C1.1, « createSpecimen ... not yet wired into entities ») a été mis à jour pour refléter le nouveau câblage — c'était précisément l'écart que cet epic ferme, documenté dans son propre titre.

**Résultat réellement exécuté** : `npm ci` puis `npm test` → 187 tests de règles/géométrie passent (177 existants + 10 nouveaux dans `campaign-multiply.cjs`, zéro régression — le seul test qui a changé de forme, `createSpecimen` dans `campaign-cultivars.cjs`, teste maintenant le nouveau câblage plutôt que l'ancienne fonction pure). Sortie complète :

```
# tests 187
# suites 0
# pass 187
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

**Test navigateur ciblé, réellement exécuté** (nécessaire car `public/index.html` a changé — un nouveau `<script>` ajouté) : serveur local `python3 -m http.server 4174 --directory public`, page chargée avec Playwright (`chromium.launch({executablePath: "/opt/pw-browsers/chromium"})`, la révision 1194 disponible dans cet environnement), zéro erreur console/page, `window.GardenCultivars.createSpecimen` bien exposé après chargement. `tests/garden-material-audit.cjs` (le gate complet de `npm run test:browser`) n'a **pas** pu être relancé dans cet environnement précis : `browserType.launch` échoue faute du binaire `chrome-headless-shell` de révision 1243 attendu par `playwright@1.63.0` (`package-lock.json`), non téléchargeable ici (réseau restreint), limitation d'environnement déjà documentée à l'identique dans l'entrée C1.4 ci-dessus — pas une régression de cet epic. Ce test complet n'était de toute façon pas requis par `orchestration.md`/`execution-continue.md` pour cet epic : aucun fichier de rendu (`render*.js`, `garden-models*.js`, `botany.js`) ni aucun panneau d'interface (`hud-panel.js`) n'a été touché — seule une commande de règles et le chargement d'un script supplémentaire ont changé, d'où la vérification ciblée ci-dessus plutôt que la suite visuelle complète. `npm run test:visual` non exécuté pour la même raison.

## Vérification locale du 17 septembre 2026 — représentation modulaire des hybrides, rendu (epic C1.7)

Déclenchement automatisé (routine cloud horaire). Anti-hallucination fait avant tout le reste : dernier epic « fait » du backlog était C1.6, commit `10f205a` — `git show --stat 10f205a` confirme le commit réel et son contenu correspond à l'entrée ci-dessus ; `npm ci && npm test` relancés indépendamment → 187/187, identique au rapport existant. Aucune anomalie, `docs/campagne-anomalies.md` non créé.

Premier epic de rendu de la campagne (C1.7). Ferme l'écart entre la génétique pure (C1.1-C1.6, jamais affichée) et une silhouette réelle : un squelette par valeur de `port`, une bibliothèque d'organes feuilles/fleurs compatible, une graine visuelle déterministe dérivée de l'id du cultivar, un partage strict de géométrie/matériau. Aucun rendu de spécimen dans le monde jardin lui-même (aucune commande « planter un cultivar dans le jardin » n'existe encore) : cet epic pose le module de rendu et le prouve par construction, sur le modèle déjà suivi par C1.1 (« posé mais jamais appelé ») avant son câblage ultérieur.

- Nouveau `public/game/botany-hybrids.js` (pas d'extension de `botany.js`/`garden-models*.js` : catalogue de la flore fictive de Rivebrume, séparé du rendu du jardin libre pour la même raison déjà documentée dans l'en-tête de `botany-genetics.js`). Sans dépendance canvas/texture — couleurs unies seulement — donc chargeable tel quel dans un bac à sable Node (`require` direct contre `public/vendor/three.min.js`, sans `window`/`document`), à la différence de `garden-models-leaf.js`.
  - **Squelette** : une courbe `CatmullRomCurve3` par valeur de `port` (les cinq valeurs réellement utilisées par les huit espèces fondatrices : rosette, tige-dressée, touffe, grimpant, retombant), avec un nombre fixe de points d'attache répartis dessus (`curve.getPoint(t)`), partagée entre tous les cultivars du même port (sur-ensemble de l'exigence « même cultivar → même géométrie »).
  - **Organes** : une géométrie par forme de feuille (`coupe` = demi-sphère creuse pour Oreille-de-pluie/retenir_eau, `fine` = cône, `ronde` = sphère, `palmée` = petit amas de cônes en éventail) et par forme de fleur (`cloche` = cône ouvert inversé, `étoile` = cône fin, `épi` = pile de sphères) — uniquement des primitives simples assemblées, jamais de maillage sculpté, conformément à `direction-artistique.md`. Les fleurs n'apparaissent qu'aux points d'attache les plus hauts (un seul si `groupement: "simple"`, deux points avec triplet si `"groupee"`).
  - **Graine visuelle déterministe** : hash FNV-1a de l'id du cultivar → PRNG mulberry32 (même famille que `tests/campagne-pot.cjs`) → fixe l'azimut/inclinaison de chaque organe. Dépend uniquement de la chaîne `id`, jamais de `Math.random`/`Date.now` : deux appels avec le même id produisent un agencement d'organes strictement identique (position ET rotation), deux id différents avec les mêmes traits divergent — les deux propriétés sont prouvées par test, pas supposées.
  - **Palette** : table `PALETTE` (label du design → hex), convention fixée par cet epic et documentée dans le fichier : `dominante1` → feuillage, `dominante2` → tige, `accent` → fleur/organe de fonction. Chaque valeur est soit un hex déjà documenté dans `direction-artistique.md`, soit une teinte dérivée justifiée en commentaire ligne par ligne : notamment **rouge** (`0xc9776a`, distinct du rouge d'erreur `0xb5654f`/`0xb65e48` réservé aux états invalides — seul autre rouge du jeu, justifié par le trait de palette du design pour Fraise timide) et **violet** (`0x8064a8`, dérivé de la lavande `0x9a83b5` déjà réservée à l'Aster des vents par le guide sans hex fixé, rendu distinct du mauve). Un label non mappé lève une erreur explicite (`paletteColor`), jamais une couleur par défaut silencieuse.
  - **Matériaux** : jamais de `metalness` posé (défaut Three.js = 0) ; `roughness` choisi dans la plage déjà observée par le guide (0,15–0,48). Émissif réservé au signal (fonction `eclairer`, Clochette du soir), intensité 0,35 — valeur déjà documentée par le guide, pas inventée pour cet epic.
  - **Partage** : deux appels à `buildSpecimenGroup` pour le même cultivar retournent deux `Group` distincts (positionnables indépendamment) dont les maillages d'organes référencent les **mêmes objets** géométrie/matériau (cache par forme/couleur), jamais recalculés — prouvé par comparaison d'identité (`===`), pas seulement d'égalité de valeur.

- Nouveau `tests/campaign-hybrids-render.cjs` (13 tests), ajouté à la commande `test` de `package.json` : chaque label de palette réellement utilisé par les huit fondatrices se résout ; un label inconnu est refusé explicitement ; les cinq valeurs de port se construisent sans erreur ; un port inconnu est refusé explicitement ; déterminisme strict (même id ⇒ organes byte-identiques deux fois) ; divergence par id (mêmes traits, id différents ⇒ agencement différent) ; chaque organe est exactement sur son point d'attache déclaré (distance < 1e-9, sur le modèle de `tests/garden-model-attachments.cjs`) ; absence de fleur ⇒ zéro organe fleur, présence ⇒ au moins un ; deux spécimens du même cultivar partagent littéralement (`===`) géométrie et matériau d'un organe feuille tout en restant des `Group` distincts ; aucun matériau construit n'a de `metalness`, tous ont un `roughness` dans la plage documentée ; l'émissif n'apparaît que pour la fonction `eclairer` (Clochette du soir), à l'intensité 0,35 documentée, et jamais ailleurs (comparaison sur la couleur émissive, `emissiveIntensity` valant par défaut 1 chez Three.js même sans émission réelle — écueil rencontré puis corrigé pendant l'implémentation, gardé en commentaire dans le test) ; aucun sommet non fini sur les huit fondatrices ; un vrai croisement (pas seulement une fondatrice), tiré depuis `GardenGenetics.enumerateReachableTraitSets`, se construit et reste déterministe.

- **`public/index.html`** : nouveau `<script src="/game/botany-hybrids.js" defer>`, chargé juste après `botany-genetics.js` (aucune dépendance d'ordre stricte au runtime — seul `THREE`, chargé en tout premier, est requis — mais placé ici pour la lisibilité thématique).

**Audit programmatique du graphe de scène, gate primaire (pas une formalité) — `tests/garden-material-audit.cjs` réellement étendu et réellement exécuté.** Conformément à `execution-continue.md`/`direction-artistique.md`, le nouveau maillage n'est pas seulement testé isolément (Node) : le script d'audit lui-même a été étendu pour instancier, dans la page réelle, les huit spécimens fondateurs via `window.GardenBotanyHybrids.buildSpecimenGroup` avant la traversée de la scène — le gate inspecte donc réellement le nouveau code, pas seulement les entités préexistantes du jardin libre.

- Environnement : le même contournement local que documenté dans l'entrée C1.4 (revue de code Playwright, cache `/opt/pw-browsers/`) était de nouveau nécessaire (`chrome-headless-shell` de révision 1243 absent, seule la 1194 est installée) ; cette fois recréé et **réellement vérifié fonctionnel** dans cet environnement précis : liens symboliques `chromium_headless_shell-1243/chrome-headless-shell-linux64/chrome-headless-shell` → binaire 1194, plus `chromium-1243/chrome-linux/chrome` → binaire 1194 pour les lancements avec `executablePath` explicite. Non versionné, propre à cette machine, comme la fois précédente.
- Résultat réel : `PASS material/geometry audit: {"materialsInspected":90,"transparentAllowed":1,"timesSampled":5}` (72 matériaux avant cet epic → 90, soit 18 nouveaux matériaux réellement inspectés issus des huit spécimens ajoutés ; `transparentAllowed` inchangé à 1 : aucun nouveau matériau transparent, donc aucune entrée `TRANSPARENT_ALLOWLIST` à ajouter — vérifié, pas supposé). Zéro normale retournée, zéro sommet non fini, zéro erreur console.
- `npm run test:browser` complet non rejoué de bout en bout dans cette session (le chaînage s'arrête au premier échec, `tests/garden-play.cjs`, qui expire sur un délai de déplacement chronométré — limitation d'environnement déjà documentée à l'identique dans l'entrée C1.4, reconfirmée ici sur le code d'avant cet epic avant tout changement, donc non liée à ce diff) ; chaque script individuellement pertinent pour cet epic (`garden-material-audit.cjs`, plus une vérification ciblée `index.html`/zéro erreur console/`GardenBotanyHybrids` exposé) a, lui, réellement tourné et est vert, comme ci-dessus. `npm run test:visual` non exécuté : aucune capture de référence existante ne couvre ce nouveau module (c'est le rôle de C1.8, pas de cet epic).

**Relecture multimodale, en complément seulement (jamais le gate) — réellement effectuée.** Capture Playwright dédiée (scène isolée : les huit fondatrices plus un croisement menthe-de-velours × fougère-d'écho, tous les autres objets de la scène masqués pour la lisibilité de la comparaison, lue avec l'outil Read) :
- Conforme au guide : aucune surface brillante/métallique, silhouettes composées uniquement de primitives simples (cônes, sphères, tubes), couleurs cohérentes avec les familles documentées (grappe mauve/violette pour les fleurs en épi, tons verts variés pour le feuillage), aucune teinte saturée hors palette.
- Limite honnête observée, notée pour C1.8 (dont c'est explicitement le rôle — « vérifier lisibilité des caractères... sans libellés ») plutôt que corrigée ici hors mandat : les feuilles fines (`fine`, cônes minces) se distinguent mal à distance sur les ports `tige-dressée`/`grimpant`, la silhouette lit alors surtout la tige. Le squelette, les points d'attache et le partage de géométrie/matériau — le critère de sortie réel de C1.7 — sont, eux, prouvés programmatiquement ci-dessus, indépendamment de ce constat esthétique.

**Résultat réellement exécuté** : `npm ci` puis `npm test` → 200 tests de règles/géométrie passent (187 existants + 13 nouveaux dans `tests/campaign-hybrids-render.cjs`, zéro régression), plus les vérifications HTML/site existantes. Sortie complète :

```
# tests 200
# suites 0
# pass 200
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

## Vérification locale du 17 septembre 2026 — prototype visuel de validation, porte de sortie de la phase 1 (epic C1.8)

Déclenchement automatisé (routine cloud horaire). Anti-hallucination fait avant tout le reste : dernier epic « fait » du backlog était C1.7, commit `b39dd16` — `git show --stat b39dd16` confirme le commit réel et son contenu correspond exactement à l'entrée ci-dessus ; `npm ci && npm test` relancés indépendamment → 200/200, identique au rapport existant. Aucune anomalie, `docs/campagne-anomalies.md` non créé. Pas de bandeau de pause en tête de `campagne-backlog.md`.

Dernier epic détaillé de la phase 1 (Laboratoire botanique) : C1.5 (épinglage) reste `todo`, sa vraie dépendance (C4.3, phase 4 non détaillée) l'exclut légitimement — décision déjà prise et documentée à l'epic C1.6, non remise en cause ici. C1.8 était donc le seul epic `todo` de la phase dont les dépendances (C1.7) sont `fait`.

**Six fondatrices sur huit, pas toutes.** `public/game/botany-genetics.js` avait déjà anticipé ce choix dans son propre en-tête depuis C1.2 (« C1.8 (visual prototype) picks six of these to model first »), cohérent avec le chiffre de `game-design.md` §14/§15. Choisies pour couvrir les cinq squelettes de port, un cas sans fleur, un cas émissif (éclairer) et un cas sans fonction : `oreille-de-pluie` (rosette, feuille coupe, retenir_eau), `clochette-du-soir` (tige-dressée, éclairer/émissif), `menthe-de-velours` (touffe, parfumer), `ronce-a-rubans` (grimpant, sans fleur), `fraise-timide` (retombant, sans fonction), `aster-des-vents` (tige-dressée, violet, sans fonction).

**Stades de croissance, posés mais absents depuis C1.7.** `public/game/cultivars.js` documentait déjà l'écart depuis C1.1 (« stage 0 is a freshly planted cutting/seedling ... later epics (C1.7/C1.8) attach a rendered form per stage »). `public/game/botany-hybrids.js` : `buildSpecimenGroup(cultivar, stage = MATURE_STAGE)`, `STAGE_SCALE = [0.38, 0.68, 1]` (3 stades, requis par le critère de sortie). L'appel sans deuxième argument (tous les sites C1.7 existants, aucun ne passe de stade) reproduit exactement l'ancien comportement — prouvé par un test dédié, pas supposé. Le plus jeune stade a un feuillage éclairci (un point d'attache sur deux) plutôt qu'une simple réduction d'échelle du plein feuillage ; aucune fleur n'apparaît avant le stade mature (cohérent avec le vocabulaire de croissance déjà utilisé par `render-frame.js` pour le jardin libre : graine/germe/feuillage par seuils, pas une continuité).

- Nouveau `tests/campaign-hybrids-render.cjs` (+6 tests, 19 au total dans ce fichier) : le stade omis reproduit bit pour bit la sortie mature de C1.7 ; un stade inconnu est refusé explicitement (jamais silencieusement borné) ; les trois stades croissent strictement en échelle ; aucune fondatrice ne fleurit avant maturité ; le plus jeune stade a strictement moins de feuilles que la maturité ; les trois stades restent déterministes et sans sommet non fini pour les huit fondatrices (pas seulement les six retenues pour le prototype, pour couvrir aussi les deux non montrées visuellement).
- Nouveau `tests/campaign-phase1-gate.cjs` (3 tests) : le scénario de bout en bout que la porte de sortie nomme littéralement, à travers la vraie couche de commandes (`GardenState`), pas une répétition des tests unitaires déjà existants : (1) un croisement réel produit un cultivar dont chacun des six axes provient bien de l'un des deux parents ; (2) le pot peut cibler un besoin énoncé — recherche parmi 500 graines d'un tirage `eclairer` sur un croisement compatible, puis ce résultat devient un vrai cultivar via la couche d'état ; (3) cultivar, spécimen et pot survivent exactement à un rechargement JSON réel, et une nuit sans rien semé ne crédite pas un second cultivar (scénario de non-double-crédit de la section 16 du design).
- Nouveau `tests/campaign-botany-visual.cjs` (le pointeur de fichier déjà annoncé par le backlog) : construit la scène de comparaison réelle exigée par le design (six fondatrices × trois stades, deux croisements compatibles affichés à côté de leurs parents — `oreille-de-pluie`×`clochette-du-soir` et `aster-des-vents`×`menthe-de-velours`, tous deux vérifiés `crossCompatible` puis résolus via `enumerateReachableTraitSets(...).find(r => r.valid)`, jamais supposés). Vérifications programmatiques (le vrai gate, comme `garden-material-audit.cjs`) : zéro sommet non fini ; zéro pénétration grossière entre spécimens voisins (boîtes englobantes Three.js réelles, `Box3.intersectsBox`, avec l'espacement de grille choisi) ; cohérence d'échelle stricte entre les trois stades mesurée sur la boîte englobante *rendue* (pas seulement le facteur d'échelle Node) ; les deux croisements produisent bien une combinaison valide. Pas de vérification « au-dessus du sol » : le port `retombant` est conçu pour descendre sous son origine locale (commentaire déjà présent dans `botany-hybrids.js` depuis C1.7, « trails downward from a rim height »), donc un seuil à y=0 aurait fait échouer un rendu de suspension volontaire, pas détecté un vrai défaut — leçon consignée dans le fichier de test lui-même après l'avoir vue échouer une première fois pour cette mauvaise raison.

**Contournement d'environnement, cette fois la solution documentée par la session elle-même, pas un bricolage ad hoc.** `tests/garden-material-audit.cjs` et `tests/campaign-botany-visual.cjs` échouaient tous deux au lancement (`chrome-headless-shell` révision 1243 absente, seule la 1194 est présente dans `/opt/pw-browsers/`, comme documenté aux epics C1.4/C1.6/C1.7). Contrairement aux contournements précédents (liens symboliques locaux, non versionnés, refaits à chaque session), cette session dispose d'une note d'environnement explicite indiquant le binaire `/opt/pw-browsers/chromium` comme solution prévue pour ce cas exact. Les deux fichiers ont donc reçu `executablePath: "/opt/pw-browsers/chromium"` (repli conditionnel, seulement si le fichier existe) — un changement minimal, versionné, propre à ces deux tests directement requis par cet epic, pas une réécriture de toute la suite `test:browser` (hors mandat). Les autres fichiers `test:browser` non touchés par cet epic gardent la limitation déjà documentée.

**Audit programmatique du graphe de scène, gate primaire — réellement exécuté.** `tests/garden-material-audit.cjs` : `PASS material/geometry audit: {"materialsInspected":90,"transparentAllowed":1,"timesSampled":5}` — identique à C1.7 (90 matériaux, aucun nouveau) : les stades de croissance et l'élargissement de deux géométries (ci-dessous) réutilisent le cache de matériaux existant (clé = couleur/rugosité/émissif, pas la géométrie), donc n'en créent aucun de plus. Zéro normale retournée, zéro sommet non fini, zéro erreur console.

**Gate dédié de l'epic, réellement exécuté.** `tests/campaign-botany-visual.cjs` : `PASS campaign-botany-visual: {"specimenCount":24,"founders":6,"stagesPerFounder":3,"crossings":2,"overlaps":0}`.

**Relecture multimodale réelle (complément, jamais le gate) — a mené à deux corrections concrètes, pas seulement une note.** Trois captures Playwright dédiées, prises avec une caméra encadrée exactement sur la boîte englobante de chaque sous-ensemble (fonction `frameBox`, calculée sur les huit coins réels de la boîte, jamais devinée à l'œil) : les six fondatrices matures côte à côte, les dix-huit cellules stade×fondatrice, les deux croisements avec leurs parents. Lues avec l'outil Read (image) et comparées à `direction-artistique.md` :
- Conforme au guide : aucune surface brillante, palette cohérente avec les familles documentées (grappe mauve de `menthe-de-velours`, éclat jaune/violet groupé d'`aster-des-vents`, blob vert dense d'`oreille-de-pluie`), les deux croisements montrent visiblement un mélange de traits des deux parents (tige/fleur d'un parent, feuillage de l'autre), cohérence d'échelle des trois stades confirmée à l'œil en plus de l'assertion programmatique.
- **Problème réel trouvé, déjà anticipé par C1.7** : la limite notée dans l'entrée C1.7 ci-dessus (« les feuilles fines... la silhouette lit alors surtout la tige ») s'est confirmée sur la première capture — `clochette-du-soir` et `aster-des-vents` (feuille `fine`) lisaient comme une tige presque nue. `public/game/botany-hybrids.js` : rayon de la géométrie `fine` élargi de `0.032` à `0.055` (`ConeGeometry`), et par la même lecture la fleur `etoile` non groupée (`fraise-timide`) était quasi invisible : rayon élargi de `0.025` à `0.045`. Les deux restent nettement plus fins que `ronde` (0,1) et `cloche` (0,07) — un ajustement proportionné, pas une refonte de forme. `npm test` (209/209), `garden-material-audit.cjs` et `campaign-botany-visual.cjs` relancés après chaque changement, toujours verts.
- **Limite résiduelle honnête, non bloquante** : même élargis, les spécimens restent visuellement fins/graciles à l'échelle du banc de comparaison (plantes stylisées à tiges minces, cohérent avec `direction-artistique.md` qui documente des « matériaux mats » et des « volumes lisibles » sans exiger un feuillage dense) ; un futur epic de contenu pourrait revisiter l'épaisseur générale des tiges/organes une fois plus de cultivars réels observés en jeu. Ceci ne bloque pas la porte de phase : le critère de sortie littéral porte sur la reconnaissance des caractères parentaux et la persistance exacte, prouvées programmatiquement ci-dessus et par `tests/campaign-phase1-gate.cjs`, pas sur un jugement esthétique global (voir `direction-artistique.md`, « Ce que ce guide ne remplace pas »).

`npm run test:browser` complet non rejoué de bout en bout (échoue toujours dès `garden-play.cjs`, limitation d'environnement déjà documentée aux epics C1.4/C1.6/C1.7, non liée à ce diff — ce fichier n'a pas reçu le correctif `executablePath` car hors du périmètre de cet epic) ; les deux scripts directement requis par cet epic (`garden-material-audit.cjs`, `campaign-botany-visual.cjs`) ont, eux, réellement tourné jusqu'au bout et sont verts, comme ci-dessus. `npm run test:visual` non exécuté : ce script (`garden-visual.cjs`) couvre le jardin libre, sans rapport avec ce module.

**Résultat réellement exécuté** : `npm ci` puis `npm test` → 209 tests de règles/géométrie passent (200 existants + 6 dans `campaign-hybrids-render.cjs` + 3 dans `campaign-phase1-gate.cjs`, zéro régression), plus les vérifications HTML/site existantes. Sortie complète :

```
# tests 209
# suites 0
# pass 209
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

### Clôture de la phase 1 (Laboratoire botanique) — 17 septembre 2026

Porte de sortie (design §15, reprise dans `orchestration.md`) : *« un joueur reconnaît des caractères parentaux, cible un besoin donné et retrouve exactement son cultivar après rechargement. »* Les scénarios de validation pertinents de la section 16 du design (repris en tests de non-régression, `tests/campaign-phase1-gate.cjs` en plus des tests d'epic existants) passent réellement, ci-dessus. C1.5 (épinglage) reste `todo`, correctement différé à C4.3 (phase 4 non détaillée) depuis la décision documentée à l'epic C1.6 — le critère de sortie littéral ne le mentionne pas, et l'implémenter maintenant aurait exigé d'inventer un déclencheur narratif hors ordre.

Conformément à la politique de porte de phase révisée pour l'exécution non supervisée (`execution-continue.md` : « aucun pipeline n'est connecté à `main` ... fusionner une phase validée ... est une étape d'implémentation comme une autre, pas un acte de publication »), la branche `maison-des-possibles` est fusionnée dans `main` en local puis les deux branches sont poussées vers `github.com/EdwinDayot/edwindayot.fr`. Aucun déploiement, aucune publication : `main` de ce dépôt n'est relié à aucun service d'hébergement.

## Vérification locale du 17 septembre 2026 — passage de nuit atomique, couche moteur (epic C2.2)

Déclenchement automatisé (routine cloud horaire). Anti-hallucination fait avant tout le reste : dernier epic « fait » du backlog était C1.8, commit `5a0816d` — `git show --stat 5a0816d` confirme le commit réel et son contenu correspond exactement à l'entrée ci-dessus (clôture de la phase 1, fusion vers `main` déjà documentée) ; `npm ci && npm test` relancés indépendamment → 209/209, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` non créé.

Premier epic de la phase 2 après son ouverture (C2.1, horloge pure). Choix de l'epic : C2.2 est le seul epic `todo` de la phase dont la dépendance (C2.1) est `fait` — C2.3 à C2.11 dépendent tous, directement ou en cascade, de C2.2.

**Scope réduit à la couche moteur, écart documenté (voir aussi campagne-backlog.md).** Le critère de sortie littéral de C2.2 mélange cinq clauses, dont trois de rendu/interface (déclenchement réel à 23h, annulation d'un placement en cours, transition caméra + écran de confirmation du pot) qui supposeraient un mode « campagne » jouable dans `garden.js`/`garden-frame.js` — qui n'existe pas encore : `campaign-clock.js` (C2.1) n'est toujours pas chargé par `index.html`, et aucune scène « maison » de campagne n'a été construite (la maison de la campagne est un chantier de la phase 3, pas de la phase 2). Construire cette interface maintenant aurait exigé d'inventer d'un bloc, sans passage par le Cartographe, un mode de jeu entier — précisément ce qu'`orchestration.md` (« Règles avant rendu : la simulation doit être prouvée par des tests Node... avant que l'habillage Three.js ne soit construit dessus ») déconseille. Les deux clauses restantes, elles, sont pleinement des règles de simulation et sont prouvées programmatiquement ci-dessous : le bilan de nuit appliqué une seule fois (y compris après un rechargement réel) et « dormir plus tôt » (le bilan ne dépend jamais de l'heure atteinte, aucune heure sautée n'est simulée). Les trois clauses différées deviennent le nouvel epic **C2.2v** (todo, dépend de C2.2), ajouté à `campagne-backlog.md` à côté de C2.2 plutôt que de réduire silencieusement le critère de sortie de celui-ci.

- Nouveau champ de sauvegarde `s.campaignDay` (entier ≥ 1, commence à 1) et `s.campaignClock` (miroir JSON plat d'un `CampaignClock` neuf de `public/game/campaign-clock.js` — `activeSeconds`/`gameSeconds`/`paused` seulement, jamais l'instance de classe elle-même ni son `_lastWall` transitoire, non significatif entre deux sauvegardes). `public/garden-state-lifecycle.js`/`public/garden-state-validate.js` suivent exactement le patron déjà utilisé pour `campaignPot`/`specimens` : champ optionnel, valeur par défaut sur partie neuve, validation stricte sinon (bornes de `gameSeconds` alignées sur `Clock.DAY_SECONDS`, exactement la borne déjà appliquée par `CampaignClock.tick()` lui-même, donc aucun état atteignable en jeu ne peut être rejeté par la validation). `campaign-clock.js` lui-même est **inchangé** : ses 8 tests existants (`tests/campaign-clock.cjs`) passent sans modification.
- `public/garden-state-cmd-f.js` : la commande `sleep` (déjà existante depuis C1.3, résolution du pot) est étendue — sans renommage, pour ne casser aucun test existant qui l'invoque par son nom — pour incrémenter `s.campaignDay` et remettre `s.campaignClock` à 7h non pausé (`gameSeconds: 0, paused: false`) à chaque appel, qu'il soit déclenché plus tard par un vrai passage à 23h (C2.2v) ou par un coucher volontaire anticipé (« dormir plus tôt », déjà autorisé aujourd'hui puisque `sleep` ne lit jamais l'heure de `campaignClock` avant de résoudre). `sleep` ne touche ni `s.elapsed` ni `s.inventory` : aucune heure sautée n'est donc jamais simulée, par construction et pas seulement par absence de code qui le ferait.
- Nouveau `tests/campaign-nightfall.cjs` (8 tests), ajouté à la commande `test` de `package.json` : partie neuve démarre à `campaignDay: 1`/horloge à 7h par défaut ; `sleep` avance `campaignDay` d'exactement un et remet l'horloge à 7h non pausée même si elle avait déjà avancé et avait été mise en pause ; un coucher anticipé (horloge à ~8h) réussit, compte comme une nuit complète, et laisse `s.elapsed`/`s.inventory` strictement inchangés ; **non-double-crédit sur un rechargement réel** — un croisement réel puis `sleep` (1 cultivar, jour 2), rechargement JSON réel (`JSON.parse(JSON.stringify(...))`), un second `sleep` avec un pot vide ne crédite pas de second cultivar mais fait bien progresser vers le jour 3 (une nuit vide authentiquement nouvelle, jamais une répétition de la première) ; migration silencieuse d'une sauvegarde sans `campaignDay`/`campaignClock`, comparaison JSON complète avant/après ; six formes malformées de `campaignClock` rejetées (dont l'horloge exactement à la borne des 23h, acceptée) et six de `campaignDay`.

**Résultat réellement exécuté** : `npm ci` puis `npm test` → 217 tests de règles/géométrie passent (209 existants + 8 nouveaux dans `campaign-nightfall.cjs`, zéro régression), plus les vérifications HTML/site existantes. Sortie complète :

```
# tests 217
# suites 0
# pass 217
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

`/code-review` (niveau medium) lancé sur le diff : aucun défaut relevé — vérifie notamment que `campaignDay`/`campaignClock` sont toujours initialisés avant toute commande (constructeur de `GardenState`), que les bornes de validation correspondent exactement au plafond déjà appliqué par `CampaignClock.tick()`, et que l'absence de câblage rendu est cohérente avec les commentaires du code lui-même plutôt qu'un oubli.

Aucun test navigateur ni visuel requis : aucun fichier de rendu (`render*.js`, `hud*.js`, `garden.js`, `garden-frame.js`) ni `index.html` n'a été touché — seuls la couche de sauvegarde/validation et un segment de commande existant ont changé.

## Vérification locale du 17 septembre 2026 — apparition de la première Rainelle, couche moteur (epic C2.3)

Déclenchement automatisé (routine cloud horaire). Anti-hallucination fait avant tout le reste : dernier epic « fait » du backlog était C2.2, commit `2a410b6` — `git show --stat 2a410b6` confirme le commit réel et son contenu correspond exactement à l'entrée ci-dessus ; `npm ci && npm test` relancés indépendamment → 217/217, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` non créé.

**Choix de l'epic : C2.2v sauté, justifié dans `campagne-backlog.md`.** C2.2v (dépendance déclarée C2.2, `fait`) suppose une scène « maison » de campagne et un point d'ancrage caméra que la phase 3 seule doit construire — son propre statut le notait déjà (« probablement à détailler avec le Cartographe si aucun ne préexiste »). L'implémenter aurait exigé d'inventer d'un bloc un mode de jeu entier, sans passage par le Cartographe. C2.3 (dépendances C1.3/C2.2, toutes deux `fait`) ne porte, elle, aucune dépendance narrative cachée comparable : le design place la scène de la grenouille au chapitre 4 (Acte II, phase 4, non détaillée), mais le critère de sortie littéral de C2.3 dans le backlog ne mentionne aucun contenu de chapitre — seulement une mécanique générique (« une nuit scénarisée... produit... »).

Premier epic de Rainelle de la campagne. Pose la mécanique générique — sans aucun contenu narratif de chapitre 4, qui reste un chantier de la phase 4 — sur le modèle « règles avant rendu » déjà suivi par C1.1/C1.6 : une rencontre peut être armée, se résout sur la prochaine vraie nuit de pot, produit une Rainelle liée au cultivar tiré cette nuit-là sans jamais toucher à la résolution normale du pot.

- Nouveau `public/game/rainelles.js` : `createRainelle(s, {cultivarId, name})` — fonction pure, même patron que `Cultivars.createCultivar` (id stable `r<n>`, jamais recalculé). Aucune donnée de geste/automatisation (C2.4 et epics suivants) : cet epic prouve seulement qu'une Rainelle peut exister, être nommée, et garder un id stable après rechargement.
- Nouveaux champs de sauvegarde `s.rainelles = []`, `s.rainelleNextId = 1`, `s.campaignFrogEncounterPending = false`. `public/garden-state-lifecycle.js`/`public/garden-state-validate.js` suivent exactement le patron déjà utilisé pour `specimens`/`campaignPot` : optionnel, valeur par défaut sur partie neuve, validation stricte sinon (dont un contrôle de collision d'id identique à `specimenNextId`, et une vérification que `cultivarId` référence bien un cultivar existant dans `s.cultivars`, comme `specimens` le fait déjà pour `sp.cultivarId`).
- Nouveau segment de commande `public/garden-state-cmd-i.js` (`public/garden-state.js` étendu pour le charger, en Node comme dans le navigateur) :
  - `triggerFrogEncounter {}` : arme `s.campaignFrogEncounterPending`, refusée explicitement si une Rainelle existe déjà (« La première Rainelle est déjà née » — ce mécanisme ne produit jamais qu'une seule Rainelle ; les suivantes viendront du bourgeon/nurserie, design §5 chapitre 6, un epic différent) ou si une rencontre est déjà armée.
  - `renameRainelle {id, name}` : même validation que `renameCultivar` (`garden-state-cmd-g.js`) — nom non vide après `trim()`, borné à 40 caractères.
- `public/garden-state-cmd-f.js` : la commande `sleep` (déjà étendue par C1.3/C2.2) est étendue une troisième fois — toujours sans renommage, pour ne casser aucun test existant. Pendant la résolution normale du pot (inchangée : chaque paire en attente produit un cultivar exactement comme avant), si une rencontre est armée et qu'aucune Rainelle n'existe encore, le cultivar produit **cette nuit-là** sert aussi à créer la première Rainelle ; la rencontre est alors désarmée. Si la rencontre est armée mais qu'aucune paire n'était en attente (nuit vide), elle **n'est pas perdue** : `campaignFrogEncounterPending` reste `true`, reportée telle quelle à la prochaine nuit qui résout réellement une paire — jamais résolue deux fois, jamais oubliée. Le cultivar lui-même n'est jamais modifié ni retiré par la rencontre : c'est la preuve littérale du critère « sans perte du spécimen attendu ».
- `public/index.html` : nouveau `<script src="/game/rainelles.js" defer>` (chargé avant `garden-state-cmd-f.js`/`-i.js`, qui lisent `root.GardenRainelles` au moment de leur exécution, même contrainte d'ordre que `botany-genetics.js`/`botany-pot.js`/`cultivars.js`) et `<script src="/garden-state-cmd-i.js" defer>`.

**Anomalie pré-existante trouvée et corrigée en vérifiant cet epic dans le navigateur, documentée ici plutôt que passée sous silence.** La vérification navigateur ciblée (nécessaire car `index.html` a changé) a d'abord échoué avant tout changement de cet epic : `TypeError: Cannot read properties of undefined (reading 'DEFAULT_ACTIVE_SECONDS')` dès le chargement de la page, empêchant toute création de partie neuve dans le navigateur (le jardin libre entier était donc cassé en navigateur, pas seulement la campagne). Cause : depuis l'epic C2.2, `garden-state-lifecycle.js`/`garden-state-validate.js` lisent `root.GardenCampaignClock` sans condition (`Clock.DEFAULT_ACTIVE_SECONDS`/`Clock.DAY_SECONDS`), mais `public/game/campaign-clock.js` n'a jamais été ajouté à `index.html` — l'entrée C2.2 documentait explicitement « `campaign-clock.js` n'est pas encore chargé par `index.html` » sans réaliser que les fichiers qu'elle modifiait, eux, en dépendaient déjà inconditionnellement au chargement de la page. **Confirmé indépendamment comme préexistant, pas une régression de cet epic** : `git stash -u` (retirant tout le diff de C2.3), relance du serveur local sur le commit `2a410b6` inchangé, même exception à l'identique ; `git stash pop` a restauré les modifications sans perte. Les tests Node ne l'avaient jamais détecté parce qu'ils `require()` directement `campaign-clock.js`, contournant le chargement navigateur. Correctif minimal et documenté : `<script src="/game/campaign-clock.js" defer></script>` ajouté dans `index.html`, avant `garden-state-validate.js`/`garden-state-lifecycle.js` (avec un commentaire expliquant pourquoi), sans autre changement.

**Test navigateur ciblé, réellement exécuté** (nécessaire car `index.html` a changé) : serveur local `python3 -m http.server 4174 --directory public`, page chargée avec Playwright (`chromium.launch({executablePath: "/opt/pw-browsers/chromium"})`) : après le correctif ci-dessus, **zéro erreur console/page** (contre une exception bloquante avant), `window.GardenRainelles.createRainelle` bien exposé. `tests/garden-material-audit.cjs` (le gate complet de `npm run test:browser`) relancé aussi : `PASS material/geometry audit: {"materialsInspected":90,"transparentAllowed":1,"timesSampled":5}`, strictement identique à C2.2 — aucun rendu de Rainelle n'existe encore, donc aucun nouveau matériau. `npm run test:browser` complet non rejoué de bout en bout : limitation d'environnement déjà documentée aux epics précédents (`garden-play.cjs` expire sur un délai de déplacement chronométré, non lié à ce diff) ; les deux vérifications directement pertinentes pour cet epic (`garden-material-audit.cjs`, la vérification ciblée `index.html`/zéro erreur console/`GardenRainelles` exposé) ont, elles, réellement tourné et sont vertes. `npm run test:visual` non exécuté : aucune géométrie/matériau nouveau.

Nouveau `tests/campaign-rainelles.cjs` (11 tests), ajouté à la commande `test` de `package.json` : partie neuve sans Rainelle ni rencontre armée ; `triggerFrogEncounter` arme la rencontre, une seconde tentative est refusée explicitement sans désarmer la première ; une nuit avec une vraie paire semée crée le cultivar exactement comme d'habitude plus une Rainelle portant son `cultivarId`, rencontre désarmée ; une rencontre armée sur une nuit vide (rien semé) n'est pas perdue — elle se reporte, sans Rainelle créée, jusqu'à la prochaine vraie paire résolue ; une seule Rainelle possible par ce mécanisme, `triggerFrogEncounter` refusé une fois la première née ; `renameRainelle` persiste un nom réel (`trim()`), refuse un id inconnu et un nom vide sans muter l'entrée ; un aller-retour JSON réel garde id/cultivarId/name strictement identiques ; migration silencieuse d'une sauvegarde sans les trois nouveaux champs, comparaison JSON complète avant/après ; six formes malformées de `rainelles` rejetées (mauvais format d'id, `cultivarId` inconnu, nom non-string, doublon d'id, tableau invalide, entrée `null`) ; collision d'id sur `rainelleNextId` rejetée, correction acceptée ; quatre formes malformées de `campaignFrogEncounterPending` rejetées.

**Résultat réellement exécuté** : `npm ci` puis `npm test` → 228 tests de règles/géométrie passent (217 existants + 11 nouveaux dans `campaign-rainelles.cjs`, zéro régression), plus les vérifications HTML/site existantes. Sortie complète :

```
# tests 228
# suites 0
# pass 228
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

## Vérification locale du 17 septembre 2026 — schéma du geste unique (epic C2.4)

Déclenchement automatisé (routine cloud horaire). Anti-hallucination fait avant tout le reste : dernier epic « fait » du backlog était C2.3, commit `7bbaad7` — `git show --stat 7bbaad7` confirme le commit réel et son contenu correspond exactement à l'entrée ci-dessus ; `npm ci && npm test` relancés indépendamment → 228/228, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` non créé.

**Choix de l'epic : C2.2v sauté une nouvelle fois, pour la même raison exacte que la fois précédente** (voir `campagne-backlog.md`) : son critère de sortie suppose toujours une scène « maison » de campagne et un point d'ancrage caméra que seule la phase 3 doit construire — rien n'a changé depuis le déclenchement C2.3. C2.4 (dépendance C2.3, `fait`) est un epic de schéma pur, sans dépendance narrative ni de rendu cachée : le seul epic réellement disponible.

Pose le contrat du « geste unique » (design §5) comme schéma de données : une Rainelle mémorise au plus un `{verbe, poste, source, destination, condition}` à la fois, jamais une liste. Toujours aucune exécution (`automation.js` inchangé — le tick des postes de travail reste le chantier de C2.6+) : cet epic prouve seulement que la mémoire existe, qu'elle ne contient jamais qu'un seul geste, et que réenseigner remplace le geste précédent dans son intégralité plutôt que de l'étendre ou de le refuser.

- `public/game/rainelles.js` : nouvelle constante `VERBS` (six verbes du tableau du design §5 — `arroser`, `recolter`, `transporter`, `replanter`, `preparer`, `trier` — **`multiplier` volontairement exclu** : le design l'exclut lui-même explicitement des Rainelles). `createRainelle` initialise désormais `geste: null`.
- Nouveau `public/garden-state-cmd-j.js` : commande `teachGesture {id, verbe, poste, source, destination, condition}`. Refuse explicitement un id de Rainelle inconnu, un verbe hors `VERBS`, ou un `poste`/`source`/`destination` vide après `trim()` (`condition` est optionnelle, `""` par défaut). Sur succès, remplace **tout** `rainelle.geste` par un nouvel objet à cinq champs — jamais une fusion avec l'ancien — ce qui rend « réenseigner remplace intégralement l'ancien geste (jamais un ajout) » vrai par construction. Le message de retour distingue explicitement un premier apprentissage (« Geste appris : *verbe*. ») d'un remplacement (« Ancien geste remplacé : *verbe*. »), pour que le remplacement ne soit jamais silencieux.
- `public/garden-state-validate.js` : validation de `rainelle.geste` (si présent et non `null`, doit être un objet avec `verbe` dans `VERBS`, `poste`/`source`/`destination` des chaînes non vides, `condition` une chaîne) ; une entrée d'avant cet epic sans champ `geste` du tout reste acceptée (même patron que `disposition` sur les cultivars, C1.4).
- `public/garden-state.js` : la liste des segments de commande chargés en Node gagne `"j"`.
- `public/index.html` : nouveau `<script src="/garden-state-cmd-j.js" defer>`.

**Bug réel trouvé et corrigé avant tout commit, par `/code-review` (niveau medium), pas seulement par les tests Node.** Le premier passage de la revue a signalé que `garden-state-validate.js` lit désormais `root.GardenRainelles.VERBS` au moment de l'exécution du script, mais `rainelles.js` était chargé *après* `garden-state-validate.js` dans `index.html` (ligne 80 contre ligne 57) — exactement la même classe de bug d'ordre de script que celle trouvée et corrigée pour `campaign-clock.js` à l'epic C2.3, sauf que les tests Node ne pouvaient pas la détecter (ils `require()` les modules directement, sans passer par l'ordre de chargement du navigateur). **Corrigé avant tout commit** : `<script src="/game/rainelles.js" defer>` déplacé juste après `campaign-clock.js`, avant `garden-state-util.js`/`-migrate.js`/`-validate.js` ; l'ancienne balise (devenue un doublon) retirée de sa position précédente, avec un commentaire expliquant le nouveau point d'ordre. Reproduit puis vérifié réellement corrigé dans un vrai navigateur (ci-dessous), pas seulement supposé d'après la lecture du diff.

**Résultat réellement exécuté** : `npm ci` puis `npm test` → 239 tests de règles/géométrie passent (228 existants + 11 nouveaux dans un nouveau `tests/campaign-gestures.cjs`, zéro régression), plus les vérifications HTML/site existantes. Sortie complète :

```
# tests 239
# suites 0
# pass 239
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

Nouveau `tests/campaign-gestures.cjs` (11 tests), ajouté à la commande `test` de `package.json` : une Rainelle fraîche démarre sans geste (`null`) ; `teachGesture` assigne exactement le geste enseigné, `condition` par défaut à `""` si omise ; enseigner un second geste remplace le premier dans son intégralité (aucune fusion, toujours exactement cinq champs, message de remplacement explicite distinct du message d'apprentissage initial) ; refus explicite sur un id de Rainelle inconnu (aucun geste créé ailleurs) ; refus sur un verbe inconnu, y compris `multiplier` explicitement exclu ; refus sur un `poste`/`source`/`destination` vide sans jamais muter le geste existant ; `poste`/`source`/`destination`/`condition` sont `trim()`és avant sauvegarde ; un aller-retour JSON réel garde le geste enseigné strictement identique ; une sauvegarde d'avant cet epic (entrée de Rainelle sans champ `geste` du tout) continue de valider sans erreur ; formes de geste malformées rejetées (verbe invalide, `multiplier`, champs vides ou non-chaînes) tandis qu'un geste bien formé et un `null` explicite restent acceptés.

**Test navigateur ciblé, réellement exécuté** (nécessaire car `index.html` a changé, deux fois : l'ajout initial de `garden-state-cmd-j.js`, puis le correctif d'ordre de script) : serveur local `python3 -m http.server 4174 --directory public`, page chargée avec Playwright (`chromium.launch({executablePath: "/opt/pw-browsers/chromium"})`). **Avant le correctif d'ordre de script, ce test n'a délibérément pas été lancé** (le bug a été repéré par la revue de code avant tout chargement navigateur) ; après le correctif : **zéro erreur console/page**, `window.GardenRainelles.VERBS` exposé avec les six verbes attendus, et un scénario complet exécuté dans la page réelle (`GardenRules.GardenState`, `triggerFrogEncounter` → `sowPot` → `sleep` → deux appels `teachGesture` successifs) confirme le remplacement explicite (`"Geste appris : arroser."` puis `"Ancien geste remplacé : recolter."`) et le geste final strictement égal au second appel, aucune trace du premier. `tests/garden-material-audit.cjs` (le gate complet de `npm run test:browser`) relancé aussi : `PASS material/geometry audit: {"materialsInspected":90,"transparentAllowed":1,"timesSampled":5}`, strictement identique à C2.3 — aucun rendu nouveau, donc aucun nouveau matériau. `npm run test:browser` complet non rejoué de bout en bout : limitation d'environnement déjà documentée aux epics précédents (`garden-play.cjs` expire sur un délai de déplacement chronométré, non lié à ce diff) ; les vérifications directement pertinentes pour cet epic (`garden-material-audit.cjs`, la vérification ciblée `index.html`/zéro erreur console/scénario `teachGesture` complet) ont, elles, réellement tourné et sont vertes. `npm run test:visual` non exécuté : aucune géométrie/matériau nouveau, aucun fichier de rendu touché.

`/code-review` (niveau medium) relancé une seconde fois après le correctif de script : aucun défaut relevé — confirme notamment que `teachGesture` valide tous les champs requis avant toute mutation (pas de mutation partielle en cas de refus), que le validateur miroir bien la même forme/contrainte (dont l'exclusion de `multiplier`), et que `rainelles.js` n'est chargé qu'une seule fois dans `index.html` malgré le déplacement.

## Vérification locale du 18 septembre 2026 — enseignement en quatre moments, couche moteur (epic C2.5)

Déclenchement automatisé (routine cloud horaire). Anti-hallucination fait avant tout le reste : dernier epic « fait » du backlog était C2.4, commit `2a1f002` — `git show --stat 2a1f002d8a987a7b21fdee6f6bbcc23e68d7dbb0` confirme le commit réel et son contenu correspond exactement à l'entrée ci-dessus ; `npm ci && npm test` relancés indépendamment → 239/239, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` non créé (fichier toujours absent).

**Choix de l'epic : C2.2v sauté une troisième fois, pour la même raison exacte que les deux précédentes** (voir `campagne-backlog.md`) : son critère de sortie suppose toujours une scène « maison » de campagne et un point d'ancrage caméra que seule la phase 3 doit construire — rien n'a changé depuis le déclenchement C2.4. C2.5 (dépendance C2.4, `fait`) était le seul autre epic `todo` de la phase disponible.

Pose le contrat « enseignement en quatre moments » (design §5) comme machine à états pure au-dessus de la mémoire à un seul geste posée par C2.4 : « Regarde-moi » (suspend le temps), une démonstration capturée, une phrase proposée et modifiable, un essai montrant la trajectoire prévue avant confirmation, puis la « répétition courte » vers une autre Rainelle une fois un geste réellement démontré une première fois.

**Scope réduit à la couche moteur, écart documenté (voir aussi campagne-backlog.md).** Le critère de sortie littéral mélange cinq clauses de règles avec leur affichage (un écran, une ligne de trajectoire dans le monde, une mise en scène caméra pour « Regarde-moi ») — aucune interface de campagne câblée à un rendu réel n'existe encore (le carnet `hud-panel.js` affiche déjà des données de campagne depuis C1.4, mais aucun poste/zone de Rainelle n'est placé dans le monde 3D, et « Regarde-moi » suppose une caméra dédiée qui n'existe pas). Construire cette interface maintenant aurait exigé d'inventer d'un bloc, sans passage par le Cartographe, une scène de jeu qui n'existe pas — même raisonnement que C2.2/C2.2v. Le critère l'autorise d'ailleurs explicitement : « une démonstration **réelle ou assistée** au poste est capturée » — la voie assistée (une commande, pas un geste réel dans le monde) satisfait donc littéralement cette clause sans attendre l'interface. Les cinq clauses sont prouvées ci-dessous comme état/données pures ; leur affichage devient le nouvel epic **C2.5v** (todo, dépend de C2.5), ajouté à `campagne-backlog.md` à côté de C2.5 plutôt que réduit en silence.

- `public/game/rainelles.js` étendu (pas de nouveau fichier — cette logique appartient à la même responsabilité que `VERBS`/`createRainelle`) :
  - `validateGestureFields`/`normalizeGesture`/`applyGesture` : la validation et la mutation déjà écrites par C2.4 pour `teachGesture` sont extraites en fonctions pures partagées, pour que la règle « remplacement intégral, jamais une fusion » et la validation des cinq champs ne puissent jamais diverger entre le chemin direct (`teachGesture`) et le nouveau flux en quatre moments (`confirmTeaching`/`teachGestureQuick`). `garden-state-cmd-j.js` (C2.4) est réécrit pour appeler `applyGesture` plutôt que de dupliquer sa propre validation — comportement strictement identique, prouvé par les 11 tests existants de `campaign-gestures.cjs`, inchangés et toujours verts.
  - `defaultPhrase` : une phrase par verbe (gabarit tiré de l'exemple du design lui-même pour « récolter » — « Récolter les fruits mûrs dans ce carré ; déposer dans ce panier. »), qui nomme directement `poste`/`source`/`destination` (encore des identifiants techniques opaques à ce stade moteur, comme pour `teachGesture` lui-même) et ajoute la clause de condition entre parenthèses si présente.
  - `plannedTrajectory` : la donnée derrière « un essai montre la trajectoire prévue » — la séquence générique `[source, poste, destination]`, avec fusion d'une étape consécutive identique à la précédente (le cas « arroser » du design, où poste et destination désignent la même zone).
  - **Borne de longueur ajoutée à `validateGestureFields`, trouvée manquante par `/code-review` avant tout commit** (voir ci-dessous) : `poste`/`source`/`destination`/`condition` bornés à 40 caractères, même convention déjà utilisée pour `renameCultivar`/`renameRainelle`. Corrige aussi un trou latent de C2.4 lui-même (`teachGesture` n'avait jamais borné ces quatre champs).
- Nouveau `public/garden-state-cmd-k.js` : six commandes, `beginTeaching`/`demonstrateGesture`/`reviseGesturePhrase`/`confirmTeaching`/`cancelTeaching`/`teachGestureQuick`. `beginTeaching` refuse une Rainelle inconnue et refuse d'ouvrir une seconde leçon tant que la première n'est pas confirmée/annulée ; il pose `s.campaignTeaching = {rainelleId, step: "watching", draft: null}` et met `s.campaignClock.paused = true` (« Regarde-moi » suspend le temps, en réutilisant l'horloge déjà posée par C2.2 — aucune nouvelle horloge). `demonstrateGesture` valide les cinq champs, calcule phrase et trajectoire, les pose dans `draft`, passe `step` à `"reviewing"` — ne touche jamais `rainelle.geste` (l'essai est un aperçu, pas un engagement). `reviseGesturePhrase` permet au joueur de corriger la phrase proposée (non vide, ≤ 240 caractères). `confirmTeaching` applique le brouillon via `applyGesture`, enregistre `s.campaignLastDemonstration` (les cinq champs, sans la phrase/trajectoire qui ne font pas partie de ce qu'une Rainelle retient), remet l'horloge en marche, referme la session. `cancelTeaching` abandonne le brouillon sans jamais toucher au geste existant, remet l'horloge en marche. `teachGestureQuick` réapplique directement `s.campaignLastDemonstration` à une autre Rainelle, sans ouvrir de session ni suspendre l'horloge — la preuve littérale de « une répétition courte, sans refaire tout le tutoriel » —, refusée explicitement tant qu'aucune démonstration n'a jamais été confirmée dans la partie.
- Nouveaux champs de sauvegarde `s.campaignTeaching` (`null` hors leçon) et `s.campaignLastDemonstration` (`null` avant la première leçon confirmée). `public/garden-state-lifecycle.js`/`public/garden-state-validate.js` suivent le patron déjà utilisé pour `campaignPot`/`rainelles` : optionnel, valeur par défaut sur partie neuve, validation stricte sinon (dont l'invariant `step: "watching" ⇒ draft: null` / `step: "reviewing" ⇒ draft` bien formé, phrase et trajectoire comprises).
- `public/garden-state.js` : la liste des segments de commande chargés en Node gagne `"k"`. `public/index.html` : nouveau `<script src="/garden-state-cmd-k.js" defer>`, après `garden-state-cmd-j.js`.

**Deux bugs réels trouvés par `/code-review` (niveau medium), corrigés avant tout commit, pas seulement par les tests Node.**

1. `sleep` (`garden-state-cmd-f.js`) remettait déjà l'horloge à 7h non pausée à chaque nuit (C2.2), mais ne touchait jamais à `s.campaignTeaching` : une leçon laissée ouverte (par exemple si une nuit scénarisée survient pendant une démonstration) aurait donc silencieusement désynchronisé l'horloge (remise en marche) de l'état de leçon (resté actif), bloquant ensuite tout nouveau `beginTeaching` (« leçon déjà en cours ») sans qu'aucune leçon ne soit réellement en cours. **Corrigé** : `sleep` remet `s.campaignTeaching = null` — une leçon non confirmée est exactement le « placement non validé... annulé sans coût » que le design (§3) attribue déjà au passage de nuit.
2. `teachGestureQuick` ne vérifiait pas qu'une leçon était en cours avant d'appliquer `s.campaignLastDemonstration` : un appel pendant qu'une leçon est ouverte (sur la même Rainelle ou une autre) aurait muté un geste pendant que `campaignTeaching` restait actif avec un brouillon sans rapport. **Corrigé** : `teachGestureQuick` est désormais refusé explicitement tant qu'une leçon est en cours, quelle que soit la Rainelle visée.

Une troisième piste (bornage de longueur des champs, ci-dessus) a été trouvée par un second passage de `/code-review` après les deux premiers correctifs — voir le détail dans `rainelles.js` ci-dessus. Un troisième passage n'a plus rien trouvé de nouveau ; une garde défensive suggérée (mais non actuellement atteignable, aucun code ne retire jamais une Rainelle de `s.rainelles`) a été ajoutée par prudence à `confirmTeaching` à l'identique des trois autres commandes de ce fichier.

Nouveau `tests/campaign-teaching.cjs` (19 tests, ajouté à la commande `test` de `package.json`) : `beginTeaching` suspend l'horloge et ouvre une session, refuse une Rainelle inconnue et une seconde leçon simultanée ; `demonstrateGesture` exige une session active en observation, valide exactement les mêmes champs que `teachGesture`, calcule une phrase non vide contenant les identifiants fournis et la clause de condition, calcule une trajectoire avec fusion des étapes consécutives identiques (trois variantes : fusion en tête, fusion en fin, répétition non consécutive conservée), ne touche jamais `rainelle.geste`, refuse une seconde démonstration en cours de relecture ; `reviseGesturePhrase` corrige la phrase (`trim()`), refuse vide/trop longue/hors étape ; `confirmTeaching` applique le geste (message distinct premier apprentissage/remplacement, prouvé aussi via le flux en quatre moments et pas seulement `teachGesture`), enregistre `campaignLastDemonstration`, relance l'horloge, referme la session, refuse sans session/brouillon ; `cancelTeaching` abandonne sans toucher au geste ni enregistrer de dernière démonstration, refuse hors session ; `teachGestureQuick` refuse tant que rien n'a été démontré, réapplique la dernière démonstration confirmée à une seconde Rainelle (créée directement via `Rainelles.createRainelle` comme fixture de test — le seul mécanisme de jeu produisant une Rainelle, la rencontre de la grenouille C2.3, reste plafonné à une seule par partie) sans ouvrir de session ni suspendre l'horloge, distingue le message de remplacement, refuse une Rainelle inconnue ; un aller-retour JSON réel garde `campaignTeaching` (leçon en cours) et `campaignLastDemonstration` strictement identiques ; migration silencieuse d'une sauvegarde sans les deux nouveaux champs, comparaison JSON complète avant/après ; sept formes malformées de `campaignTeaching` rejetées (Rainelle inconnue, étape invalide, brouillon présent en observation, brouillon absent en relecture, phrase vide, trajectoire vide, verbe invalide) et une forme valide acceptée ; quatre formes malformées de `campaignLastDemonstration` rejetées, `null` et une forme valide acceptées ; `sleep` referme une leçon restée ouverte sans coût et sans laisser l'horloge bloquée ; `teachGestureQuick` refusé pendant qu'une leçon est en cours (sur la Rainelle visée par la leçon et sur une autre) ; chacun des six verbes produit une phrase non vide (et lève une erreur explicite sur un verbe inconnu) ; chaque verbe reste sous la borne de sauvegarde de 240 caractères même avec quatre champs à leur longueur maximale de 40 caractères.

**Résultat réellement exécuté** : `npm ci` puis `npm test` → 258 tests de règles/géométrie passent (239 existants + 19 nouveaux dans `campaign-teaching.cjs`, zéro régression), plus les vérifications HTML/site existantes. Sortie complète :

```
# tests 258
# suites 0
# pass 258
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

**Test navigateur ciblé, réellement exécuté** (nécessaire car `index.html` a changé) : serveur local `python3 -m http.server 4174 --directory public`, page chargée avec Playwright (`chromium.launch({executablePath: "/opt/pw-browsers/chromium"})`, seule révision Chromium disponible hors ligne dans cet environnement, limitation déjà documentée aux epics C1.4/C1.6/C1.7/C1.8) : **zéro erreur console/page**, `window.GardenRainelles.applyGesture`/`defaultPhrase` exposés. Un scénario complet exécuté dans la page réelle (`GardenRules.GardenState`, `triggerFrogEncounter` → `sowPot` → `sleep` → `beginTeaching` → `demonstrateGesture` → `confirmTeaching`) confirme : horloge en pause après `beginTeaching`, phrase générée cohérente (« Remplir l'arrosoir à borne-1 ; humidifier les plantes de zone-fraisiers. »), horloge relancée et geste exactement appliqué après `confirmTeaching`. `tests/garden-material-audit.cjs` (le gate complet de `npm run test:browser`) relancé aussi : `PASS material/geometry audit: {"materialsInspected":90,"transparentAllowed":1,"timesSampled":5}`, strictement identique à C2.4 — aucun rendu nouveau, donc aucun nouveau matériau. `npm run test:browser` complet non rejoué de bout en bout : limitation d'environnement déjà documentée aux epics précédents (`garden-play.cjs` expire sur un délai de déplacement chronométré, non lié à ce diff) ; les vérifications directement pertinentes pour cet epic ont, elles, réellement tourné et sont vertes. `npm run test:visual` non exécuté : aucune géométrie/matériau nouveau, aucun fichier de rendu touché.

Aucune relecture narrative adverse requise au sens strict de l'orchestration (ce n'est pas un epic de Scénariste — aucun personnage, aucun dialogue de chapitre) ; les phrases générées par `defaultPhrase` ont néanmoins été relues contre le registre donné par l'exemple du design lui-même (§5, « Récolter les fruits mûrs dans ce carré ; déposer dans ce panier. ») et ne touchent à aucun choix déjà confirmé (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne).

## Vérification locale du 18 septembre 2026 — refus de multiplication, réenseignement gratuit (epic C2.10)

Déclenchement automatisé (routine cloud horaire). Anti-hallucination fait avant tout le reste : dernier epic « fait » du backlog était C2.5, commit `9968f29a2804631d37de013272c5f54dc4ec29b1` — `git show --stat 9968f29` confirme le commit réel et son contenu (12 fichiers, dont `tests/campaign-teaching.cjs` +469 lignes) correspond exactement à l'entrée existante ; `npm ci && npm test` relancés indépendamment → 258/258, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent.

**Choix de l'epic : C2.6 sauté délibérément malgré une dépendance satisfaite (C2.5, fait), au profit de C2.10 (dépendance C2.4, fait également).** Raison détaillée dans le journal des décisions de `campagne-backlog.md` : le critère de sortie littéral de C2.6 suppose trois mécaniques qui n'existent encore nulle part dans le code (un registre de bornes/paniers, une humidité de zone, une maturité/production de spécimen) — `poste`/`source`/`destination` restent des identifiants de chaîne opaques depuis C2.4 (voir le commentaire d'en-tête de `rainelles.js`), et `s.specimens` (C1.6) n'a ni humidité ni production. L'implémenter aurait exigé une refonte simultanée de plusieurs systèmes, ce qu'`orchestration.md` interdit explicitement pour un seul epic. C2.10 est en comparaison un epic de schéma pur et petit, dont la dépendance (C2.4) est satisfaite depuis longtemps et dont la règle centrale (exclusion de `multiplier`) existe déjà — restait à en faire un événement explicite plutôt qu'une erreur générique, et à vérifier le second volet du critère (« réenseigner ne coûte rien ») plutôt que de le supposer.

Design §5 (« Multiplication et vie propre ») : « Les Rainelles ne se bouturent pas elles-mêmes et n'acceptent aucun ordre visant à multiplier une autre Rainelle. Cette limite est montrée dès les premières tentatives : elles protègent leur bourgeon, puis reprennent tranquillement leur activité. » Jusqu'ici, tenter d'enseigner le verbe `multiplier` (déjà exclu de `VERBS` depuis C2.4) renvoyait le même message générique « Geste inconnu. » que n'importe quelle faute de frappe — un refus techniquement présent mais pas « montré » comme un événement distinct.

- `public/game/rainelles.js` : `validateGestureFields` teste désormais `verbe === "multiplier"` en tout premier, avant le test générique `VERBS.includes(verbe)`, et renvoie un message dédié exporté (`Rainelles.MULTIPLY_REFUSAL` = « Elle protège son bourgeon : ce geste ne s'apprend jamais à une Rainelle. »). Comme `teachGesture` (`garden-state-cmd-j.js`, via `applyGesture`) et `demonstrateGesture` (`garden-state-cmd-k.js`) sont les deux seuls points d'entrée qui assignent jamais un verbe à `geste`, et qu'ils passent tous deux par cette même fonction, le refus explicite couvre les deux chemins par construction — aucune troisième voie à couvrir séparément. Aucun changement à `garden-state-validate.js` : la forme persistée d'un `geste` (cinq champs, `verbe` dans `VERBS`) est inchangée, `multiplier` était déjà rejeté au chargement comme avant.
- Le second volet du critère (« réenseigner un geste ne coûte rien ») ne demandait aucun changement de code — vrai par construction depuis C2.4 (aucune commande de geste ne lit ni n'écrit `s.inventory`) — mais était jusqu'ici seulement supposé, jamais testé explicitement pour ce critère précis.
- Nouveau `tests/campaign-refusal.cjs` (8 tests, ajouté à la commande `test` de `package.json`) : `teachGesture` sur `multiplier` renvoie exactement `MULTIPLY_REFUSAL` (pas le message générique), sans créer de geste ; un autre verbe inconnu (`voler`) renvoie toujours le message générique, prouvant que les deux restent distincts ; une tentative `multiplier` n'écrase jamais un geste déjà enseigné ; `demonstrateGesture` refuse `multiplier` de la même façon, en laissant la leçon en `step: "watching"` sans brouillon (pas de session corrompue) ; réenseigner un geste par le chemin direct (`teachGesture`) et par le chemin en quatre moments (`beginTeaching`/`demonstrateGesture`/`confirmTeaching`) ne modifie `s.inventory` dans aucun des deux cas ; `multiplySpecimen` appelé avec l'id d'une Rainelle échoue explicitement (« Spécimen source inconnu. »), sans créer de spécimen ni toucher à la Rainelle — jamais un no-op silencieux ; le `multiply` du monde libre (établi/pépinière) ne peut structurellement pas viser une Rainelle (espaces d'id disjoints `r<n>`/`e<n>`, la Rainelle reste intacte après le refus).

**Résultat réellement exécuté** : `npm ci` puis `npm test` → d'abord 5 échecs sur les 8 nouveaux tests (`r.error` au lieu de `r.message` — la forme réelle renvoyée par `fail()` dans `garden-state.js` est `{ok, message}`, pas `{ok, error}` ; corrigé dans le test, pas dans le code de production, avant tout commit). Après correction : **266/266 tests passent** (258 existants + 8 nouveaux dans `campaign-refusal.cjs`, zéro régression). Sortie complète :

```
# tests 266
# suites 0
# pass 266
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

`/code-review` (niveau medium) exécuté sur le diff : aucun défaut relevé — confirme notamment que le cas `multiplier` est testé avant le test générique `VERBS.includes` (donc prioritaire), que le message se propage correctement via `fail()` sur les deux chemins (`teachGesture`, `demonstrateGesture`), et qu'aucun troisième point d'entrée n'assigne un verbe à `geste` en contournant `validateGestureFields`.

Aucun test navigateur (`test:browser`/`test:visual`) : `public/index.html` n'a pas changé (`rainelles.js` était déjà chargé depuis C2.3), aucun fichier de rendu touché, aucune géométrie/matériau nouveau — hors du périmètre déclenché par cet epic (voir `execution-continue.md`, ces vérifications ne sont requises que si le rendu ou l'interface sont concernés).

Aucune relecture narrative adverse requise au sens strict de l'orchestration (pas un epic de Scénariste au sens du chapitre de campagne — aucun dialogue de personnage nommé) ; le message `MULTIPLY_REFUSAL` a néanmoins été relu contre design §5 lui-même (« elles protègent leur bourgeon, puis reprennent tranquillement leur activité » — le message choisi ne fabrique aucune faute, aucune accusation, reste factuel sur le refus) et ne touche à aucun choix déjà confirmé (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne).

## Vérification locale du 18 septembre 2026 — registre des bornes d'eau, zones de culture et paniers de campagne (epic C2.6a)

Déclenchement automatisé (routine cloud horaire). Anti-hallucination fait avant tout le reste : dernier epic « fait » du backlog était C2.10, commit `3cc89c9b9826b0399a78ef476256f1046437546e` — `git show --stat 3cc89c9` confirme le commit réel (5 fichiers, correspond exactement à l'entrée de ce fichier ci-dessus) ; `npm ci && npm test` relancés indépendamment → 266/266, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent.

**Choix de l'epic : C2.6a (dépendance C2.4, fait), premier des deux tiers de la reformulation de C2.6 disponibles ce déclenchement** (l'autre, C2.6b, dépend de C1.6 également fait — aucune raison de préférer l'un à l'autre autrement que l'ordre déjà écrit dans le backlog ; C2.6c reste indisponible, dépendant des deux). C2.2v/C2.5v restent sautés pour la même raison inchangée depuis plusieurs déclenchements (absence de scène « maison » de campagne, chantier de la phase 3).

Design §5 : le tableau des gestes distingue trois types d'équipement (bornes d'eau, zones/carrés de culture, paniers) que `poste`/`source`/`destination` (C2.4/C2.5) nomment déjà en texte libre, sans aucune entrée réelle derrière. Cet epic pose le registre et sa résolution, sans toucher au comportement existant de `teachGesture`/`demonstrateGesture`.

- Nouveau `public/game/campaign-stations.js` : trois collections séparées (`bornes`/`zones`/`paniers`, chacune avec son propre compteur `borneNextId`/`zoneNextId`/`panierNextId`) plutôt qu'un registre plat — une même chaîne (`poste`, `source` ou `destination`) peut nommer n'importe lequel des trois selon le verbe (`arroser` : `source` = borne, `poste` = zone ; `recolter` : `destination` = panier — voir `PHRASE_BUILDERS` dans `rainelles.js`), donc `resolveStation` cherche dans les trois collections sans qu'on ait à lui dire laquelle interroger. Préfixes d'id disjoints (`b`/`z`/`pn`) garantissant qu'un id n'est jamais ambigu entre types. `registerStation(registry, kind, {x, z})` : fonction pure, même patron « posée, pas encore câblée à une commande » que `Cultivars.createCultivar`/`Rainelles.createRainelle` à leur propre introduction — aucune commande ne l'appelle encore, aucune scène de campagne n'existe pour y placer une borne/zone/panier (même lacune que C2.2/C2.5 ont laissée à C2.2v/C2.5v). `resolveStation(registry, id)` : jamais un `undefined` silencieux — retourne toujours `{ok: true, kind, station}` ou `{ok: false, error}` explicite.
- `public/garden-state-lifecycle.js` : `fresh()` initialise `s.campaignStations` à un registre vide (trois tableaux vides, trois compteurs à 1).
- `public/garden-state-validate.js` : nouvel import de `campaign-stations.js` (réutilise `Stations.KINDS` pour les préfixes plutôt que de les dupliquer) ; validation de chaque collection (format d'id conforme au préfixe, positions finies bornées à [-64, 64] comme les autres entités, unicité des ids au sein de la collection, compteur strictement supérieur au plus grand id existant) ; `result.campaignStations ??= {...}` pour la migration d'une sauvegarde existante, même patron que `campaignTeaching`/`campaignLastDemonstration`.
- `public/index.html` : nouveau `<script src="/game/campaign-stations.js" defer>`, juste après `rainelles.js` et avant `garden-state-util.js`/`-validate.js` — même contrainte d'ordre déjà documentée et corrigée pour `campaign-clock.js`/`rainelles.js` (le validateur lit `GardenCampaignStations` au moment de l'exécution du script).

**Bug réel trouvé et corrigé avant tout commit, par les tests du epic lui-même (pas par `/code-review`).** Le test « duplicate id » (deux entrées `b1` dans `bornes`) ne levait pas d'erreur : la vérification d'unicité croisée entre collections (`allIds.has(st.id)`, peuplé seulement après le passage sur chaque collection) ne détectait jamais un doublon *à l'intérieur* de la même collection, puisque `allIds` était encore vide pendant le `.some()` qui parcourt cette collection-là. **Corrigé** : ajout d'une vérification d'unicité directe par collection (`new Set(list.map(st => st.id)).size !== list.length`), sur le même patron déjà utilisé pour `rainelles`/`specimens`/`cultivars` plus haut dans le même fichier.

**Résultat réellement exécuté** : `npm ci` puis `npm test` → 278 tests de règles/géométrie passent (266 existants + 12 nouveaux dans un nouveau `tests/campaign-stations.cjs`, zéro régression). Sortie complète :

```
# tests 278
# suites 0
# pass 278
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

Nouveau `tests/campaign-stations.cjs` (12 tests, ajouté à la commande `test` de `package.json`) : une sauvegarde neuve déclare un registre vide aux trois collections/compteurs à 1 ; `registerStation` assigne un id stable préfixé par type et l'ajoute à la bonne collection ; les compteurs sont bien par-type (jamais partagés) ; un type inconnu est refusé explicitement, sans créer d'entrée malformée ; `resolveStation` trouve un id connu dans n'importe laquelle des trois collections et échoue explicitement (jamais un `undefined`) sur un id inconnu, y compris contre un registre vide ; un aller-retour JSON réel garde un registre peuplé strictement identique ; une sauvegarde d'avant cet epic (sans `campaignStations`) migre vers le registre vide par défaut sans erreur ; un registre peuplé bien formé valide sans changement ; un registre malformé (préfixe d'id incorrect, id dupliqué, position non finie, compteur resté en retard sur l'id maximal existant) est rejeté dans les quatre cas ; `teachGesture` produit exactement le même résultat que le registre soit vide ou peuplé, preuve directe qu'aucune commande de geste existante ne change de comportement.

**Test navigateur ciblé, réellement exécuté** (nécessaire car `index.html` a changé) : serveur local `python3 -m http.server 4174 --directory public`, page chargée avec Playwright (`chromium.launch({executablePath: "/opt/pw-browsers/chromium"})`) : **zéro erreur console/page**, `window.GardenCampaignStations` exposé avec les trois types (`borne`/`zone`/`panier`). Scénario complet exécuté dans la page réelle (`GardenRules.GardenState`, `registerStation` deux fois, `resolveStation` sur un id connu puis un id inconnu) : résolution correcte du type et de l'entrée, échec explicite (`"Identifiant de station inconnu : \"z999\"."`) sur l'id absent. `tests/garden-material-audit.cjs` (le gate complet de `npm run test:browser`) relancé aussi : `PASS material/geometry audit: {"materialsInspected":90,"transparentAllowed":1,"timesSampled":5}`, strictement identique aux epics précédents — aucun rendu nouveau, donc aucun nouveau matériau. `npm run test:browser` complet non rejoué de bout en bout : même limitation d'environnement déjà documentée aux epics précédents (`garden-play.cjs` expire sur un délai de déplacement chronométré, non lié à ce diff) ; les vérifications directement pertinentes pour cet epic (`garden-material-audit.cjs`, la vérification ciblée `index.html`/zéro erreur console/scénario `campaign-stations` complet) ont, elles, réellement tourné et sont vertes. `npm run test:visual` non exécuté : aucune géométrie/matériau nouveau, aucun fichier de rendu touché.

`/code-review` (niveau medium) exécuté deux fois sur le diff. Premier passage : a relevé que la vérification d'unicité croisée entre collections (`allIds`) était du code mort — les préfixes disjoints (`b`/`z`/`pn`) rendent une collision entre collections structurellement impossible, donc `allIds` ne pouvait jamais correspondre à rien au-delà de ce que la vérification par-collection couvre déjà (une fois celle-ci ajoutée par le correctif ci-dessus). Retiré. Second passage après le retrait : aucun défaut relevé.

Aucune relecture narrative adverse requise (pas un epic de Scénariste — aucun dialogue, aucun personnage) ; aucun choix déjà confirmé touché (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne, direction artistique — cet epic ne produit aucun rendu).

## Vérification locale du 18 septembre 2026 — humidité et maturité des spécimens de campagne (epic C2.6b)

Déclenchement automatisé (routine cloud horaire). Anti-hallucination fait avant tout le reste : dernier epic « fait » du backlog était C2.6a, commit `33bb83c84602813b4503a7c226297c63c847fbb5` — `git show --stat 33bb83c` confirme le commit réel (8 fichiers, correspond exactement à l'entrée de ce fichier ci-dessus) ; `npm ci && npm test` relancés indépendamment → 278/278, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent.

**Choix de l'epic : C2.6b (dépendance C1.6, fait), seul candidat disponible à dépendances satisfaites** (C2.2v/C2.5v restent sautés pour la même raison inchangée depuis plusieurs déclenchements — absence de scène « maison » de campagne, chantier de la phase 3 ; C2.6c reste indisponible, dépendant de C2.6a *et* C2.6b).

Design §5 (tableau des gestes) et §4 (grammaire des attributs, « Cycle », « Production ») supposent qu'un spécimen a un niveau d'humidité et un état de maturité, mais `s.specimens` (C1.6) ne portait jusqu'ici que `{id, cultivarId, x, z, stage}`. Cet epic ajoute les deux, sans câbler aucune commande ni aucune boucle de tick dessus (réservé à C2.6c, une fois C2.6a et ce epic disponibles ensemble).

- `public/game/cultivars.js` : humidité **paresseuse**, jamais un champ décrémenté à chaque tick — aucune boucle de simulation ne touche encore `s.specimens` (vérifié : aucune référence dans `garden-state.js#tick()`), donc un champ mutable que rien ne fait avancer resterait silencieusement faux pour toujours. À la place, `createSpecimen` pose `moistureAt: s.elapsed` (l'instant où le spécimen a été « pleinement humide » pour la dernière fois — la création compte comme un arrosage) ; `specimenMoisture(specimen, elapsed)` recalcule à la demande `max(0, 100 - taux × (elapsed - moistureAt))`, exactement le principe déjà cité par le critère de sortie (« même principe que le dopage `boostUntil` du jardin libre » : un timestamp stocké comparé à l'elapsed courant, jamais une valeur mutée en direct). `waterSpecimen(specimen, elapsed)` réinitialise l'horloge de séchage à « pleinement humide maintenant » — posée mais non encore appelée par aucune commande, comme `registerStation`/`createCultivar` à leur propre introduction. Taux de séchage choisi arbitrairement (séchage complet en 3h de temps simulé écoulé) : aucune automatisation n'existe encore pour le calibrer contre un vrai geste d'arrosage (C2.6c), donc seule la déterminisme du calcul compte pour ce critère de sortie, pas la valeur exacte.
- Maturité dérivée du stade, jamais stockée redondamment : `isMature(specimen)` compare `specimen.stage` à `MATURE_STAGE = 2`, dupliqué en dur depuis la constante équivalente de `botany-hybrids.js` (`STAGE_COUNT`/`MATURE_STAGE`, C1.8) plutôt qu'importé — ce fichier ne s'exécute pas et ne définit rien sans `THREE.js` chargé (module de rendu navigateur uniquement), et la plupart du code/tests moteur tournent sans `THREE`. Commentaire de synchronisation posé des deux côtés.
- `setReadyToProduce(specimen, ready)` : le seul garde-fou exigé par le critère de sortie — refuse explicitement (lève une erreur, jamais un `undefined` silencieux) de marquer prêt à produire un spécimen dont le stade n'est pas mature ; ne mute jamais le spécimen sur un refus. Lève une erreur plutôt que de renvoyer une forme `{ok, message}` : c'est un invariant de couche schéma (comme les `throw` déjà en usage dans `garden-state-validate.js`), pas encore atteignable depuis aucune commande.
- `public/garden-state-validate.js` : `moistureAt`/`readyToProduce` restent des champs optionnels sur un spécimen existant (validés seulement s'ils sont présents, même patron que `disposition` sur un cultivar depuis C1.4) — mais la migration, elle, leur donne une vraie valeur par défaut sur **chaque** spécimen d'une sauvegarde existante (`result.specimens = (result.specimens ?? []).map((sp) => ({moistureAt: s.elapsed ?? 0, readyToProduce: false, ...sp}))`), pas seulement sur le tableau vide comme les migrations `??=` déjà en place pour les autres champs de campagne. `moistureAt` par défaut vaut l'`s.elapsed` **de la sauvegarde elle-même** (pas `0`) : un spécimen d'avant cet epic apparaît ainsi « pleinement humide à l'instant du chargement » plutôt que artificiellement asséché depuis le début de la partie. Invariant `readyToProduce === true ⇒ stade mature` revérifié indépendamment ici (pas seulement dans `setReadyToProduce`), au cas où une sauvegarde malformée contournerait la fonction pure.
- Aucun changement à `public/garden-state-lifecycle.js` : une sauvegarde neuve démarre avec `s.specimens = []` (déjà le cas depuis C1.6), donc aucun spécimen n'existe encore à qui donner une valeur par défaut à `fresh()` — contrairement à ce que le fichier probable du backlog anticipait, la totalité de la migration tient dans `garden-state-validate.js`.

**Test existant mis à jour, pas seulement des tests neufs ajoutés.** `tests/campaign-cultivars.cjs` contenait une assertion `deepEqual` sur la forme exacte d'un spécimen fraîchement créé (`{id, cultivarId, x, z, stage}`) — mise à jour pour inclure les deux nouveaux champs (`moistureAt: 0, readyToProduce: false`), sans quoi elle aurait échoué sur un changement de schéma pourtant correct. Vérifié qu'aucun autre test du dépôt ne fait de comparaison stricte de forme sur un spécimen (`grep` sur `deepEqual.*specimen` dans `tests/`) : les autres comparaisons sont relatives (sauvegarde vs rechargement), donc insensibles à l'ajout de champs.

Nouveau `tests/campaign-specimen-lifecycle.cjs` (13 tests, ajouté à la commande `test` de `package.json`) : `createSpecimen` pose `moistureAt` à l'`s.elapsed` courant et `readyToProduce` à `false` ; `specimenMoisture` vaut exactement 100 à `moistureAt` puis décroît selon la formule exacte à plusieurs durées écoulées (1, 100, 3600, 5000 secondes simulées) ; ne descend jamais sous 0 même très loin dans le futur et reste pure (deux appels identiques renvoient la même valeur) ; ignore un elapsed antérieur à `moistureAt` (jamais une durée négative) ; `waterSpecimen` réinitialise l'horloge et restaure 100 % à l'instant donné ; `isMature` est vrai uniquement au dernier stade (2), faux aux stades 0 et 1 ; `setReadyToProduce` refuse un spécimen immature (message contenant « immature », spécimen non muté) et accepte un spécimen mature (peut ensuite être démarqué à `false` quel que soit le stade) ; une sauvegarde avec `readyToProduce: true` sur un spécimen immature est rejetée au chargement, la même combinaison sur un spécimen mature charge sans erreur ; cinq formes malformées de `moistureAt`/`readyToProduce` (négatif, infini, chaîne, valeur non booléenne) sont rejetées ; un spécimen sauvegardé avant cet epic (champs absents) migre vers `moistureAt` = l'`s.elapsed` de la sauvegarde et `readyToProduce: false`, sans toucher `stage`/`x`/`z` ; un aller-retour JSON réel garde `moistureAt`/`readyToProduce` strictement identiques.

**Résultat réellement exécuté** : `npm ci` puis `npm test` → 291 tests de règles/géométrie passent (278 existants + 13 nouveaux dans `campaign-specimen-lifecycle.cjs`, zéro régression). Sortie complète :

```
# tests 291
# suites 0
# pass 291
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

`/code-review` (niveau medium) exécuté sur le diff : aucun défaut relevé — confirme notamment qu'aucune boucle de tick/automatisation ne touche `s.specimens`, que les nouveaux champs sont additifs et migrés en sécurité, que l'invariant de maturité est vérifié à la fois dans le mutateur et indépendamment dans la validation, et qu'aucun autre site d'appel ni test n'est affecté par les nouveaux champs.

Aucun test navigateur (`test:browser`/`test:visual`) : `public/index.html` n'a pas changé (`cultivars.js` était déjà chargé depuis C1.4), aucun fichier de rendu touché, aucune géométrie/matériau nouveau — hors du périmètre déclenché par cet epic (voir `execution-continue.md`, ces vérifications ne sont requises que si le rendu ou l'interface sont concernés).

Aucune relecture narrative adverse requise (pas un epic de Scénariste — aucun dialogue, aucun personnage) ; aucun choix déjà confirmé touché (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne, direction artistique — cet epic ne produit aucun rendu).

## Vérification locale du 18 septembre 2026 — gestes Arroser et Récolter, branchés sur le registre et les spécimens (epic C2.6c)

Déclenchement automatisé (routine cloud horaire). Anti-hallucination fait avant tout le reste : dernier epic « fait » du backlog était C2.6b, commit `86ce50a1c27fd095e696d442f0a7f713ca7b9e95` — `git show --stat 86ce50a` confirme le commit réel (7 fichiers, correspond exactement à l'entrée de ce fichier ci-dessus) ; `npm ci && npm test` relancés indépendamment → 291/291, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent.

**Choix de l'epic : C2.6c (dépendances C2.6a/C2.6b/C2.5, toutes fait), seul candidat disponible** (C2.2v/C2.5v restent sautés pour la même raison inchangée depuis plusieurs déclenchements — absence de scène « maison » de campagne, chantier de la phase 3). Troisième et dernier tiers de la reformulation de C2.6 : le registre de stations (C2.6a) et l'humidité/maturité des spécimens (C2.6b) existent désormais tous les deux, rendant le critère de sortie littéral de C2.6 réalisable sans inventer de sous-système supplémentaire.

**Délégation à un sous-agent Artisan moteur, puis vérification indépendante avant commit.** Vu la taille de l'epic (première exécution réelle de la couche campagne à chaque tick, jamais seulement posée jusqu'ici), l'implémentation a été confiée à un sous-agent avec un mandat complet et autoporteur. Conformément à la mise en garde explicite d'`orchestration.md` (« le rapport d'un agent délégué n'est jamais la preuve »), l'orchestrateur a ensuite : relu ligne à ligne l'intégralité du diff produit (`git diff --stat`, puis chaque fichier) ; relancé lui-même `npm test` indépendamment (301/301, confirmé identique au rapport du sous-agent) ; rejoué lui-même une vérification Playwright ciblée contre le serveur local, avec un scénario différent de celui du sous-agent (créer une Rainelle via la rencontre scriptée, un spécimen mûr, une zone et un panier, enseigner « récolter », avancer le temps) — zéro erreur console, `window.GardenCampaignAutomation` exposé avec les bonnes constantes, buffer du panier peuplé exactement comme attendu (`{c1: 1}`) après un cycle complet.

Design §5 (tableau des gestes) : « Arroser » (borne du poste → plantes de la zone humidifiées, « la borne doit être alimentée ; petit rayon de travail ») et « Récolter » (plantes mûres → produits dans un panier adjacent, « aucun transfert vers un atelier distant »). Jusqu'ici `poste`/`source`/`destination` (C2.4/C2.5) étaient de simples chaînes jamais résolues, et aucune boucle ne touchait `s.rainelles`/`s.specimens`/`s.campaignStations` à chaque tick.

- Nouveau `public/game/campaign-automation.js` : `tickRainelle(rainelle, s)` dispatché sur `geste.verbe` (seuls `arroser`/`recolter` implémentés ; `transporter`/`replanter`/`preparer`/`trier` restent des no-op silencieux, réservés à C2.7+ — enseigner l'un de ces quatre verbes réussit déjà depuis C2.4 mais ne fait toujours rien ici, ce qui est le comportement honnête attendu tant que son epic n'existe pas). Fichier volontairement séparé de `game/automation.js` : ce dernier opère sur `s.entities`/`D.recipes`, un espace de données totalement disjoint des Rainelles/spécimens/stations de campagne — le patron `job`/`buffer` cité par le critère de sortie (countdown minuté façon `tickJob`, objet de remplissage clé→quantité façon `tickBuffer`) est réimplémenté contre ce nouvel espace de données, jamais importé.
  - `arroser` : résout `geste.source` (doit être une `borne`) et `geste.poste` (doit être une `zone`) via `resolveStation` (C2.6a) ; résolution échouée ou mauvais type de station → Rainelle inactive, jamais d'exception (pas encore de système d'états lisibles, C2.8). Sinon, countdown `rainelle.job = {remaining}` (20 secondes simulées, `CYCLE_SECONDS`) ; à 0, arrose (`Cultivars.waterSpecimen`) tout spécimen à moins de 4 unités (`ZONE_WORK_RANGE`) de la position de la zone, puis relance le cycle — sans intervention manuelle par construction.
  - `recolter` : résout `geste.poste` (zone) et `geste.destination` (panier). Même countdown ; à 0, tout spécimen mûr et `readyToProduce` en portée de la zone est déposé dans `panier.buffer[cultivarId]` (clé = id du cultivar, faute de notion de « produit » distincte pour un cultivar de campagne) puis `Cultivars.setReadyToProduce(specimen, false)`.
  - `updateSpecimenReadiness(s)` : mécanisme minimal comblant un vrai trou laissé par C2.6b (rien ne mettait jamais `readyToProduce` à `true`) — un spécimen mûr encore non prêt le devient automatiquement au tick suivant. **Limite honnête assumée et documentée dans le code** : sans minuteur de régénération ni quota, un spécimen resté mûr est re-récolté à chaque cycle complet tant qu'une Rainelle le dessert, indéfiniment — un vrai cycle de production (cooldown, limite de rendement) reste un chantier futur, hors périmètre de cet epic.
  - Portée (`ZONE_WORK_RANGE = 4`) et cadence (`CYCLE_SECONDS = 20`) : aucune valeur du design n'est chiffrée ; reprises des précédents les plus proches déjà dans le code (`D.recipes.collector.range` pour un rayon de récolte fixe, `D.recipes.seedDispenser.dispense.every` pour une cadence répétitive courte) plutôt qu'inventées, facilement réajustables une fois un vrai geste observé en jeu.
- `public/garden-state.js#tick()` : nouvelle boucle `for (const r of s.rainelles) CampaignAutomation.tickRainelle(r, s)`, précédée de `updateSpecimenReadiness(s)`, ajoutée sans toucher à la boucle existante sur `s.entities`. Première fois que `s.rainelles`/`s.specimens`/`s.campaignStations` sont réellement exécutés à chaque tick, pas seulement posés — comblant le gap documenté depuis C2.3/C2.6a/C2.6b.
- `public/game/rainelles.js` : `createRainelle` pose `job: null` (jamais touché par `applyGesture` — réenseigner un geste, design « réenseigner remplace le geste », laisse un cycle en cours poursuivre son décompte, le minuteur ne faisant pas partie de ce qui a été enseigné).
- `public/game/campaign-stations.js` : `registerStation` pose `buffer: {}` sur un panier à sa création (seul type de station qui en a besoin).
- `public/garden-state-validate.js` : validation stricte de `rainelle.job` (objet `{remaining}` borné à `[0, CYCLE_SECONDS]`, optionnel) et de `panier.buffer` (clés = id de cultivar existant, valeurs = quantités finies non négatives, optionnel) ; migration d'une sauvegarde existante (`rainelles.map(r => ({job: null, ...r}))`, `paniers.map(p => ({buffer: {}, ...p}))`), même patron que les migrations de champ déjà en place pour `specimens`/`campaignStations`.
- **Bug latent réel trouvé et corrigé pendant la vérification navigateur, avant tout commit** (pas par les tests Node, qui ne peuvent pas le voir — `require()` ignore l'ordre des balises `<script>`) : `public/index.html` chargeait `cultivars.js` *après* `garden-state-validate.js`, alors que ce dernier lit `root.GardenCultivars` depuis C2.6b — resté dormant tout ce temps parce qu'aucune sauvegarde n'avait jamais eu `readyToProduce: true` avant que le tick de cet epic n'en crée un. Corrigé en déplaçant `cultivars.js` avant `garden-state-validate.js` (il n'a aucune dépendance propre, comme `rainelles.js`/`campaign-stations.js`), et `campaign-automation.js` juste après ses trois dépendances (`construction.js`, `campaign-stations.js`, `cultivars.js`).

**Résultat réellement exécuté (par le sous-agent, puis reconfirmé indépendamment par l'orchestrateur)** : `npm ci` puis `npm test` → **301/301** (291 existants + 10 nouveaux dans `tests/campaign-automation.cjs`, zéro régression ; une assertion existante de `tests/campaign-stations.cjs` mise à jour pour refléter le nouveau champ `buffer: {}` d'un panier fraîchement créé). Sortie complète (rejouée par l'orchestrateur) :

```
# tests 301
# suites 0
# pass 301
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

Nouveau `tests/campaign-automation.cjs` (10 tests) : arroser humidifie un spécimen en portée sans aucune commande autre que l'avancement du temps (humidité <1 % → >95 % après un cycle) ; un spécimen hors portée n'est jamais affecté ; une source/un poste inconnu ou du mauvais type laisse la Rainelle inactive sans jamais lever d'exception ; récolter fait passer un spécimen mûr à `readyToProduce` automatiquement puis le dépose dans le panier destination, `readyToProduce` retombant à `false` (et un second cycle le récolte à nouveau, prouvant explicitement la limite de régénération non bornée documentée ci-dessus) ; un spécimen immature n'est jamais récolté ; un spécimen mûr hors portée n'est jamais récolté ; les quatre verbes hors périmètre ne créent jamais de job et ne plantent jamais ; une sauvegarde sans `job`/`buffer` (pré-epic) migre sans erreur vers les valeurs par défaut ; `validate()` rejette un `job`/`buffer` malformé ; un aller-retour JSON réel garde `rainelles`/`campaignStations` strictement identiques.

**Vérification Playwright ciblée, réellement exécutée deux fois** (nécessaire car `index.html` a changé) — une fois par le sous-agent, une seconde fois indépendamment par l'orchestrateur avec un scénario légèrement différent pour ne pas se contenter de rejouer le même test : serveur local (`python3 -m http.server ... --directory public`), page chargée avec Playwright (`chromium.launch({executablePath: "/opt/pw-browsers/chromium"})`). Les deux fois : **zéro erreur console/page**, `window.GardenCampaignAutomation` exposé (`CYCLE_SECONDS: 20`, `ZONE_WORK_RANGE: 4`). Scénario de l'orchestrateur (rencontre scriptée → cultivar/Rainelle, `registerStation` zone/panier, spécimen mûr, `applyGesture` « recolter », `g.step(25)`) : `panier.buffer` = `{c1: 1}`, `rainelle.job.remaining` = 15 (cohérent avec un cycle de 20 secondes déjà consommé une fois puis 5 secondes dans le suivant) — comportement exactement attendu. `tests/garden-material-audit.cjs` non requis et non rejoué séparément : aucun rendu/géométrie/matériau touché par cet epic (voir `execution-continue.md`, cette exemption est la même que pour C2.6a/C2.6b), seule la vérification navigateur ciblée l'était parce qu'`index.html` change. `npm run test:visual` non exécuté, même raison.

`/code-review` (niveau medium) exécuté par le sous-agent sur son diff complet : aucun défaut relevé. L'orchestrateur a également relu l'intégralité du diff (`campaign-automation.js`, `garden-state.js`, `garden-state-validate.js`, `rainelles.js`, `campaign-stations.js`, `index.html`, les tests) avant de committer, sans trouver de défaut supplémentaire ni de divergence entre le rapport du sous-agent et l'état réel du dépôt.

Aucune relecture narrative adverse requise (pas un epic de Scénariste — aucun dialogue, aucun personnage) ; aucun choix déjà confirmé touché (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne) ; aucune teinte/matériau introduit, donc rien à comparer à `direction-artistique.md` (epic entièrement moteur, aucun rendu).

**Limites honnêtes restantes, documentées dans le code et non dissimulées** : pas de vrai cycle de régénération de production (un spécimen mûr desservi est re-récolté indéfiniment, sans cooldown ni quota) ; pas de système d'états de blocage lisible par le joueur (« source vide », « poste manquant »... — toujours C2.8) ; `transporter`/`replanter`/`preparer`/`trier` restent des no-op complets (C2.7 et epics ultérieurs) ; une zone reste un point, pas encore un polygone (le registre C2.6a ne stocke qu'une position), donc « en portée de la zone » est une approximation par distance fixe.

## Vérification locale du 18 septembre 2026 — geste Transporter (epic C2.7)

Déclenchement automatisé (routine cloud horaire). Anti-hallucination fait avant tout le reste : dernier epic « fait » du backlog était C2.6c, commit `91e166575376b9b8990d644b8b883bbb29522feb` — `git show --stat 91e1665` confirme le commit réel (11 fichiers, correspond exactement à l'entrée de ce fichier ci-dessus) ; `npm ci && npm test` relancés indépendamment → 301/301, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Les trois derniers epics consignés (C2.6a/C2.6b/C2.6c) sont tous `fait` : aucune pause anti-emballement à déclencher.

**Choix de l'epic : C2.7 (dépendance C2.6c, fait), l'un des deux seuls candidats disponibles avec C2.8 (même dépendance).** C2.2v/C2.5v restent sautés pour la raison inchangée depuis plusieurs déclenchements (absence de scène « maison » de campagne, chantier de la phase 3). Entre C2.7 et C2.8, choisi C2.7 : premier des deux dans l'ordre du backlog, et prérequis naturel de C2.11 (chaîne « fraises de la cuisine ») au même titre que C2.8 — aucune raison de préférer l'un à l'autre autrement que par l'ordre déjà écrit. Epic de schéma/moteur pur (design §5, tableau des gestes, ligne « Transporter » : « Panier A → Panier B », limite « un trajet et un filtre de ressource actifs »), implémenté directement par l'orchestrateur sans délégation (taille jugée suffisamment petite pour une session unique).

**Ce qui existait déjà avant cet epic, contrairement à C2.6c.** Le geste « transporter » était déjà teachable depuis C2.4 (dans `Rainelles.VERBS`) et déjà doté d'une phrase (`PHRASE_BUILDERS.transporter`, C2.5) ; seule son exécution à chaque tick manquait, exactement comme documenté par le header de `campaign-automation.js` depuis C2.6c (« C2.7 (transporter) et later epics are where each gets its own tick behaviour »).

- Nouveau `tickTransporter(rainelle, s)` dans `public/game/campaign-automation.js` (pas `rainelles.js` : ce dernier ne porte que le schéma/la validation du geste, jamais son exécution — même séparation déjà en place pour arroser/récolter). Résout `geste.source`/`geste.destination` comme des `panier` via `resolveStation` (C2.6a) ; résolution échouée, mauvais type de station, ou source === destination (transporter un panier vers lui-même, configuration dégénérée) → Rainelle inactive, jamais d'exception, même posture d'échec que arroser/récolter. Sinon, même countdown de cycle que les deux autres gestes (`advanceCycle`, `CYCLE_SECONDS = 20`, réutilisé tel quel, aucune nouvelle constante) ; à 0, déplace la ressource filtrée du buffer source vers le buffer destination.
- **Filtre de ressource** : réutilise le champ `condition`, déjà existant et optionnel sur tout geste depuis C2.4 (`validateGestureFields`) — aucun nouveau champ de schéma, aucune migration de sauvegarde nécessaire. Un `condition` non vide (un cultivarId) déplace uniquement cette ressource ; un `condition` vide déplace tout le contenu courant du panier source. Les deux cas restent « un seul trajet actif » par construction : une Rainelle ne porte qu'un seul `geste` à la fois (C2.4), donc qu'une seule paire source/destination et qu'un seul filtre — aucun code supplémentaire n'était nécessaire pour cette clause, à l'identique du « petit rayon de travail » d'arroser qui n'avait rien demandé de plus que la lecture de `geste.poste`.
- `geste.poste` reste inutilisé par ce verbe (comme il l'était déjà par `PHRASE_BUILDERS.transporter` depuis C2.5) : le champ reste obligatoire au niveau du schéma générique (`validateGestureFields`, commun aux six verbes) mais le design §5 ne nomme que deux stations pour « transporter » (panier A, panier B) — documenté explicitement dans le code pour ne pas laisser croire à un oubli.

**Résultat réellement exécuté** : `npm ci` puis `npm test` → **306/306** (301 existants + 5 nouveaux dans `tests/campaign-automation.cjs`, zéro régression). Sortie complète :

```
# tests 306
# suites 0
# pass 306
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

Cinq nouveaux tests dans `tests/campaign-automation.cjs` (l'ancien test générique « les quatre verbes hors périmètre » est devenu « les trois verbes restants hors périmètre », `transporter` en étant sorti) : un déplacement filtré déplace exactement la ressource nommée par `condition`, en un seul cycle, ni avant ; un `condition` vide déplace tout le buffer source, quelles que soient les ressources qu'il contient ; source === destination ne démarre jamais de job (dégénéré, jamais d'exception) ; un id source/destination inconnu ou du mauvais type (une zone à la place d'un panier) laisse la Rainelle inactive sans jamais lever d'exception ; un test de chaîne bout en bout (« chain ») enseigne arroser/récolter/transporter à trois Rainelles distinctes (la deuxième et la troisième créées directement via `Rainelles.createRainelle`, comme le fait déjà `tests/campaign-teaching.cjs`'s `secondRainelle()`, puisque design §5 limite la rencontre scriptée du pot à la toute première Rainelle) et vérifie qu'un seul cycle de 20 secondes, sans aucune commande manuelle après l'enseignement, humidifie le spécimen, vide le panier local et crédite le panier « cuisine » — la chaîne du critère de sortie littéral de cet epic, réellement rejouée, pas seulement décrite.

Aucun test navigateur (`test:browser`/`test:visual`) : `public/index.html` n'a pas changé (aucun nouveau fichier chargé, `campaign-automation.js` l'était déjà depuis C2.6c), aucun fichier de rendu touché, aucune géométrie/matériau nouveau — hors du périmètre déclenché par cet epic (même exemption que C2.6a/b/c, voir `execution-continue.md`).

`/code-review` (niveau medium) exécuté sur le diff complet (`public/game/campaign-automation.js`, `tests/campaign-automation.cjs`) via le skill `code-review` : aucun défaut relevé. La relecture a explicitement vérifié la comparaison d'identité `from === to` contre `resolveStation` (retourne bien la même référence d'objet pour un même id, pas une copie), l'ordre de tick des Rainelles dans `garden-state.js` (correspond à l'hypothèse du test de chaîne) et la cohérence entre `PHRASE_BUILDERS.transporter`/`validateGestureFields` de `rainelles.js` (confirmant que `poste` est bien exigé par le schéma générique mais légitimement inutilisé par ce verbe).

Aucune relecture narrative adverse requise (pas un epic de Scénariste) ; aucun choix déjà confirmé touché (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne) ; aucune teinte/matériau introduit, epic entièrement moteur, aucun rendu.

**Limites honnêtes restantes, inchangées par cet epic, documentées dans le code** : pas de vrai cycle de régénération de production (C2.6c) ; pas de système d'états de blocage lisible par le joueur (C2.8) ; `replanter`/`preparer`/`trier` restent des no-op complets ; une zone reste un point, pas un polygone.

## Vérification locale du 18 septembre 2026 — réservations et états de blocage (epic C2.8, scope réduit)

Déclenchement automatisé (routine cloud horaire). Anti-hallucination faite avant tout le reste : dernier epic « fait » du backlog était C2.7, commit `fe6a901e2247954df3573eb9fb5a3d00cecb10d6` — `git show --stat fe6a901` confirme le commit réel (4 fichiers : `docs/campagne-backlog.md`, `docs/campagne.md`, `public/game/campaign-automation.js`, `tests/campaign-automation.cjs`, correspond exactement à l'entrée de ce fichier ci-dessus) ; `npm ci && npm test` relancés indépendamment → 306/306, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Les trois derniers epics consignés (C2.6c/C2.7, C2.10 avant eux) ne sont pas tous `bloqué` : aucune pause anti-emballement à déclencher.

**Choix de l'epic : C2.8 (dépendance C2.6c, fait), seul candidat disponible.** C2.2v/C2.5v restent sautés pour la raison inchangée depuis plusieurs déclenchements (absence de scène « maison » de campagne, chantier de la phase 3). C2.9/C2.11 restent hors de portée (leur propre dépendance inclut C2.8, alors encore `todo`). Epic de schéma/moteur pur (design §5, « Conditions, réservations et lecture des blocages »), implémenté directement par l'orchestrateur sans délégation.

**Scope réduit, écart documenté avant tout code écrit, pas après coup.** Le critère de sortie littéral nomme sept états, dont « passage bloqué » (design §5 : « les croisements de passage utilisent une priorité stable et un temps d'attente borné ») et un sauvetage vers un « bac de secours » pour une ressource « déjà portée » dont la cible aurait disparu. Vérification faite avant d'implémenter : une Rainelle de ce codebase n'a aujourd'hui **aucune position dans le monde** — son geste s'exécute directement sur les stations résolues par `campaign-stations.js`, jamais en s'y déplaçant (`campaign-automation.js` n'a jamais modélisé de trajet, seulement un countdown abstrait) — donc il n'existe rien qu'un « passage » puisse bloquer. De même, aucune commande n'existe encore pour retirer une borne/zone/panier une fois enregistrée, et chaque geste ramasse puis livre sa ressource dans le même pas atomique (jamais étalé sur plusieurs ticks) : une ressource ne peut donc jamais se retrouver « en transit » avec une cible qui disparaît sous elle. Construire ces deux mécaniques (déplacement réel + notion de transit multi-tick) dans ce seul epic aurait signifié inventer d'un bloc un système de navigation complet en plus du système de diagnostics demandé — exactement ce qu'interdit `orchestration.md` (« jamais une refonte simultanée de plusieurs systèmes »), le même raisonnement qui a justifié C2.2/C2.2v et C2.5/C2.5v. Reporté au nouvel epic **C2.8v** (todo, backlog), plutôt que réduit en silence ou improvisé avec une fausse simulation de déplacement.

**Ce qui est réellement livré, correspondant au reste du critère :**

- `public/game/campaign-stations.js` : un panier porte désormais `capacity`/`min` (design §5, « le réglage avancé d'un panier définit un minimum et un maximum »), posés à la création (`DEFAULT_PANIER_CAPACITY = 24`, repris de `D.recipes.collector.capacity` — même précédent déjà réutilisé pour `ZONE_WORK_RANGE` à C2.6c ; `DEFAULT_PANIER_MIN = 0`, aucun plancher par défaut). Nouvelle fonction pure `panierTotal(panier)`, la même quantité comparée aux deux bornes. `garden-state-validate.js` valide ces deux champs quand présents (capacité finie strictement positive, `min` dans `[0, capacity]`) et migre tout panier antérieur vers ces mêmes valeurs par défaut.
- `public/game/campaign-automation.js` : `tickRecolter` s'arrête proprement dès que `panierTotal(panier) >= panier.capacity` — le spécimen suivant reste `readyToProduce`, rien n'est perdu, retenté au cycle suivant. `tickTransporter` calcule un budget unique = min(place disponible à la destination, quantité prélevable à la source sans descendre sous son `min`), puis déplace chaque ressource filtrée à hauteur de ce budget partagé — jamais tout, jamais rien, exactement la quantité qui respecte les deux limites à la fois (design §5 : « un blocage arrête proprement la production, sans détruire le stock »).
- **La clause « deux Rainelles ne peuvent pas prendre la même cible »** est vraie par construction, pas par une nouvelle structure de verrouillage : `s.rainelles` est tické strictement dans l'ordre du tableau (`garden-state.js`), donc la première Rainelle à s'exécuter dans un tick a déjà mis à jour `panier.buffer`/le total avant que la seconde ne lise le même chiffre — aucune des deux ne peut compter deux fois le même mètre carré de marge. Prouvé par un test dédié (deux récolteuses visant la même zone et un panier de capacité 1) plutôt que supposé.
- Nouveau `public/game/rainelles-status.js` (chargé dans `public/index.html`, juste après `campaign-automation.js`, sa dernière dépendance — même précaution que pour campaign-clock.js/rainelles.js/cultivars.js, dont l'absence de cette ligne avait déjà cassé la page par le passé) : `status(rainelle, s)` retourne `{kind, message}`, six des sept états nommés — `repos` (aucun geste, ou un verbe sans exécution encore implémentée : `replanter`/`preparer`/`trier`), `poste-manquant` (station non résolue ou du mauvais type), `source-vide` (rien de disponible à prendre), `stock-cible-atteint` (transporter : la source est à son plancher protégé), `sortie-pleine` (récolter/transporter : la destination est à sa capacité), `au-travail` (tout va bien). `arroser` n'expose que `poste-manquant`/`au-travail` : aucune notion de capacité/pénurie n'existe encore pour une borne d'eau (documenté comme un écart déjà présent depuis C2.6a, pas introduit ici).

**Résultat réellement exécuté** : `npm ci` puis `npm test` → **329/329** (306 existants + 23 nouveaux : 3 dans `tests/campaign-stations.cjs`, 6 dans `tests/campaign-automation.cjs`, 14 dans le nouveau `tests/campaign-rainelles-status.cjs`, zéro régression). Sortie complète :

```
# tests 329
# suites 0
# pass 329
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

Nouveaux tests notables : un panier plein arrête proprement la récolte (le spécimen non récolté reste `readyToProduce`) ; deux récolteuses visant un panier de capacité 1 dans le même tick ne le dépassent jamais ; un transport vers une destination pleine ne bouge rien (source intacte) ; un transport vers une destination partiellement pleine ne déplace que ce qui tient (le reste attend en source) ; un plancher de source protège la réserve (déplace jusqu'au plancher, jamais en dessous) ; une source déjà à son plancher ne bouge rien ; `validate()` rejette une capacité nulle/négative ou un `min` hors de `[0, capacity]`, accepte `min === capacity` ; un panier pré-epic sans `capacity`/`min` migre vers les mêmes valeurs que `registerStation` poserait aujourd'hui ; les six états de `rainelles-status.js` sont chacun exercés par au moins un scénario positif et un scénario de blocage.

**Vérification navigateur ciblée, réellement exécutée** (nécessaire car `public/index.html` a changé — une nouvelle balise `<script>`) : serveur local (`python3 -m http.server 4174 --directory public`), page chargée avec Playwright (`chromium.launch({executablePath: "/opt/pw-browsers/chromium"})`, contournement documenté pour cet environnement où la version de Playwright installée par `npm ci` ne correspond pas au binaire Chromium préinstallé). Résultat : **zéro erreur console, zéro erreur de page**, `window.GardenRainellesStatus` bien défini. `tests/garden-material-audit.cjs`/`npm run test:visual` non requis et non exécutés : aucun rendu/géométrie/matériau touché par cet epic (même exemption que C2.6a/b/c/C2.7, voir `execution-continue.md`) ; la vérification ciblée ci-dessus couvre le risque réel introduit (ordre de chargement des scripts), pas un risque de rendu.

`/code-review` (niveau medium, skill `code-review`) exécuté sur le diff complet. Deux améliorations mineures relevées, toutes deux corrigées avant ce commit (pas seulement notées) : (1) `tickRecolter` recalculait `Stations.panierTotal(panier)` à chaque spécimen visité au lieu d'un compteur incrémental — remplacé par un total suivi localement, incrémenté à chaque dépôt ; (2) `rainelles-status.js` réimplémentait une fonction `distance` locale identique à celle déjà exportée par `construction.js` — remplacée par `C.distance`, `construction.js` maintenant requis par ce fichier comme il l'est déjà par `campaign-automation.js`. `npm test` relancé après ces deux corrections : toujours 329/329, aucune régression introduite par le correctif lui-même.

Aucune relecture narrative adverse requise (pas un epic de Scénariste). Aucun choix déjà confirmé touché (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne). Aucune teinte/matériau introduit — epic entièrement moteur, aucun rendu au sens de `direction-artistique.md`.

**Limites honnêtes restantes, documentées dans le code et le backlog, pas dissimulées** : « passage bloqué » et le sauvetage vers un bac de secours en transit restent non implémentés, reportés à **C2.8v** (dépend d'un modèle de déplacement réel des Rainelles à construire, probablement en phase 3) ; pas de vrai cycle de régénération de production (C2.6c, inchangé) ; `replanter`/`preparer`/`trier` restent des no-op complets, donc toujours `repos` dans `rainelles-status.js` ; une zone reste un point, pas un polygone ; aucune commande ne permet encore de régler `capacity`/`min` d'un panier en jeu (posés à la création avec une valeur par défaut, comme `buffer` l'était depuis C2.6c).

## Vérification locale du 18 septembre 2026 — chaîne de validation « fraises de la cuisine », porte de sortie de la phase 2 (epic C2.11)

Déclenchement automatisé (routine cloud horaire). Anti-hallucination faite avant tout le reste : dernier epic « fait » du backlog était C2.8, commit `a3ed61f51d37a7bc1d46c32c3ab032462af08c69` — `git show --stat a3ed61f` confirme le commit réel (11 fichiers, correspond exactement à l'entrée précédente de ce fichier) ; `npm ci && npm test` relancés indépendamment → 329/329, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Trois derniers epics consignés (C2.6c/C2.7/C2.8) ne sont pas tous `bloqué` : aucune pause anti-emballement à déclencher.

**Choix de l'epic : C2.11 (dépendances C2.7/C2.8/C2.10, toutes `fait`), préféré à C2.9 pour une raison de fond, pas seulement l'ordre du backlog.** C2.8 venant de passer `fait`, deux epics à dépendances désormais satisfaites apparaissent : C2.9 (mode d'observation) et C2.11. Vérifié avant de choisir : le critère littéral de C2.9 (« une vue dédiée montre chemins, transferts et cadence... suivre un objet du plant au présentoir ») suppose une représentation spatiale des Rainelles/stations dans le monde que le jeu ne construit toujours pas — `public/game/hud-world.js` inspecté directement, il ne porte que `drawWorldActions` (la carte ancrée du jardin libre), rien de campagne. L'implémenter maintenant aurait très probablement rejoué la même scission « v » que C2.2/C2.2v, C2.5/C2.5v et C2.8/C2.8v. C2.11, lui, est un epic de test pur sur une couche moteur déjà entièrement livrée (C2.6c/C2.7/C2.8/C2.10), sans aucune dépendance de rendu, et sa propre entrée de backlog le nomme explicitement comme la porte de sortie de la phase — le bon candidat pour clore la phase 2 plutôt que de la laisser en suspens derrière un chantier de rendu de la phase 3. Implémenté directement par l'orchestrateur (épic de test pur, aucune délégation).

**`tests/campaign-rainelles-chain.cjs` (fichier probable du backlog, aucun écart).** Une seule Rainelle enseignée à arroser, une à récolter, une à transporter (`g.command({type:"teachGesture", ...})`, le vrai vecteur de commandes plutôt que `Rainelles.applyGesture` directement, pour coller au plus près d'un joueur réel) ; un seul spécimen mûr suffit à nourrir la chaîne indéfiniment (aucun système de régénération borné n'existe encore, C2.6c — le spécimen est réarmé chaque tick). 60 cycles (1200 s simulées) suffisent à observer la double saturation propre : panier local et réserve de cuisine atteignent chacun leur capacité par défaut (24) sans jamais rien perdre — le spécimen mûr reste `readyToProduce` une fois les deux paniers pleins, jamais jeté. Une mutation directe de `cuisine.buffer` (aucune commande de consommation n'existe encore dans ce périmètre — documenté explicitement dans le fichier comme un futur système « cuisiner »/« consommer » hors périmètre de cet epic, pas une intervention manuelle sur la chaîne elle-même) simule un tiers qui vide dix unités de la réserve ; le cycle suivant montre le transport reprendre tout seul (dix unités déplacées d'un coup dès que la place se libère), puis la récolte reprendre à son tour dès que le panier local a de nouveau de la place — sans aucun réenseignement. Le test se termine en poursuivant la simulation jusqu'à dépasser trois jours de campagne au sens de `campaign-clock.js` (`DAY_SECONDS`, 57 600 s — constante réutilisée telle quelle plutôt qu'un nombre inventé, même si cette horloge n'est pas encore câblée à `s.elapsed`), vérifiant qu'aucune capacité n'est jamais dépassée et que l'arrosage reste actif après plusieurs jours simulés sans intervention.

**`tests/campaign-phase2-gate.cjs` (nouveau, non anticipé par le gabarit initial — même patron que `campaign-phase1-gate.cjs`).** La porte de sortie de la phase a deux volets (design §15 : « un joueur enseigne un geste à une Rainelle sans guide externe ; une chaîne fonctionne, sature proprement puis redémarre ») ; le second est entièrement couvert par `campaign-rainelles-chain.cjs` ci-dessus, ce fichier couvre le premier plus le volet correspondant de la section 16 du design (« chaque Rainelle conserve un seul geste ; le réenseignement le remplace et les refus de multiplication couvrent elle-même comme ses congénères »). Scénario 1/2 : `teachGesture` avec seulement les cinq champs que la commande elle-même demande, puis `Rainelles.defaultPhrase` vérifiant que le jeu propose sa propre explication sans documentation externe. Scénario 2/2, sur deux Rainelles distinctes (une nuance que `campaign-refusal.cjs` existant, avec sa seule Rainelle, ne prouvait pas encore) : geste unique, réenseignement qui remplace intégralement, puis le refus de multiplication identique sur chacune des deux (« elle-même comme ses congénères ») et sur `multiplySpecimen` visant directement l'identifiant de la seconde.

**Résultat réellement exécuté** : les deux nouveaux fichiers ont d'abord dû être ajoutés à la liste explicite du script `test` de `package.json` (qui énumère les fichiers un par un, pas de glob — sans cet ajout `npm test` restait silencieusement à 329, vérifié avant correction). Après correction, `npm ci` puis `npm test` → **332/332** (329 existants + 1 dans `campaign-rainelles-chain.cjs` + 2 dans `campaign-phase2-gate.cjs`, zéro régression). Sortie complète :

```
# tests 332
# suites 0
# pass 332
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

`/code-review` (niveau medium, skill `code-review`) exécuté sur le diff complet : aucun défaut relevé — diff entièrement composé de fichiers de test plus le câblage `package.json`, aucun code applicatif modifié.

Aucun rendu/géométrie/matériau touché (epic entièrement fait de tests Node) : `test:browser`/`test:visual` non requis, même exemption que C2.6a/b/c/C2.7 (voir `execution-continue.md`) ; `public/index.html` inchangé (les deux nouveaux fichiers sont des tests Node, jamais chargés par le navigateur). Aucune relecture narrative adverse requise (pas un epic de Scénariste). Aucun choix déjà confirmé touché (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne).

**Limites honnêtes inchangées par cet epic, documentées dans le code et le backlog** : « passage bloqué » et le sauvetage vers un bac de secours restent non implémentés (C2.8v) ; pas de vrai cycle de régénération de production (C2.6c) ; `replanter`/`preparer`/`trier` restent des no-op complets ; une zone reste un point, pas un polygone ; aucune commande ne permet encore de régler `capacity`/`min` d'un panier ni d'enregistrer une borne/zone/panier en jeu.

### Clôture de la phase 2 (Jardin d'imitation) — 18 septembre 2026

Porte de sortie (design §15, reprise dans `orchestration.md`) : *« un joueur enseigne un geste à une Rainelle sans guide externe ; une chaîne fonctionne, sature proprement puis redémarre. »* Les deux tests de porte ci-dessus (`tests/campaign-rainelles-chain.cjs`, `tests/campaign-phase2-gate.cjs`) prouvent littéralement ces deux clauses à travers le vrai vecteur de commandes. Les scénarios de validation pertinents de la section 16 du design déjà repris en tests de non-régression (« chaque Rainelle conserve un seul geste... refus de multiplication », « saturer puis libérer un bac... conserve les ressources », « aucune commande, récolte, nuit ou naissance ne crédite deux fois », « multiplier un cultivar conserve exactement ses traits ») passent tous réellement, ci-dessus et dans la suite existante. Deux volets du même tableau restent honnêtement hors de portée à ce jour — « passage coupé » et « déplacer un poste... rangement » — le critère de sortie littéral de la phase ne les nomme pas, exactement comme C1.5/épinglage était resté hors de portée à la clôture de la phase 1 sans bloquer sa fusion.

**C2.2v, C2.5v, C2.8v et C2.9 restent `todo` malgré cette fusion**, décision assumée et non silencieuse : les quatre partagent la même dépendance de fond (une position/un déplacement réels des Rainelles dans le monde de campagne, un chantier de la phase 3 — vérifié une nouvelle fois pour C2.9 ci-dessus), et aucun de leurs critères de sortie propres ne fait partie du critère de sortie de la phase cité par le design. Même précédent exact que C1.5 (épinglage), resté `todo` et différé à C4.3 sans empêcher la fusion de la phase 1.

Conformément à la politique de porte de phase révisée pour l'exécution non supervisée (`execution-continue.md` : « aucun pipeline n'est connecté à `main` ... fusionner une phase validée ... est une étape d'implémentation comme une autre, pas un acte de publication » — revérifié : toujours aucun workflow CI dans ce dépôt), la branche `maison-des-possibles` est fusionnée dans `main` en local puis les deux branches sont poussées vers `github.com/EdwinDayot/edwindayot.fr`. Aucun déploiement, aucune publication : `main` de ce dépôt n'est relié à aucun service d'hébergement.

## Déclenchement du 18 septembre 2026 — Cartographe, premier lot d'epics de la phase 3 (Tranche verticale)

Déclenchement automatisé (routine cloud horaire). Anti-hallucination faite avant tout le reste : dernier epic « fait » du backlog était C2.11, commit `bc2506b4aedb18a8c77a1a5e8fec1b29baea3a1b` — `git show --stat bc2506b` confirme le commit réel (5 fichiers, correspond exactement à l'entrée précédente de ce fichier) ; `npm ci && npm test` relancés indépendamment → **332/332**, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Les trois derniers epics consignés (C2.6c/C2.7/C2.8, puis C2.11) ne sont pas tous `bloqué` : aucune pause anti-emballement à déclencher.

**Rôle Cartographe endossé, aucun code ni test modifié.** La phase 2 est close et fusionnée dans `main` (rapport ci-dessus). Les seuls epics `todo` à dépendances *formellement* satisfaites restant en phase 1/2 — C2.2v, C2.5v, C2.9 — sont, comme documenté à répétition sur plusieurs déclenchements, bloqués de fait par l'absence de toute scène « maison » ou de position de Rainelle dans le monde : un chantier propre à la phase 3, pas une dépendance déclarée dans le backlog mais une réalité du code vérifiée à nouveau ce déclenchement (voir ci-dessous). La phase 3 elle-même n'avait encore aucun epic détaillé — seulement un paragraphe de contour — alors que sa porte d'entrée (clôture de la phase 2) vient tout juste de s'ouvrir : exactement la condition où `orchestration.md`/`execution-continue.md` demandent d'endosser le rôle Cartographe plutôt que de forcer un epic non détaillé ou de deviner une implémentation.

**Recherche factuelle menée avant d'écrire un seul epic** (déléguée à un sous-agent d'exploration en lecture seule, résultats vérifiés contre les fichiers cités plutôt qu'acceptés tels quels) :
- Aucune maison de joueur, scène intérieure ou système de mobilier n'existe : `grep -i "maison|refuge"` sur `public/game` ne remonte que les maisons de visiteurs déjà génériques (`data-buildings.js`, `render-houses.js`) et les notes de `campagne-backlog.md` documentant l'absence, déjà écrites pour C2.2v/C2.5v/C2.8v.
- `data-buildings.js`/`render-houses.js` rendent déjà des maisons purement depuis des données (murs en pierre, toit à deux pans escamotable, aucune branche de code par PNJ) — mais `buildHouse(b)` (`render-houses.js:295-307`) suppose aujourd'hui un visiteur obligatoire (`D.visitors.find((v) => v.id === b.visitorId).name`, qui lèverait si `visitorId` ne correspond à aucun visiteur) : pas un simple drop-in pour une maison refuge sans habitant. Noté explicitement dans le critère de sortie de C3.2 plutôt que laissé à découvrir en silence par un futur Artisan rendu.
- `construction.js` ne connaît que le placement extérieur (pots, tuyaux, réservoirs...) ; aucun concept de mobilier d'intérieur n'existe nulle part.
- Le système de quêtes (`data-quests.js`/`quests.js`/`garden-state-cmd-e.js`) est câblé et testé mais explicitement documenté comme sans aucun PNJ porteur du rôle `quest-giver` en jeu (en-tête de `data-quests.js`).
- `s.campaignDay` existe déjà et s'incrémente à chaque `sleep` (`garden-state-cmd-f.js`) — prêt à compter trois nuits sans travail supplémentaire sur l'horloge elle-même.
- La création d'une deuxième Rainelle n'a aujourd'hui strictement aucun chemin joueur réel : seul un appel direct à `Rainelles.createRainelle`, utilisé par les tests (`tests/campaign-rainelles-chain.cjs`), la fait apparaître ; la commande `sleep` ne crée la première Rainelle qu'une seule fois (`s.campaignFrogEncounterPending && !s.rainelles.length`).
- Aucune zone « village » n'existe dans `data-world.js` (quatre zones seulement : pépinière, sous-bois, prairie, rocaille).
- Design §5 (« Multiplication et vie propre ») décrit un mécanisme légitime et déjà écrit pour une deuxième Rainelle — un bourgeon prélevé après familiarisation, mûri à la nurserie après une nuit de sommeil, sur le même principe temporel que l'établi déjà existant côté jardin libre (`balance.nurserySeconds`). Repris ici comme mécanique générique, sans la mise en scène du chapitre 6 (Acte II, phase 4) — même discipline que C2.3 pour l'apparition de la première Rainelle.

**Sept epics détaillés dans `docs/campagne-backlog.md`, C3.1 à C3.7**, au même gabarit que les phases 1 et 2 :
1. **C3.1** — schéma et réparation de la maison refuge (moteur pur) : six espaces du design §6 (pièce d'accueil, cuisine, atelier, serre attenante, grenier, véranda), statuts `delabre`/`repare`/`locked`, seule la pièce d'accueil déverrouillée au départ, commande `repairHouseSpace` consommant l'inventaire existant.
2. **C3.2** — rendu de la maison refuge, dépend de C3.1, avec la mise en garde ci-dessus sur `buildHouse` à généraliser plutôt qu'à contourner, capture Playwright + relecture multimodale + `garden-material-audit.cjs` obligatoires avant de marquer fait.
3. **C3.3** — habitats de Rainelle (places de vie), extension de la quatrième collection dans `campaign-stations.js` plutôt qu'un nouveau système séparé — « généraliser plutôt que spécialiser ».
4. **C3.4** — bourgeon et nurserie, dépend de C3.3, deuxième Rainelle réellement obtenue par une vraie commande joueur (`formBud`/`harvestBud`), plus de recours à l'appel de test direct.
5. **C3.5** — coin de village et deux habitants, **délibérément sans identité narrative** (pas de nom d'Acte I-III comme Noé/Mira/Basile — ces personnages et leurs quêtes sont un chantier de la phase 4, §10 du design, non encore écrit) ; réutilise entièrement le système générique de maisons de visiteurs, aucune nouvelle branche de rendu attendue.
6. **C3.6** — première quête d'outil, dépend de C3.5, extension additive `reward.tools` du schéma de récompense existant (même patron que `reward.plans`) plutôt qu'un nouveau système de quête ; limite honnête documentée par avance — aucun geste de campagne ne consomme encore un outil comme prérequis, l'epic ne prouve que la persistance de la récompense.
7. **C3.7** — porte de sortie de la phase, dépend de C3.2/C3.4/C3.6 : test d'intégration Node rejouant les quatre promesses (habiter/croiser/organiser/rencontrer) sur trois nuits jouées via le vrai vecteur de commandes, aller-retour de sauvegarde vérifié ; si les scénarios pertinents de la section 16 passent, fusion vers `main` et rapport de clôture, comme pour les phases 1 et 2.

Dépendances internes du lot enchaînées dans l'ordre d'écriture (C3.1→C3.2, C3.3→C3.4, C3.5→C3.6, tous convergent vers C3.7) ; aucune epic du lot ne saute une dépendance non satisfaite au moment où elle sera choisie.

**Un second lot reste délibérément non détaillé** — rendu enrichi du village, câblage narratif réel une fois la phase 4 ouverte, et une réévaluation de C2.2v/C2.5v/C2.8v/C2.9 une fois qu'une vraie scène « maison »/un déplacement de Rainelles existent grâce à ce premier lot — laissé à un futur passage du Cartographe plutôt que figé d'un bloc maintenant, même principe que la note de tête de fichier sur les phases 3 à 7 de `campagne-backlog.md`.

**Vérifications de ce déclenchement** : aucun code applicatif ni fichier de test modifié — uniquement `docs/campagne-backlog.md` (sept nouvelles entrées d'epics + entrée de journal) et cette entrée. `npm test` non requis pour ce type de déclenchement (aucun changement de code, comme pour le précédent passage Cartographe du 18 septembre), mais déjà rejoué ci-dessus dans le cadre de la vérification anti-hallucination obligatoire. Aucun choix déjà confirmé du design touché (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne) ; aucune teinte/matériau introduit.

## Déclenchement du 18 septembre 2026 — schéma et réparation de la maison refuge (epic C3.1)

Déclenchement automatisé (routine cloud horaire). Anti-hallucination faite avant tout le reste : dernier epic « fait » du backlog était C2.11, commit `bc2506b4aedb18a8c77a1a5e8fec1b29baea3a1b` — `git show --stat bc2506b` confirme le commit réel (5 fichiers, correspond exactement à l'entrée précédente de ce fichier) ; `npm ci && npm test` relancés indépendamment → **332/332**, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Les trois derniers epics consignés (C2.6c/C2.7/C2.8, puis C2.11) ne sont pas tous `bloqué` : aucune pause anti-emballement à déclencher. Le commit `f686e2d` (Cartographe, premier lot d'epics de la phase 3) était déjà en tête de la branche, correspondant exactement à l'entrée précédente de ce fichier.

**Choix de l'epic.** Deux epics `todo` de la phase 3 ont leurs dépendances satisfaites (« rien ») : **C3.1** (schéma/réparation de la maison refuge) et **C3.5** (coin de village). Choisi **C3.1**, pas seulement par ordre du backlog : il débloque transitivement le plus d'epics du lot (C3.2 en dépend directement, C3.3 en dépend aussi, et C3.7 par la chaîne C3.1→C3.2→C3.7), alors que C3.5 ne débloque que C3.6. Epic de schéma/moteur pur (design §6, « Restaurer donne des fonctions ») : aucun rendu, aucune dépendance narrative.

**Fait avant d'écrire du code, comme documenté dans `campaign-house.js`** : vérification que les familles de ressources « bois, pierre, argile, fibres » citées par le design §6/§16 existent bien dans le jardin libre. Résultat : seules `wood`/`stone`/`clay` existent réellement (`garden-state-util.js`'s `knownItem`, `garden-state-lifecycle.js`'s inventaire initial, `data.js`'s trois seuls types de ressource minées) — aucune « fibre » n'est un item d'inventaire aujourd'hui (`vert-fibre` dans `botany-hybrids.js` est un nom de teinte, sans rapport). Inventer une quatrième ressource dans cet epic aurait été un second système non demandé par le critère de sortie littéral ; les coûts de réparation n'utilisent donc que bois/pierre/argile, une fibre réelle restant pour un futur epic qui en aurait réellement besoin.

**Livré, exactement les fichiers probables du backlog plus un nouveau fichier de test** :
- Nouveau `public/game/campaign-house.js` : catalogue statique `SPACES` (les six espaces du design §6 — accueil, cuisine, atelier, serre, grenier, veranda — chacun avec un nom et un coût en `wood`/`stone`/`clay`) et `freshHouse()` (un espace par défaut `{status:"delabre", locked: id !== "accueil"}` — la pièce d'accueil démarre déverrouillée mais **toujours délabrée**, exactement comme le design §6 la qualifie lui-même de « couchage réparable » : déverrouillé et réparé restent deux champs distincts, jamais confondus). `canRepair(house, spaceId)` est une vérification pure de précondition (espace connu, non verrouillé, pas déjà réparé), sans toucher à l'inventaire — le débit reste la responsabilité de la commande, même séparation que `economy.js`/`irrigation.js` ailleurs.
- Nouveau `public/garden-state-cmd-l.js` (première commande de la lettre L) : `repairHouseSpace` suit le patron déjà existant `this.has(cost)` puis `this.pay(cost)` (`garden-state-cmd-a.js`'s `unlock`, `-b.js`'s constructions) — un échec (verrouillé/déjà réparé/ressources manquantes) ne débite jamais partiellement l'inventaire, la double vérification de `has` dans `canRepair`'s appelant et dans `economy.js`'s `debit` le garantit. Ajouté à la liste des segments de commande dans `garden-state.js` (`"l"` ajouté au tableau des lettres chargées côté Node).
- `public/garden-state-lifecycle.js` : une partie neuve démarre avec `campaignHouse: House.freshHouse()`.
- `public/garden-state-validate.js` : `campaignHouse` suit le patron déjà utilisé pour `campaignStations` — validé strictement si présent (les six espaces doivent tous exister, statut `delabre`/`repare`, `locked` booléen), `result.campaignHouse ??= House.freshHouse()` sinon. Pas de migration partielle par espace : le champ entier est introduit par cet epic, donc une sauvegarde plus ancienne ne l'a simplement pas du tout.
- `public/index.html` : deux nouvelles balises `<script>` — `game/campaign-house.js` (juste après `campaign-stations.js`, même raison : lu par `garden-state-lifecycle.js`/`garden-state-validate.js` juste après) et `garden-state-cmd-l.js` (juste après `garden-state-cmd-k.js`, avant `save.js`).
- Nouveau `tests/campaign-house.cjs` (10 tests), ajouté à la liste explicite du script `test` de `package.json` : partie neuve avec les six espaces, seule la pièce d'accueil déverrouillée ; réparation réussie débite exactement le coût annoncé ; refus explicite sur un espace verrouillé sans toucher l'inventaire ; refus explicite sur un espace déjà réparé sans second débit ; refus explicite sur un identifiant d'espace inconnu ; **réparation à ressources insuffisantes échoue sans consommer partiellement l'inventaire** (le scénario explicitement demandé par le critère de sortie de l'epic) ; migration d'une sauvegarde sans `campaignHouse` vers `freshHouse()` sans erreur ; une sauvegarde partiellement réparée valide sans changement ; rejet d'un `campaignHouse` malformé (espace manquant, statut invalide, `locked` non booléen) ; aller-retour JSON strictement identique sur une maison partiellement réparée.

**Résultat réellement exécuté** : `npm ci` puis `npm test` → **342/342** (332 existants + 10 nouveaux, zéro régression). Sortie complète :

```
# tests 342
# suites 0
# pass 342
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

`/code-review` (niveau medium, skill `code-review`) exécuté sur le diff complet : aucun défaut relevé — ordre des vérifications dans `validate.js` correct (rejet avant valeur par défaut), chemin de migration cohérent, gestionnaire de commande suivant exactement le patron `has`/`pay`/`fail` existant, ordre de chargement des scripts dans `index.html` cohérent avec les entrées existantes, couverture de test jugée complète.

Aucune géométrie ni matériau touché (epic entièrement moteur, aucun rendu) : `test:browser`/`test:visual` complets non requis (voir `execution-continue.md`, même exemption que C2.6a/b/c/C2.7/C2.8/C2.11). Deux nouvelles balises `<script>` ajoutées à `index.html` malgré tout — vérification ciblée faite comme à C2.8 : page servie localement (`python3 -m http.server`), chargée avec Playwright (exécutable Chromium préinstallé) — aucune erreur console ni erreur de page, `window.GardenCampaignHouse` bien défini avec les six identifiants d'espace attendus. Aucune relecture narrative adverse requise (pas un epic de Scénariste). Aucun choix déjà confirmé du design touché (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne) ; aucune teinte/matériau de `direction-artistique.md` utilisé (aucun rendu dans cet epic).

**Limite honnête documentée, pas contournée** : les coûts de réparation eux-mêmes (`SPACES[...].cost`) sont un choix de dosage de cet epic, pas une valeur donnée littéralement par le design (qui ne cite que la fonction de chaque espace, jamais un coût précis) — dimensionnés par rapport à l'inventaire initial et au rendement de récolte existants, documentés comme tels dans `campaign-house.js` plutôt que présentés comme dérivés d'une source. C3.2 (rendu de la maison refuge) reste `todo`, sa propre entrée de backlog documentant déjà la généralisation nécessaire de `buildHouse()`.

## Déclenchement du 18 septembre 2026 — rendu de la maison refuge (epic C3.2)

Déclenchement automatisé (routine cloud horaire). Anti-hallucination faite avant tout le reste : dernier epic « fait » du backlog était C3.1, commit `5564102ce24b0bfac13fd9f0c021b21048fb9650` — `git show --stat 5564102` confirme le commit réel (correspond exactement à l'entrée précédente de `docs/campagne-backlog.md`) ; `npm ci && npm test` relancés indépendamment → 342/342, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Aucun blocage récent : aucune pause anti-emballement à déclencher.

**Choix de l'epic : C3.2** (dépendance C3.1, `fait`), en continuité de la chaîne C3.1→C3.2 déjà engagée par le lot Cartographe de la phase 3, plutôt que C3.3/C3.5 (également disponibles) — voir le raisonnement complet dans `docs/campagne-backlog.md`.

**Vérification préalable faite avant d'écrire du code**, comme la propre entrée de backlog de cet epic le demandait explicitement : `render-houses.js:295-307` (`buildHouse(b)`) lu directement — confirme l'appel sans garde à `D.visitors.find((v) => v.id === b.visitorId).name`, donc une exception certaine sur une maison sans visiteur. Décision documentée : un module frère autonome plutôt qu'une branche visiteur optionnelle dans `buildHouse` ou une entrée `data-buildings.js` (les deux auraient mélangé la donnée « une rangée par PNJ » du jardin libre avec la maison du joueur, qui n'a pas de visiteur) — voir le détail complet dans `docs/campagne-backlog.md`.

**Livré** :
- Nouveau `public/game/render-campaign-house.js` : `buildRefugeHouseGroup(house)`, autonome (seul `THREE` requis, testable en Node pur comme `botany-hybrids.js`), lisant `house.spaces.accueil.status` (schéma C3.1). Réutilise les couleurs exactes déjà en usage pour toute maison du jeu (`render.js`'s stone/bark/cream, `render-houses.js`'s stoneDark) et la même technique de pignon que `gableGeometry`. Distinction repare/delabre par trois différences nommables : pierre claire/toit à deux pans intacts/porte fermée contre pierre sombre/un seul pan de toit survivant (l'autre manquant, charpente visible)/planches croisées sur l'ouverture. Aucun émissif introduit.
- `public/index.html` : une nouvelle balise `<script>` après `render-houses.js`.
- Nouveau `tests/campaign-house-render.cjs` (7 tests Node purs) : construction sans exception des deux états, différences roof/porte testées explicitement, aucune métallicité, rugosité 0,72 (voir écart documenté ci-dessous), couleurs limitées à l'ensemble déjà en usage, déterminisme, fidélité du profil de pignon.
- `tests/garden-material-audit.cjs` étendu : injecte les deux états de la maison dans la scène réelle de la page servie, loin de la zone jouable — le vrai gate, exécuté et passé.
- Nouveau `tests/campaign-house-visual.cjs` (banc dédié, sur le modèle de `campaign-botany-visual.cjs`) : les deux états côte à côte, vérifications programmatiques (sommets finis, pas de chevauchement, différences roof/porte), capture sauvegardée pour relecture multimodale.
- `package.json` : les deux nouveaux fichiers de test ajoutés aux listes explicites de `test`/`test:browser`.

**Résultat réellement exécuté** : `npm ci` puis `npm test` → **349/349** (342 existants + 7 nouveaux, zéro régression).

```
# tests 349
# suites 0
# pass 349
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

**Audit programmatique du rendu, le vrai gate** : page servie localement (`python3 -m http.server 4174 --directory public`) puis `node tests/garden-material-audit.cjs` exécuté directement → `PASS material/geometry audit: {"materialsInspected":97,"transparentAllowed":1,"timesSampled":5}` (les deux états de la maison refuge font partie des matériaux inspectés ; aucune anomalie relevée sur les nouveaux maillages). `node tests/campaign-house-visual.cjs` exécuté directement → `PASS campaign-house-visual: {"overlap":false,"delabreRoofPanelCount":1,"repareRoofPanelCount":2,"delabreDoorStyle":"boarded","repareDoorStyle":"closed"}`.

`/code-review` (niveau medium, skill `code-review`) exécuté sur le diff complet : aucun défaut relevé.

**Limite honnête sur `npm run test:browser` en tant que chaîne complète** : échoue dès son premier script, `tests/garden-play.cjs` (non modifié par cet epic), faute du repli d'exécutable Chromium (`/opt/pw-browsers/chromium`) que ce script n'a jamais porté, contrairement à `garden-material-audit.cjs`/`campaign-botany-visual.cjs` — un défaut d'environnement préexistant, sans rapport avec cet epic, non corrigé ici (hors périmètre). Les deux scripts navigateur réellement concernés par ce rendu ont donc été exécutés directement, avec succès, comme rapporté ci-dessus.

**Relecture multimodale réelle** (capture `/tmp/campaign-house-visual.png`, lue avec l'outil Read, comparée explicitement à `direction-artistique.md`) : les deux états se distinguent nettement à la silhouette seule — repare : pierre claire, toit brun intact, porte en bois fermée, identique en teinte à toute maison de visiteur déjà en jeu ; delabre : pierre plus sombre, un seul pan de toit (l'autre manquant, intérieur sombre et vide du faîtage visibles), grandes planches croisées sur l'ouverture. Matériaux mats, aucune couleur hors des familles pierre/bois documentées. Conforme à direction-artistique.md §« Silhouette et géométrie ». Complément documenté, jamais le gate lui-même (déjà passé indépendamment ci-dessus).

**Écart honnête découvert, documenté sans le corriger dans cet epic** : direction-artistique.md affirme une rugosité « observée entre 0,15 et 0,48 » pour tous les matériaux du jeu, mais `render.js`'s stone/bark/wood/cream (donc toute maison déjà en jeu, avant cet epic) utilisent la rugosité par défaut de `GardenModels.mat()`, **0,72**, hors de cette plage (vérifié dans `garden-models.js:16`). Cet epic reprend fidèlement la convention déjà en usage plutôt que d'inventer une rugosité juste pour correspondre au texte du guide (que `direction-artistique.md` interdit lui-même). La correction de cette inexactitude documentaire est laissée à un futur passage, hors du mandat de cet epic de rendu.

Aucun choix déjà confirmé du design touché (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne). Aucune identité narrative introduite.

## Déclenchement du 18 septembre 2026 — habitats de Rainelle, places de vie (epic C3.3)

Déclenchement automatisé (routine cloud horaire). Anti-hallucination faite avant tout le reste : dernier epic « fait » du backlog était C3.2, commit `de31f99b1e0a6c6b0834040b46c679f29808ca19` — `git show --stat de31f99` confirme le commit réel (8 fichiers, correspond exactement à l'entrée existante de `docs/campagne-backlog.md` et à l'entrée précédente de ce fichier) ; `npm ci && npm test` relancés indépendamment → 349/349, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Les deux derniers epics consignés (C3.1, C3.2) ne sont pas tous `bloqué` : aucune pause anti-emballement à déclencher.

**Choix de l'epic : C3.3** (dépendances C2.6a/C3.1, toutes deux `fait`), plutôt que C3.5 (également disponible, dépend de rien) — en continuité de la chaîne d'écriture C3.1→C3.2→C3.3 déjà engagée par les deux déclenchements précédents, et parce qu'elle est le prérequis direct de C3.4 (deuxième Rainelle par multiplication naturelle), lui-même sur le chemin le plus contraignant vers C3.7 (porte de sortie de la phase). Epic de schéma/moteur pur, aucun rendu ni dépendance narrative : implémenté directement par l'orchestrateur, sans délégation.

**Livré** :
- `public/game/campaign-stations.js` : quatrième collection du registre, `habitats` (préfixe d'id `h`, disjoint de `bornes`/`zones`/`paniers`). `registerStation` accepte désormais un argument `capacity` pour ce kind, requis et borné en dessous par une nouvelle constante `MIN_HABITAT_CAPACITY = 2` — pas de capacité par défaut inventée : design §6 dit « un habitat fournit plusieurs places de vie » sans jamais chiffrer, contrairement à `DEFAULT_PANIER_CAPACITY` qui avait un vrai précédent (`D.recipes.collector.capacity`) à reprendre. Trois nouvelles fonctions pures : `habitatCapacityTotal` (somme des capacités de tous les habitats), `freeLivingPlaces(registry, aliveRainelleCount)` (capacité totale moins la population vivante — un appelant compare le résultat à `> 0`, jamais à sa troncité, puisque 0 est un « aucune place restante » significatif) et `removeHabitat(registry, habitatId, aliveRainelleCount)` (refuse explicitement si le retrait ferait passer la capacité totale sous la population actuelle, sinon renvoie un nouveau registre sans cet habitat). Écart documenté sur « occupé » : aucune commande n'affecte une Rainelle à un habitat précis (la population de C2.3+ n'est qu'un tableau plat, `s.rainelles`), donc `removeHabitat` lit « occupé » comme « romprait la capacité totale sous la population actuelle » — la seule lecture que les données actuelles permettent, documentée explicitement dans le code plutôt que devinée en silence.
- Conformément au critère de sortie littéral de l'epic, aucune commande de création/retrait réelle n'est câblée sur ces fonctions (C3.4 en dépendra pour le faire) : la clause « aucune Rainelle ne peut encore être créée sans une place libre » est prouvée en appelant `freeLivingPlaces` directement dans un test, pas via un vrai chemin joueur.
- `public/garden-state-lifecycle.js` (`fresh()`) : une sauvegarde neuve démarre avec `habitats: []`/`habitatNextId: 1`.
- `public/garden-state-validate.js` : valeur par défaut `campaignStations.habitats`/`habitatNextId` quand `campaignStations` est entièrement absent ; validation dédiée d'une capacité d'habitat (finie, `>= MIN_HABITAT_CAPACITY`). **Bug de migration trouvé et corrigé en cours de route, pas contourné** : la boucle de validation générique existante (`Object.keys(Stations.KINDS)`) exigeait `Array.isArray(list)` pour chaque collection sans exception ; ajouter `habitat` à `KINDS` aurait donc fait rejeter, avec une erreur, toute sauvegarde antérieure à cet epic (qui a `campaignStations` mais pas encore `habitats`) — détecté par l'échec réel du propre test de migration de cet epic avant le commit, jamais supposé passant. Corrigé en rendant cette collection explicitement optionnelle dans la boucle (seulement quand la collection ET son compteur sont tous deux absents), sur le même principe que `buffer`/`capacity`/`min` déjà optionnels par élément pour un panier pré-C2.8.
- `tests/campaign-stations.cjs` : les fixtures de registre existantes (huit occurrences) étendues aux quatre collections plutôt que laissées désynchronisées du nouveau schéma par défaut.
- Nouveau `tests/campaign-habitats.cjs` (13 tests) : collection vide par défaut, création avec capacité/id corrects, refus sous le plancher de capacité (`0`, `1`, absente), résolution par `resolveStation`, somme de capacité, calcul des places libres (y compris le cas `0` exact), la gate de création vérifiée directement (0/1/2/3 Rainelles vivantes contre une capacité de 2), refus de retrait qui romprait la population, retrait réussi renvoyant un registre sans mutation de l'original, refus explicite sur un id inconnu, migration d'une sauvegarde sans `habitats` du tout, rejet d'une capacité invalide à la validation, aller-retour JSON strictement identique.
- `package.json` : le nouveau fichier de test ajouté à la liste explicite du script `test` (leçon déjà tirée à C2.11).

**Résultat réellement exécuté** : `npm ci` puis `npm test` → **362/362** (349 existants + 13 nouveaux dans `campaign-habitats.cjs`, zéro régression — après correction du bug de migration ci-dessus, dont l'échec initial a été observé directement, pas anticipé). Sortie complète :

```
# tests 362
# suites 0
# pass 362
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

`/code-review` (skill `code-review`, niveau medium) exécuté sur le diff complet : aucun défaut relevé — changement de signature de `registerStation` (`{x,z}` → `{x,z,capacity}`) vérifié sans effet sur les appelants existants (`capacity` vaut `undefined` et reste inutilisé pour les trois kinds pré-existants), aucune duplication introduite, portée jugée correctement limitée (agrégat de capacité plutôt qu'un suivi d'occupation par individu, documenté comme un choix délibéré et non un raccourci).

Aucune géométrie ni matériau touché (epic entièrement moteur, aucun rendu) : `test:browser`/`test:visual` complets non requis (voir `execution-continue.md`, même exemption que C2.6a/b/c/C2.7/C3.1). Vérification navigateur ciblée faite malgré tout (aucune nouvelle balise `<script>` cette fois, `campaign-stations.js` étant déjà chargé depuis C2.6a) : page servie localement (`python3 -m http.server 4174 --directory public`), chargée avec Playwright (exécutable Chromium préinstallé) — aucune erreur console ni erreur de page, `window.GardenCampaignStations.freeLivingPlaces` bien défini comme fonction. Aucune relecture narrative adverse requise (pas un epic de Scénariste). Aucun choix déjà confirmé du design touché (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne) ; aucune teinte/matériau de `direction-artistique.md` en jeu (aucun rendu dans cet epic). Aucune capacité inventée hors de ce que le design §6 énonce littéralement (« plusieurs », traduit en un plancher de 2, jamais un nombre précis choisi arbitrairement).

## Déclenchement du 18 septembre 2026 — bourgeon et nurserie, deuxième Rainelle (epic C3.4)

Déclenchement automatisé (routine cloud horaire). Anti-hallucination faite avant tout le reste : dernier epic « fait » du backlog était C3.3, commit `8952e740139c57ad68193c656bc768079bb15cc3` — `git show --stat 8952e74` confirme le commit réel (correspond exactement à l'entrée existante de `docs/campagne-backlog.md` et à l'entrée précédente de ce fichier) ; `npm ci && npm test` relancés indépendamment → 362/362, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Les deux derniers epics consignés (C3.2, C3.3) ne sont pas tous `bloqué` : aucune pause anti-emballement à déclencher.

**Vérification supplémentaire faite ce déclenchement, au-delà de la seule étape 3 littérale** : le ref local `origin/main` de ce conteneur pointait encore sur l'ancien `95a79aa`, faisant d'abord craindre que la fusion de la phase 2 (commit `e24f122`, rapport de clôture du 18 septembre précédent dans ce fichier) n'ait jamais atteint le dépôt distant réel. Un `git fetch origin main` explicite (jamais fait par les déclenchements précédents, qui n'avaient fetché que `maison-des-possibles`) a corrigé ce ref local et confirmé que `e24f122` est bien la tête réelle de `origin/main`, avec `bc2506b` (porte de sortie de la phase 2) dans son ascendance (`git merge-base --is-ancestor bc2506b origin/main` → vrai). Fausse alerte, pas une anomalie réelle du dépôt distant — mais un rappel concret que même une vérification censée détecter un rapport non fiable peut elle-même être trompée par un état local non rafraîchi si elle ne va pas jusqu'à la source distante. Aucune correction nécessaire.

**Choix de l'epic : C3.4** (dépendances C3.3/C1.6, toutes deux `fait`), plutôt que C3.5 (également disponible, dépend de rien) — en continuité de la chaîne d'écriture C3.1→C3.2→C3.3→C3.4 déjà engagée, et parce qu'il est sur le chemin le plus contraignant vers C3.7 (porte de sortie de la phase), alors que C3.5 ne débloque que C3.6. Epic de schéma/moteur pur, aucun rendu ni dépendance narrative : implémenté directement par l'orchestrateur, sans délégation.

**Livré** :
- `public/game/rainelles.js` : nouveau champ `bourgeon` sur une Rainelle (`null` par défaut, `true` une fois formé — aucun état intermédiaire, design §5 : « après un petit événement de familiarisation générique, une Rainelle forme un bourgeon »). Deux nouvelles fonctions pures : `formBud(rainelle)` (refuse explicitement un second bourgeon tant que le premier n'est pas prélevé) et `harvestBud(rainelle)` (refuse une Rainelle sans bourgeon, sinon le vide et renvoie son `cultivarId` pour que l'appelant construise l'entrée de nurserie).
- Nouveau `public/garden-state-cmd-m.js` : commandes `formBud`/`harvestBud`. `harvestBud` vérifie `Stations.freeLivingPlaces` **avant** d'appeler `Rainelles.harvestBud`, jamais après — un refus ne consomme donc jamais le bourgeon. Point trouvé en écrivant les tests, pas supposé au départ : la réservation compare `freeLivingPlaces` à `s.rainelles.length + s.campaignNursery.length` (population vivante **plus** bourgeons déjà en attente de nurserie), pas seulement à la population actuelle — sans ce `+ s.campaignNursery.length`, deux prélèvements prononcés le même jour, avant toute nuit, pourraient chacun voir une place libre alors qu'une seule existe réellement, promettant deux naissances pour un seul habitat. Un test dédié (`tests/campaign-nursery.cjs`) le prouve directement : le second prélèvement est refusé tant que le premier reste non résolu.
- `public/garden-state-cmd-f.js` : la commande `sleep` résout désormais aussi `s.campaignNursery` — chaque entrée devient un nouvel individu (`Rainelles.createRainelle`, `geste: null`, `name: ""`, `cultivarId` hérité de la Rainelle qui a formé le bourgeon — « un bourgeon de sa lignée », design §5, aucune autre lecture possible sans inventer une donnée absente), puis la file est vidée. Même patron que `campaignPot.pending` déjà en place depuis C1.3 : la résolution n'a lieu que dans l'exécution de la commande `sleep` elle-même, jamais au chargement — un rechargement en cours de maturation ne peut donc jamais réveiller un bourgeon en avance ni deux fois, prouvé par un test de rechargement JSON réel.
- `public/garden-state-lifecycle.js` (`fresh()`) : `campaignNursery: []`, `campaignNurseryNextId: 1`.
- `public/garden-state-validate.js` : validation stricte de `campaignNursery` (ids uniques `nu<n>`, `cultivarId` reconnu, compteur cohérent) et de `rainelle.bourgeon` (`null`/`true`/absent uniquement) ; migration `??=` pour une sauvegarde antérieure à cet epic, `bourgeon` migré par rainelle sur le même modèle que `job` à C2.6c.
- `public/garden-state.js` : segment `"m"` ajouté à la liste des groupes de commandes chargés côté Node (`require("./garden-state-cmd-" + k + ".js")`).
- `public/index.html` : nouvelle balise `<script>` pour `garden-state-cmd-m.js`, juste après `garden-state-cmd-l.js`.
- Nouveau `tests/campaign-nursery.cjs` (15 tests) + `package.json` (liste explicite du script `test`, leçon déjà tirée à C2.11).

**Écarts documentés, aucun deviné en silence** : `cultivarId` du nouvel individu hérité de celui du parent — le design ne précise rien de plus, et inventer une autre règle (cultivar aléatoire, absent) aurait été une invention non demandée. Aucune mise en scène narrative pour `formBud` (même posture que `triggerFrogEncounter` à C2.3 : aucun contenu de chapitre 6, phase 4, utilisé hors ordre). Aucun délai de repousse après un prélèvement, ni de délai de maturation du bourgeon lui-même avant prélèvement — le design ne nomme aucun rythme au-delà de l'attente de la nurserie elle-même (« il s'éveille après une nuit de sommeil »), et ajouter un rythme supplémentaire aurait été une mécanique non demandée par le critère de sortie littéral de cet epic.

**Résultat réellement exécuté** : `npm ci` puis `npm test` → **377/377** (362 existants + 15 nouveaux dans `campaign-nursery.cjs`, zéro régression). Sortie complète :

```
# tests 377
# suites 0
# pass 377
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

`/code-review` (skill `code-review`, niveau medium) exécuté sur le diff complet : aucun défaut relevé — vérifié explicitement l'ordre de chargement du nouveau segment de script, l'arithmétique de réservation de place, l'ordre validation/migration (le défaut `campaignNursery ??= []` est garanti avant qu'une commande ne s'exécute), et l'absence de duplication avec le patron déjà établi par `campaignPot.pending`.

Aucune géométrie ni matériau touché (epic entièrement moteur, aucun rendu) : `test:browser`/`test:visual` complets non requis, même exemption que C2.6a/b/c, C2.7, C2.8, C3.1, C3.3. Vérification navigateur ciblée faite malgré tout (une nouvelle balise `<script>` introduite) : page servie localement (`python3 -m http.server 4175 --directory public`), chargée avec Playwright (exécutable Chromium préinstallé) — aucune erreur console ni erreur de page, `window.GardenRainelles.formBud`/`harvestBud` et `window.GardenCampaignStations` bien définis. Aucune relecture narrative adverse requise (pas un epic de Scénariste). Aucun choix déjà confirmé du design touché (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne) ; aucune teinte/matériau de `direction-artistique.md` en jeu (aucun rendu dans cet epic).

## Déclenchement du 18 septembre 2026 — coin de village : zone et deux habitants (epic C3.5)

Déclenchement automatisé (routine cloud horaire). Anti-hallucination faite avant tout le reste : dernier epic « fait » du backlog était C3.4, commit `4158f58321c0b3ee5a1ec19a1397b2d5ad6637a0` — `git show --stat 4158f58` confirme le commit réel (12 fichiers, correspond exactement à l'entrée existante de ce fichier et de `docs/campagne-backlog.md`) ; `npm ci && npm test` relancés indépendamment → 377/377, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Les trois derniers epics consignés (C3.2/C3.3/C3.4) ne sont pas tous `bloqué` : aucune pause anti-emballement à déclencher.

**Choix de l'epic : C3.5** (dépend de rien), seul epic `todo` de la phase 3 à dépendances satisfaites en dehors de la chaîne C3.1→C3.4 déjà achevée (C3.6/C3.7 dépendent tous deux, directement ou par transitivité, de C3.5 lui-même). C2.2v/C2.5v/C2.8v/C2.9 (phases 1/2) restent sautés pour la raison inchangée depuis plusieurs déclenchements : le rendu de la maison refuge (`render-campaign-house.js`, C3.2) existe mais n'est câblé dans aucune scène jouable réelle (`garden.js`/`garden-frame.js` ne l'instancient pas), donc aucune position de caméra/joueur de campagne n'existe encore à laquelle raccrocher une transition de nuit ou un écran d'enseignement — vérifié à nouveau (`grep GardenCampaignHouse public/garden.js public/garden-frame.js` : aucune occurrence hors du module lui-même et des fichiers d'état qui le valident).

**Vérification préalable faite avant d'écrire une seule ligne de donnée, pas supposée** : le mandat du backlog anticipait « aucune nouvelle branche de code attendue » (rendu entièrement générique). La lecture du code réel a montré que ce n'est vrai que pour le rendu — plusieurs points de données/logique génériques itèrent `D.zones` sans borne fixe mais avec des hypothèses implicites sur le nombre de zones ou leur agencement, qu'une cinquième zone rendait fausses sans un vrai code neuf :
- `public/game/render-world.js` et `public/game/render-flow.js` indexaient la teinte de sol par `z.id` dans un littéral à 4 couleurs (`[0xaabd8c, ...][z.id]`) : `undefined` à l'index 4 sans une cinquième teinte.
- `public/game/data.js` assigne 3 ressources (bois/pierre/argile) par zone depuis `resourcePositions[z.id]`, indexé par id — un cinquième tableau était obligatoire, pas optionnel, sous peine d'exception au chargement.
- `public/garden-state-validate.js` liste `[0, 1, 2, 3]` comme unique domaine valide de `s.unlocked` — sans extension, débloquer la nouvelle zone aurait fait échouer la validation de toute sauvegarde qui l'aurait fait.
- La test suite existante encode plusieurs invariants génériques mais vérifiés seulement contre les zones déjà là : `tests/garden-construction.cjs` attend qu'**aucun** visiteur ne soit injoignable depuis le portail sur une sauvegarde neuve (`unlocked: [0]` par défaut) et que **chaque** porte de maison soit franchissable sur cette même sauvegarde neuve — une cinquième zone verrouillée par défaut, séparée de la zone 0 par une zone intermédiaire elle-même verrouillée, aurait rendu ses habitants injoignables et fait échouer ces deux tests génériques, pas par un bug de logique mais par un choix de conception (zone verrouillée + îlot d'accès) incompatible avec un invariant déjà établi.

**Décision de conception qui en découle, documentée plutôt que devinée** : zone 4 (« Le coin de village ») est **toujours déverrouillée**, comme la zone 0 (`cost: {}`, `rep: 0`), et non gagnée par un coût comme les zones 1 à 3. Elle partage sa bordure sud exactement avec le bord nord réel de la zone 0 (mêmes trois points de polygone `[-16,19]`/`[-6,22]`/`[4,19]`, ni écart ni chevauchement — même convention « bordures partagées, seul le bord extérieur bombe » déjà en usage entre les zones 0 à 3) : c'est la seule zone déjà toujours ouverte dès le départ, donc le seul bord qui garantit une zone 4 réellement atteignable à pied sur une sauvegarde neuve plutôt qu'un îlot verrouillé takeaway. Écart honnête assumé sur cette frontière : les maisons déjà existantes de basile/anouk (zone 0, proches du même bord nord) débordent de quelques dixièmes d'unité sur le territoire de la zone 4 par leurs cercles de collision de mur (constatation géométrique, aucun test ne l'interdit et aucun champ `zone` n'existe sur `D.buildings` pour en être affecté) — sans conséquence fonctionnelle, documenté plutôt que découvert en silence plus tard.

**Livré** :
- `public/game/data-world.js` : cinquième zone (`id: 4`, `bounds: [-16, 4, 22, 34]`, polygone hexagonal avec bosse nord organique au-delà de son propre bord), `light: "ombre"`, `humidity: 1` (famille déjà en usage), `cost: {}`, `rep: 0`. Nouvelle constante partagée `zoneGroundColors` (5 teintes, la cinquième `0x9dbbac` tirée de la famille feuillage/vert déjà documentée par `direction-artistique.md`), exportée à côté de `zones` pour que `render-world.js`/`render-flow.js` n'aient plus chacun leur propre littéral à maintenir en parallèle — corrigé après une relecture `/code-review` qui avait relevé la duplication (voir plus bas).
- `public/game/data-buildings.js` : deux habitants génériques (`villageois-1`, `villageois-2`), rôle `resident` (le rôle déjà existant le plus simple, comme demandé par le critère de sortie littéral de l'epic — « resident au minimum, pas de nouveau rôle inventé à ce stade »), `roleData.line` neutre, teintes toutes reprises de personnages déjà en jeu (`accent`/`personColor`/`roofColor` identiques à des valeurs déjà utilisées par basile/hugo/lea/noe — aucune couleur neuve introduite pour eux). **Aucune identité narrative** : pas de nom, pas de personnalité, conformément à la note de tête de la phase 3 du backlog (les personnages d'Acte I-III sont un chantier de la phase 4).
- `public/game/data.js` : `resourcePositions[4]` (une position bois/pierre/argile) et trois entrées ajoutées à chacune des trois listes `sites.wood`/`sites.stone`/`sites.clay`, pour amener les totaux par type dans la zone 4 à 4 au minimum — même plancher que `tests/garden-rules-6.cjs` vérifie déjà génériquement pour les zones 0 à 3 (« Renewable sites are spread over all zones... »).
- `public/garden-state-lifecycle.js` : `unlocked: [0, 4]` par défaut sur une partie neuve (au lieu de `[0]`).
- `public/garden-state-validate.js` : domaine valide de `unlocked` étendu à `[0, 1, 2, 3, 4]`.
- Trois tests existants qui codaient en dur « toutes les zones » comme `[0, 1, 2, 3]`, un vrai bug latent révélé par l'ajout d'une cinquième zone (une sauvegarde de test qui « déverrouille tout » ne déverrouillait plus réellement tout) : `tests/garden-rules-6.cjs`, `tests/garden-visual.cjs`, `tests/garden-size.cjs`, tous corrigés vers `D.zones.map((z) => z.id)` (ou son équivalent côté navigateur) plutôt qu'un nouveau littéral `[0, 1, 2, 3, 4]` qui aurait reproduit exactement le même piège pour une sixième zone future.
- `tests/garden-construction.cjs` : le point `(-6, 23)`, utilisé par un test existant comme exemple de coordonnée « en dehors de la carte entière », se trouve désormais à l'intérieur de la zone 4 — mis à jour vers `(100, 100)`, sans ambiguïté possible avec l'agrandissement de la carte, le test gardant son intention initiale intacte (un point réellement hors de toute zone doit continuer à refuser un placement).
- Nouveau `tests/garden-village-zone.cjs` (5 tests) : zone 4 toujours déverrouillée avec coût nul ; bordure exacte avec la zone 0 (échantillonnage de part et d'autre de la frontière réelle, calculée plutôt que recopiée à la main après qu'une première version à la main s'est trompée — voir plus bas) ; absence de chevauchement entre la zone 4 et n'importe quelle autre zone (balayage programmatique de tout point de son propre rectangle de bornes) ; les deux villageois ont le rôle `resident`, aucune quête, et se trouvent bien dans la zone 4 ; toutes les ressources de la zone 4 résolvent réellement vers elle via `zoneAt` (données, pas une affirmation) et atteignent le plancher de 4 par type.
- `package.json` : nouveau fichier de test ajouté à la liste explicite du script `test` (leçon déjà tirée à C2.11).

**Bug trouvé en écrivant le test lui-même, corrigé avant le commit, pas contourné** : la première version de `tests/garden-village-zone.cjs` calculait à la main les coordonnées de la frontière zone0/zone4 pour cinq abscisses et s'est trompée sur l'interpolation (`(-10, 20.2)` au lieu de `(-10, 20.8)` réellement sur la droite du polygone) — le test a échoué à l'exécution, révélant l'erreur, plutôt que de valider silencieusement une hypothèse fausse. Corrigé en calculant `boundaryZ(x)` directement depuis les points du polygone de la zone 0 plutôt qu'en recopiant des valeurs à la main une seconde fois.

**Vérification du rendu, le vrai gate, pas une formalité (voir la mise en garde d'`execution-continue.md`)** : `tests/garden-material-audit.cjs` exécuté directement contre la page servie localement (`python3 -m http.server --directory public`, Playwright avec l'exécutable Chromium préinstallé) → `PASS material/geometry audit: {"materialsInspected":98,"transparentAllowed":1,"timesSampled":5}` (97 avant cet epic, +1 seulement : la seule teinte réellement nouvelle est `zoneGroundColors[4]` — chaque couleur des deux nouvelles maisons est une réutilisation exacte d'une couleur déjà en jeu ailleurs, donc déjà présente dans le cache de matériaux partagé de `GardenModels.mat()`). Aucune extension de `TRANSPARENT_ALLOWLIST` nécessaire (aucun matériau transparent introduit). `npm run test:browser` complet non rejoué en chaîne (son tout premier script, `tests/garden-play.cjs`, échoue dans ce bac à sable faute de repli Chromium documenté — limite d'environnement préexistante et sans rapport avec cet epic, déjà constatée à C3.2) ; les scripts réellement concernés par ce rendu (`garden-material-audit.cjs`, et `campaign-house-visual.cjs` rejoué par précaution pour confirmer l'absence de régression sur la maison refuge voisine) ont été exécutés directement, conformément au précédent déjà posé par C2.8/C3.2.

**Relecture multimodale réelle** (capture Playwright, caméra déplacée sur la position du joueur suivie par le jeu — `window.GardenApp.view.position` — plutôt que sur la caméra elle-même, qui est reverrouillée sur le joueur à chaque image ; lue avec l'outil Read, comparée explicitement à `direction-artistique.md`) : les deux maisons du coin de village se distinguent nettement à la silhouette (murs en pierre claire, toit à deux pans brun, identiques en facture aux maisons déjà en jeu), la teinte de sol du coin de village se distingue subtilement de celle de la pépinière tout en restant dans la même famille verte, aucune surface brillante, aucune couleur hors des familles documentées, les maisons de basile/anouk (zone 0, à l'arrière-plan de la capture) restent visuellement séparées des deux nouvelles sans chevauchement perceptible. Conforme à direction-artistique.md §« Silhouette et géométrie » et §« Palette existante ».

**Résultat réellement exécuté** : `npm ci` puis `npm test` → **382/382** (377 existants + 5 nouveaux dans `garden-village-zone.cjs`, zéro régression). Sortie complète :

```
# tests 382
# suites 0
# pass 382
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

`/code-review` (skill `code-review`, niveau medium) exécuté sur le diff complet : trois observations, toutes corrigées avant ce commit plutôt que laissées pour un futur passage — (1) rendre le fresh state par défaut `unlocked: [0, 4]` double mécaniquement la capacité de pots de départ (`Math.min(48, s.unlocked.length * 12)`, dans `garden-state-validate.js` et `garden-state-cmd-b.js`), effet de bord réel et assumé plutôt que corrigé par une exception ad hoc : la formule traitait déjà la zone 0 (gratuite) comme n'importe quelle autre zone débloquée avant cet epic, donc une seconde zone gratuite suit exactement le même principe déjà en place, pas une incohérence nouvelle — documenté ici plutôt que retouché en silence, une évolution de cette formule spécifique restant hors du mandat de cet epic ; (2)/(3) les deux littéraux de couleur dupliqués et les deux autres tests (`garden-visual.cjs`/`garden-size.cjs`) codant encore `[0, 1, 2, 3]` en dur, corrigés comme décrit plus haut (constante `zoneGroundColors` partagée, `D.zones.map(...)` généralisé).

## Vérification locale du 18 septembre 2026 — première quête d'outil (epic C3.6)

Déclenchement automatisé (routine cloud horaire). **Anti-hallucination faite avant tout le reste** : dernier epic « fait » du backlog était C3.5, commit `074d9cf9fcb05c60950b7daae19561a53d2dcb4a` — `git show --stat 074d9cf` confirme le commit réel (15 fichiers, correspond exactement à l'entrée existante de ce fichier et de `campagne-backlog.md`) ; `npm ci && npm test` relancés indépendamment → **382/382**, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Les trois derniers epics consignés (C3.3/C3.4/C3.5) ne sont pas tous `bloqué` : aucune pause anti-emballement à déclencher.

**Choix de l'epic.** Seul epic `todo` de la phase 3 à dépendances satisfaites : **C3.6** (dépend de C3.5, `fait`). C2.2v/C2.5v/C2.8v/C2.9 (phases 1/2) restent sautés pour la raison inchangée depuis plusieurs déclenchements (le rendu de la maison refuge n'est toujours câblé dans aucune scène jouable réelle, chantier de la phase 3 elle-même). C3.7 (porte de sortie) dépend directement de C3.6, pas encore accessible.

**Implémenté directement par l'orchestrateur** (épic de schéma/moteur pur, aucun rendu, aucune identité narrative à inventer : le critère de sortie littéral demande seulement que `reward.tools` existe et se persiste, pas qu'un geste de construction consomme l'outil). `public/game/data-quests.js` gagne une troisième entrée, `bois-pour-l-hiver` (npcId `villageois-1`, l'un des deux habitants du coin de village posés par C3.5), objectif `deliver` déjà supporté par `quests.js` (item `wood`, quantité 4). Écart documenté, aucune identité narrative inventée : le titre et le choix de l'item reprennent tels quels la ligne de saveur déjà existante de `villageois-1` dans `data-buildings.js` (« range le bois pour l'hiver », posée par C3.5), sans lui donner de nom ni de personnalité — conforme à la note de tête de la phase 3 (les identités d'Acte I-III sont un chantier de la phase 4). La récompense, `["hachette"]`, est un nom générique choisi sans lien avec le « sécateur » cité par le design §10 chapitre 2 (Noé, Acte I, phase 4) : ce nom-là appartient à une scène narrative pas encore écrite, le réutiliser ici l'aurait préempté.

`public/garden-state-cmd-e.js` : extension additive du gestionnaire de complétion de quête, `reward.tools` traité exactement sur le même patron que `reward.plans` déjà existant (boucle, ajout seulement si absent, jamais de doublon) — un outil rejoint `s.campaignTools` seulement à la complétion réelle, jamais à l'acceptation (vérifié par test, pas supposé). `public/garden-state-lifecycle.js` : `campaignTools: []` sur une partie neuve. `public/garden-state-validate.js` : validation stricte (liste de chaînes uniques, rejetée sinon avec message explicite) + migration `??= []` pour toute sauvegarde antérieure à cet epic.

**Limite honnête, documentée dans le critère de sortie lui-même, pas contournée** : aucun geste de construction de campagne ne consomme encore un outil comme prérequis (contrairement à hache/pioche/pelle du jardin libre, déjà des emplacements d'équipement) — cet epic prouve seulement que la récompense existe et se persiste depuis un vrai chemin de commande (`GardenState.command()`), l'usage mécanique de l'outil reste un chantier séparé, à ne pas inventer ici.

Nouveau `tests/campaign-tools-quest.cjs` (8 tests) : partie neuve sans outil ; acceptation seule ne donne rien ; complétion réelle (via `g.command()`, jamais un accès direct à l'état) donne exactement `["hachette"]` ; complétion refusée avant l'objectif atteint, rien n'est accordé ; une récompense synthétique ne duplique jamais un outil déjà possédé ; migration d'une sauvegarde sans `campaignTools` vers une liste vide ; sauvegarde bien formée validée sans changement ; rejet explicite d'un `campaignTools` malformé (non-tableau, entrée non-chaîne, doublon) ; aller-retour JSON strictement identique après une complétion réelle. `package.json` : nouveau fichier ajouté à la liste explicite du script `test` (leçon déjà tirée à C2.11).

**Régression trouvée et corrigée, pas contournée** : `tests/garden-quests-schema.cjs` contenait un test qui affirmait littéralement « `D.quests` porte deux entrées de départ » (`Object.keys(D.quests).sort()` comparé à exactement `["first-drip", "first-harvest"]`) — devenu faux par construction dès l'ajout de `bois-pour-l-hiver`, pas un faux positif. Corrigé en élargissant l'assertion et le titre du test à trois entrées plutôt que de le supprimer ou de l'affaiblir.

**Résultat réellement exécuté** : `npm ci` puis `npm test` → **390/390** (382 existants + 8 nouveaux dans `campaign-tools-quest.cjs`, zéro régression une fois `garden-quests-schema.cjs` corrigé — une première exécution avait échoué à 389/390 sur ce test devenu obsolète, corrigé avant ce commit, jamais ignoré). Sortie complète :

```
# tests 390
# suites 0
# pass 390
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

`/code-review` (skill `code-review`, niveau medium) exécuté sur le diff complet : aucun défaut relevé (flux `reward.tools` → `s.campaignTools` tracé de bout en bout — donnée de quête, gestionnaire de complétion, état initial, validation/migration — npcId et type d'objectif référencés existent tous deux, couverture de test jugée suffisante).

Aucun rendu/géométrie/matériau touché (epic de schéma/moteur pur) : `test:browser`/`test:visual` complets non requis, même exemption que C2.6a/b/c, C2.7, C2.8, C3.1, C3.3, C3.4. Vérification navigateur ciblée tout de même effectuée (aucune nouvelle balise `<script>` introduite, les quatre fichiers touchés étaient déjà chargés) : page servie localement (`python3 -m http.server --directory public`), Playwright avec l'exécutable Chromium préinstallé → aucune erreur console ni erreur de page, `window.GardenData.quests["bois-pour-l-hiver"].reward.tools[0] === "hachette"` confirmé directement dans la page.

Aucun choix déjà confirmé du design touché (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne). Aucune identité narrative introduite (conforme à la note de tête de la phase 3) ; le nom d'outil choisi (« hachette ») évite délibérément « sécateur », déjà réservé par le design à une scène narrative de la phase 4 non encore écrite.

**Cet epic ne ferme aucune porte de phase** : C3.7 (porte de sortie de la phase 3) dépend de C3.2/C3.4/C3.6, tous `fait` désormais — devient le seul candidat du prochain déclenchement. Aucune fusion vers `main` à ce stade.

Aucune relecture narrative adverse requise (pas un epic de Scénariste). Aucun choix déjà confirmé du design touché (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne). Aucune teinte/matériau hors des familles de `direction-artistique.md`. Aucune identité narrative introduite (pas de nom de PNJ, conformément à la note de tête de la phase 3) — C3.6 (première quête d'outil) pourra reconvertir l'un des deux villageois en `quest-giver` sans casser cet epic, `resident` restant une lecture strictement additive du rôle.

## Vérification locale du 18 septembre 2026 — trois nuits jouées, tranche verticale sauvegardable, porte de sortie de la phase 3 (epic C3.7)

Déclenchement automatisé (routine cloud horaire). **Anti-hallucination faite avant tout le reste** : dernier epic « fait » du backlog était C3.6, commit `904b6853123958bc5c178015b0a7d45ca085edba` — `git show --stat 904b685` confirme le commit réel, son message et son contenu correspondent exactement à l'entrée existante de ce fichier et de `campagne-backlog.md` ; `npm ci && npm test` relancés indépendamment → **390/390**, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Les trois derniers epics consignés (C3.4/C3.5/C3.6) ne sont pas tous `bloqué` : aucune pause anti-emballement à déclencher.

**Choix de l'epic.** Seul epic `todo` à dépendances satisfaites : **C3.7** (dépend de C3.2/C3.4/C3.6, tous `fait`), déjà nommé comme seul candidat par l'entrée précédente de ce journal — la porte de sortie de la phase 3 elle-même. C2.2v/C2.5v/C2.8v/C2.9 (phases 1/2) restent sautés pour la raison inchangée depuis plusieurs déclenchements (aucune position/déplacement réel des Rainelles dans le monde de campagne, chantier toujours non entamé). Implémenté directement par l'orchestrateur (épic de test d'intégration pur, aucune délégation).

**Livré** : nouveau `tests/campaign-phase3-gate.cjs` (fichier probable du backlog, aucun écart), plus `package.json` (liste explicite du script `test`, leçon déjà tirée à C2.11). Un seul test narratif, à travers le vrai vecteur de commandes (`GardenState.command()`, jamais un accès direct à l'état), rejouant les quatre promesses de la porte de sortie (design §15) sur trois nuits :

- **Habiter** : la pièce d'accueil (seul espace jouable à ce stade, C3.1) est réparée avant la première nuit, en consommant réellement son coût déclaré (`wood`/`clay`) depuis l'inventaire.
- **Croiser** : un cultivar réel est résolu au pot (C1.3) à la première nuit, rejoué dans ce contexte plutôt que réimporté d'un test existant.
- **Organiser** : la première Rainelle naît par la rencontre scénarisée (C2.3) à la première nuit ; la deuxième naît par bourgeon/nurserie (C3.4) à la deuxième nuit, après avoir réservé une place de vie libre (C3.3, un habitat enregistré au préalable). Les deux tiennent ensuite une chaîne courte arroser→récolter sans aucune intervention après leur enseignement (`teachGesture`, C2.4/C2.5), réutilisant exactement les mêmes primitives que la porte de sortie de la phase 2 (`campaign-rainelles-chain.cjs`) plutôt que d'en réinventer une : quelques cycles de simulation suffisent à observer une production réelle déposée dans le panier et l'humidité du spécimen maintenue, sans commande supplémentaire.
- **Rencontrer** : la quête `bois-pour-l-hiver` (C3.6, auprès de `villageois-1`, l'un des deux habitants du coin de village posés par C3.5) est acceptée puis complétée réellement — l'outil `hachette` n'est jamais accordé à l'acceptation, vérifié explicitement.

Une sauvegarde JSON aller-retour à mi-parcours (après la deuxième nuit, avant la troisième — pas seulement en fin de scénario) est vérifiée strictement identique par un `assert.deepEqual` sur l'état complet (`reloaded.s`/`g.s`), puis la troisième nuit est rejouée **sur l'état rechargé lui-même**, pas sur l'original, pour que le round-trip soit une vraie étape jouée du scénario plutôt qu'un contrôle isolé en fin de fichier. Chaque nuit vérifie explicitement l'absence de double crédit (design §16, « aucune commande, récolte, nuit ou naissance ne crédite deux fois ») : aucun second cultivar sur un pot vide à la deuxième nuit, aucune troisième Rainelle sans nouveau bourgeon prélevé à la troisième, l'outil de quête et la complétion de la quête elle-même ne se répètent jamais après le rechargement. La chaîne arroser/récolter continue aussi de fonctionner sans intervention après le rechargement et la troisième nuit — le panier ne perd jamais ce qu'il contenait déjà.

**Limite honnête, documentée dans le fichier lui-même plutôt que devinée en silence** : « dormir dans la maison refuge » reste, comme à chaque epic de cette phase depuis C2.2v, la seule commande générique `sleep` du jeu — aucune scène de maison, aucune transition de caméra, aucun écran de confirmation du pot n'existe encore pour l'habiller (C2.2v/C2.5v, toujours `todo`, chantier non entamé par cet epic puisqu'il ne touche aucun rendu). Le test ne prouve donc que la couche moteur réellement câblée aujourd'hui, jamais une interface simulée qui n'existe pas.

**Résultat réellement exécuté** : `npm ci` puis `npm test` → **391/391** (390 existants + 1 nouveau dans `campaign-phase3-gate.cjs`, zéro régression). Sortie complète :

```
# tests 391
# suites 0
# pass 391
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

`/code-review` (skill `code-review`, niveau medium) exécuté sur le diff complet : aucun défaut relevé — diff entièrement composé du nouveau fichier de test plus le câblage `package.json`, aucun code applicatif modifié. Le relecteur a explicitement retracé les hypothèses du test contre les implémentations réelles qu'il exerce (`formBud`/`harvestBud` et le blocage par place de vie libre dans `rainelles.js`/`campaign-stations.js`, corroborés par `campaign-nursery.cjs` ; le schéma de la quête `bois-pour-l-hiver` dans `data-quests.js`) et les a jugées cohérentes avec l'usage qu'en fait le test.

Aucun rendu/géométrie/matériau touché (epic entièrement fait d'un test Node) : `test:browser`/`test:visual` complets non requis, même exemption que C2.6a/b/c, C2.7, C2.11, C3.6. Aucune relecture narrative adverse requise (pas un epic de Scénariste, aucune identité narrative introduite). Aucun choix déjà confirmé du design touché (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne).

**Cet epic ferme la porte de sortie de la phase 3** (design §15 : « habiter, croiser, organiser, rencontrer fonctionnent ensemble en une courte partie sauvegardable » — le test ci-dessus le prouve littéralement, à travers le vrai vecteur de commandes, sur une partie sauvegardable dont l'aller-retour JSON à mi-parcours est vérifié strictement identique). Scénarios de validation de la section 16 du design pertinents à ce périmètre déjà couverts, ici et par la suite existante : « aucune commande, récolte, nuit ou naissance ne crédite deux fois » (vérifié explicitement à chaque nuit ci-dessus) ; « multiplier un cultivar conserve exactement ses traits » et « chaque Rainelle conserve un seul geste... refus de multiplication » (déjà prouvés par `campaign-phase1-gate.cjs`/`campaign-phase2-gate.cjs`, non rejoués ici pour ne pas dupliquer) ; « les sauvegardes existantes sont préservées » (round-trip strict à mi-parcours ci-dessus). Les deux volets « passage coupé » et « déplacer un poste... rangement » du même tableau restent honnêtement hors de portée — le critère de sortie littéral de la phase ne les nomme pas, même précédent exact que C1.5/phase 1 et ces deux mêmes volets/phase 2.

### Clôture de la phase 3 (Tranche verticale) — 18 septembre 2026

Porte de sortie (design §15, reprise dans `orchestration.md`) : *« habiter, croiser, organiser, rencontrer fonctionnent ensemble en une courte partie sauvegardable. »* Le test de porte ci-dessus (`tests/campaign-phase3-gate.cjs`) prouve littéralement ces quatre promesses ensemble, à travers le vrai vecteur de commandes, sur trois nuits jouées et une sauvegarde JSON aller-retour à mi-parcours vérifiée strictement identique. Les scénarios de validation pertinents de la section 16 du design sont couverts, ici et par la suite existante des phases 1/2 : aucun double crédit, traits de cultivar conservés à la multiplication, geste unique par Rainelle avec refus de multiplication, sauvegardes préservées.

Écart délibéré, assumé depuis l'ouverture de la phase et non un raccourci pris à sa clôture : aucun nom de personnage d'Acte I-III (Noé, Mira, Basile...) ni contenu de quête associé n'a été utilisé dans tout ce lot (C3.1-C3.7) — ces identités et leurs quêtes restent un chantier de la phase 4 (§10 du design). Seule la capacité mécanique générique (réparation, habitat, deuxième Rainelle, village habité, récompense d'outil) est prouvée ici, jamais un contenu narratif anticipé. « Dormir dans la maison refuge » reste, comme documenté à répétition depuis C2.2v, la seule commande générique `sleep` du jeu — aucune scène de maison, aucune transition de caméra, aucun écran de confirmation du pot ne l'habille encore.

**C2.2v, C2.5v, C2.8v et C2.9 restent `todo` malgré cette fusion**, décision assumée et non silencieuse : les quatre partagent la même dépendance de fond (une position/un déplacement réels des Rainelles dans le monde de campagne — la maison refuge rendue par C3.2 n'est câblée dans aucune scène jouable, `grep GardenCampaignHouse public/garden.js public/garden-frame.js` ne remonte toujours aucune occurrence hors du module lui-même et des fichiers d'état, revérifié à ce déclenchement), et aucun de leurs critères de sortie propres ne fait partie du critère de sortie de la phase 3 cité par le design. Même précédent exact que C1.5 (épinglage) à la clôture de la phase 1, et ces quatre mêmes epics déjà différés à la clôture de la phase 2.

Conformément à la politique de porte de phase révisée pour l'exécution non supervisée (`execution-continue.md` : « aucun pipeline n'est connecté à `main` ... fusionner une phase validée ... est une étape d'implémentation comme une autre, pas un acte de publication » — revérifié : toujours aucun workflow CI dans ce dépôt), la branche `maison-des-possibles` est fusionnée dans `main` en local puis les deux branches sont poussées vers `github.com/EdwinDayot/edwindayot.fr`. Aucun déploiement, aucune publication : `main` de ce dépôt n'est relié à aucun service d'hébergement.

**Pour le prochain déclenchement** : la phase 4 (« Première version jouable, actes I à III ») n'a encore aucun epic détaillé — seulement le paragraphe de contour du plan de réalisation. Un futur passage du Cartographe devra la détailler, comme à l'ouverture de la phase 3, avant toute implémentation.

## Planification du 18 septembre 2026 — premier lot d'epics de la phase 4 (Acte I, chapitres 1 à 3)

Déclenchement automatisé (routine cloud horaire). **Anti-hallucination faite avant tout le reste** : dernier epic « fait » du backlog était C3.7, commit `b2341e13ad27a8af76a096c36f16dc739a7cc3f1` — `git show --stat b2341e1` confirme le commit réel, correspondant exactement à l'entrée existante de ce fichier et de `campagne-backlog.md` ; `npm ci && npm test` relancés indépendamment → **391/391**, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Les trois derniers epics consignés (C3.5/C3.6/C3.7) ne sont pas tous `bloqué` : aucune pause anti-emballement à déclencher. Vérification supplémentaire de l'état distant (leçon tirée du déclenchement précédent sur C3.3, où un ref local `origin/main` non rafraîchi avait d'abord semblé indiquer une anomalie inexistante) : `git fetch origin main maison-des-possibles` explicite, puis `git ls-remote origin main maison-des-possibles` et `git merge-base --is-ancestor b2341e1 origin/main` → vrai — la fusion de la phase 3 vers `origin/main` est bien réelle, pas seulement locale à ce conteneur.

**Rôle Cartographe endossé** (aucun code ni test modifié ce déclenchement), conformément à l'étape 4 de la routine : la phase 3 est close et fusionnée dans `main` depuis le déclenchement précédent ; la phase 4 n'avait encore aucun epic détaillé, seulement le paragraphe de contour du plan de réalisation et la mention, déjà fixée d'avance par la note de tête de la phase 3, que l'epic **C4.3** porterait « chapitre 3, première hybridation et épinglage montré par Iris » (dépendance déjà déclarée par C1.5). C'est exactement la condition citée par `orchestration.md`/`execution-continue.md` pour endosser ce rôle plutôt que de forcer un epic non détaillé.

**Recherche factuelle faite avant d'écrire un seul epic** (un agent Explore dédié a inspecté le code, pas une supposition) :
- Aucun système de personnalisation du personnage (prénom/pronoms/apparence/vêtements, design §2) n'existe : `s.player` (`public/garden.js:86`) n'est qu'une position caméra `{x,y,z}` ; aucune occurrence de personnalisation/avatar/pronom au-delà de classes CSS sans rapport (`.letter-spacing` etc.).
- Aucun système de dialogue/conversation PNJ au-delà d'une ligne de statut : le rôle `resident` (`data-roles.js:63-74`) affiche seulement `roleData.line` dans la barre de statut, `command: null` ; le rôle `quest-giver` (`data-roles.js:78-88`) ouvre un panneau mécanique accepter/compléter (`hud-panel.js:52-83`), aucun champ de texte narratif sur une quête.
- Le carnet botanique existe déjà (`hud-panel.js:146-215`, panneau `"notebook"`, livré par C1.4) et peut recevoir une nouvelle entrée par une simple branche `buildPanelRows`, sans nouveau panneau.
- L'épinglage (C1.5) est à 0 % construit : `botany-genetics.js`/`botany-pot.js` ne portent aucun concept de pin/épingle.
- `createSpecimen` (C1.6, `cultivars.js:47`) accepte déjà une position `{x,z}` libre : « installer un spécimen chez soi » ne demande donc aucun nouveau champ de schéma, seulement des coordonnées proches de la maison refuge.
- Le patron `reward.tools` (C3.6, `garden-state-cmd-e.js:38-40` : un outil rejoint `s.campaignTools` seulement à la complétion réelle, jamais à l'acceptation) s'étend trivialement à un nouveau champ de récompense analogue, `reward.unlockHouseSpace`, pour déverrouiller un espace de la maison refuge (`campaign-house.js`, C3.1) à la complétion d'une quête.
- Le schéma d'un PNJ (`data-buildings.js`, voir les entrées `villageois-1`/`villageois-2` de C3.5) et le rôle `quest-giver` sont déjà génériques et réutilisables tels quels pour de nouveaux PNJ nommés (Noé, Iris), sans aucune modification de moteur.

**Trois epics détaillés dans `campagne-backlog.md` (C4.1, C4.2, C4.3)**, couvrant l'Acte I en entier (chapitres 1 à 3 du design §10) :

- **C4.1** — Chapitre 1, « La clé sous le pot vide » : outil de fortune déjà en possession du joueur dès une partie neuve (réutilise `s.campaignTools`, aucun nouveau système d'équipement), réparation de la pièce d'accueil (mécanique déjà existante depuis C3.1, aucun changement), nouvelle interaction de choix mutuellement exclusif sur les marques de meuble retrouvées (conserver/encadrer/repeindre), et surtout un **nouveau mécanisme narratif générique minimal** — un catalogue `data-narrative.js` de courts textes à affichage unique, un ensemble `s.campaignFlags` de textes déjà vus, une entrée de carnet listant les textes débloqués — introduit pour la première lettre d'Alma mais pensé pour être réutilisé tel quel par les chapitres suivants (généraliser plutôt que spécialiser, plutôt qu'un texte codé en dur propre à cette seule scène).
- **C4.2** — Chapitre 2, « Ce qu'on reconnaît encore » : un nouveau PNJ réel, Noé, avec une quête réelle de livraison d'une plante de départ (sans passage par le pot, conformément au design : « Plant ordinaire accessible avant le pot »), récompensée par `reward.unlockHouseSpace` qui déverrouille l'espace `"serre"` de la maison refuge, verrouillé par défaut depuis C3.1.
- **C4.3** — Chapitre 3, première hybridation rejouée dans son contexte narratif (pot, carnet, multiplication — toutes mécaniques déjà existantes, aucune nouveauté), spécimen installé chez soi (position libre déjà supportée par `createSpecimen`), et un nouveau PNJ réel, Iris, dont la rencontre pose le flag narratif devenant **la condition dont dépend C1.5** (l'épinglage). Cet epic ne construit pas l'épinglage lui-même — C1.5 redevient un candidat légitime une fois C4.3 `fait`.

Dépendances enchaînées dans l'ordre d'écriture (C4.1→C4.2→C4.3), aucune ne saute une dépendance non satisfaite. **Choix délibéré de ne pas détailler les neuf chapitres de la phase d'un bloc** : l'Acte II (chapitres 4-6) et l'Acte III (chapitres 7-9) dépendent de décisions — identité des PNJ restants, forme exacte des gestes Replanter/Préparer/Trier jamais implémentés — que ce seul lot doit encore éclairer, même principe que le lot initial de la phase 3.

**Écart assumé, documenté explicitement en tête de la section phase 4 du backlog** : la personnalisation du personnage (design §2 : prénom, pronoms, apparence, vêtements) reste hors de ce lot — système orthogonal que la porte de sortie littérale de la phase ne nomme pas. C4.3 utilise un nom générique par défaut pour la première page de carnet en attendant un futur epic dédié à ce système, plutôt que de l'inventer ici ou de deviner un nom.

Aucun choix déjà confirmé du design remis en cause (Alma vivante — au contraire, seule identité narrative réutilisée telle quelle par ce lot ; nom des Rainelles ; culpabilisation de fin de campagne). Aucun code ni test modifié ce déclenchement : travail de planification seul dans `docs/campagne-backlog.md`, comme prescrit par `orchestration.md`/`execution-continue.md` pour ce cas.

**Pour le prochain déclenchement** : C4.1 est le seul epic `todo` de la phase 4 à dépendances satisfaites (C3.1/C3.2, tous deux `fait`) — candidat naturel, à choisir sauf si une raison nouvelle apparaît d'ici là.

## Implémentation du 18 septembre 2026 — Épic C4.1 : « La clé sous le pot vide », outil de fortune, marques de meuble, première lettre d'Alma

Déclenchement automatisé (routine cloud horaire). **Anti-hallucination faite avant tout le reste** : dernier epic « fait » du backlog était C3.7 (le Cartographe du déclenchement précédent n'a modifié que la planification, aucun statut), commit `b2341e13ad27a8af76a096c36f16dc739a7cc3f1` — `git show --stat b2341e1` confirme le commit réel, correspondant exactement à l'entrée existante de ce fichier et de `campagne-backlog.md` ; `npm ci && npm test` relancés indépendamment → **391/391**, identique au rapport existant. Aucun bandeau de pause en tête de `campagne-backlog.md`. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Les trois derniers epics consignés (C3.6/C3.7, plus la session de planification du Cartographe qui n'est pas un epic bloqué) ne sont pas trois blocages consécutifs : aucune pause anti-emballement à déclencher.

**Choix de l'epic.** Phase 4 déjà détaillée (lot Acte I, chapitres 1-3, déclenchement précédent). **C4.1** est le seul epic `todo` à dépendances satisfaites (C3.1/C3.2, tous deux `fait`) — exactement le candidat déjà annoncé par l'entrée précédente de ce journal.

**Livré, exactement dans les fichiers probables de l'entrée de backlog, plus deux écarts documentés** :
- Nouveau `public/game/data-narrative.js` : catalogue générique de textes narratifs à affichage unique (`TEXTS`, entrées `{id, trigger, title, text}`), pensé réutilisable par les chapitres suivants dès ce premier epic (demande explicite du backlog) — `trigger` est un simple signal texte, jamais un moteur de règles qui re-dérive une condition depuis l'état : la commande qui le déclenche sait déjà que la condition est vraie (elle vient de la rendre vraie). Une seule entrée aujourd'hui, `alma-marques-meuble`, dont le texte est repris **mot pour mot** du design (§10, chapitre 1 : « Tu peux déplacer les meubles. Même ceux dont tu crois te souvenir exactement. »).
- Nouveau `public/garden-state-cmd-n.js` : `chooseFurnitureTreatment` enregistre un choix explicite et mutuellement exclusif (conserver/encadrer/repeindre — vocabulaire repris tel quel du design) sur les marques de taille retrouvées de l'armoire, refusé si un choix a déjà été fait (jamais réinitialisé après confirmation, comme demandé), puis résout le déclencheur narratif générique (`Narrative.pendingReveal`) et l'ajoute à `s.campaignFlags` s'il n'y est pas déjà.
- `public/game/campaign-house.js` étendu (pas un nouveau fichier) : `freshHouse()` porte désormais aussi `furnitureMarks: null` (un champ de premier niveau de `campaignHouse`, pas un septième « espace » : il n'a ni coût de réparation ni verrou) ; `FURNITURE_TREATMENTS` exporté une seule fois ici plutôt que redéfini dans `garden-state-cmd-n.js`/`garden-state-validate.js`, même principe que `SPACE_IDS` déjà partagé.
- `public/garden-state-lifecycle.js` : `s.campaignTools` démarre désormais avec `["outil-de-fortune"]` au lieu de `[]` (design §10 : « un outil de fortune déjà en possession du joueur » — réutilise le sac existant, aucun nouveau système d'équipement) ; nouveau champ `s.campaignFlags: []`.
- `public/garden-state-validate.js` : validation étendue de `campaignHouse` (accepte `furnitureMarks` absent, `null`, ou une des trois valeurs — migration à deux niveaux, même patron que `campaignStations.habitats`) ; nouvelle validation de `campaignFlags` (liste plate de chaînes uniques, même forme que `campaignTools`) ; migrations par défaut ajoutées pour les deux champs.
- `public/game/hud-panel.js` : le panneau `"notebook"` gagne une branche de plus (pas un nouveau panneau) listant, en lecture seule (aucune action), les textes narratifs déjà présents dans `s.campaignFlags`.
- **Écart 1, non prévu par la liste de fichiers probables** : `public/index.html` (deux balises `<script>` ajoutées, `data-narrative.js` et `garden-state-cmd-n.js`, dans l'ordre de chargement déjà établi par les epics précédents) et surtout **`public/garden-state.js`** — bug latent trouvé et corrigé avant tout test : la liste des lettres de segments de commande chargés côté Node (`["a", ..., "m"]`) n'incluait pas `"n"`, ce qui aurait rendu `chooseFurnitureTreatment` invisible à tous les tests Node malgré un fichier syntaxiquement correct. Trouvé en lisant `garden-state.js` avant d'écrire un seul test, pas après un échec silencieux.
- **Écart 2, régression trouvée et corrigée, pas contournée** : faire démarrer `campaignTools` avec `["outil-de-fortune"]` cassait trois assertions existantes qui supposaient un sac vide au départ (`tests/campaign-tools-quest.cjs` : « a fresh campaign save starts with no campaign tools », « a tool reward is granted only on completion, never on acceptance », « completing before the objective is met is rejected and grants nothing » ; `tests/campaign-phase3-gate.cjs` : deux assertions sur `campaignTools`). Les cinq corrigées pour partir de `["outil-de-fortune"]` (et `["outil-de-fortune", "hachette"]` après la quête `bois-pour-l-hiver`), jamais affaiblies ni supprimées — même discipline que la correction de `garden-quests-schema.cjs` à l'epic C3.6.

**Limite honnête, documentée plutôt que devinée** : aucun objet « armoire » cliquable n'existe dans le monde 3D. `chooseFurnitureTreatment` est une commande réelle, testée en Node et en navigateur via le vrai vecteur `GardenState.command()`, mais rien dans le monde de jeu ne la déclenche encore depuis une interaction du joueur — même posture « posé, pas encore câblé au monde » que C2.2v/C2.5v/C2.8v, cohérente avec la liste de fichiers probables de cette entrée de backlog (aucun fichier de rendu n'y figurait). Réparer la pièce d'accueil reste, sans changement, le seul geste mécanique requis pour ce chapitre (aucune nouvelle mécanique de réparation ajoutée). Aucune identité de PNJ nouvelle (seule Alma, déjà confirmée par l'utilisateur).

**Résultat réellement exécuté** : `npm ci` puis `npm test` → **406/406** (391 existants + 15 nouveaux dans `tests/campaign-narrative.cjs`, zéro régression une fois les cinq assertions `campaignTools` ci-dessus corrigées). Sortie complète :

```
# tests 406
# suites 0
# pass 406
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

Cet epic touche l'interface (`hud-panel.js`) : `npm run test:browser` requis par `execution-continue.md`. La chaîne complète n'a pas pu aller au bout dans ce conteneur : `browserType.launch` échoue sur `tests/garden-play.cjs` (premier test de la chaîne) avec « Executable doesn't exist at /opt/pw-browsers/chromium_headless_shell-1243/... » — la révision préinstallée dans ce conteneur est `chromium_headless_shell-1194`, pas `1243`, et `garden-play.cjs` (comme 13 autres fichiers de test navigateur plus anciens) ne passe pas l'option `executablePath` de secours déjà utilisée par les fichiers plus récents (`garden-material-audit.cjs`, `campaign-house-visual.cjs`, `campaign-botany-visual.cjs`). **Vérifié que ce n'est pas une régression de cet epic** : `git stash` puis `node tests/garden-play.cjs` sur le commit `022434f` (avant tout changement de cet epic) reproduit l'échec à l'identique. Anomalie d'environnement pré-existante, non corrigée ici (corriger 14 fichiers sans rapport avec cet epic aurait été hors périmètre — `orchestration.md` : « jamais une refonte simultanée de plusieurs systèmes »), consignée telle quelle plutôt que dissimulée. **Vérification directe des tests navigateur réellement pertinents pour ce diff**, chacun lancé individuellement avec `node` (contournant la chaîne `&&` cassée) contre `python3 -m http.server 4174 --directory public` :
- `node tests/garden-material-audit.cjs` → **PASS** (`{"materialsInspected":98,"transparentAllowed":1,"timesSampled":5}`) : aucun matériau/maillage « au sol » nouveau introduit par cet epic (`hud-panel.js` est un panneau HUD dessiné sur canvas 2D, pas une scène Three.js), le gate visuel reste vert.
- Nouveau `tests/campaign-narrative-browser.cjs` (ajouté à `test:browser` dans `package.json`) → **PASS** : charge une vraie sauvegarde fraîche dans la page réelle, confirme `s.campaignTools` déjà peuplé de `outil-de-fortune` au chargement, appelle `chooseFurnitureTreatment` via `GardenState.command()` réel, vérifie que le carnet (touche J, `buildPanelRows` réel) affiche la lettre d'Alma en lecture seule exactement une fois, qu'un second choix est refusé, et qu'aucune erreur console n'apparaît.
- `tests/campaign-notebook-browser.cjs` (test existant, notebook du jardin libre côté cultivars) n'a pas pu être relancé dans ce conteneur pour la même raison d'environnement — vérifié par lecture du diff que la seule modification de `hud-panel.js` est une nouvelle boucle **ajoutée après** la boucle des espèces existante, sans toucher une seule ligne des branches `notebook` déjà présentes (cultivars en attente, espèces) : changement strictement additif, sans risque de régression identifiable, corroboré par le fait que `campaign-narrative-browser.cjs` exerce exactement le même `buildPanelRows` réel dans la même page sans erreur.

`test:visual` non requis : aucun changement de rendu 3D/shader/couleur (voir `direction-artistique.md`, aucune teinte introduite par cet epic).

`/code-review` (skill `code-review`, niveau medium) exécuté sur le diff complet : aucun défaut relevé — mécanisme générique bien testé (double-déclenchement, valeurs malformées, aller-retour JSON, migration d'une sauvegarde pré-epic), garde-fous de migration/validation cohérents avec le reste du fichier.

**Relecture adverse de ce contenu narratif** (epic touchant à la narration, requis par `orchestration.md`/`execution-continue.md`) : le texte de la lettre est repris **mot pour mot** du design §10 (aucune invention) ; les trois choix de traitement des marques (conserver/encadrer/repeindre) reprennent le vocabulaire exact du design §10 ; aucun des trois n'est présenté comme supérieur aux deux autres (pas de récompense différenciée, conforme à « trois choix légitimes »). Grammaire narrative des sections 9-11 : ce chapitre relève entièrement du verbe **recevoir** (§9, « d'abord le joueur reçoit une maison et des gestes ») — aucune mise en scène de culpabilité/exploitation (§11) n'est pertinente ici, le joueur n'a encore ni Rainelle ni automatisation. Aucun choix déjà confirmé du design remis en cause (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne). Aucune identité de PNJ nouvelle introduite.

**Cet epic ne ferme aucune porte de phase** (la porte de sortie de la phase 4 exige l'arc complet actes I-III, §15) : C4.2 (dépend de C4.1, maintenant `fait`) devient le candidat naturel du prochain déclenchement.
