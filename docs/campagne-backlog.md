# Backlog de la campagne — file d'epics

Source de vérité sur l'avancement. Voir [orchestration.md](orchestration.md) pour le processus, [game-design.md](game-design.md) pour la conception. Chaque epic a la taille d'une session d'agent, sur le modèle de ceux déjà livrés dans [garden.md](garden.md).

## Gabarit d'un epic

```
### C<phase>.<n> — <titre court>
Dépend de : <epics requis, ou « rien »>
Critère de sortie : <ce qu'un test/parcours doit prouver, formulé comme un fait observable>
Fichiers probables : <pointeurs>
Statut : todo | en cours | bloqué (raison) | fait (date, lien commit)
```

Seules les deux phases immédiatement exécutables (1 et 2) sont détaillées jusqu'au dernier epic. Les phases 3 à 7 gardent un contour de plan de réalisation (repris de la section 15 du design) ; le Cartographe les détaille epic par epic seulement en approchant leur tour, pour ne pas figer des décisions que les phases précédentes doivent encore éclairer.

---

## Phase 1 — Laboratoire botanique

Porte de sortie (design §15) : *le joueur reconnaît des caractères parentaux, cible un besoin et retrouve exactement son cultivar après rechargement.*

### C1.1 — Schéma cultivar/spécimen
Dépend de : rien
Critère de sortie : le modèle de sauvegarde distingue espèce fondatrice (déjà `D.species` existant), cultivar (id stable, nom, `parentIds`, traits figés) et spécimen (référence à un cultivar + emplacement + stade) ; une sauvegarde v3 existante charge sans cultivar (liste vide) et sans erreur ; un test vérifie qu'un cultivar créé puis rechargé garde exactement les mêmes traits et le même id.
Fichiers probables : `public/garden-state.js`, `public/garden-state-migrate.js`, nouveau `public/game/data-cultivars.js`
Statut : fait (2026-09-17, voir docs/campagne.md et commit sur `maison-des-possibles`). En pratique implémenté dans `public/game/cultivars.js` (pas `data-cultivars.js` : ce n'est pas un catalogue statique comme `data-species.js`, mais des fonctions pures sur l'état de sauvegarde, sur le modèle de `quests.js` face à `data-quests.js`) + `garden-state-lifecycle.js` + `garden-state-validate.js`.

### C1.2 — Grammaire des six premiers attributs
Dépend de : C1.1
Critère de sortie : chaque espèce fondatrice porte des valeurs sur les six axes retenus pour le premier prototype (port, feuilles, fleurs, palette, humidité préférée, fonction remarquable) ; une table de compatibilité déclare quelles paires d'espèces peuvent être croisées ; un test énumère toutes les paires déclarées compatibles et vérifie qu'aucune ne produit une combinaison invalide (ex. fonction sans support valide, cf. §4 « règles de compatibilité »).
Fichiers probables : `public/game/data-species.js`, nouveau `public/game/botany-genetics.js`
Statut : fait (2026-09-17, voir docs/campagne.md et commit sur `maison-des-possibles`). Écart volontaire, justifié dans le changelog : n'étend pas `data-species.js` (catalogue réel du jardin libre, sans rapport avec la flore fictive de Rivebrume) — `botany-genetics.js` porte son propre catalogue des huit espèces fondatrices du design §4.

### C1.3 — Le pot : croisement, héritage par locus, résolution au réveil
Dépend de : C1.2, C2.1 (l'horloge quotidienne doit exister pour qu'« une nuit » ait un sens)
Critère de sortie : poser deux graines compatibles dans le pot et dormir produit un cultivar dont chaque locus discret vient bien de l'un des deux parents à probabilité égale (vérifié statistiquement sur un grand nombre de tirages, seed fixée pour la reproductibilité du test) ; le résultat est fixé et sauvegardé à la confirmation de l'essai — recharger la partie ne le tire pas une seconde fois ; une seule paire par nuit au départ.
Fichiers probables : `public/garden-state-cmd-*.js` (nouveau segment), `public/game/botany-genetics.js`
Statut : fait (2026-09-17, voir docs/campagne.md et commit sur `maison-des-possibles`). En pratique le tirage vit dans un nouveau `public/game/botany-pot.js` (pas dans `botany-genetics.js`, laissé inchangé) ; le nouveau segment de commande est `public/garden-state-cmd-f.js` ; le champ de sauvegarde est `s.campaignPot` (pas `s.pot`, ambigu avec le type d'entité `"pot"` du jardin libre).

### C1.4 — Carnet de botanique
Dépend de : C1.3
Critère de sortie : après un réveil, l'interface montre filiation et différences par rapport aux parents ; le joueur peut nommer, conserver, mettre en réserve, donner ou composter le résultat ; la première conservation enregistre une graine mère de sécurité, récupérable gratuitement et sans valeur de revente dans la boîte de semences de la maison ; le carnet garde la découverte même si l'exemplaire est ensuite composté.
Fichiers probables : `public/game/hud-panel.js`, nouveau panneau, `public/game/data-cultivars.js`
Statut : fait (2026-09-17, voir docs/campagne.md et commit sur `maison-des-possibles`). En pratique dans `public/game/cultivars.js` (champ `disposition` ajouté aux entrées existantes, pas un nouveau `data-cultivars.js`) + nouveau `public/garden-state-cmd-g.js` (`renameCultivar`/`keepCultivar`/`storeCultivar`/`giveCultivar`/`compostCultivar`/`retrieveSeedBoxSeed`) + nouveau champ de sauvegarde `s.campaignSeedBox`. Deux écarts volontaires, justifiés dans docs/campagne.md : (1) les quatre décisions (garder/réserver/donner/composter) plus le renommage sont réparties sur **trois** rows par cultivar en attente (deux boutons chacune) plutôt que deux, et les identifiants d'action Canvas portent un suffixe `-cultivar` (`keep-cultivar`, etc.) pour ne jamais entrer en collision avec l'action `"store"` déjà existante du jardin libre (stockage d'un objet construit) ; (2) le renommage réutilise un `<input type="text">` HTML ordinaire caché par défaut (même principe que le sélecteur de fichier système, voir garden-structure.md) plutôt qu'un nouveau widget de saisie Canvas. Complète aussi le chargement navigateur resté incomplet depuis C1.1–C1.3 (`botany-genetics.js`/`botany-pot.js`/`cultivars.js`/`garden-state-cmd-f.js` n'étaient chargés que côté Node jusqu'ici) : `public/index.html` charge désormais toute la chaîne, condition nécessaire pour que les commandes de cet epic fonctionnent réellement dans la page.

### C1.5 — Épingler un caractère
Dépend de : C1.4
Critère de sortie : après le chapitre de botanique correspondant (voir C4.3), le joueur peut épingler un trait déjà observé chez un parent ; l'essai suivant garantit ce trait, les autres loci restent variables ; un test vérifie que 100 tirages avec un trait épinglé le conservent toujours et que les autres loci varient.
Fichiers probables : `public/game/botany-genetics.js`
Statut : todo (sauté ce déclenchement au profit de C1.6 : sa dépendance réelle inclut C4.3 — « Iris montre comment épingler un caractère déjà vu », design l.383 — qui vit en phase 4, pas encore détaillée ; implémenter le déverrouillage maintenant aurait forcé à inventer un contenu narratif hors ordre, ce que `orchestration.md` interdit explicitement pour les dépendances d'implémentation, pas seulement narratives)

### C1.6 — Multiplication fidèle
Dépend de : C1.1
Critère de sortie : multiplier un cultivar (bouture ou graine issue d'un spécimen déjà obtenu) crée un nouveau spécimen du même cultivar sans relancer aucun tirage ; un test compare les traits du spécimen source et du spécimen multiplié — identiques trait à trait.
Fichiers probables : `public/garden-state-cmd-*.js`
Statut : fait (2026-09-17, voir docs/campagne.md et commit sur `maison-des-possibles`). En pratique dans un nouveau `public/garden-state-cmd-h.js` (commandes `plantSpecimen`/`multiplySpecimen`, noms épelés en entier pour ne jamais entrer en collision avec les commandes physiques existantes `plant`/`multiply` de l'établi) ; `Cultivars.createSpecimen` (posé sans être câblé par C1.1) est maintenant câblé sur un nouveau champ de sauvegarde `s.specimens`.

### C1.7 — Représentation modulaire des hybrides (rendu)
Dépend de : C1.2 (schéma d'attributs stable), C1.3 (au moins un cultivar réel à afficher)
Critère de sortie : le port fournit un squelette à points d'attache ; feuilles/fleurs/fruits proviennent d'une bibliothèque compatible avec le port du cultivar ; une graine visuelle déterministe (dérivée de l'id du cultivar) fixe la distribution des organes et reste identique entre deux sessions ; deux spécimens du même cultivar partagent géométrie/matériau (pas de doublon coûteux en triangles). Les teintes et matériaux utilisés doivent venir de [direction-artistique.md](direction-artistique.md) (palette/matériaux déjà en usage) — pas de nouvelle couleur inventée hors de ces familles sans raison documentée. Avant de marquer l'epic fait : capturer une scène de rendu, la lire avec Read (image), et comparer explicitement au guide (voir « Relecture multimodale réelle » dans [execution-continue.md](execution-continue.md)) ; consigner le résultat de cette relecture dans l'entrée de campagne.md.
Fichiers probables : `public/game/botany.js`, `public/garden-models.js`
Statut : fait (2026-09-17, voir docs/campagne.md et commit sur `maison-des-possibles`). En pratique dans un nouveau `public/game/botany-hybrids.js` (pas `botany.js`/`garden-models.js` : catalogue de la flore fictive de Rivebrume, séparé du rendu du jardin libre pour la même raison que `botany-genetics.js` l'est de `data-species.js`).

### C1.8 — Prototype visuel de validation
Dépend de : C1.7
Critère de sortie : six plantes fondatrices affichées à trois stades, croisements entre familles compatibles affichés côte à côte avec leurs parents ; capture Playwright vérifiant absence de pénétration de maillage grossière et cohérence d'échelle ; relecture multimodale de ces captures contre `direction-artistique.md` (silhouette distincte sans étiquette, palette cohérente, proportions). C'est la porte de sortie de la phase entière (design §15, ligne « ne pas produire les centaines de meubles avant que cette grammaire soit convaincante ») — son résultat devient la référence photographique citée en fin de `direction-artistique.md`.
Fichiers probables : nouveau `tests/campaign-botany-visual.cjs`
Statut : fait (2026-09-17, voir docs/campagne.md et commit sur `maison-des-possibles`). **Ferme la porte de sortie de la phase 1** (voir rapport de clôture daté dans docs/campagne.md) ; C1.5 (épinglage) reste `todo`, correctement différé à C4.3 — le critère de sortie littéral de la phase ne le mentionne pas. Six fondatrices sur les huit du catalogue (`oreille-de-pluie`, `clochette-du-soir`, `menthe-de-velours`, `ronce-a-rubans`, `fraise-timide`, `aster-des-vents`), déjà annoncées par le commentaire d'en-tête de `botany-genetics.js` depuis C1.2. Ajoute aussi des stades de croissance (0/1/2) à `botany-hybrids.js`, posés mais non couverts par C1.7 (`cultivars.js` anticipait déjà ce manque explicitement).

---

## Phase 2 — Jardin d'imitation

Porte de sortie (design §15) : *un joueur enseigne un geste sans guide externe ; une chaîne fonctionne, sature proprement puis redémarre.*

### C2.1 — Horloge quotidienne
Dépend de : rien
Critère de sortie : une journée de jeu va de 7h à 23h en ~24 minutes actives (réglable) ; dialogues/inventaire/construction/carnet/apprentissage mettent le temps en pause ; masquer/fermer l'onglet suspend la partie ; un rappel discret apparaît à 22h30 ; un test vérifie qu'une session pausée puis reprise n'a pas avancé l'horloge de simulation pendant la pause.
Fichiers probables : nouveau `public/game/clock.js`, `public/game/lighting.js` (déjà porteur du cycle visuel, à distinguer explicitement de cette horloge de jeu)
Statut : fait (2026-09-17, voir docs/campagne.md et commit sur `maison-des-possibles`). En pratique `public/game/campaign-clock.js` (pas `clock.js` : nommé pour rendre explicite qu'il s'agit de l'horloge de campagne, distincte de toute horloge future du jardin libre).

### C2.2 — Passage de nuit atomique
Dépend de : C2.1
Critère de sortie : à 23h, l'action atomique en cours se termine, tout placement non validé est annulé sans coût, une transition ramène à la maison ; le jeu propose de confirmer le pot avant la transition ; le bilan de nuit est appliqué une seule fois, y compris après un rechargement en plein milieu de la résolution (test de non-double-crédit, dans l'esprit de `garden-living.cjs` existant) ; dormir plus tôt est autorisé et ne simule ni travail ni vente sur les heures sautées.
Fichiers probables : `public/game/save.js`, `public/garden-state.js`
Statut : fait (2026-09-17, voir docs/campagne.md et commit sur `maison-des-possibles`). **Scope réduit à la couche moteur, écart documenté ici et dans campagne.md** : `s.campaignDay`/`s.campaignClock` (persistés, migrés) et l'extension de la commande `sleep` (déjà existante depuis C1.3) prouvent programmatiquement les deux clauses testables sans rendu — « bilan appliqué une seule fois, y compris après rechargement » et « dormir plus tôt autorisé, aucune heure sautée simulée ». Les trois clauses restantes (déclenchement automatique à 23h, annulation du placement en cours, transition caméra + écran de confirmation du pot) sont du rendu/interface pur et n'ont **aucune surface existante à habiller aujourd'hui** : aucun mode « campagne » jouable n'existe encore dans `garden.js`/`garden-frame.js` (la campagne n'est aujourd'hui pilotée que par `GardenState.command()` depuis les tests), et `campaign-clock.js` reste non câblé au navigateur exactement comme documenté par C2.1. Les construire maintenant aurait exigé d'inventer d'un bloc un mode de jeu entier, une scène « maison » et un panneau HUD sans passage par le Cartographe — précisément ce qu'`orchestration.md` (« Règles avant rendu ») et le principe « généraliser plutôt que spécialiser » déconseillent. Reportées à C2.2v (nouvel epic ci-dessous, todo, dépend de C2.2) plutôt que réduites en silence.

### C2.2v — Transition de nuit : mode campagne, caméra maison, écran de confirmation du pot
Dépend de : C2.2
Critère de sortie : les trois clauses de rendu/interface différées par C2.2 — à 23h (câblage réel de `campaign-clock.js` au navigateur, `garden-frame.js`/`garden.js`), l'action atomique en cours se termine et tout placement non validé est annulé sans coût ; une transition caméra ramène le personnage à la maison ; un écran (panneau HUD) propose de confirmer ou modifier le pot avant cette transition, puis appelle la commande `sleep`. Suppose une scène « maison » et un point d'ancrage caméra identifiables (probablement à détailler avec le Cartographe si aucun ne préexiste au moment où cet epic est pris, la maison de la campagne n'étant pas encore construite — voir phase 3).
Fichiers probables : `public/game/campaign-clock.js` (câblage), nouveau segment de `public/garden-frame.js`/`public/garden.js`, `public/game/hud-panel.js`
Statut : todo (créé le 2026-09-17, scission de C2.2 — voir sa propre entrée et docs/campagne.md)

### C2.3 — Apparition de la première Rainelle
Dépend de : C1.3 (il faut un cultivar réel dans le pot), C2.2
Critère de sortie : une nuit scénarisée où une grenouille tombe dans le pot pendant un essai produit, au matin, une Rainelle portant le feuillage du cultivar croisé cette nuit-là, sans perte du spécimen attendu (il reste aussi conservé normalement) ; la Rainelle a un prénom modifiable et un identifiant stable.
Fichiers probables : nouveau `public/game/rainelles.js`
Statut : fait (2026-09-17, voir docs/campagne.md et commit sur `maison-des-possibles`). Choisi plutôt que C2.2v (dépendances déclarées satisfaites — C1.3/C2.2 tous deux `fait` — mais son propre critère de sortie littéral suppose une scène « maison » de campagne qui n'existe pas encore, phase 3 : implémenter maintenant aurait exigé d'inventer d'un bloc un mode de jeu entier, comme pour C1.5/C4.3). En pratique, « nuit scénarisée » et « essai » sont traduits en une couche de règles pure, sans câbler aucun contenu narratif de chapitre 4 (Acte II, hors ordre — hors phase 4 non détaillée) : une commande générique `triggerFrogEncounter` arme une rencontre en attente, résolue par la prochaine vraie nuit de pot (`sleep`, `garden-state-cmd-f.js`) plutôt que par un événement scripté câblé à une scène. Toujours aucun rendu (pas de représentation 3D de la Rainelle), sur le même modèle « posé mais pas encore affiché » que C1.1/C1.6.

### C2.4 — Schéma du geste unique
Dépend de : C2.3
Critère de sortie : une Rainelle mémorise au plus un `{verbe, poste/zone, source, destination, condition}` à la fois ; réenseigner remplace intégralement l'ancien geste (jamais un ajout) ; un test tente d'assigner un second geste simultané et vérifie le refus/remplacement explicite.
Fichiers probables : `public/game/rainelles.js`, `public/game/automation.js` (réutiliser `job`/`buffer` plutôt que dupliquer)
Statut : fait (2026-09-17, voir docs/campagne.md et commit sur `maison-des-possibles`). En pratique aucun changement à `automation.js` (l'exécution du geste, C2.6+, n'est pas dans le périmètre de cet epic) : le champ `geste` vit sur l'entrée `rainelles.js`, et la commande vit dans un nouveau `public/garden-state-cmd-j.js` (`teachGesture`), sur le même patron que `renameCultivar`/`renameRainelle`.

### C2.5 — Enseignement en quatre moments
Dépend de : C2.4
Critère de sortie : « Regarde-moi » suspend le temps ; une démonstration réelle ou assistée au poste est capturée ; le jeu propose une phrase de geste modifiable par le joueur ; un essai montre la trajectoire prévue avant confirmation ; démontrer une fois permet d'enseigner ensuite le même geste à une autre Rainelle par une répétition courte, sans refaire tout le tutoriel.
Fichiers probables : `public/game/hud-panel.js`, `public/game/rainelles.js`
Statut : fait (2026-09-18, voir docs/campagne.md et commit sur `maison-des-possibles`). **Scope réduit à la couche moteur, écart documenté ici et dans campagne.md** : les cinq clauses sont prouvées comme état/données pures (`beginTeaching`/`demonstrateGesture`/`reviseGesturePhrase`/`confirmTeaching`/`cancelTeaching`/`teachGestureQuick` dans `public/garden-state-cmd-k.js`) — démonstration « assistée » (le critère accepte explicitement « réelle ou assistée »), phrase et trajectoire calculées et modifiables via commande. Aucune de ces cinq clauses n'est encore montrée au joueur (aucun écran `hud-panel.js`, aucune caméra « Regarde-moi », aucune ligne de trajectoire dans le monde) : reportée au nouvel epic **C2.5v** ci-dessous, même scission que C2.2/C2.2v et pour la même raison (aucune interface de campagne encore câblée à un écran réel).

### C2.5v — Interface d'enseignement : écran de leçon, phrase et trajectoire affichées
Dépend de : C2.5
Critère de sortie : les clauses d'interface différées par C2.5 — un écran (`hud-panel.js`) affiche la phrase proposée par `demonstrateGesture` et permet de la corriger (`reviseGesturePhrase`) avant `confirmTeaching`/`cancelTeaching` ; la trajectoire planifiée (`draft.trajectory`) est visible dans le monde avant confirmation ; une mise en scène caméra accompagne « Regarde-moi » (`beginTeaching`). Suppose un poste/une zone réellement placés dans le monde de campagne (probablement à détailler avec le Cartographe si aucun ne préexiste au moment où cet epic est pris — la maison/les postes de la campagne sont un chantier de la phase 3, comme déjà noté pour C2.2v).
Fichiers probables : `public/game/hud-panel.js`, nouveau segment de `public/garden-frame.js`/`public/garden.js`
Statut : todo (créé le 2026-09-18, scission de C2.5 — voir sa propre entrée et docs/campagne.md)

### C2.6 — Gestes Arroser et Récolter
Dépend de : C2.5
Critère de sortie : une Rainelle enseignée à arroser remplit son arrosoir à la borne du poste puis humidifie les plantes de la zone sans intervention ; une Rainelle enseignée à récolter dépose les productions mûres dans le panier adjacent ; les deux réutilisent les primitives génériques `job`/`buffer` déjà présentes dans `automation.js` plutôt que d'en créer de nouvelles.
Fichiers probables : `public/game/automation.js`, `public/game/rainelles.js`
Statut : todo (reformulé par le Cartographe le 2026-09-18 en trois epics plus petits, C2.6a/C2.6b/C2.6c ci-dessous, plutôt que tenté tel quel — voir le journal des décisions : son critère littéral suppose trois mécaniques encore inexistantes à la fois — bornes/paniers, humidité de zone, maturité de spécimen — ce qu'`orchestration.md` interdit pour un seul epic (« jamais une refonte simultanée de plusieurs systèmes »). Conservé ici pour mémoire ; ne plus choisir cet epic directement, choisir C2.6a/b/c à sa place.)

### C2.6a — Registre des bornes d'eau, zones de culture et paniers de campagne
Dépend de : C2.4
Critère de sortie : la sauvegarde de campagne déclare un registre de bornes d'eau, de zones de culture et de paniers de campagne, chacun avec un id stable et une position ; une fonction pure permet de résoudre les identifiants `poste`/`source`/`destination` déjà acceptés en texte libre par `teachGesture`/`demonstrateGesture` (C2.4/C2.5) contre ce registre (id connu → entrée trouvée ; id inconnu → échec explicite, jamais un `undefined` silencieux) ; une sauvegarde existante sans ce registre migre vers un registre vide sans erreur ; aucune commande de geste existante ne change de comportement (les identifiants restent acceptés tels quels, la résolution est une fonction séparée, pas encore appelée par `teachGesture`/`demonstrateGesture`).
Fichiers probables : nouveau `public/game/campaign-stations.js`, `public/garden-state-lifecycle.js`, `public/garden-state-validate.js`
Statut : fait (2026-09-18, voir docs/campagne.md et commit sur `maison-des-possibles`). En pratique exactement dans les fichiers probables (aucun écart) : trois collections séparées (`bornes`/`zones`/`paniers`) plutôt qu'un registre plat, avec des préfixes d'id distincts (`b`/`z`/`pn`) qui rendent `resolveStation` capable de chercher dans les trois sans ambiguïté et sans qu'on ait à lui dire quel type chercher — nécessaire puisqu'un même champ `poste`/`source`/`destination` peut nommer n'importe lequel des trois selon le verbe (voir `PHRASE_BUILDERS` dans `rainelles.js`).

### C2.6b — Humidité et maturité des spécimens de campagne
Dépend de : C1.6
Critère de sortie : un spécimen de cultivar (`s.specimens`, C1.6) porte une humidité qui décroît avec le temps de simulation (`s.elapsed`, jamais l'horloge murale — même principe que le dopage `boostUntil` du jardin libre) et un état de maturité dérivé de son stade de croissance (C1.8) ; un test vérifie la décroissance déterministe de l'humidité sur une durée simulée donnée et qu'un spécimen immature ne peut pas être marqué prêt à produire ; une sauvegarde existante sans ces champs migre vers des valeurs par défaut sans erreur.
Fichiers probables : `public/game/cultivars.js`, `public/garden-state-lifecycle.js`, `public/garden-state-validate.js`
Statut : fait (2026-09-18, voir docs/campagne.md et commit sur `maison-des-possibles`). En pratique aucun changement à `garden-state-lifecycle.js` (les spécimens neufs n'ont besoin d'aucune valeur par défaut à `fresh()` : `s.specimens` y reste `[]`, et `createSpecimen` pose désormais `moistureAt`/`readyToProduce` lui-même à la création) ; la migration par défaut vit dans `garden-state-validate.js` à la place. Humidité calculée paresseusement (`Cultivars.specimenMoisture(specimen, elapsed)`, jamais un champ décrémenté à chaque tick — aucune boucle ne touche encore `s.specimens`) à partir d'un simple timestamp `moistureAt`, exactement le principe de `boostUntil` cité par le critère. Maturité dérivée du stade (`Cultivars.isMature`, `MATURE_STAGE = 2`, dupliqué en dur depuis `botany-hybrids.js` avec commentaire de synchronisation plutôt qu'importé, ce dernier ne s'exécutant pas sans `THREE` chargé). `Cultivars.setReadyToProduce` lève une erreur explicite sur un spécimen immature, invariant revérifié indépendamment par `garden-state-validate.js` au chargement.

### C2.6c — Gestes Arroser et Récolter, branchés sur le registre et les spécimens
Dépend de : C2.6a, C2.6b, C2.5
Critère de sortie : identique au critère littéral de C2.6 ci-dessus (une Rainelle enseignée à arroser remplit son arrosoir à la borne du poste puis humidifie les plantes de la zone sans intervention ; une Rainelle enseignée à récolter dépose les productions mûres dans le panier adjacent ; les deux réutilisent les primitives génériques `job`/`buffer` déjà présentes dans `automation.js`), désormais réalisable sans inventer de sous-système supplémentaire puisque bornes/zones/paniers (C2.6a) et humidité/maturité de spécimen (C2.6b) existent déjà.
Fichiers probables : `public/game/automation.js`, `public/game/rainelles.js`
Statut : fait (2026-09-18, voir docs/campagne.md et commit sur `maison-des-possibles`). En pratique dans un nouveau `public/game/campaign-automation.js` (pas `automation.js`/`rainelles.js` : ce dernier opère sur `s.entities`/`D.recipes`, un espace de données disjoint des Rainelles/spécimens/stations de campagne — le patron `job`/`buffer` est réimplémenté, pas importé, même raison que `botany-hybrids.js` face à `garden-models.js`). Première fois que la couche campagne est réellement exécutée à chaque tick (`GardenState.tick()` boucle désormais aussi sur `s.rainelles`), pas seulement posée. Écart minimal documenté : un spécimen mûr devient `readyToProduce` automatiquement au tick suivant (rien d'autre ne le faisait jusqu'ici) et reste re-récolté à chaque cycle complet tant qu'il reste mûr — pas de vrai cycle de régénération (cooldown/quota), assumé comme le plus petit mécanisme additif nécessaire pour que le geste "récolter" ait jamais quelque chose à faire. Bug latent réel trouvé et corrigé en cours de route : `cultivars.js` chargeait après `garden-state-validate.js` dans `public/index.html` alors que ce dernier lit `root.GardenCultivars` depuis C2.6b — resté dormant faute d'avoir jamais eu `readyToProduce: true` avant cet epic ; corrigé par réordonnancement des balises `<script>`.

### C2.7 — Geste Transporter
Dépend de : C2.6c
Critère de sortie : une Rainelle transporteuse déplace les productions d'un panier A vers un panier B selon un filtre de ressource, avec un seul trajet actif à la fois ; enchaîner arrosage → récolte → transport sans intervention manuelle fait circuler une ressource de bout en bout dans un test.
Fichiers probables : `public/game/rainelles.js`
Statut : fait (2026-09-18, voir docs/campagne.md et commit sur `maison-des-possibles`). En pratique dans `public/game/campaign-automation.js` (pas `rainelles.js` : ce dernier ne porte que le schéma/la validation du geste, jamais son exécution — même séparation que arroser/récolter depuis C2.6c), nouveau `tickTransporter`. « Un seul trajet actif » et « un filtre de ressource actif » sont déjà vrais par construction (un `geste` est un seul enregistrement depuis C2.4, jamais une liste) : aucun code supplémentaire n'était nécessaire pour ces deux clauses, seulement pour le déplacement lui-même. Le filtre de ressource réutilise le champ `condition` déjà existant (optionnel depuis C2.4) : un `condition` non vide déplace uniquement ce cultivarId, un `condition` vide déplace tout le contenu courant du panier source (toujours un seul trajet, simplement non filtré). Transporter un panier vers lui-même est traité comme une configuration dégénérée (aucun job ne démarre), sur le même modèle qu'une station non résolue.

### C2.8 — Réservations et états de blocage
Dépend de : C2.6c
Critère de sortie : une ressource et un emplacement de sortie sont réservés avant le départ d'une Rainelle, empêchant deux Rainelles de prendre la même cible ; si la cible disparaît, la réservation se libère et l'objet déjà porté rejoint un bac de secours identifié ; les sept états (au travail/stock atteint/source vide/sortie pleine/passage bloqué/poste manquant/repos) sont exposés avec une phrase d'action, sur le modèle des diagnostics déjà existants dans `irrigation-status.js`.
Fichiers probables : nouveau `public/game/rainelles-status.js`
Statut : fait (2026-09-18, voir docs/campagne.md et commit sur `maison-des-possibles`). **Scope réduit à six des sept états et à une réservation « par construction », écart documenté ici et dans campagne.md** : nouveau `public/game/rainelles-status.js` expose `au-travail`/`source-vide`/`stock-cible-atteint`/`sortie-pleine`/`poste-manquant`/`repos` (pas `passage bloqué` — voir C2.8v ci-dessous). `public/game/campaign-stations.js` ajoute `capacity`/`min` à un panier (design §5 « réglage avancé... minimum et maximum », valeurs par défaut 24/0, migrées pour tout panier antérieur). `campaign-automation.js` fait respecter ces deux bornes avant toute mutation de buffer dans `tickRecolter`/`tickTransporter` : une récolte qui dépasserait la capacité s'arrête proprement (le spécimen reste `readyToProduce`, rien n'est perdu) et un transport partage un budget unique entre la capacité de sortie et le plancher de la source (déplace ce qu'il peut en toute sécurité, jamais tout ou rien). La clause « deux Rainelles ne peuvent pas prendre la même cible » est vraie par construction (le tick est strictement séquentiel, `s.rainelles` dans l'ordre du tableau) plutôt que par une nouvelle structure de réservation — prouvé par un test dédié (deux récolteuses sur un panier presque plein) plutôt que supposé.

### C2.8v — Passage bloqué et sauvetage en transit
Dépend de : C2.8, un modèle de déplacement réel des Rainelles (aucun n'existe encore : une Rainelle n'a aujourd'hui aucune position dans le monde, son geste s'exécute directement sur poste/source/destination sans jamais s'y rendre)
Critère de sortie : les deux clauses différées par C2.8 — l'état `passage bloqué` (design §5 : « les croisements de passage utilisent une priorité stable et un temps d'attente borné », qui suppose un graphe/une grille de déplacement partagés) ; et l'usage réel du bac de secours (une ressource déjà « en transit » qui rejoint un bac identifié si sa cible disparaît avant livraison — aujourd'hui impossible à observer, puisqu'aucune commande ne retire encore une borne/zone/panier une fois enregistrée, et que la livraison reste atomique/instantanée, jamais étalée sur plusieurs ticks). Probablement à détailler avec le Cartographe au moment où il sera pris — la forme exacte du déplacement des Rainelles est un chantier de la phase 3 (tranche verticale), comme déjà noté pour C2.2v/C2.5v.
Fichiers probables : `public/game/rainelles-status.js`, `public/game/campaign-automation.js`, nouveau module de déplacement
Statut : todo (créé le 2026-09-18, scission de C2.8 — voir sa propre entrée et docs/campagne.md)

### C2.9 — Mode d'observation
Dépend de : C2.8
Critère de sortie : une vue dédiée montre chemins, transferts et cadence par journée ; elle permet de suivre un objet du plant au présentoir et de lancer un cycle pas à pas, sans modifier la simulation en cours d'observation.
Fichiers probables : `public/game/hud-world.js`
Statut : todo

### C2.10 — Refus de multiplication, réenseignement gratuit
Dépend de : C2.4
Critère de sortie : aucune commande ne permet à une Rainelle de multiplier une autre Rainelle ni elle-même ; le refus est un événement explicite (pas un silence), montré dès la première tentative ; réenseigner un geste ne coûte rien et remplace proprement l'ancien (déjà couvert en partie par C2.4, ce epic ajoute le côté narratif du refus).
Fichiers probables : `public/game/rainelles.js`
Statut : fait (2026-09-18, voir docs/campagne.md et commit sur `maison-des-possibles`). `validateGestureFields` (partagée par `teachGesture` et `demonstrateGesture`, seuls points d'entrée qui assignent un verbe à `geste`) distingue désormais `verbe === "multiplier"` avec un message narratif dédié (`Rainelles.MULTIPLY_REFUSAL`), plutôt que le "Geste inconnu." générique : événement explicite, jamais un silence, montré dès la première tentative sur l'un ou l'autre chemin. "Réenseigner ne coûte rien" était déjà vrai par construction depuis C2.4 (aucune commande de geste ne touche `s.inventory`) ; ajouté ici un test de régression explicite plutôt que de le supposer. Aucune commande ne permettait déjà de multiplier une Rainelle (espaces d'id disjoints `r<n>`/`sp<n>`/`e<n>`) ; `multiplySpecimen` et le `multiply` du monde libre le confirment par un refus explicite (jamais un no-op silencieux), vérifié par test plutôt qu'affirmé. C2.6 (gestes Arroser/Récolter, dépendance C2.5) délibérément sauté ce déclenchement : voir journal des décisions ci-dessous.

### C2.11 — Chaîne de validation « fraises de la cuisine »
Dépend de : C2.7, C2.8, C2.10
Critère de sortie : la chaîne complète décrite en §5 du design (eau → arroseuse → fraisiers → récolteuse → panier → transporteuse → réserve de cuisine) fonctionne sans intervention manuelle sur une durée de test représentant plusieurs jours simulés, sature proprement quand la réserve est pleine, puis redémarre seule quand elle se vide. C'est la porte de sortie de la phase.
Fichiers probables : nouveau `tests/campaign-rainelles-chain.cjs`
Statut : fait (2026-09-18, voir docs/campagne.md et commit sur `maison-des-possibles`). **Ferme la porte de sortie de la phase 2** (voir rapport de clôture daté dans docs/campagne.md) ; C2.2v/C2.5v/C2.8v/C2.9 restent `todo`, correctement différés (chantier de rendu/déplacement de la phase 3, ou — pour C2.9 — probablement le même chantier une fois pris) — le critère de sortie littéral de la phase ne les mentionne pas, même raisonnement que C1.5 différé à la clôture de la phase 1. Exactement dans le fichier probable (aucun écart), plus un second nouveau fichier non anticipé par le gabarit initial : `tests/campaign-phase2-gate.cjs` (même patron que `campaign-phase1-gate.cjs`), pour le second volet du critère de sortie de phase (« sans guide externe » : un seul geste, réenseignement, refus de multiplication couvrant aussi un congénère distinct) que `campaign-rainelles-chain.cjs` ne couvre pas lui-même.

---

## Phase 3 — Tranche verticale

Porte de sortie (design §15) : *habiter, croiser, organiser, rencontrer fonctionnent ensemble en une courte partie sauvegardable.*

Assemble les phases 1 et 2 avec : maison refuge minimale (réparation lit/table/réchaud), une serre, un coin de village, deux habitants, deux Rainelles, trois nuits jouées, une première quête d'outil. Phase 2 close le 18 septembre 2026 (voir rapport de clôture dans `docs/campagne.md`) : premier lot d'epics détaillé ci-dessous par le Cartographe, couvrant la fondation moteur/rendu de « habiter » et « rencontrer » (croiser et organiser sont déjà acquis des phases 1/2, il reste à les relier à la maison/au village). Écart délibéré par rapport au design littéral : aucun nom de personnage d'Acte I-III (Noé, Mira, Basile...) ni contenu de quête associé n'est utilisé ici — ces identités et leurs quêtes sont un chantier de la phase 4 (§10 du design, chapitres 1-9) ; le lot ci-dessous ne prouve que la capacité mécanique générique (réparation, habitat, deuxième Rainelle, village habité, récompense d'outil), jamais un contenu narratif anticipé. Même principe déjà appliqué à C1.5/C4.3 et C2.3 (rencontre de grenouille scénarisée sans câblage narratif). Un second lot (rendu du village enrichi, câblage narratif réel, éventuellement C2.2v/C2.5v/C2.8v une fois la maison/le déplacement des Rainelles posés par ce lot) sera détaillé par un futur passage du Cartographe, pas figé d'un bloc maintenant.

### C3.1 — Schéma et réparation de la maison refuge
Dépend de : rien
Critère de sortie : la sauvegarde de campagne déclare une maison refuge avec les six espaces nommés par le design §6 (pièce d'accueil, cuisine, atelier, serre attenante, grenier, véranda), chacun avec un statut `delabre`/`repare`, un coût de réparation en ressources (familles déjà existantes du jardin libre : bois, pierre, argile, fibres) et un indicateur `locked` ; seule la pièce d'accueil démarre `locked: false` (design §6 : « ce premier confort ne dépend d'aucune quête botanique encore inaccessible »), les cinq autres espaces restent `locked: true` par défaut — leur déverrouillage narratif est un chantier futur, non construit ici ; une commande `repairHouseSpace` transitionne un espace non verrouillé de `delabre` à `repare`, consommant les ressources déclarées depuis l'inventaire existant (`economy.js`), refusée explicitement (jamais un no-op silencieux) si les ressources manquent, si l'espace est verrouillé ou déjà réparé ; un test vérifie qu'une réparation aux ressources insuffisantes échoue sans consommer partiellement l'inventaire ; une sauvegarde existante migre vers cet état par défaut (tout délabré sauf pièce d'accueil déverrouillée) sans erreur.
Fichiers probables : nouveau `public/game/campaign-house.js`, `public/garden-state-lifecycle.js`, `public/garden-state-validate.js`, nouveau `public/garden-state-cmd-l.js`
Statut : fait (2026-09-18, voir docs/campagne.md et commit sur `maison-des-possibles`). Exactement dans les fichiers probables (aucun écart), plus un nouveau `tests/campaign-house.cjs` non anticipé par le gabarit initial. Écart documenté sur les coûts de réparation : le design §6/§16 cite « bois, pierre, argile, fibres » comme familles déjà existantes, mais seules `wood`/`stone`/`clay` sont des items d'inventaire réels du jardin libre (vérifié avant d'écrire du code, pas supposé) — aucune fibre n'existe, donc les coûts de `SPACES` n'utilisent que les trois ressources réelles ; en inventer une quatrième aurait été un second système hors du critère de sortie littéral de cet epic.

### C3.2 — Rendu de la maison refuge
Dépend de : C3.1
Critère de sortie : la maison refuge apparaît dans le monde 3D en réutilisant les murs/toiture escamotables déjà en usage pour les maisons de visiteurs (`render-houses.js`, design §14 : « maison à murs et toiture escamotables, sans imposer une caméra intérieure complexe ») ; **vérification préalable requise avant d'implémenter** — `buildHouse(b)` suppose aujourd'hui un visiteur obligatoire (`b.visitorId`, `D.visitors.find(...).name`, ligne ~295-307 de `render-houses.js`) : la maison refuge n'a pas de visiteur (le joueur y habite), donc soit `buildHouse` gagne une branche visiteur optionnelle (généralisation, pas un epic séparé pour la maison refuge), soit une fonction sœur réutilise les mêmes primitives (`houseWalls`, `gableGeometry`) sans visiteur — choix à trancher par l'Artisan rendu et documenté dans le commit, jamais deviné en silence. L'aspect distingue visuellement un espace `delabre` d'un espace `repare` (au minimum la pièce d'accueil, seul espace jouable à ce stade), avec des teintes/matériaux tenus depuis `direction-artistique.md` (pas de couleur inventée hors de ses familles). Capture Playwright + relecture multimodale contre `direction-artistique.md` avant de marquer fait ; `tests/garden-material-audit.cjs` passé et étendu si un nouveau matériau/maillage « au sol » apparaît (voir « le point le plus important » d'`execution-continue.md`).
Fichiers probables : `public/game/render-houses.js`, `public/game/data-buildings.js` (nouvelle entrée), `tests/garden-material-audit.cjs` (extension si nécessaire)
Statut : fait (2026-09-18, voir docs/campagne.md et commit sur `maison-des-possibles`). **Écart sur les fichiers probables, justifié dans le commit** : la vérification préalable demandée par cette entrée elle-même a confirmé que `buildHouse(b)` (`render-houses.js:295-307`) suppose un visiteur obligatoire — plutôt qu'une branche visiteur optionnelle dans cette fonction (aurait mêlé la donnée `D.buildings`/`D.visitors` du jardin libre à la maison refuge, qui n'a pas de visiteur) ou une entrée dans `data-buildings.js` (même mélange), nouveau module autonome `public/game/render-campaign-house.js` — même séparation que `botany-hybrids.js` face à `botany.js`/`garden-models.js`, sans dépendance à `window.GardenModels`/`window.GardenView` (seul `THREE` requis), donc testable en Node pur comme `botany-hybrids.js`.

### C3.3 — Habitats de Rainelle : places de vie
Dépend de : C2.6a (registre de stations à étendre), C3.1
Critère de sortie : le registre de campagne (`campaign-stations.js`) gagne une quatrième collection `habitats`, chacun avec un id stable, une position et une `capacity` (design §5 : « un habitat fournit plusieurs places de vie ») ; une fonction pure calcule les places libres (`capacity` total des habitats moins le nombre de Rainelles vivantes) ; aucune Rainelle ne peut encore être créée sans une place libre (vérifié par un test, la fonction de vérification est appelée mais pas encore par une commande de création réelle puisque C3.4 ne dépend que de cet epic) ; une sauvegarde existante migre vers un registre d'habitats vide sans erreur ; un habitat occupé ne peut pas être retiré sans destination de relogement (design §5) — une commande de retrait explicite refuse si elle romprait ce nombre de places en dessous de la population actuelle.
Fichiers probables : `public/game/campaign-stations.js`, `public/garden-state-validate.js`
Statut : fait (2026-09-18, voir docs/campagne.md et commit sur `maison-des-possibles`). Exactement dans les fichiers probables, plus `public/garden-state-lifecycle.js` (valeur par défaut dans `fresh()`, oubliée du gabarit initial mais nécessaire par symétrie avec les trois autres collections), `tests/campaign-stations.cjs` (les fixtures de registre existantes étendues aux quatre collections), nouveau `tests/campaign-habitats.cjs` et `package.json`. Écart documenté sur la capacité : design §6 dit « un habitat fournit plusieurs places de vie » sans jamais chiffrer, donc aucune capacité par défaut inventée (contrairement à `DEFAULT_PANIER_CAPACITY`, qui avait un précédent réel) — capacité obligatoire à l'enregistrement, bornée seulement par ce que « plusieurs » signifie littéralement (`MIN_HABITAT_CAPACITY = 2`). « Habitat occupé » n'a pas de suivi par individu (aucune commande n'affecte une Rainelle à un habitat précis) : lu comme « le retrait romprait la capacité totale sous la population actuelle », la seule lecture que les données actuelles permettent — documenté explicitement dans `removeHabitat`, pas deviné en silence.

### C3.4 — Bourgeon et nurserie : deuxième Rainelle par multiplication naturelle
Dépend de : C3.3, C1.6 (patron déjà utilisé par la nurserie du jardin libre, `balance.nurserySeconds`)
Critère de sortie : après un petit événement de familiarisation générique (une commande `formBud`, sans mise en scène narrative — même posture que `triggerFrogEncounter`, C2.3), une Rainelle porte un `bourgeon` prêt à prélever ; le prélèvement (`harvestBud`) exige une place de vie libre (C3.3) et transfère le bourgeon vers la nurserie (même position temporelle que l'établi du jardin libre : posé, mûrit après une nuit de sommeil, jamais un tirage instantané) ; au réveil suivant, un nouvel individu Rainelle apparaît, doté d'un id stable et d'un prénom par défaut modifiable, sans geste enseigné (design §5 : « il ne copie pas un souvenir ni une obligation de métier ») ; un test vérifie qu'aucun bourgeon ne peut être prélevé sans place de vie libre, et qu'un bourgeon en cours de maturation résiste à un rechargement (pas de double-éveil).
Fichiers probables : `public/game/rainelles.js`, `public/garden-state-cmd-f.js` (même segment que `sleep`, résolution au réveil sur le modèle de `botany-pot.js`), nouveau `public/garden-state-cmd-m.js` (`formBud`/`harvestBud`)
Statut : fait (2026-09-18, voir docs/campagne.md et commit sur `maison-des-possibles`). Exactement dans les fichiers probables (aucun écart). `formBud`/`harvestBud` vivent dans `rainelles.js` comme fonctions pures ; le nouveau `garden-state-cmd-m.js` porte les deux commandes. Écart documenté sur la réservation de place : `harvestBud` compare `freeLivingPlaces` à `s.rainelles.length + s.campaignNursery.length` (population vivante **plus** bourgeons déjà en attente), pas seulement à la population actuelle — sans ce `+ s.campaignNursery.length`, deux prélèvements successifs avant toute nuit pourraient tous les deux réussir contre une seule place réellement libre, promettant deux naissances pour un seul habitat. cultivarId du nouvel individu hérité de celui du parent (« un bourgeon de sa lignée », design §5) : aucune autre lecture n'était possible sans inventer une donnée absente. Aucune mise en scène narrative pour `formBud` (même posture que `triggerFrogEncounter`, C2.3) ; aucun délai de repousse après un prélèvement (le design ne nomme aucun rythme au-delà de l'attente de la nurserie elle-même).

### C3.5 — Coin de village : zone et deux habitants
Dépend de : rien (données pures, comme C1.2/C2.6a)
Critère de sortie : `data-world.js` déclare une nouvelle zone « coin de village », polygone cohérent avec les quatre zones existantes (pas de chevauchement, voir convention `geometry.js`) ; deux nouvelles entrées `data-buildings.js` y placent deux maisons habitées, chacune avec un rôle déjà existant dans `data-roles.js` (`resident` au minimum, pas de nouveau rôle inventé à ce stade) — **aucune identité narrative n'est assignée** (pas de nom/personnalité d'Acte I-III, voir la note de tête de phase 3) : deux id génériques (`villageois-1`/`villageois-2` ou équivalent, à remplacer par le Scénariste en phase 4) suffisent à prouver la capacité mécanique. Rendu réutilise entièrement le système générique existant (`render-houses.js`, `buildHouses`), aucune nouvelle branche de code attendue. Capture Playwright + relecture multimodale contre `direction-artistique.md` ; `tests/garden-material-audit.cjs` passé (pas d'extension attendue, matériaux déjà en liste blanche pour les maisons existantes).
Fichiers probables : `public/game/data-world.js`, `public/game/data-buildings.js`, `public/game/data-roles.js` (uniquement si `resident` s'avère insuffisant)
Statut : fait (2026-09-18, voir docs/campagne.md et commit sur `maison-des-possibles`). **Écarts documentés, aucun deviné en silence** : (1) zone 4 toujours déverrouillée (`cost: {}`, `rep: 0`, comme la zone 0) plutôt que gagnée par un coût — la seule façon de la rendre réellement atteignable à pied sur une sauvegarde neuve sans passer par une zone encore verrouillée, et le seul moyen de ne pas casser deux tests génériques existants qui exigent que tout visiteur/toute porte soit franchissable sur une sauvegarde neuve ; (2) fichiers touchés bien au-delà des trois fichiers probables — la vérification préalable a montré que plusieurs points de code généraient un tableau/littéral indexé par nombre fixe de zones (teintes de sol dans `render-world.js`/`render-flow.js`, `resourcePositions[z.id]` dans `data.js`, domaine `unlocked` dans `garden-state-validate.js`, valeur par défaut dans `garden-state-lifecycle.js`) et que trois tests codaient en dur `[0,1,2,3]` comme « toutes les zones » — tous corrigés, le mandat « aucune nouvelle branche de code attendue » ne s'étant vérifié que pour le rendu proprement dit, pas pour ces points de données.

### C3.6 — Première quête d'outil
Dépend de : C3.5
Critère de sortie : le schéma de récompense de quête (`data-quests.js`/`garden-state-cmd-e.js`) gagne un champ `reward.tools` (extension additive du même patron que `reward.plans`, pas un nouveau système de quête) qui ajoute un outil nommé à un nouvel ensemble `s.campaignTools` à la complétion ; une quête de départ (`npcId` un des deux habitants de C3.5, objectif `deliver` déjà supporté par `quests.js`) accorde un premier outil ; un test vérifie qu'un outil n'apparaît dans `s.campaignTools` qu'après complétion réelle de la quête, jamais à l'acceptation. **Limite honnête à documenter, pas à contourner** : aucun geste de construction de campagne ne consomme encore un outil comme prérequis (contrairement au jardin libre où hache/pioche/pelle sont déjà des emplacements d'équipement) — cet epic prouve seulement que la récompense existe et se persiste, l'usage mécanique de l'outil reste un chantier séparé, à ne pas inventer ici.
Fichiers probables : `public/game/data-quests.js`, `public/garden-state-cmd-e.js`, `public/garden-state-lifecycle.js`, `public/garden-state-validate.js`
Statut : fait (2026-09-18, voir docs/campagne.md et commit sur `maison-des-possibles`). Exactement dans les fichiers probables (aucun écart), plus un nouveau `tests/campaign-tools-quest.cjs` non anticipé par le gabarit initial. Quête réelle `bois-pour-l-hiver` (npcId `villageois-1`, objectif `deliver` item `wood` quantité 4, récompense `reward.tools: ["hachette"]`) — titre et item repris de la ligne de saveur déjà existante de `villageois-1` (C3.5, « range le bois pour l'hiver »), aucune identité narrative inventée. Nom d'outil « hachette » choisi pour éviter « sécateur », déjà réservé par le design §10 chapitre 2 à une scène narrative de la phase 4 non encore écrite.

### C3.7 — Porte de sortie de la phase 3 : trois nuits jouées, tranche verticale sauvegardable
Dépend de : C3.2, C3.4, C3.6
Critère de sortie : un test d'intégration Node rejoue, via le vrai vecteur de commandes (`GardenState.command()`, jamais un accès direct à l'état), une courte partie couvrant les quatre promesses de la porte de sortie (design §15) : **habiter** (réparer la pièce d'accueil, dormir dans la maison refuge), **croiser** (un cultivar obtenu au pot, déjà acquis en phase 1, rejoué ici dans le contexte de la maison), **organiser** (les deux Rainelles — la première existante, la seconde via C3.4 — tiennent une chaîne courte sans intervention, réutilisant les tests déjà existants de la phase 2), **rencontrer** (compléter la première quête d'outil de C3.6 auprès d'un habitant du village) ; la partie traverse trois nuits (`s.campaignDay` avance de 3 par `sleep` répétés) sans double-crédit ni perte ; sauvegarde JSON aller-retour à mi-parcours vérifiée strictement identique. Si ce test passe : exécuter les scénarios de validation de la section 16 du design pertinents à ce périmètre (déjà en grande partie couverts par les suites existantes), fusionner `maison-des-possibles` dans `main` et ajouter un rapport de clôture de phase daté dans `docs/campagne.md`, conformément à `execution-continue.md`.
Fichiers probables : nouveau `tests/campaign-phase3-gate.cjs`
Statut : fait (2026-09-18, voir docs/campagne.md et commit sur `maison-des-possibles`). Exactement dans le fichier probable (aucun écart), plus `package.json` (liste explicite du script `test`, leçon déjà tirée à C2.11). **Ferme la porte de sortie de la phase 3** (voir rapport de clôture daté dans docs/campagne.md) ; C2.2v/C2.5v/C2.8v/C2.9 restent `todo`, toujours différés pour la même raison inchangée (aucune position/déplacement réel des Rainelles dans le monde de campagne construit par cet epic, qui reste entièrement fait de tests Node sans rendu).

## Phase 4 — Première version jouable (actes I à III)

Porte de sortie (design §15) : *l'arc « retour puis ouverture au village » se conclut sans dépendre de contenu annoncé mais absent.*

Chapitres 1 à 9 du design (§10). Phase 3 close et fusionnée dans `main` le 18 septembre 2026 (voir rapport de clôture dans `docs/campagne.md`) : premier lot d'epics détaillé ci-dessous par le Cartographe, couvrant l'Acte I en entier (chapitres 1 à 3, « Une pièce habitable »). Choix délibéré de ne pas détailler d'un bloc les neuf chapitres de la phase (Actes I à III) : l'Acte I est la seule tranche immédiatement exécutable sans décision que les chapitres suivants doivent encore éclairer (identité des PNJ de l'Acte II/III, forme exacte des gestes Replanter/Préparer/Trier non encore implémentés) — même principe que la note de tête de fichier sur les phases 3 à 7 et que le lot initial de la phase 3.

**Recherche factuelle faite avant d'écrire ces epics, pas une supposition** : aucun système de personnalisation du personnage (prénom/pronoms/apparence/vêtements, design §2) n'existe dans le code (vérifié : aucune occurrence de personnalisation/avatar/pronom au-delà de classes CSS sans rapport ; `s.player` n'est qu'une position caméra `{x,y,z}`) ; aucun système de dialogue/conversation PNJ n'existe au-delà d'une ligne de statut (`roleData.line`, un seul PNJ générique `resident`) et du panneau de quête mécanique (accepter/compléter, aucun texte narratif) ; le carnel botanique (`hud-panel.js`, panneau `"notebook"`) existe déjà depuis C1.4 et peut recevoir de nouvelles entrées ; l'épinglage (C1.5) est à 0 % construit ; `createSpecimen` (C1.6) accepte déjà une position `{x,z}` libre, donc « installer un spécimen chez soi » ne demande aucun nouveau champ de schéma, seulement des coordonnées proches de la maison refuge ; le patron `reward.tools` (C3.6, `garden-state-cmd-e.js`) s'étend trivialement à un nouveau champ de récompense analogue. **Écart assumé pour ce lot** : la personnalisation du personnage (design §2) est un système orthogonal que la porte de sortie littérale de la phase ne nomme pas — non construite ici, laissée à un futur epic dédié ; la page de carnet du chapitre 3 utilise donc un nom générique en attendant, écart documenté dans sa propre entrée plutôt que deviné en silence.

### C4.1 — Chapitre 1 : « La clé sous le pot vide » — outil de fortune, marques du meuble, première lettre d'Alma
Dépend de : C3.1, C3.2 (maison refuge, schéma et rendu, déjà `fait`)
Critère de sortie : une partie de campagne neuve démarre avec un outil de fortune déjà en possession du joueur (`s.campaignTools` non vide dès `fresh()`, une entrée `"outil-de-fortune"` plutôt qu'un nouvel emplacement d'équipement — réutilise le sac `campaignTools` posé par C3.6, aucun nouveau système d'équipement) ; réparer la pièce d'accueil (déjà possible depuis C3.1/`repairHouseSpace`) reste le seul geste mécanique requis pour ce chapitre, aucune nouvelle mécanique de réparation. Nouvelle interaction « déplacer l'armoire » : un choix explicite et mutuellement exclusif entre conserver/encadrer/repeindre les marques de taille retrouvées, persisté dans la sauvegarde (nouveau champ, ex. `s.campaignHouse.furnitureMarks: null | "conserve" | "encadre" | "repeint"`), modifiable tant que non confirmé mais jamais réinitialisé après confirmation ; un test vérifie qu'un seul choix peut être actif à la fois et qu'il survit à un rechargement. Cette interaction déclenche une première lettre d'Alma (« Tu peux déplacer les meubles. Même ceux dont tu crois te souvenir exactement. ») via un **nouveau mécanisme générique minimal, réutilisable par les chapitres suivants** : un catalogue de courts textes narratifs à affichage unique (nouveau `public/game/data-narrative.js`, entrées `{id, text, trigger}` où `trigger` est une condition déjà exprimable par l'état, pas un moteur de scénario complet), un ensemble `s.campaignFlags` (chaînes) marquant les textes déjà vus, et une nouvelle entrée dans le carnet existant (`hud-panel.js`, panneau `"notebook"`, une branche `buildPanelRows` de plus — pas un nouveau panneau) listant les textes narratifs débloqués ; un texte déclenché une fois ne se redéclenche jamais après rechargement (test dédié). Aucune identité de PNJ nouvelle nécessaire (seule Alma, déjà nommée et confirmée par l'utilisateur — voir game-design.md §2).
Fichiers probables : nouveau `public/game/data-narrative.js`, `public/garden-state-lifecycle.js`, `public/garden-state-validate.js`, nouveau `public/garden-state-cmd-n.js` (`chooseFurnitureTreatment`, résolution des triggers narratifs), extension de `public/game/hud-panel.js`
Statut : todo

### C4.2 — Chapitre 2 : « Ce qu'on reconnaît encore » — Noé, la quête de la fenêtre, le sécateur qui ouvre la serre
Dépend de : C4.1, C2.6a-style registre de quêtes déjà existant (`data-quests.js`/`quests.js`/`garden-state-cmd-e.js`, tous `fait`)
Critère de sortie : un nouveau PNJ réel, Noé (menuisier, design §10), ajouté à `data-buildings.js`/positionné dans une zone déjà accessible dès le début d'une partie neuve (**vérification préalable requise avant d'implémenter, comme pour C3.2/C3.5** : choisir une zone/position déjà déverrouillée par défaut — probablement la zone 0, cohérente avec « Cour et serre condamnée » comme premier lieu du tableau §8 — jamais une nouvelle zone inventée pour ce chapitre), rôle `quest-giver` (patron déjà existant, `data-roles.js`). Une quête réelle « une plante pour sa fenêtre » : objectif `deliver` d'une plante/bouture déjà accessible avec les semences de départ, sans passage par le pot (**vérification préalable requise** : identifier un item de départ réellement livrable dès une partie neuve, sur le modèle de `"cutting:pilea"` déjà utilisé par `first-harvest` — le design l'exige explicitement, « Plant ordinaire accessible avant le pot »). La récompense étend le schéma de récompense de quête d'un nouveau champ additif, `reward.unlockHouseSpace` (liste d'ids d'espaces de la maison, même patron exact que `reward.tools` dans `garden-state-cmd-e.js` : ajouté seulement à la complétion réelle, jamais à l'acceptation) qui déverrouille (`locked: false`, jamais `repare` automatiquement) l'espace `"serre"` déclaré `locked: true` par C3.1. Un test vérifie que la serre reste verrouillée avant complétion, déverrouillée après, et qu'aucune commande de réparation n'est déclenchée automatiquement (le joueur répare ensuite lui-même, mécanique déjà existante).
Fichiers probables : `public/game/data-buildings.js`, `public/game/data-quests.js`, `public/garden-state-cmd-e.js` (extension additive de la boucle de récompense), `public/game/campaign-house.js`/`garden-state-cmd-l.js` (si un déverrouillage direct de `locked` s'avère nécessaire côté commande plutôt que dans le seul gestionnaire de récompense)
Statut : todo

### C4.3 — Chapitre 3 : première hybridation, nommer/multiplier/installer un spécimen chez soi, épinglage montré par Iris
Dépend de : C4.2 (serre débloquée), C1.3/C1.4/C1.6 (pot, carnet, multiplication, tous `fait`)
Critère de sortie : dans la serre nouvellement accessible, une note narrative (« Deux graines. Une nuit. Regarder avant de décider. ») réutilise le mécanisme générique de C4.1 (`data-narrative.js`/`s.campaignFlags`), pas un second système. Le joueur peut réaliser un croisement réel au pot (déjà existant depuis la phase 1, rejoué ici dans ce contexte narratif — aucune nouvelle mécanique de croisement), nommer le cultivar obtenu et le multiplier (déjà existants, C1.4/C1.6), puis « installer un exemplaire chez soi » en plaçant un spécimen à des coordonnées proches de la maison refuge (`createSpecimen`/`plantSpecimen` acceptent déjà une position libre depuis C1.6 — aucun nouveau champ de schéma nécessaire, seul un test vérifiant qu'un spécimen positionné près de la maison persiste tel quel au rechargement). Un nouveau PNJ réel, Iris (botaniste, design §10), montre comment épingler un caractère déjà vu ; sa rencontre pose le flag narratif `s.campaignFlags` (même ensemble que C4.1) qui devient **la condition nécessaire et suffisante dont dépend C1.5** (l'épinglage lui-même n'est pas implémenté par cet epic : C4.3 pose seulement la porte narrative que C1.5 attend depuis sa propre entrée — « après le chapitre de botanique correspondant »). **Écart documenté, pas deviné** : la première page du carnet est censée « porter le nom du héros comme créateur » (design, chapitre 3) — aucun système de personnalisation du personnage n'existe (voir note de tête de phase) ; cette entrée utilise donc un nom générique par défaut, en attendant un futur epic de personnalisation, jamais un nom de PNJ existant réutilisé à sa place.
Fichiers probables : `public/game/data-buildings.js` (Iris), `public/game/data-narrative.js` (extension), aucun nouveau fichier de schéma botanique (mécaniques déjà existantes réutilisées telles quelles)
Statut : todo

**Un second lot restera à détailler par un futur passage du Cartographe** (Acte II — chapitres 4 à 6, rencontre de la grenouille/première Rainelle déjà mécaniquement acquise en phase 2-3 mais jamais rejouée avec une vraie mise en scène ; Acte III — chapitres 7 à 9, gestes Replanter/Préparer/Trier encore jamais implémentés) — non détaillé maintenant pour ne pas figer des décisions que ce premier lot (notamment le système narratif minimal de C4.1, à éprouver avant de l'étendre) doit encore éclairer.

## Phase 5 — Prototype du retournement

Porte de sortie : *les joueurs comprennent les effets des veilleuses/prélèvements/repos ; le scénario distingue une partie attentive d'une partie intensive.*

Design §11 : mémoire factuelle bornée, trois leviers d'intensification, une scène de culpabilité et sa réparation. À expérimenter tôt avec peu de contenu (design §15, dernier paragraphe de la table) — ne pas attendre la phase 6 pour la tester.

## Phase 6 — Campagne complète (actes IV à VI)

Porte de sortie : *tous les parcours narratifs concluent sans imposer l'exploitation ni inventer une faute.*

Chapitres 10 à 18, autres lieux, personnages restants, enjeux de disponibilité, épilogues à trois orientations.

## Phase 7 — Profondeur et finition

Porte de sortie : *ajouts validés sur parties réelles ; confort, accès et performances tenus sur les appareils de référence.*

Saisons, extensions de maison, catalogue décoratif, chaînes avancées, tactile, optimisation, histoires secondaires (§10 fin).

---

## Journal des décisions d'orchestration

- 2026-09-17 — Démarrage. Branche `maison-des-possibles` créée depuis `main`. Choix de ne détailler que les phases 1 et 2 immédiatement, pour ne pas figer de décisions de contenu que ces deux phases doivent encore éclairer (voir principe « généraliser plutôt que spécialiser » dans `orchestration.md`).
- 2026-09-17 — C2.2v (dépendances satisfaites : C2.2 fait) sauté au profit de C2.3 : son critère de sortie littéral suppose une scène « maison » de campagne et un point d'ancrage caméra que la phase 3 seule doit construire (déjà noté dans son propre statut « probablement à détailler avec le Cartographe »). L'implémenter maintenant aurait exigé d'inventer d'un bloc, sans passage par le Cartographe, un mode de jeu entier — même raisonnement que le report de C1.5 à C4.3. C2.3, elle, ne dépend que de C1.3/C2.2 (tous deux `fait`) et se réduit à une couche de règles pure (voir sa propre entrée).
- 2026-09-17 — C2.2v sauté une nouvelle fois au profit de C2.4, pour la même raison exacte que la fois précédente (scène « maison » de campagne toujours absente, chantier de la phase 3) : ce n'est pas un nouvel arbitrage, seulement la reconfirmation que rien n'a changé depuis. C2.4 (dépendance C2.3, `fait`) est un epic de schéma pur, sans dépendance narrative ni de rendu cachée.
- 2026-09-18 — C2.2v sauté une troisième fois, toujours pour la même raison (aucune scène « maison » de campagne construite, chantier de la phase 3). C2.5 (dépendance C2.4, `fait`) était le seul autre epic `todo` de la phase disponible ; implémenté avec le même écart « scope réduit à la couche moteur » que C2.2, la scission de son volet interface devenant le nouvel epic C2.5v (todo, dépend de C2.5) plutôt qu'une réduction silencieuse du critère de sortie.
- 2026-09-18 — C2.6 (« Gestes Arroser et Récolter », dépendance C2.5, `fait`) sauté au profit de C2.10, malgré une dépendance satisfaite : son critère de sortie littéral exige qu'une Rainelle « remplisse son arrosoir à la borne du poste » et « humidifie les plantes de la zone »/« dépose dans le panier adjacent » sans intervention — or aucune de ces trois choses n'existe encore comme donnée à ce stade (`poste`/`source`/`destination` restent des identifiants de chaîne opaques depuis C2.4, sans registre de bornes/zones/paniers ; les spécimens de cultivars (`s.specimens`, C1.6) n'ont ni humidité ni production/maturité — ce sont des mécaniques entièrement nouvelles à inventer, pas une extension d'un système existant). Implémenter C2.6 tel quel aurait exigé de construire d'un bloc, sans passage par le Cartographe, au moins trois sous-systèmes de simulation à la fois (bornes d'eau, humidité de zone, maturité/production) — exactement ce qu'interdit `orchestration.md` (« un epic ajoute une capacité vérifiable en une session, jamais une refonte simultanée de plusieurs systèmes »). C2.10 (dépendance C2.4, `fait` également) est en comparaison un epic de schéma pur, borné à une seule règle déjà quasi entièrement vraie par construction (l'exclusion de `multiplier` de `VERBS` existe depuis C2.4) : le bon candidat pour ce déclenchement. C2.6 reste `todo` ; sa reformulation par un futur passage du Cartographe (probablement en deux epics — un registre minimal de bornes/zones/paniers, puis le branchement du tick automation dessus) est laissée à un déclenchement ultérieur plutôt que devinée ici.
- 2026-09-18 — Déclenchement automatisé (routine cloud horaire). Anti-hallucination : dernier epic `fait` du backlog était C2.10, commit `3cc89c9` — `git show --stat 3cc89c9` confirme le commit réel (5 fichiers, correspond exactement à l'entrée de `campagne.md`) ; `npm ci && npm test` relancés indépendamment → 266/266, identique au rapport existant. Aucun bandeau de pause en tête de ce fichier. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. **Rôle Cartographe endossé** (pas d'implémentation ce déclenchement) : les trois seuls epics `todo` de phase 1/2 à dépendances satisfaites étaient C2.2v, C2.5v (tous deux bloqués depuis plusieurs déclenchements par l'absence de scène « maison » de campagne, chantier de la phase 3 — aucun changement depuis la dernière fois) et C2.6, dont la reformulation par le Cartographe était explicitement laissée en suspens par l'entrée précédente de ce journal. Reformulé C2.6 en trois epics plus petits, chacun de la taille d'une session, au lieu de forcer un epic qui aurait mélangé trois sous-systèmes à la fois : **C2.6a** (registre de bornes d'eau/zones/paniers, pur schéma + fonction de résolution, sans brancher encore les commandes de geste dessus), **C2.6b** (humidité et maturité des spécimens, extension de `s.specimens` posée par C1.6, sur le modèle de l'horloge de simulation `s.elapsed` déjà utilisée par le dopage engrais du jardin libre), **C2.6c** (le critère littéral originel de C2.6, désormais réalisable sans invention simultanée puisque C2.6a/b lui fournissent leurs briques). Dépendances de C2.7/C2.8 mises à jour de C2.6 vers C2.6c en conséquence. C2.6 original conservé dans le fichier pour mémoire (todo, avec renvoi vers la reformulation), jamais à choisir directement désormais. Aucun code ni test modifié ce déclenchement — travail de planification seul, comme prescrit par `orchestration.md`/`execution-continue.md` pour ce cas.
- 2026-09-18 — Déclenchement automatisé (routine cloud horaire). Anti-hallucination : dernier epic `fait` du backlog toujours C2.10, commit `3cc89c9` — `git show --stat 3cc89c9` confirme le commit réel ; `npm ci && npm test` relancés indépendamment → 266/266, identique au rapport existant. Aucun bandeau de pause en tête de ce fichier. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Epics `todo` à dépendances satisfaites disponibles : C2.2v/C2.5v (toujours bloqués par l'absence de scène « maison », chantier de la phase 3, aucun changement), **C2.6a** (dépendance C2.4, `fait`) et **C2.6b** (dépendance C1.6, `fait`) — les deux premiers tiers de la reformulation de C2.6 posée au déclenchement précédent. Choisi **C2.6a** : premier des deux tiers listés dans le backlog, et prérequis naturel de C2.6c au même titre que C2.6b (aucune raison de préférer l'un à l'autre autrement que par l'ordre déjà écrit). Epic de schéma pur, sans dépendance de rendu ni narrative : nouveau `public/game/campaign-stations.js` (trois collections `bornes`/`zones`/`paniers`, préfixes d'id disjoints `b`/`z`/`pn`, `registerStation`/`resolveStation` purs, non encore appelés par aucune commande), `public/garden-state-lifecycle.js` (valeur par défaut dans `fresh()`), `public/garden-state-validate.js` (validation stricte + migration `??=` par défaut, même patron que `campaignTeaching`/`campaignLastDemonstration`). `teachGesture`/`demonstrateGesture` inchangés (toujours du texte libre), vérifié par un test dédié plutôt que supposé. Détail complet dans `docs/campagne.md`.
- 2026-09-18 — Déclenchement automatisé (routine cloud horaire). Anti-hallucination : dernier epic `fait` du backlog était C2.6a, commit `33bb83c` — `git show --stat 33bb83c` confirme le commit réel (8 fichiers, correspond exactement à l'entrée de `campagne.md`) ; `npm ci && npm test` relancés indépendamment → 278/278, identique au rapport existant. Aucun bandeau de pause en tête de ce fichier. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Seul epic `todo` à dépendance satisfaite restant en phase 1/2, hors C2.2v/C2.5v (toujours bloqués par l'absence de scène « maison », chantier de la phase 3, aucun changement) : **C2.6b** (dépendance C1.6, `fait`), deuxième tiers de la reformulation de C2.6. Choisi sans ambiguïté (seul candidat disponible). Epic de schéma pur, sans dépendance de rendu ni narrative : voir sa propre entrée ci-dessus et le détail dans `docs/campagne.md`.
- 2026-09-18 — Déclenchement automatisé (routine cloud horaire). Anti-hallucination : dernier epic `fait` du backlog était C2.6b, commit `86ce50a` — `git show --stat 86ce50a` confirme le commit réel (7 fichiers, correspond exactement à l'entrée de `campagne.md`) ; `npm ci && npm test` relancés indépendamment → 291/291, identique au rapport existant. Aucun bandeau de pause en tête de ce fichier. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Seul epic `todo` à dépendances satisfaites restant en phase 1/2, hors C2.2v/C2.5v (toujours bloqués par l'absence de scène « maison », chantier de la phase 3, aucun changement) : **C2.6c** (dépendances C2.6a/C2.6b/C2.5, toutes `fait`), troisième et dernier tiers de la reformulation de C2.6. Choisi sans ambiguïté (seul candidat disponible). Implémenté par un sous-agent Artisan moteur avec mandat complet, puis vérifié indépendamment avant commit (relecture ligne à ligne du diff, `npm test` relancé une deuxième fois par l'orchestrateur lui-même → 301/301, vérification Playwright ciblée rejouée indépendamment avec un scénario différent de celui du sous-agent, confirmant absence d'erreur console et fonctionnement réel du geste « récolter » de bout en bout) — conformément à la mise en garde d'`orchestration.md` (« le rapport d'un agent délégué n'est jamais la preuve »). Détail complet dans `docs/campagne.md`.
- 2026-09-18 — Déclenchement automatisé (routine cloud horaire). Anti-hallucination : dernier epic `fait` du backlog était C2.6c, commit `91e1665` — `git show --stat 91e1665` confirme le commit réel (11 fichiers, correspond exactement à l'entrée de `campagne.md`) ; `npm ci && npm test` relancés indépendamment → 301/301, identique au rapport existant. Aucun bandeau de pause en tête de ce fichier. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Trois derniers epics consignés (C2.6a/C2.6b/C2.6c) tous `fait` : aucune pause anti-emballement à déclencher. Deux epics `todo` à dépendances satisfaites disponibles : **C2.7** (Transporter, dépendance C2.6c `fait`) et **C2.8** (Réservations/états de blocage, même dépendance) — choisi **C2.7**, premier des deux dans l'ordre du backlog, prérequis naturel de C2.11 (chaîne « fraises de la cuisine ») au même titre que C2.8. Epic de schéma/moteur pur (aucun rendu, `campaign-automation.js` seul modifié, `rainelles.js` inchangé) : implémenté directement par l'orchestrateur (pas de délégation, taille jugée suffisamment petite pour une session unique), voir sa propre entrée ci-dessus et le détail complet dans `docs/campagne.md`.
- 2026-09-18 — Déclenchement automatisé (routine cloud horaire). Anti-hallucination : dernier epic `fait` du backlog était C2.7, commit `fe6a901` — `git show --stat fe6a901` confirme le commit réel (4 fichiers, correspond exactement à l'entrée de `campagne.md`) ; `npm ci && npm test` relancés indépendamment → 306/306, identique au rapport existant. Aucun bandeau de pause en tête de ce fichier. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Trois derniers epics consignés (C2.6c/C2.7, et C2.10 avant eux) ne sont pas tous `bloqué` : aucune pause anti-emballement à déclencher. Seul epic `todo` à dépendances satisfaites en phase 1/2, hors C2.2v/C2.5v (toujours bloqués par l'absence de scène « maison » de campagne, chantier de la phase 3, aucun changement) : **C2.8** (Réservations et états de blocage, dépendance C2.6c `fait`). Choisi sans ambiguïté (seul candidat disponible ; C2.9/C2.11 restent hors de portée, leurs dépendances incluant C2.8 lui-même). Implémenté directement par l'orchestrateur (épic de schéma/moteur pur, aucun rendu). **Scope réduit à six des sept états nommés par le design et à une réservation « par construction »** plutôt qu'une structure de réservation dédiée : le critère littéral suppose deux mécaniques qui n'existent pas encore ailleurs dans le code — un graphe/une grille de déplacement réel des Rainelles (pour « passage bloqué », design §5 : « les croisements de passage utilisent une priorité stable et un temps d'attente borné ») et une notion de ressource « en transit » sur plusieurs ticks (pour le sauvetage vers un bac de secours si une cible disparaît en cours de route) — alors qu'aujourd'hui une Rainelle n'a aucune position dans le monde et qu'un geste s'exécute directement sur ses stations, jamais en s'y déplaçant, et qu'aucune commande ne retire encore une station une fois enregistrée. Les construire maintenant aurait exigé d'inventer d'un bloc, dans ce seul epic, un système de déplacement complet — exactement ce qu'`orchestration.md` interdit (« jamais une refonte simultanée de plusieurs systèmes »), le même raisonnement exact qui a justifié la scission C2.2/C2.2v et C2.5/C2.5v. Reporté au nouvel epic **C2.8v** (todo, dépend de C2.8 et d'un modèle de déplacement à construire, probablement en phase 3) plutôt que réduit en silence. Ce qui est réellement livré : `panier.capacity`/`panier.min` (design §5, « réglage avancé... minimum et maximum »), appliqués avant toute mutation de buffer dans `tickRecolter`/`tickTransporter` (une récolte/un transport qui dépasserait une limite s'arrête proprement, sans perte — jamais tout-ou-rien pour le transport, qui partage un budget unique entre la capacité de sortie et le plancher de la source) ; et `public/game/rainelles-status.js`, nouveau, qui expose `au-travail`/`source-vide`/`stock-cible-atteint`/`sortie-pleine`/`poste-manquant`/`repos` avec une phrase, sur le modèle d'`irrigation-status.js`. La clause « deux Rainelles ne peuvent pas prendre la même cible » est vraie par construction (tick strictement séquentiel, `s.rainelles` dans l'ordre du tableau) et prouvée par un test dédié (deux récolteuses visant un panier presque plein dans le même tick) plutôt que par une nouvelle structure de verrouillage. `/code-review` (niveau medium) exécuté sur le diff complet : deux améliorations mineures relevées (un `Stations.panierTotal` recalculé à chaque spécimen dans `tickRecolter` au lieu d'un compteur incrémental ; une fonction `distance` locale dans `rainelles-status.js` dupliquant celle déjà exportée par `construction.js`) — toutes deux corrigées avant ce commit, `npm test` relancé après correction (329/329, aucune régression). Vérification navigateur ciblée (page servie localement via `python3 -m http.server`, Playwright avec l'exécutable Chromium préinstallé) : la page se charge sans erreur console ni erreur de page, `window.GardenRainellesStatus` est bien défini — aucun test `test:browser` complet nécessaire au sens strict (aucun rendu/interface modifié), mais cette vérification ciblée couvre le risque réel introduit ici (une nouvelle balise `<script>` dans `public/index.html`), le même type de régression d'ordre de chargement déjà documenté à plusieurs reprises dans ce fichier. Détail complet dans `docs/campagne.md`.
- 2026-09-18 — Déclenchement automatisé (routine cloud horaire). Anti-hallucination faite avant tout le reste : dernier epic « fait » du backlog était C2.8, commit `a3ed61f51d37a7bc1d46c32c3ab032462af08c69` — `git show --stat a3ed61f` confirme le commit réel (11 fichiers, correspond exactement à l'entrée de `campagne.md`) ; `npm ci && npm test` relancés indépendamment → 329/329, identique au rapport existant. Aucun bandeau de pause en tête de ce fichier. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Trois derniers epics consignés (C2.6c/C2.7/C2.8) ne sont pas tous `bloqué` : aucune pause anti-emballement à déclencher.

  **Choix de l'epic.** C2.8 venant de passer `fait`, deux nouveaux candidats à dépendances désormais satisfaites apparaissent : **C2.9** (mode d'observation, dépendance C2.8) et **C2.11** (chaîne « fraises de la cuisine », dépendances C2.7/C2.8/C2.10, toutes `fait`) — en plus de C2.2v/C2.5v/C2.8v, toujours sautés pour la raison inchangée depuis plusieurs déclenchements (absence de scène/déplacement de campagne, chantier de la phase 3). Entre C2.9 et C2.11, **choisi C2.11**, pas seulement par ordre d'écriture cette fois : le critère littéral de C2.9 (« une vue dédiée montre chemins, transferts et cadence... permet de suivre un objet du plant au présentoir ») suppose la même représentation spatiale des Rainelles dans le monde que C2.2v/C2.5v/C2.8v n'ont toujours pas (vérifié : `hud-world.js` existant ne porte que la carte ancrée du jardin libre, `drawWorldActions`, rien de campagne) — l'implémenter maintenant aurait très probablement fini par la même scission « v » que les trois autres. C2.11, lui, est un epic de test pur sur une couche moteur entièrement déjà livrée (C2.6c/C2.7/C2.8/C2.10), sans aucune dépendance de rendu, et sa propre entrée de backlog le nomme explicitement comme la porte de sortie de la phase — le bon candidat pour clore la phase 2 plutôt que de la laisser en suspens derrière un chantier de rendu.

  **Implémenté directement par l'orchestrateur** (épic de test pur, aucune délégation). Exactement le fichier probable du backlog, `tests/campaign-rainelles-chain.cjs`, plus un second fichier non anticipé par le gabarit initial : `tests/campaign-phase2-gate.cjs` (même patron que `campaign-phase1-gate.cjs` à la clôture de la phase 1), nécessaire parce que C2.11 ferme la porte de sortie de la phase et que le critère de cette porte a deux volets — « une chaîne fonctionne, sature proprement puis redémarre » (entièrement couvert par `campaign-rainelles-chain.cjs`) et « un joueur enseigne un geste... sans guide externe » (single-geste/réenseignement/refus de multiplication couvrant aussi un congénère distinct, le volet de la grille §16 non encore prouvé comme un seul récit de bout en bout).

  `tests/campaign-rainelles-chain.cjs` : une seule Rainelle mûre suffit (aucun système de régénération borné n'existe encore, C2.6c — le spécimen est réarmé chaque tick, donc une seule fraisier peut nourrir la chaîne indéfiniment) ; trois Rainelles enseignées via le vrai `g.command({type:"teachGesture"})` (arroser/récolter/transporter, ce dernier filtré par `condition`) plutôt que `Rainelles.applyGesture` directement, pour rester le plus proche possible du vecteur de commandes réel. 60 cycles (1200 s simulées) suffisent à observer la double saturation propre (panier local et réserve de cuisine tous deux à leur capacité par défaut, 24) sans jamais rien perdre (le spécimen mûr reste `readyToProduce`) ; une mutation directe de `cuisine.buffer` (aucune commande de consommation n'existe encore dans ce périmètre — documenté explicitement dans le fichier comme un futur système « cuisiner »/« consommer » hors périmètre de cet epic) simule un tiers qui vide la réserve, et le cycle suivant montre le transport puis la récolte reprendre tout seuls, sans aucun réenseignement. Le test se termine en poursuivant la simulation jusqu'à dépasser trois jours de campagne au sens de `campaign-clock.js` (`DAY_SECONDS`, 57 600 s — constante réutilisée telle quelle plutôt qu'un nombre inventé, bien que cette horloge ne soit pas encore câblée à `s.elapsed`), vérifiant qu'aucune limite n'est jamais dépassée et que l'arrosage reste actif après plusieurs jours simulés sans intervention.

  `tests/campaign-phase2-gate.cjs` : scénario 1/2, `teachGesture` avec seulement les cinq champs que la commande elle-même demande, plus `Rainelles.defaultPhrase` pour vérifier que le jeu propose sa propre explication sans documentation externe. Scénario 2/2, sur deux Rainelles distinctes (une nuance que `campaign-refusal.cjs` existant, avec sa seule Rainelle, ne prouvait pas encore) : geste unique, réenseignement qui remplace intégralement, puis refus de multiplication identique sur chacune des deux (« elle-même comme ses congénères », design §5) et sur `multiplySpecimen` visant directement l'identifiant de la seconde Rainelle.

  **Résultat réellement exécuté** : `npm ci` puis `npm test` (après ajout des deux nouveaux fichiers à la liste explicite du script `test` de `package.json`, qui n'énumère pas les fichiers par glob) → **332/332** (329 existants + 1 dans `campaign-rainelles-chain.cjs` + 2 dans `campaign-phase2-gate.cjs`, zéro régression). Sortie complète :

  ```
  # tests 332
  # suites 0
  # pass 332
  # fail 0
  # cancelled 0
  # skipped 0
  # todo 0
  ```

  `/code-review` (niveau medium, skill `code-review`) exécuté sur le diff complet : aucun défaut relevé (diff entièrement composé de fichiers de test plus le câblage `package.json`, aucun code applicatif modifié).

  Aucun rendu/géométrie/matériau touché (epic entièrement fait de tests Node) : `test:browser`/`test:visual` non requis, même exemption que C2.6a/b/c/C2.7 (voir `execution-continue.md`) — `public/index.html` inchangé, aucune nouvelle balise `<script>` (les deux nouveaux fichiers sont des tests Node, jamais chargés par le navigateur). Aucune relecture narrative adverse requise (pas un epic de Scénariste). Aucun choix déjà confirmé touché (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne).

  **Cet epic ferme la porte de sortie de la phase 2** (design §15 : « un joueur enseigne un geste à une Rainelle sans guide externe ; une chaîne fonctionne, sature proprement puis redémarre » — les deux tests ci-dessus le prouvent littéralement). Scénarios de validation de la section 16 du design rejoués en tests avant de considérer la porte franchie : outre les deux ci-dessus, la suite complète (332 tests) couvre déjà, sans changement à ce déclenchement, « aucune commande, récolte, nuit ou naissance ne crédite deux fois » (C2.2/C1.1) et « multiplier un cultivar conserve exactement ses traits » (C1.6/phase1-gate). Les deux volets du tableau §16 restant hors de portée aujourd'hui — « passage coupé » (aucune Rainelle n'a de position dans le monde, C2.8v) et « déplacer un poste... rangement » (aucune commande ne bouge/range encore une borne/zone/panier) — sont honnêtement documentés comme non prouvés plutôt que simulés artificiellement, exactement comme C1.5/épinglage l'était resté à la clôture de la phase 1 : le critère de sortie littéral de la phase ne les nomme pas.

  **Fusion vers `main`, conformément à `execution-continue.md`** (« fusionner une phase validée dans main... est une étape d'implémentation comme une autre, pas un acte de publication » — aucun pipeline connecté à `main`, vérifié à nouveau : toujours aucun workflow CI dans ce dépôt) : `maison-des-possibles` fusionnée localement dans `main` après ce commit, rapport de clôture de phase daté ajouté dans `docs/campagne.md`, `main` et `maison-des-possibles` tous deux poussés vers `github.com/EdwinDayot/edwindayot.fr` (le seul remote autorisé). Aucun déploiement, aucun pipeline de publication touché. **Note honnête laissée pour le prochain déclenchement** : C2.2v/C2.5v/C2.8v/C2.9 restent `todo` dans le backlog malgré la fusion — leur propre critère de sortie ne fait pas partie du critère de sortie de la phase citée par le design, exactement le précédent déjà posé par C1.5 à la clôture de la phase 1 (resté `todo`, différé à C4.3, sans bloquer la fusion de la phase 1). Rien n'empêche de les reprendre plus tard, y compris après le début de la phase 3, si leur chantier commun (position/déplacement des Rainelles dans le monde) avance.
- 2026-09-18 — Déclenchement automatisé (routine cloud horaire). Anti-hallucination faite avant tout le reste : dernier epic « fait » du backlog était C2.11, commit `bc2506b4aedb18a8c77a1a5e8fec1b29baea3a1b` — `git show --stat bc2506b` confirme le commit réel (5 fichiers : `docs/campagne-backlog.md`, `docs/campagne.md`, `package.json`, `tests/campaign-phase2-gate.cjs`, `tests/campaign-rainelles-chain.cjs`, correspond exactement à l'entrée de ce fichier et de `campagne.md`) ; `npm ci && npm test` relancés indépendamment → 332/332, identique au rapport existant. Aucun bandeau de pause en tête de ce fichier. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Les trois derniers epics consignés (C2.6c/C2.7/C2.8, puis C2.11) ne sont pas tous `bloqué` : aucune pause anti-emballement à déclencher.

  **Rôle Cartographe endossé** (pas d'implémentation ce déclenchement), conformément à l'étape 4 de la routine : la phase 2 est close et fusionnée dans `main` depuis le déclenchement précédent ; les seuls epics `todo` à dépendances formellement satisfaites en phase 1/2 (C2.2v, C2.5v, C2.9) restent, comme documenté à chaque déclenchement depuis plusieurs heures et reconfirmé une nouvelle fois avant de choisir cette voie, bloqués de fait par l'absence de toute scène « maison »/position de Rainelle dans le monde — un chantier de la phase 3 elle-même. La phase 3 (« Tranche verticale »), elle, n'a encore aucun epic détaillé (`docs/campagne-backlog.md` ne portait qu'un paragraphe de contour) alors que sa porte d'entrée (clôture de la phase 2) vient de s'ouvrir — exactement la condition citée par `orchestration.md`/`execution-continue.md` pour endosser le rôle Cartographe plutôt que de forcer un epic non détaillé.

  Recherche factuelle faite avant d'écrire un seul epic (pas une supposition) : aucune maison de joueur, aucune scène intérieure, aucun système de mobilier n'existe dans le code (`grep -i "maison|refuge"` ne remonte que les maisons de visiteurs génériques `data-buildings.js`/`render-houses.js`) ; `data-buildings.js`/`render-houses.js` rendent déjà des maisons génériquement depuis des données pures, mais `buildHouse()` suppose aujourd'hui un visiteur obligatoire (`D.visitors.find(...).name`), donc pas un simple drop-in pour une maison refuge sans habitant — noté explicitement dans C3.2 plutôt que découvert en silence par un futur Artisan rendu ; `construction.js` ne connaît que le placement extérieur, aucun mobilier d'intérieur ; le système de quêtes (`data-quests.js`/`quests.js`/`garden-state-cmd-e.js`) est câblé mais sans aucun PNJ porteur du rôle `quest-giver` en jeu ; `s.campaignDay` existe déjà et s'incrémente à chaque `sleep` (`garden-state-cmd-f.js`), prêt à compter trois nuits ; la création d'une deuxième Rainelle n'a aujourd'hui aucun chemin joueur réel, seulement l'appel direct `Rainelles.createRainelle` utilisé par les tests ; aucune zone « village » n'existe dans `data-world.js` (quatre zones seulement). Le design (§5 « Multiplication et vie propre ») décrit explicitement un mécanisme légitime pour une deuxième Rainelle — bourgeon prélevé puis mûri à la nurserie après une nuit de sommeil, sur le même principe temporel que l'établi déjà existant — repris ici comme mécanique générique, sans la mise en scène du chapitre 6 (Acte II, phase 4), exactement la même discipline que C2.3 pour la première Rainelle.

  **Sept epics détaillés (C3.1 à C3.7)**, voir leurs propres entrées ci-dessus : C3.1/C3.2 (schéma puis rendu de la maison refuge, six espaces du design §6, un seul déverrouillé au départ) ; C3.3 (registre d'habitats de Rainelle, extension de `campaign-stations.js` plutôt qu'un nouveau système — généraliser plutôt que spécialiser) ; C3.4 (bourgeon/nurserie, deuxième Rainelle réellement obtenue en jeu) ; C3.5 (zone de village et deux habitants, **délibérément sans identité narrative** — aucun nom d'Acte I-III utilisé, ces personnages et leurs quêtes appartiennent à la phase 4 non encore écrite, écart documenté en tête de la section phase 3) ; C3.6 (première quête d'outil, extension additive `reward.tools` du schéma de récompense existant, sans inventer de nouveau système de quête) ; C3.7 (porte de sortie de la phase, test d'intégration des quatre promesses — habiter/croiser/organiser/rencontrer — sur trois nuits jouées, fusion vers `main` si les scénarios de la section 16 passent). Dépendances internes au lot enchaînées dans l'ordre d'écriture (C3.1→C3.2, C3.3→C3.4, C3.5→C3.6, tous convergent vers C3.7) ; aucune ne saute une dépendance non satisfaite.

  **Un second lot restera à détailler par un futur passage du Cartographe** (rendu enrichi du village, câblage narratif réel une fois la phase 4 ouverte, et une réévaluation de C2.2v/C2.5v/C2.8v/C2.9 une fois qu'une vraie scène « maison »/un déplacement de Rainelles existent grâce à ce lot) — non détaillé maintenant pour ne pas figer des décisions que ce premier lot doit encore éclairer, même principe que la note de tête de fichier sur les phases 3 à 7.

  Aucun code ni test modifié ce déclenchement — travail de planification seul dans `docs/campagne-backlog.md`, comme prescrit par `orchestration.md`/`execution-continue.md` pour ce cas. Détail complet également consigné dans `docs/campagne.md`.
- 2026-09-18 — Déclenchement automatisé (routine cloud horaire). Anti-hallucination faite avant tout le reste : dernier epic « fait » du backlog était C2.11, commit `bc2506b4aedb18a8c77a1a5e8fec1b29baea3a1b` — `git show --stat bc2506b` confirme le commit réel (5 fichiers, correspond exactement à l'entrée existante de `campagne.md`) ; `npm ci && npm test` relancés indépendamment → 332/332, identique au rapport existant. Aucun bandeau de pause en tête de ce fichier. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Les trois derniers epics consignés (C2.6c/C2.7/C2.8, puis C2.11) ne sont pas tous `bloqué` : aucune pause anti-emballement à déclencher. Le commit `f686e2d` (Cartographe, premier lot de la phase 3) était déjà en tête de branche, correspondant à l'entrée précédente de ce journal.

  **Choix de l'epic.** Deux epics `todo` de la phase 3 ont leurs dépendances satisfaites (« rien ») : **C3.1** et **C3.5**. Choisi **C3.1**, pas seulement par ordre d'écriture : il débloque transitivement le plus d'epics du lot (C3.2 en dépend directement, C3.3 aussi, et C3.7 par la chaîne C3.1→C3.2→C3.7), alors que C3.5 ne débloque que C3.6. Epic de schéma/moteur pur, aucun rendu, aucune dépendance narrative.

  **Implémenté directement par l'orchestrateur** (épic de schéma/moteur pur, aucune délégation). Vérification factuelle faite avant d'écrire du code : les familles de ressources « bois, pierre, argile, fibres » citées par le design §6/§16 n'existent que partiellement dans le jardin libre réel — `wood`/`stone`/`clay` seulement, aucune « fibre » (vérifié dans `garden-state-util.js`/`garden-state-lifecycle.js`/`data.js`, pas supposé). Voir sa propre entrée ci-dessus et le détail complet dans `docs/campagne.md`. `npm test` → 342/342 (332 existants + 10 nouveaux dans `tests/campaign-house.cjs`, zéro régression). `/code-review` niveau medium : aucun défaut relevé. Vérification navigateur ciblée (deux nouvelles balises `<script>` dans `index.html`, même précédent que C2.8) : page chargée sans erreur console, `window.GardenCampaignHouse` bien défini.
- 2026-09-18 — Déclenchement automatisé (routine cloud horaire). Anti-hallucination faite avant tout le reste : dernier epic « fait » du backlog était C3.1, commit `5564102ce24b0bfac13fd9f0c021b21048fb9650` — `git show --stat 5564102` confirme le commit réel (correspond exactement à l'entrée précédente de ce fichier) ; `npm ci && npm test` relancés indépendamment → 342/342, identique au rapport existant. Aucun bandeau de pause en tête de ce fichier. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Aucun blocage récent : aucune pause anti-emballement à déclencher.

  **Choix de l'epic.** Trois epics `todo` de la phase 3 ont leurs dépendances satisfaites : **C3.2** (dépend de C3.1, `fait`), **C3.3** (dépend de C2.6a/C3.1, tous deux `fait`) et **C3.5** (dépend de rien). Choisi **C3.2**, en continuité explicite de la chaîne C3.1→C3.2 déjà engagée par le déclenchement précédent (le journal du lot Cartographe du 18 septembre l'énonce lui-même : « Dépendances internes au lot enchaînées dans l'ordre d'écriture (C3.1→C3.2...) ») plutôt que par un critère de blocage transitif (les trois candidats convergent également vers C3.7 sans se débloquer directement l'un l'autre). C'est aussi le seul des trois qui touche au rendu — l'occasion de traiter, dès que possible après C3.1, la mise en garde que sa propre entrée de backlog documentait déjà (« vérification préalable requise avant d'implémenter »), plutôt que de la laisser vieillir.

  **Vérification préalable faite avant d'écrire du code, comme demandé par l'entrée du backlog** : `render-houses.js:295-307` (`buildHouse(b)`) lu directement — confirme que la fonction appelle `D.visitors.find((v) => v.id === b.visitorId).name` sans garde, donc lèverait sur une maison sans visiteur. Décision : ni une branche visiteur optionnelle dans `buildHouse` (aurait mélangé la donnée `D.buildings`/`D.visitors` du jardin libre — un système qui signifie « une rangée par PNJ », `data-buildings.js` l'énonce lui-même — avec la maison du joueur, qui n'a justement aucun visiteur), ni une entrée `data-buildings.js` (même mélange). Choisi : un module frère autonome, `public/game/render-campaign-house.js`, qui réutilise la même *technique* (mur à moellons, pignon suivant la pente du toit via une géométrie identique à `gableGeometry`, toit à deux pans) sans dépendre de `window.GardenModels`/`window.GardenView` — seul `THREE` requis, donc testable en Node pur, exactement la séparation déjà documentée par `botany-hybrids.js` face à `botany.js`/`garden-models.js` pour la même raison (deux catalogues sans rapport, jamais mélangés).

  **Livré :**
  - Nouveau `public/game/render-campaign-house.js` : `buildRefugeHouseGroup(house)` lit uniquement `house.spaces.accueil.status` (schéma C3.1) — « au minimum la pièce d'accueil, seul espace jouable à ce stade », conformément au critère de sortie littéral de cet epic. Couleurs strictement reprises des hex déjà utilisés par `render.js`'s `this.mat.stone`/`bark`/`cream` et `render-houses.js`'s `stoneDark` (jamais de teinte inventée) — vérifiées avant écriture, pas supposées. Distinction visuelle delabre/repare par trois différences nommables et testables plutôt qu'un simple changement de teinte : pierre claire dominante + toit à deux pans intacts + porte fermée (repare) contre pierre sombre dominante + un seul pan de toit survivant (l'autre manquant, laissant voir la charpente/le ciel, plutôt qu'un toit intact simplement reteinté) + planches croisées clouées sur l'ouverture (delabre). Aucun matériau émissif introduit (direction-artistique.md réserve l'émissif à l'eau qui coule et aux lanternes ; cette distinction n'en avait pas besoin).
  - `public/index.html` : une nouvelle balise `<script>` (`game/render-campaign-house.js`, juste après `render-houses.js`, avant `render-scene.js` — aucune dépendance de chargement au-delà de `THREE`, même position relative que `botany-hybrids.js` par rapport à `three.min.js`).
  - Nouveau `tests/campaign-house-render.cjs` (7 tests, Node pur, sur le modèle de `campaign-hybrids-render.cjs`) : construction sans exception pour les deux états, `userData.repaired` correct, différences roof/porte testées explicitement (1 vs 2 panneaux de toit, `"boarded"` vs `"closed"`), aucune métallicité, roughness 0,72 identique à la convention existante de `render.js` (voir écart documenté ci-dessous sur direction-artistique.md), toutes les couleurs utilisées appartiennent à l'ensemble exact déjà utilisé par les maisons existantes, déterminisme (deux constructions du même état sont géométriquement identiques), et le pignon reproduit exactement le même profil que `gableGeometry` (sommets à hauteur de mur et de faîtage vérifiés avec une tolérance flottante, la précision Float32Array empêchant une égalité stricte).
  - Extension de `tests/garden-material-audit.cjs` (le vrai gate, pas une formalité) : injecte les deux états de la maison refuge dans la scène réelle de la page servie, loin de la zone jouable (même convention que l'injection des hybrides C1.7), pour que l'audit programmatique du graphe de scène (transparence, normales de sol, sommets non finis) porte réellement sur les nouveaux maillages/matériaux — aucune extension de `TRANSPARENT_ALLOWLIST` nécessaire (aucun matériau transparent introduit).
  - Nouveau `tests/campaign-house-visual.cjs` (banc de comparaison dédié, sur le modèle de `campaign-botany-visual.cjs`) : construit les deux états côte à côte sur une scène hors-jeu dédiée, vérifie programmatiquement l'absence de sommets non finis, l'absence de chevauchement des deux boîtes englobantes, et que le nombre de panneaux de toit et le style de porte diffèrent bien entre les deux états — puis sauvegarde une capture pour la relecture multimodale ci-dessous.
  - `package.json` : les deux nouveaux fichiers de test ajoutés aux listes explicites de `test`/`test:browser` (le script n'énumère pas par glob, leçon déjà tirée à C2.11).

  **Résultat réellement exécuté** : `npm ci` puis `npm test` → **349/349** (342 existants + 7 nouveaux dans `campaign-house-render.cjs`, zéro régression). Sortie complète :

  ```
  # tests 349
  # suites 0
  # pass 349
  # fail 0
  # cancelled 0
  # skipped 0
  # todo 0
  ```

  **Audit programmatique du rendu (le vrai gate)** : page servie localement (`python3 -m http.server 4174 --directory public`), `node tests/garden-material-audit.cjs` exécuté directement contre elle → `PASS material/geometry audit: {"materialsInspected":97,"transparentAllowed":1,"timesSampled":5}` — la maison refuge (les deux états) fait bien partie des 97 matériaux inspectés, aucune anomalie (transparence non documentée, normale de sol retournée, sommet non fini) relevée sur les nouveaux maillages. `node tests/campaign-house-visual.cjs` exécuté directement → `PASS campaign-house-visual: {"overlap":false,"delabreRoofPanelCount":1,"repareRoofPanelCount":2,"delabreDoorStyle":"boarded","repareDoorStyle":"closed"}`.

  **Limite honnête sur `npm run test:browser` en tant que chaîne complète** : la chaîne échoue dès son premier script, `tests/garden-play.cjs` (non modifié par cet epic), faute d'exécutable Chromium à son chemin par défaut dans ce bac à sable — ce script n'a jamais porté le repli documenté (`/opt/pw-browsers/chromium`) que `garden-material-audit.cjs`/`campaign-botany-visual.cjs` portent déjà, un défaut d'environnement préexistant et sans rapport avec cet epic (vérifié : `garden-play.cjs` inchangé par ce commit). Les deux scripts navigateur réellement concernés par ce rendu (`garden-material-audit.cjs`, `campaign-house-visual.cjs`, tous deux dotés du repli) ont donc été exécutés directement plutôt que via `npm run test:browser`, conformément au précédent déjà posé par l'entrée C2.8 (« vérification navigateur ciblée... aucun test test:browser complet nécessaire au sens strict »). Cette limite d'environnement n'est pas corrigée ici (`garden-play.cjs` est hors du périmètre de cet epic ; le corriger serait une refonte non demandée d'un fichier non lié).

  **Relecture multimodale réelle** (capture `/tmp/campaign-house-visual.png`, lue avec l'outil Read, comparée explicitement à `direction-artistique.md`) : les deux maisons se distinguent nettement à la silhouette, sans lire d'étiquette — pierre claire/toit brun intact/porte en bois fermée pour l'état réparé (identique en teinte à toute maison de visiteur déjà en jeu) contre pierre plus sombre/un seul pan de toit (l'autre manquant, laissant voir l'intérieur sombre et le vide du faîtage)/grandes planches croisées sur l'ouverture pour l'état délabré. Matériaux mats (aucun reflet), aucune couleur hors des familles pierre/bois documentées. Conforme à direction-artistique.md §« Silhouette et géométrie » (« la silhouette porte l'identité, pas la texture »). Ce contrôle visuel reste un complément documenté, jamais le gate — celui-ci est l'audit programmatique ci-dessus, déjà passé indépendamment.

  **Écart honnête découvert, documenté sans le corriger dans cet epic** : direction-artistique.md affirme une rugosité (`roughness`) « observée entre 0,15 et 0,48 » pour tous les matériaux du jeu — mais `render.js`'s `this.mat.stone`/`bark`/`wood`/`cream` (donc toute maison déjà en jeu, avant même cet epic) utilisent la rugosité par défaut de `GardenModels.mat()`, **0,72**, hors de cette plage documentée (vérifié dans `garden-models.js:16`, pas supposé). Cet epic reprend fidèlement cette convention déjà en usage pour les maisons (plutôt que d'inventer une nouvelle rugosité juste pour correspondre au texte du guide, ce que `direction-artistique.md` interdit lui-même : « ce guide part de ce qui existe déjà »), donc `tests/campaign-house-render.cjs` teste explicitement 0,72, pas la plage 0,15-0,48. La correction de cette inexactitude dans `direction-artistique.md` lui-même est laissée à un futur passage (probablement Cartographe ou une relecture dédiée), hors du mandat de cet epic de rendu.

  Aucun choix déjà confirmé du design touché (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne). Aucune identité narrative introduite (pas de nom de PNJ, conformément à la note de tête de la phase 3).
- 2026-09-18 — Déclenchement automatisé (routine cloud horaire). Anti-hallucination faite avant tout le reste : dernier epic « fait » du backlog était C3.2, commit `de31f99b1e0a6c6b0834040b46c679f29808ca19` — `git show --stat de31f99` confirme le commit réel (8 fichiers, correspond exactement à l'entrée existante de ce fichier et de `campagne.md`) ; `npm ci && npm test` relancés indépendamment → 349/349, identique au rapport existant. Aucun bandeau de pause en tête de ce fichier. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Les deux derniers epics consignés (C3.1, C3.2) ne sont pas tous `bloqué` : aucune pause anti-emballement à déclencher.

  **Choix de l'epic.** Deux epics `todo` de la phase 3 ont leurs dépendances satisfaites : **C3.3** (dépend de C2.6a/C3.1, tous deux `fait`) et **C3.5** (dépend de rien). Choisi **C3.3**, en continuité de la chaîne d'écriture C3.1→C3.2→C3.3 déjà engagée par les deux déclenchements précédents, et parce qu'elle est le prérequis direct de C3.4 (deuxième Rainelle), lui-même sur le chemin le plus contraignant vers C3.7 (porte de sortie). Epic de schéma/moteur pur (aucun rendu, aucune dépendance narrative) : implémenté directement par l'orchestrateur.

  **Livré** : `public/game/campaign-stations.js` gagne une quatrième collection, `habitats` (préfixe d'id `h`, disjoint des trois autres), plus trois fonctions pures — `habitatCapacityTotal` (somme des capacités), `freeLivingPlaces` (capacité totale moins la population vivante actuelle, un caller doit comparer à `> 0`, jamais à la troncité) et `removeHabitat` (refus explicite si le retrait romprait la capacité totale sous la population actuelle, sinon renvoie un nouveau registre sans cet habitat). Écart documenté : design §6 dit « un habitat fournit plusieurs places de vie » sans jamais donner de nombre — aucune capacité par défaut inventée (contrairement à `DEFAULT_PANIER_CAPACITY`, qui avait un vrai précédent à emprunter, `D.recipes.collector.capacity`) ; la capacité est un argument obligatoire à `registerStation`, seulement bornée en dessous par ce que « plusieurs » signifie littéralement (`MIN_HABITAT_CAPACITY = 2`, une capacité de 0 ou 1 est refusée). « Habitat occupé » n'a aucun suivi par individu (aucune commande n'affecte une Rainelle à un habitat précis, la population de C2.3+ n'est qu'un tableau plat) : `removeHabitat` lit donc « occupé » comme « le retirer ferait passer la capacité totale sous la population actuelle », la seule lecture que les données actuelles permettent — documenté explicitement dans le code, pas deviné en silence. Exactement comme demandé par le critère de sortie littéral, aucune commande de création/retrait réelle n'est câblée dessus (C3.4 dépend de cet epic pour le faire) : la gate « aucune Rainelle ne peut être créée sans place libre » est prouvée en appelant `freeLivingPlaces` directement dans un test, pas via un vrai chemin joueur.

  `public/garden-state-lifecycle.js` (`fresh()`) et `public/garden-state-validate.js` étendus en conséquence : une sauvegarde neuve démarre avec `habitats: []`/`habitatNextId: 1` ; une sauvegarde qui a `campaignStations` mais pas encore `habitats` du tout (toute sauvegarde d'avant cet epic) migre silencieusement vers ce même registre vide, sans lever d'erreur — bug découvert et corrigé en cours de route par le test de migration lui-même : la boucle de validation générique existante (`Object.keys(Stations.KINDS)`) exigeait `Array.isArray(list)` pour chaque collection sans exception, ce qui aurait rejeté toute sauvegarde pré-C3.3 dès l'ajout de `habitat` à `KINDS` ; corrigé en rendant cette collection explicitement optionnelle dans la boucle de validation (seulement quand la collection ET son compteur sont tous deux absents), sur le même principe que `buffer`/`capacity`/`min` déjà optionnels par élément pour un panier pré-C2.8. `tests/campaign-stations.cjs` (fixtures existantes) étendu aux quatre collections, nouveau `tests/campaign-habitats.cjs` (14 tests : création, refus sous le plancher, résolution, somme de capacité, places libres, gate de création vérifiée directement, refus/succès de retrait, migration, validation, aller-retour JSON), et les deux fichiers ajoutés à la liste explicite du script `test` de `package.json` (leçon déjà tirée à C2.11).

  **Résultat réellement exécuté** : `npm ci` puis `npm test` → **362/362** (349 existants + 13 nouveaux dans `campaign-habitats.cjs`, zéro régression — une première exécution avait révélé le bug de migration ci-dessus par un échec réel, corrigé avant ce commit, jamais contourné). `/code-review` (skill `code-review`, niveau medium) exécuté sur le diff complet : aucun défaut relevé (un changement de signature de `registerStation` de `{x,z}` à `{x,z,capacity}` vérifié sans effet sur les appelants existants, `capacity` étant `undefined` et inutilisé pour les trois kinds pré-existants). Vérification navigateur ciblée (page servie localement via `python3 -m http.server 4174 --directory public`, Playwright avec l'exécutable Chromium préinstallé) : page chargée sans erreur console ni erreur de page, `window.GardenCampaignStations.freeLivingPlaces` bien défini — aucune nouvelle balise `<script>` introduite par cet epic (aucun rendu/interface touché), donc `test:browser`/`test:visual` complets non requis, même exemption que les epics de schéma/moteur précédents (C2.6a/b/c, C2.7, C3.1).

  Aucun choix déjà confirmé du design touché (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne). Aucune identité narrative introduite. Aucune capacité inventée hors de ce que le design §6 énonce littéralement (« plusieurs », traduit en un plancher de 2, jamais un nombre précis choisi arbitrairement).
- 2026-09-18 — Déclenchement automatisé (routine cloud horaire). Anti-hallucination faite avant tout le reste : dernier epic « fait » du backlog était C3.3, commit `8952e740139c57ad68193c656bc768079bb15cc3` — `git show --stat 8952e74` confirme le commit réel (correspond exactement à l'entrée existante de ce fichier et de `campagne.md`) ; `npm ci && npm test` relancés indépendamment → 362/362, identique au rapport existant. Aucun bandeau de pause en tête de ce fichier. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Les deux derniers epics consignés (C3.2, C3.3) ne sont pas tous `bloqué` : aucune pause anti-emballement à déclencher.

  **Vérification supplémentaire faite ce déclenchement, au-delà de la seule étape 3 littérale** : `git ls-remote origin main` a d'abord semblé indiquer que la fusion de la phase 2 (commit `e24f122`, journal du 18 septembre précédent) n'avait jamais atteint `origin/main` — le ref local `origin/main` mis en cache par ce conteneur pointait encore sur l'ancien `95a79aa`. Un `git fetch origin main` explicite (jamais fait par les déclenchements précédents, qui n'avaient fetché que `maison-des-possibles`) a corrigé ce ref local et confirmé que `e24f122` est bien la tête réelle de `origin/main`, avec `bc2506b` (porte de sortie de la phase 2) dans son ascendance (`git merge-base --is-ancestor bc2506b origin/main` → vrai). Fausse alerte, pas une anomalie réelle du dépôt distant — mais un rappel concret de la consigne « ne fais confiance à aucun rapport non vérifié » : la vérification initiale (ref local non rafraîchi) aurait elle-même produit un faux positif si elle n'avait pas été creusée jusqu'à la source distante réelle. Aucune correction nécessaire, aucun fichier modifié pour ce point.

  **Choix de l'epic.** Deux epics `todo` de la phase 3 ont leurs dépendances satisfaites : **C3.4** (dépend de C3.3 `fait`, C1.6 `fait`) et **C3.5** (dépend de rien). Choisi **C3.4**, en continuité de la chaîne d'écriture C3.1→C3.2→C3.3→C3.4 déjà engagée, et parce qu'il est sur le chemin le plus contraignant vers C3.7 (porte de sortie) via C3.4 lui-même, alors que C3.5 ne débloque que C3.6. Epic de schéma/moteur pur (aucun rendu, aucune dépendance narrative) : implémenté directement par l'orchestrateur.

  **Livré** : `public/game/rainelles.js` gagne `bourgeon` (champ `null`/`true`, sans état intermédiaire — design §5 : « après un petit événement de familiarisation générique, une Rainelle forme un bourgeon ») et deux fonctions pures, `formBud` (refuse un second bourgeon tant que le premier n'est pas prélevé) et `harvestBud` (refuse une Rainelle sans bourgeon, sinon le vide et renvoie son `cultivarId`). Nouveau `public/garden-state-cmd-m.js` : `formBud`/`harvestBud` comme commandes, la seconde vérifiant `Stations.freeLivingPlaces` **avant** d'appeler `Rainelles.harvestBud` — jamais un bourgeon à moitié consommé sur un refus. Point important trouvé en écrivant les tests, pas supposé : la réservation compare `freeLivingPlaces` à `s.rainelles.length + s.campaignNursery.length`, pas seulement à la population vivante — sinon deux prélèvements pris le même jour, avant toute nuit, pourraient chacun voir une place libre alors qu'une seule existe réellement, promettant deux naissances pour un seul habitat. `public/garden-state-cmd-f.js`'s `sleep` résout chaque entrée de `s.campaignNursery` en un nouvel individu (`Rainelles.createRainelle`, `geste: null`, `name: ""`, cultivar hérité du parent), puis vide la file — même patron que `campaignPot.pending`, donc un rechargement en cours de maturation ne peut jamais réveiller un bourgeon en avance ni deux fois (la résolution n'a lieu que dans la commande `sleep` elle-même, jamais au chargement). `public/garden-state-lifecycle.js` (`fresh()` : `campaignNursery: []`, `campaignNurseryNextId: 1`) et `public/garden-state-validate.js` étendus en conséquence (validation stricte + migration `??=`, `bourgeon` migré par rainelle sur le même modèle que `job` à C2.6c). `public/garden-state.js` : segment `"m"` ajouté à la liste des groupes de commandes chargés côté Node. `public/index.html` : nouvelle balise `<script>` pour `garden-state-cmd-m.js`, juste après `garden-state-cmd-l.js`. Nouveau `tests/campaign-nursery.cjs` (15 tests) + `package.json` (liste explicite du script `test`, leçon déjà tirée à C2.11).

  **Écarts documentés, aucun deviné en silence** : `cultivarId` du nouvel individu hérité de celui du parent (« un bourgeon de sa lignée », design §5) — aucune autre lecture n'était possible sans inventer une donnée absente, le design ne dit rien de plus précis. Aucune mise en scène narrative pour `formBud` (même posture que `triggerFrogEncounter` à C2.3 : aucun contenu de chapitre 6 hors ordre). Aucun délai de repousse après un prélèvement — le design ne nomme aucun rythme au-delà de l'attente de la nurserie elle-même, et l'ajouter aurait été une mécanique non demandée par ce seul epic.

  **Résultat réellement exécuté** : `npm ci` puis `npm test` → **377/377** (362 existants + 15 nouveaux dans `campaign-nursery.cjs`, zéro régression). `/code-review` (skill `code-review`, niveau medium) exécuté sur le diff complet : aucun défaut relevé (vérifié explicitement : l'ordre de chargement du nouveau segment de script, l'arithmétique de réservation de place, l'ordre validation/migration, et que `validate()` garantit toujours `campaignNursery` défini avant qu'une commande ne s'exécute). Vérification navigateur ciblée (page servie localement via `python3 -m http.server 4175 --directory public`, Playwright avec l'exécutable Chromium préinstallé) : aucune erreur console ni erreur de page, `window.GardenRainelles.formBud`/`harvestBud` et `window.GardenCampaignStations` bien définis — aucun rendu/géométrie/matériau touché par cet epic (épic de schéma/moteur pur), donc `test:browser`/`test:visual` complets non requis, même exemption que C2.6a/b/c, C2.7, C2.8, C3.1, C3.3.

  Aucun choix déjà confirmé du design touché (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne). Aucune identité narrative introduite.
- 2026-09-18 — Déclenchement automatisé (routine cloud horaire). Anti-hallucination faite avant tout le reste : dernier epic « fait » du backlog était C3.4, commit `4158f58321c0b3ee5a1ec19a1397b2d5ad6637a0` — `git show --stat 4158f58` confirme le commit réel (12 fichiers, correspond exactement à l'entrée existante de ce fichier et de `docs/campagne.md`) ; `npm ci && npm test` relancés indépendamment → 377/377, identique au rapport existant. Aucun bandeau de pause en tête de ce fichier. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Les trois derniers epics consignés (C3.2/C3.3/C3.4) ne sont pas tous `bloqué` : aucune pause anti-emballement à déclencher.

  **Choix de l'epic.** Seul epic `todo` de la phase 3 à dépendances satisfaites en dehors de la chaîne C3.1→C3.4 déjà achevée : **C3.5** (dépend de rien). C3.6/C3.7 en dépendent directement ou par transitivité. C2.2v/C2.5v/C2.8v/C2.9 (phases 1/2) restent sautés pour la raison inchangée depuis plusieurs déclenchements, reconfirmée : le rendu de la maison refuge (C3.2) n'est câblé dans aucune scène jouable réelle (`grep GardenCampaignHouse public/garden.js public/garden-frame.js` : aucune occurrence hors du module lui-même et des fichiers d'état), donc toujours aucune position de caméra/joueur de campagne à laquelle raccrocher une transition de nuit ou un écran d'enseignement.

  **Vérification préalable faite avant d'écrire une seule ligne de donnée** : contrairement à l'attente du mandat du backlog (« aucune nouvelle branche de code attendue », rendu entièrement générique), la lecture du code réel a montré plusieurs points de données/logique itérant `D.zones` avec une hypothèse implicite de nombre de zones fixe : deux littéraux de couleur de sol indexés par `z.id` à 4 entrées (`render-world.js`/`render-flow.js`), `resourcePositions[z.id]` dans `data.js` (obligatoire, pas optionnel), le domaine valide de `s.unlocked` dans `garden-state-validate.js` (`[0,1,2,3]`), et surtout deux tests génériques existants (`tests/garden-construction.cjs`) qui exigent qu'**aucun** visiteur/qu'**aucune** porte ne soit injoignable sur une sauvegarde neuve (`unlocked: [0]`) — une cinquième zone verrouillée par défaut et séparée de la zone 0 par une zone intermédiaire elle-même verrouillée les aurait fait échouer, pas par un bug mais par une incompatibilité de conception. D'où la décision, documentée dans le backlog et `campagne.md` : zone 4 toujours déverrouillée (comme la zone 0), bordant exactement le bord nord réel de la zone 0 (mêmes points de polygone, ni écart ni chevauchement).

  **Implémenté directement par l'orchestrateur** (épic de données pures avec un peu de rendu générique déjà existant réutilisé, aucune délégation). Trois tests trouvés codant en dur `[0,1,2,3]` comme « toutes les zones » (`garden-rules-6.cjs`, `garden-visual.cjs`, `garden-size.cjs`) — un vrai bug latent révélé par l'ajout d'une cinquième zone, corrigés vers `D.zones.map((z) => z.id)` plutôt qu'un nouveau littéral qui aurait reproduit le même piège pour une sixième zone future. Un point de coordonnée d'un test existant (`(-6,23)`, utilisé comme exemple « hors de la carte entière ») se trouvait désormais à l'intérieur de la zone 4 — mis à jour vers `(100,100)`. Nouveau `tests/garden-village-zone.cjs` (5 tests, dont un a révélé sa propre erreur d'interpolation à la main avant d'être corrigé pour calculer la frontière depuis les points réels du polygone plutôt que de la recopier).

  **Vérification du rendu, le vrai gate** : `tests/garden-material-audit.cjs` exécuté directement contre la page servie localement → `PASS {"materialsInspected":98,"transparentAllowed":1,"timesSampled":5}` (97 avant, +1 seulement : toutes les couleurs des deux nouvelles maisons réutilisent des couleurs déjà en jeu, seule la teinte de sol de la zone est neuve). Capture Playwright (caméra suivant `window.GardenApp.view.position`, déplacée sur zone 4) lue avec Read et comparée à `direction-artistique.md` : silhouettes cohérentes avec les maisons existantes, teinte de sol distincte mais dans la même famille verte, aucune couleur ni matériau hors guide.

  **Résultat réellement exécuté** : `npm ci` puis `npm test` → **382/382** (377 existants + 5 nouveaux, zéro régression). `/code-review` (niveau medium) : trois observations, toutes corrigées avant ce commit (capacité de pots de départ doublée par le nouveau `unlocked: [0,4]` — effet de bord assumé et documenté, pas corrigé par une exception ad hoc, la formule traitant déjà la zone 0 gratuite comme n'importe quelle zone débloquée avant cet epic ; duplication des deux littéraux de couleur, remplacée par une constante partagée `zoneGroundColors` dans `data-world.js` ; les deux autres tests `[0,1,2,3]` trouvés et corrigés). Détail complet dans `docs/campagne.md`.

  Aucun choix déjà confirmé du design touché (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne). Aucune identité narrative introduite (conforme à la note de tête de la phase 3).
- 2026-09-18 — Déclenchement automatisé (routine cloud horaire). Anti-hallucination faite avant tout le reste : dernier epic « fait » du backlog était C3.5, commit `074d9cf9fcb05c60950b7daae19561a53d2dcb4a` — `git show --stat 074d9cf` confirme le commit réel (15 fichiers, correspond exactement à l'entrée existante de ce fichier et de `docs/campagne.md`) ; `npm ci && npm test` relancés indépendamment → 382/382, identique au rapport existant. Aucun bandeau de pause en tête de ce fichier. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Les trois derniers epics consignés (C3.3/C3.4/C3.5) ne sont pas tous `bloqué` : aucune pause anti-emballement à déclencher.

  **Choix de l'epic.** Seul epic `todo` de la phase 3 à dépendances satisfaites : **C3.6** (dépend de C3.5, `fait`). C2.2v/C2.5v/C2.8v/C2.9 (phases 1/2) restent sautés pour la raison inchangée depuis plusieurs déclenchements (rendu de la maison refuge toujours non câblé dans aucune scène jouable réelle). Implémenté directement par l'orchestrateur (épic de schéma/moteur pur). Voir sa propre entrée ci-dessus et le détail complet dans `docs/campagne.md`. Une régression trouvée et corrigée en cours de route (pas contournée) : `tests/garden-quests-schema.cjs` affirmait littéralement « deux entrées de départ » dans `D.quests`, devenu faux par construction avec l'ajout de `bois-pour-l-hiver` — élargi à trois entrées plutôt que supprimé ou affaibli. `npm test` → 390/390 (382 existants + 8 nouveaux, zéro régression après cette correction). `/code-review` (medium) : aucun défaut relevé. Aucun rendu touché, `test:browser`/`test:visual` complets non requis (même exemption que les epics de schéma/moteur précédents) ; vérification navigateur ciblée effectuée tout de même (aucune nouvelle balise `<script>`, page chargée sans erreur console, `D.quests["bois-pour-l-hiver"].reward.tools[0] === "hachette"` confirmé dans la page).

  **Cet epic ne ferme aucune porte de phase.** C3.7 (porte de sortie de la phase 3) dépend de C3.2/C3.4/C3.6, tous `fait` désormais — seul candidat du prochain déclenchement. Aucune fusion vers `main` à ce stade.
- 2026-09-18 — Déclenchement automatisé (routine cloud horaire). Anti-hallucination faite avant tout le reste : dernier epic « fait » du backlog était C3.6, commit `904b6853123958bc5c178015b0a7d45ca085edba` — `git show --stat 904b685` confirme le commit réel, son contenu (message et description) correspond exactement à l'entrée existante de ce fichier et de `docs/campagne.md` ; `npm ci && npm test` relancés indépendamment → 390/390, identique au rapport existant. Aucun bandeau de pause en tête de ce fichier. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Les trois derniers epics consignés (C3.4/C3.5/C3.6) ne sont pas tous `bloqué` : aucune pause anti-emballement à déclencher.

  **Choix de l'epic.** Seul epic `todo` à dépendances satisfaites : **C3.7** (dépend de C3.2/C3.4/C3.6, tous `fait`), la porte de sortie de la phase 3 elle-même, déjà nommée comme seul candidat par l'entrée précédente de ce journal. C2.2v/C2.5v/C2.8v/C2.9 (phases 1/2) restent sautés pour la raison inchangée depuis plusieurs déclenchements (aucune position/déplacement réel des Rainelles dans le monde de campagne, chantier toujours non entamé). Implémenté directement par l'orchestrateur (épic de test d'intégration pur, aucune délégation).

  **Livré** : nouveau `tests/campaign-phase3-gate.cjs` (fichier probable du backlog, aucun écart), plus `package.json` (liste explicite du script `test`, leçon déjà tirée à C2.11). Un seul test narratif, à travers le vrai vecteur de commandes (`GardenState.command()`, jamais un accès direct à l'état), rejouant les quatre promesses de la porte de sortie (design §15) sur trois nuits : **habiter** (réparation de la pièce d'accueil, C3.1, avant la première nuit) ; **croiser** (un cultivar résolu au pot, C1.3, rejoué dans ce contexte plutôt que réimporté d'un test existant) ; **rencontrer** (la quête `bois-pour-l-hiver`, C3.6, acceptée puis complétée réellement — jamais à l'acceptation) ; **organiser** (la première Rainelle par rencontre scénarisée, C2.3, et la seconde par bourgeon/nurserie, C3.4, tiennent une chaîne courte arroser→récolter sans aucune intervention après leur enseignement, réutilisant exactement les mêmes primitives que la porte de sortie de la phase 2 plutôt que d'en réinventer une). Une sauvegarde JSON aller-retour à mi-parcours (après la deuxième nuit, avant la troisième) est vérifiée strictement identique par un `assert.deepEqual` sur l'état complet, puis la troisième nuit est rejouée sur l'état rechargé lui-même — pas sur l'original — pour que le round-trip soit une vraie étape du scénario. Chaque nuit vérifie explicitement l'absence de double crédit (design §16) : aucun second cultivar sur un pot vide, aucune troisième Rainelle sans nouveau bourgeon, l'outil de quête et la complétion de la quête ne se répètent jamais.

  **Limite honnête, documentée dans le fichier lui-même plutôt que devinée en silence** : « dormir dans la maison refuge » reste, comme à chaque epic de cette phase depuis C2.2v, la seule commande générique `sleep` du jeu — aucune scène de maison, aucune transition de caméra, aucun écran de confirmation du pot n'existe encore pour l'habiller (C2.2v/C2.5v, toujours `todo`, chantier non entamé par cet epic puisqu'il ne touche aucun rendu). Le test ne prouve donc que la couche moteur réellement câblée aujourd'hui, jamais une interface simulée.

  **Résultat réellement exécuté** : `npm ci` puis `npm test` → **391/391** (390 existants + 1 nouveau dans `campaign-phase3-gate.cjs`, zéro régression). Sortie complète :

  ```
  # tests 391
  # suites 0
  # pass 391
  # fail 0
  # cancelled 0
  # skipped 0
  # todo 0
  ```

  `/code-review` (skill `code-review`, niveau medium) exécuté sur le diff complet : aucun défaut relevé — diff entièrement composé du nouveau fichier de test plus le câblage `package.json`, aucun code applicatif modifié ; les hypothèses du test contre les implémentations réelles (`formBud`/`harvestBud`, gate d'habitat, schéma de la quête `bois-pour-l-hiver`) vérifiées explicitement par le relecteur, cohérentes.

  Aucun rendu/géométrie/matériau touché (epic entièrement fait d'un test Node) : `test:browser`/`test:visual` non requis, même exemption que C2.6a/b/c/C2.7/C2.11/C3.6. Aucune relecture narrative adverse requise (pas un epic de Scénariste, aucune identité narrative introduite). Aucun choix déjà confirmé du design touché (Alma vivante, nom des Rainelles, culpabilisation de fin de campagne).

  **Cet epic ferme la porte de sortie de la phase 3** (design §15 : « habiter, croiser, organiser, rencontrer fonctionnent ensemble en une courte partie sauvegardable » — le test ci-dessus le prouve littéralement, à travers le vrai vecteur de commandes, sur une partie sauvegardable dont l'aller-retour JSON à mi-parcours est vérifié strictement identique). Scénarios de validation de la section 16 du design pertinents à ce périmètre déjà couverts, ici et par la suite existante : « aucune commande, récolte, nuit ou naissance ne crédite deux fois » (vérifié explicitement à chaque nuit ci-dessus) ; « multiplier un cultivar conserve exactement ses traits » et « chaque Rainelle conserve un seul geste... refus de multiplication » (phase1-gate/phase2-gate, non rejoués ici pour ne pas dupliquer) ; « les sauvegardes existantes sont préservées » (round-trip strict ci-dessus). Les deux volets « passage coupé » et « déplacer un poste... rangement » restent honnêtement hors de portée, le critère de sortie littéral de la phase ne les nommant pas — même précédent que C1.5/phase 1 et « passage coupé »/phase 2.

  **Fusion vers `main`, conformément à `execution-continue.md`** (« fusionner une phase validée dans main... est une étape d'implémentation comme une autre, pas un acte de publication » — aucun pipeline connecté à `main`, revérifié : toujours aucun workflow CI dans ce dépôt) : `maison-des-possibles` fusionnée localement dans `main` après ce commit, rapport de clôture de phase daté ajouté dans `docs/campagne.md`, `main` et `maison-des-possibles` tous deux poussés vers `github.com/EdwinDayot/edwindayot.fr` (le seul remote autorisé). Aucun déploiement, aucun pipeline de publication touché. **Note honnête laissée pour le prochain déclenchement** : C2.2v/C2.5v/C2.8v/C2.9 restent `todo` malgré la fusion — leur critère de sortie propre ne fait pas partie du critère de sortie de la phase 3 cité par le design, même précédent que C1.5 à la clôture de la phase 1 et C2.2v/C2.5v/C2.8v/C2.9 déjà à la clôture de la phase 2. La phase 4 (« Première version jouable, actes I à III ») n'a encore aucun epic détaillé : un futur passage du Cartographe devra endosser ce rôle avant toute implémentation, exactement comme à l'ouverture de la phase 3.
- 2026-09-18 — Déclenchement automatisé (routine cloud horaire). Anti-hallucination faite avant tout le reste : dernier epic « fait » du backlog était C3.7, commit `b2341e13ad27a8af76a096c36f16dc739a7cc3f1` — `git show --stat b2341e1` confirme le commit réel, message et contenu correspondant exactement à l'entrée existante de ce fichier et de `docs/campagne.md` ; `npm ci && npm test` relancés indépendamment → **391/391**, identique au rapport existant. Aucun bandeau de pause en tête de ce fichier. Aucune anomalie, `docs/campagne-anomalies.md` toujours absent. Les trois derniers epics consignés (C3.5/C3.6/C3.7) ne sont pas tous `bloqué` : aucune pause anti-emballement à déclencher. Vérification supplémentaire de l'état distant (leçon du déclenchement du 18 septembre précédent sur C3.3, ref local `origin/main` non rafraîchi) : `git fetch origin main maison-des-possibles` explicite, puis `git ls-remote origin main maison-des-possibles` et `git merge-base --is-ancestor b2341e1 origin/main` → vrai — la fusion de la phase 3 vers `origin/main` est bien réelle, pas seulement locale.

  **Rôle Cartographe endossé** (pas d'implémentation ce déclenchement), conformément à l'étape 4 de la routine : la phase 3 est close et fusionnée dans `main` depuis le déclenchement précédent ; la phase 4 (« Première version jouable, actes I à III ») n'avait encore aucun epic détaillé (seulement le paragraphe de contour du plan de réalisation et la mention, déjà fixée d'avance, que l'epic **C4.3** porterait le chapitre 3/l'épinglage) — exactement la condition citée par `orchestration.md`/`execution-continue.md` pour endosser ce rôle plutôt que de forcer un epic non détaillé.

  Recherche factuelle faite avant d'écrire un seul epic (agent Explore dédié, pas une supposition) : aucun système de personnalisation du personnage (prénom/pronoms/apparence/vêtements, design §2) n'existe dans le code — `s.player` n'est qu'une position caméra `{x,y,z}` (`public/garden.js:86`), aucune occurrence de personnalisation/avatar/pronom au-delà de classes CSS sans rapport ; aucun système de dialogue/conversation PNJ au-delà d'une ligne de statut (`roleData.line`, rôle `resident`, `data-roles.js:63-74`) et du panneau de quête mécanique (accepter/compléter, `hud-panel.js:52-83`, aucun texte narratif) ; le carnet botanique (`hud-panel.js:146-215`, panneau `"notebook"`) existe déjà depuis C1.4 et peut recevoir de nouvelles entrées par une simple branche `buildPanelRows` ; l'épinglage (C1.5) est à 0 % construit (`botany-genetics.js`/`botany-pot.js` ne portent aucun concept de pin/épingle) ; `createSpecimen` (C1.6, `cultivars.js:47`) accepte déjà une position `{x,z}` libre, donc « installer un spécimen chez soi » ne demande aucun nouveau champ de schéma ; le patron `reward.tools` (C3.6, `garden-state-cmd-e.js:38-40`) s'étend trivialement à un nouveau champ de récompense analogue (`reward.unlockHouseSpace`) ; le schéma d'un PNJ (`data-buildings.js`) et le rôle `quest-giver` (`data-roles.js:78-88`) sont déjà génériques et réutilisables tels quels pour Noé/Iris, sans aucune modification de moteur.

  **Trois epics détaillés (C4.1 à C4.3)**, voir leurs propres entrées ci-dessus, couvrant l'Acte I en entier (chapitres 1 à 3 du design §10) — **délibérément pas les neuf chapitres de la phase d'un bloc** : l'Acte II (chapitres 4-6, identité des PNJ pas encore choisie au-delà de la rencontre déjà mécaniquement acquise en phase 2/3) et l'Acte III (chapitres 7-9, gestes Replanter/Préparer/Trier jamais implémentés) dépendent de décisions que ce seul lot doit encore éclairer, même principe que le lot initial de la phase 3. C4.1 (chapitre 1 : outil de fortune déjà acquis, choix des marques de meuble, première lettre d'Alma) introduit un **mécanisme narratif générique minimal** (catalogue `data-narrative.js` + `s.campaignFlags` + une entrée de carnet) plutôt qu'un texte codé en dur pour cette seule scène — généraliser plutôt que spécialiser, réutilisé immédiatement par C4.3 pour la note de la serre et le flag posé par Iris. C4.2 (chapitre 2 : Noé, quête de la fenêtre, sécateur qui déverrouille l'espace `"serre"` déjà déclaré verrouillé par C3.1) introduit `reward.unlockHouseSpace`, extension additive du même patron exact que `reward.tools`. C4.3 (chapitre 3 : première hybridation rejouée dans son contexte narratif, spécimen installé chez soi, Iris) **pose la porte narrative dont dépend C1.5** (épinglage) sans l'implémenter elle-même — C1.5 redevient un candidat légitime une fois C4.3 `fait`. Dépendances enchaînées dans l'ordre d'écriture (C4.1→C4.2→C4.3), aucune ne saute une dépendance non satisfaite.

  **Écart assumé, documenté en tête de la section phase 4** : la personnalisation du personnage (design §2) reste hors de ce lot — système orthogonal que la porte de sortie littérale de la phase ne nomme pas ; C4.3 utilise un nom générique par défaut pour la première page de carnet en attendant un futur epic dédié, plutôt que d'inventer ce système ici ou de deviner un nom.

  Aucun choix déjà confirmé du design touché (Alma vivante — au contraire, réutilisée telle quelle comme seule identité narrative de ce lot ; nom des Rainelles ; culpabilisation de fin de campagne). Aucun code ni test modifié ce déclenchement — travail de planification seul dans `docs/campagne-backlog.md`, comme prescrit par `orchestration.md`/`execution-continue.md` pour ce cas. Détail complet également consigné dans `docs/campagne.md`.
