# Le jardin des jours tranquilles

Accueil jouable en Three.js, entièrement statique, sans compte ni serveur de jeu. Le portfolio factuel est conservé sur `/portfolio/`. Les anciennes ancres (`/#projets`, `/#plant-calendar`, `/#parcours`, etc.) redirigent vers cette page. Sans JavaScript ou WebGL, l’identité d’Edwin et un lien vers le portfolio restent visibles. Le portfolio reste lisible sans JavaScript.

## Table des matières

- [Boucle de jeu](#boucle-de-jeu)
- [Commandes et interface](garden-structure.md#commandes-et-interface)
- [Visiteurs et progression](#visiteurs-et-progression)
- [Les maisons de rencontre](#les-maisons-de-rencontre)
- [Construction et réseaux](garden-structure.md#construction-et-réseaux)
- [Architecture](garden-structure.md#architecture)
- [Lumière et horloge](#lumière-et-horloge)
- [Sauvegardes et absence](#sauvegardes-et-absence)
- [Vérifications reproductibles](#vérifications-reproductibles)
- [Résultats locaux du 10 septembre 2026](#résultats-locaux-du-10-septembre-2026)
- [Vérification locale du 11 septembre 2026 — jardin vivant](#vérification-locale-du-11-septembre-2026-—-jardin-vivant)
- [Vérification locale du 14 septembre 2026 — maisons et fiches détaillées](#vérification-locale-du-14-septembre-2026-—-maisons-et-fiches-détaillées)

## Boucle de jeu

Explorer, couper du bois, piocher la pierre, creuser l’argile et découvrir des graines, planter, récolter sans arracher, échanger, aménager puis automatiser. Aucun soin ne crée de monnaie. Les espèces ne meurent pas et les productions attendent sans expiration (trois objets par plante).

Départ : un Pilea adulte avec une bouture prête, deux pots vides, deux graines de chacune des trois premières espèces, un arrosoir plein, une hache, une pioche, une pelle et les matériaux d’un pot. L’inventaire empile les objets sans limite de cases.

Les quatre zones ouvrent progressivement douze espèces. Chaque zone apporte douze places de pots, jusqu’à 48 pots et 192 autres éléments. Les objets rangés comptent dans ces limites et conservent leur contenu. Une plante ou un travail de multiplication rangé est en pause.

Les rythmes et préférences sont des simplifications ludiques. La première germination arrive après environ 26 secondes d’humidité favorable. Le Pilea devient adulte en environ trois minutes ; les espèces suivantes demandent environ quatre à douze minutes. Soleil, ombre et humidité influencent la vitesse, sans dégâts irréversibles.

## Visiteurs et progression

Léa, Noé et Iris habitent chacun leur maison dans le rang central de la pépinière ; rencontrer un visiteur suppose d’y entrer par sa porte (voir [Les maisons de rencontre](#les-maisons-de-rencontre)).

Léa propose trois demandes sans expiration portant sur des productions découvertes. Remplacement gratuit. Un échange donne 18 feuilles, une graine de la même espèce et un point de réputation. Le premier ajoute les plans de citerne, tuyau et goutteur ainsi que des matériaux et une lanterne.

Chaque visiteur de rôle `vendor` (Noé et le quartier des artisans) tient boutique : `roleData.wares` liste ses articles (`{item, cost}`), achetés contre des feuilles directement à la réserve, sans matériaux. Noé vend un pot (8 feuilles) ; dans le quartier des artisans, Mira vend un pot à réserve (14) et un banc (10), Basile une lanterne (6), Anouk un pas japonais (4), Ines un pot (8). Un article non abordable reste affiché mais désactivé. Les équipements restent aussi fabricables depuis le sac (I) avec leurs matériaux habituels — acheter est un raccourci contre feuilles, pas une exclusivité. Iris accompagne le carnet et récompense les collections de 3, 6, 9 et 12 espèces. Les caches d’exploration se découvrent une fois ; les espèces obtenues deviennent ensuite reproductibles.

Les ressources sont réparties sur 85 sites permanents : 53 arbres, 16 veines de pierre et 16 bancs d’argile, avec chaque type présent dans les quatre zones. Tous les arbres, y compris ceux du sous-bois, peuvent être coupés.

| Ressource | Outil  | Gestes | Renouvellement |
| --------- | ------ | ------ | -------------- |
| Bois      | Hache  | 3      | 120 secondes   |
| Pierre    | Pioche | 4      | 100 secondes   |
| Argile    | Pelle  | 3      | 90 secondes    |

Chaque site donne trois unités uniquement au dernier geste. Le travail partiel est sauvegardé. Les gestes sont espacés d’au moins 0,65 seconde ; une mauvaise cible, un outil incorrect ou un geste trop rapide ne donne rien. Souches, éclats et excavations restent visibles pendant le renouvellement. Les souches et les veines gardent leur empreinte au sol pour que la repousse ne puisse pas bloquer le personnage. Le renouvellement avance aussi pendant l’absence. L’eau est gratuite au ponton. Le paquet de secours est gratuit lorsqu’il ne reste aucune plante ni graine/jeune plant utilisable.

## Les maisons de rencontre

Trois petites maisons fermées forment un premier rang dans la zone centrale de la pépinière, une par visiteur (Léa, Noé, Iris). Deux rangs supplémentaires forment le quartier des artisans juste au nord : Mira et Ines, puis — dans le bulge organique nord de l’Épic 4.1 — Basile et Anouk, formant un vrai petit village à trois rues plutôt que des maisons isolées. Chacune occupe une emprise de 3,2 × 3,2 unités avec une porte de 2 unités sur la façade ; le visiteur se tient à l’intérieur, à 0,4 unité du seuil. Les murs sont en pierre (moellons visibles à deux tons, chaînages d’angle empilés, solin de base), plus le toit à deux pans dont la crête est bien le point le plus haut (corrigé — une inversion de signe faisait autrefois tomber la crête au niveau du mur et monter les avant-toits, un vrai « toit en creux »). La collision est un jeu de cercles muraux (le même principe que les troncs et les gisements) : les murs bloquent le passage partout sauf dans l’ouverture de la porte. Cette ouverture est volontairement plus large que la grille de navigation ne l’exigerait, pour absorber la dérive diagonale que l’angle de caméra ajoute à toute marche en ligne droite (joystick ou clavier). Marcher ou cliquer sur un visiteur, même de loin, fait passer le personnage jusqu’à sa porte (au besoin jusqu’à l’intérieur) avant de proposer « Rencontrer ». Un clic sur le toit ou les murs sélectionne aussi le visiteur qu’ils abritent. La liste des maisons (`D.buildings`) est purement des données : ajouter ou déplacer un visiteur n’a demandé aucune nouvelle branche de code (`game/data-roles.js`, `game/render-houses.js`).

La porte est un battant visuel qui s’ouvre quand le personnage approche du seuil ou se trouve à l’intérieur, et se referme sinon ; elle ne fait pas partie de la règle de collision, qui reste un vide permanent dans le mur. En mouvement réduit, elle bascule directement sans animation. Une étiquette flotte au-dessus de chaque toit ; une lampe éclaire l’intérieur la nuit, dans la limite existante de quatre lumières locales partagées avec les lanternes.

Aucun objet ne peut être posé dans l’emprise d’une maison, avec une marge de 0,4 unité ; le jeu répond « Garde la maison de [nom] libre. » pour n’importe lequel des sept visiteurs. Les arbres du paysage évitent aussi les maisons. Les anciennes sauvegardes dont un objet se trouvait exactement dans l’emprise d’une nouvelle maison le voient déplacé vers la case libre la plus proche lors du chargement, sans perdre sa plante (marqueur de paysage 5 pour les trois premières maisons, marqueur 6 pour le quartier des artisans).

## Lumière et horloge

Le cycle dure 1 200 secondes de simulation : `θ = 2π × temps / 1200 + π/4`, avec la direction normalisée `(cos θ, sin θ, 0.35 × cos θ)`. Une nouvelle partie commence le matin. Jour et nuit durent chacun dix minutes. La fraction de seconde, la pause, les sauvegardes et le rattrapage pilotent cette même horloge. Ce cycle est visuel : croissance et irrigation conservent leurs règles de parcelle.

Le soleil et la lune opposée modifient continuellement l’intensité, la teinte, l’ambiance, le ciel et le brouillard. Le soleil ne fournit aucune lumière sous l’horizon. Les cartes d’ombres suivent la vue avec une marge de neuf unités pour les objets hors champ et une projection calée sur les texels ; construction et inspection adaptent leur étendue. Le feuillage qui s’efface conserve une silhouette dédiée aux ombres. Les aperçus de construction ne projettent pas d’ombres et les propriétés de projection/réception sont séparées dans les lots instanciés.

À la tombée du jour, les lanternes deviennent lumineuses et leurs halos apparaissent. Quatre lumières locales au maximum éclairent la zone observée, dont les deux plus proches projettent des ombres sur ordinateur, une sur mobile. Le choix conserve une marge d’hystérésis. Leurs cartes font 512 px, puis 256 au niveau bas. La variation d’intensité partage les matériaux sans reconstruire les lots. Un indicateur soleil/lune accompagne le compteur, dont la monnaie, les coûts et récompenses sont dessinés avec une feuille et le montant ; les libellés accessibles conservent le mot « feuilles ».

## Sauvegardes et absence

La clé `edwin-garden-v3` contient inventaire, entités identifiées, plantes, parcelles, raccords, demandes, découvertes, réputation, réglages, position du personnage, barre rapide et date de simulation. La dernière copie valide est conservée sous `edwin-garden-v3-backup`.

La migration lit `edwin-garden-v2` et en conserve une copie brute sous `edwin-garden-v2-backup` avant écriture. Elle conserve les six pots, espèces, croissance, humidité, eau et monnaie ; les pots améliorés deviennent des pots à réserve et gardent leur niveau d’origine dans les données de migration. Les données brutes des anciens pots restent dans la sauvegarde.

Les parties v3 antérieures sont adaptées au terrain élargi et aux nouveaux sites via le marqueur de paysage 4 : délai de renouvellement et travail partiel conservés, nouveaux sites ajoutés sans crédit de matériaux, objets et plantes conservés, barre rapide initialisée si absente. Le marqueur de paysage 5 ajoute les trois maisons de rencontre : un objet ancien resté dans une emprise est repositionné sur la case valide la plus proche, sans perdre sa plante.

Sauvegarde après chaque commande réussie, toutes les cinq secondes et à la sortie. Export JSON, import après validation et confirmation, restauration de la copie précédente. Une erreur de stockage laisse le jeu utilisable en mémoire avec un message invitant à exporter.

Le rattrapage réutilise exactement les pas de simulation actifs, avec les stocks et capacités existants, dans une limite de huit heures. Une date future ne donne aucune avance. Le rattrapage est sauvegardé immédiatement pour ne pas être crédité à nouveau au rechargement. Un import restaure un instantané sans lui ajouter de rattrapage. La reprise présente les nouvelles productions et les installations en attente.

## Vérifications reproductibles

```sh
npm ci
npx playwright install chromium
npm test
python3 -m http.server 4174 --directory public
# Dans un autre terminal :
npm run test:browser
npm run test:visual
```

`GARDEN_URL` cible une autre instance locale. Sur macOS, les tests utilisent ANGLE Metal ; `GARDEN_ANGLE=swiftshader` permet le diagnostic en rendu logiciel. Les captures et rapports vont dans `/tmp/garden-*.png`, `/tmp/garden-browser-result.json` et `/tmp/garden-performance.json`.

`npm test` vérifie les transactions, les sauvegardes, la migration, les accès, la conservation lors des déplacements, les découvertes, les réseaux, la saturation, la multiplication, le rattrapage et les géométries botaniques. Le scénario de vingt minutes utilise une nouvelle sauvegarde et uniquement les commandes du jeu, sans crédit de ressources de test : échange à 3 min, pot à 4 min, sous-bois à 7 min, irrigation à 12 min. La fin de la session vérifie la production avec irrigation.

Le parcours navigateur joue une partie réelle par les commandes clavier, souris et tactiles : déplacement sans clic initial, affectation et échange des raccourcis, plantation, récolte, coupe/pioche/creusage avec action maintenue, vérification du gain unique et de l’état épuisé, échange direct, fabrication à la pose, poses successives, rotation, marche pendant la construction, citerne remplie et raccordée dans le monde, conservation des liens lors d’un déplacement, rangement/repose et coupe d’arbre tactile, rechargement, portfolio et anciennes ancres. L’extension et son financement sont vérifiés dans le parcours de simulation de vingt minutes. Les réglages Canvas, le son, l’export, le refus d’un import invalide, la confirmation d’un import valide et la disparition d’un arbre du paysage après coupe sont également vérifiés. Les replis sans JS et sans WebGL sont vérifiés. Les quatre articles de projets sont comparés à des empreintes SHA-256 prises avant leur déplacement.

Les tests de sens d’eau couvrent les raccords inversés, les branches sans demande, les cycles, plusieurs consommations, les citernes vides/pleines et la simulation hors ligne. `garden-flow.cjs` vérifie aussi dans WebGL le mouvement des gouttes, l’orientation des flèches, l’arrêt immédiat après déconnexion et le mode de mouvement réduit.

Le test `garden-size.cjs` vérifie 252 combinaisons (12 espèces, 3 stades de croissance, 7 distances/zooms) : le modèle et ses dimensions dans le monde restent identiques lors des déplacements et changements de vue, et la croissance continue de modifier sa taille.

Le test visuel inspecte les douze silhouettes à plusieurs tailles/stades puis mesure une scène de 48 pots et 192 décorations avec le HUD Canvas actif. Cette scène de charge est un jeu de données de test distinct de la partie économique.

## Résultats locaux du 10 septembre 2026

- 26 tests de règles/géométrie, contrôles HTML/liens et préservation des contenus : passent.
- Parcours Chromium ordinateur et tactile, y compris Nginx/CSP : passent sans erreur applicative de console.
- Image Docker locale et `nginx -t` : passent. Aucun déploiement OVH effectué.
- Charge maximale, Apple M5 Max via ANGLE Metal, résolution de rendu 1440 × 900, ombres désactivées : environ 60 images/s, 67 appels de dessin et 1 142 490 triangles dans la mesure. Huit heures simulées en environ 180 ms.
- Le diagnostic antérieur en SwiftShader atteignait environ 8 images/s : c’est du rendu logiciel, pas une mesure de téléphone ; cette mesure précède l’agrandissement du terrain.
- Les commandes mobiles sont vérifiées en émulation Chromium. L’objectif de 30 images/s sur un téléphone physique de milieu de gamme reste à mesurer sur cet appareil ; il n’est pas présenté comme acquis.

La bibliothèque Three.js existante émet son avertissement de dépréciation du bundle classique r150+ dans les tests Node. Le bundle local a été conservé pour éviter une migration de moteur hors du périmètre.

## Vérification locale du 11 septembre 2026 — jardin vivant

- 33 tests de règles et géométrie passent, avec refus atomiques, durée exacte, récupération unique, travaux rangés/prêts sauvegardés, équivalence active/hors ligne et invariance des cultures/eau lors du calcul solaire.
- Parcours Chromium ordinateur et tactile : interactions existantes, choix/confirmation/récupération à l’établi, inspection des douze espèces, rotation, immobilité, retour caméra, pause et mouvement réduit. Captures des citernes à 0, 16, 80 et 160 unités sous quatre angles, de l’aube, du midi, du couchant et de la nuit dans `/tmp/garden-*.png`.
- `garden-shadows.cjs` compare les pixels avec et sans ombre locale : les objets occultent bien les lanternes. Il vérifie aussi l’absence d’ombre des aperçus, la stabilité des lots lors d’un changement d’intensité et la descente jusqu’à 512/256 px sans désactiver les ombres.
- Charge maximale : 48 pots et 192 décorations, Canvas actif, Apple M5 Max via ANGLE Metal, rendu 1440 × 900. Sur deux fenêtres de 14 secondes : environ 60,1 images/s le jour et 59,0 la nuit, ombres célestes à 2 048 px, deux ombres locales la nuit, 107 appels de dessin et 1 165 416 triangles pour la vue principale. Rattrapage de huit heures : environ 272 ms. Rapport `/tmp/garden-performance.json`.
- Les vérifications mobiles utilisent l’émulation Chromium sur ce même ordinateur. Les performances d’un téléphone physique restent à mesurer.
- Livraison exclusivement locale sur `http://127.0.0.1:4175` : image précédente `edwindayot-garden:rollback-20260911-living`, code précédent `/tmp/garden-before-living-20260911.tar` et configuration Nginx correspondante dans `/tmp`. Portfolio et sauvegardes navigateur préservés ; aucun déploiement OVH.

## Vérification locale du 14 septembre 2026 — maisons et fiches détaillées

- 45 tests de règles et géométrie passent (7 nouveaux sur les maisons et 5 sur les jauges) : porte praticable avec une tolérance de dérive d’un demi-mètre de part et d’autre, mur bloqué plus loin, chemin central et accès portail-visiteur intacts, refus de pose dans l’emprise d’une maison avec le message nommé, arbres du paysage écartés des trois maisons, migration de paysage 5 qui déplace un objet piégé sans perdre sa plante et laisse les autres intacts.
- Parcours Chromium ordinateur : `use(p, "lea")` (clic sur la position projetée de Léa, sélection via le toit ou la marche, entrée par la porte, échange à l’arrivée) passe sans modification du scénario existant. La carte monde compacte (hauteur ≤ 100 px contre 104 fixes auparavant) et `V` ouvrant la fiche détail générique sur une ressource sont vérifiés séparément. Nouveau fixture `garden-houses.cjs` : portes fermées vues de loin, ouverture réelle à l’arrivée près du seuil, captures du rang des trois maisons et de l’intérieur dans `/tmp/garden-houses-*.png`.
- Mobile : le joystick fait entrer puis ressortir le personnage d’une maison par sa porte, sans régression du reste du parcours tactile.
- Charge maximale inchangée dans sa méthode (48 pots, 192 décorations, Apple M5 Max via ANGLE Metal, rendu 1440 × 900) : environ 60,1 images/s le jour et 59,9 la nuit, 117 appels de dessin (contre 107) et 1 165 686 triangles, ombres célestes à 2 048 px. La hausse des appels de dessin vient des nouveaux matériaux d’accent par maison ; le budget de 60 images/s reste tenu. Rattrapage de huit heures : environ 270 ms.
- Aucun déploiement OVH : vérifications exclusivement locales (`python3 -m http.server` sur `public/`).
- **Refactor de fondations (sans changement de comportement)** : le test point-dans-zone est désormais centralisé dans `game/geometry.js` (avant, dupliqué dans `construction.js` et deux fois dans `data.js`). L’interaction de chaque visiteur (Léa, Noé, Iris) est passée d’un câblage par id littéral à un dispatch par rôle (`trader`/`vendor`/`botanist`) dans le nouveau `game/data-roles.js` : un futur visiteur n’ajoutera qu’une ligne de données, aucune branche de code. Les comportements de l’établi et du collecteur (job minuté, réserve automatique) sont devenus deux primitives génériques dans `game/automation.js`, pilotées par les métadonnées de la recette (`job`/`buffer`) plutôt que par un `if` par type dans la boucle de simulation. 58 tests de règles/géométrie passent (5 nouveaux : `garden-geometry.cjs`, `garden-roles.cjs`, `garden-automation.cjs`), le parcours Chromium ordinateur/tactile complet ainsi que les échanges avec Léa, l’atelier de Noé et le don d’Iris sont vérifiés de bout en bout sans régression.
- **Réseau d’irrigation généralisé aux substances multiples** : le même réseau (citerne/tuyau/goutteur) transporte maintenant de l’eau (pompe) ou de l’engrais (nouveau composteur, débloqué avec la rocaille), sans dupliquer les types de conduites — la substance se déduit de la source qui alimente chaque composante connexe, jamais stockée sur l’entité. L’engrais dope la croissance (+50 %) d’un pot pendant 90 secondes de jeu sans toucher son humidité. Relier deux circuits de substances différentes est refusé à la commande et rejeté à la validation d’une sauvegarde. `game/irrigation-sim.js` a été scindé en deux (`irrigation-sim.js` pour le tick, nouveau `irrigation-status.js` pour les diagnostics) afin de rester sous 250 lignes. 64 tests de règles passent (6 nouveaux dans `garden-network-substance.cjs`, dont la non-régression complète des réseaux 100 % eau existants) ; nouveau fixture Chromium `garden-composter.cjs` qui construit un circuit composteur → citerne → tuyau → goutteur → pot en direct, vérifie l’absence d’erreur console, le dopage effectif de la plante et la fiche détail (`V`) de la citerne et du composteur ; capture dans `/tmp/garden-composter-*.png`. Aucune régression sur les 117 appels de dessin / ~60 images/s mesurés précédemment.
- **Plomberie de fondations supplémentaire (sans changement de comportement)** : un champ de sauvegarde `quests: {active: [], completed: []}` est réservé (patron « valeur par défaut » comme `hotbar`, aucune sauvegarde ancienne cassée) avec un catalogue `game/data-quests.js` encore vide et un rôle `quest-giver` enregistré mais non assigné, en attente du moteur d’objectifs. Le rendu obtient un point d’insertion pour un futur relief : `game/terrain.js` expose `terrainHeight(x, z)`, qui vaut 0 partout aujourd’hui, câblé dans le plan de clic-au-sol, le centre du cadrage d’ombre solaire, le personnage, chaque objet posé, l’aperçu de construction, les ressources/caches, les trois maisons et leurs étiquettes, les portails de zone, les troncs et la mousse — la collision et le pathfinding restent purement 2D. 70 tests de règles passent (2 nouveaux : `garden-quests-schema.cjs`, `garden-terrain-seam.cjs`) ; suite Chromium et test visuel/perf inchangés au pixel et à l’image près.
- **Épic 1.1 — vague 1, le quartier des artisans** : quatre nouveaux PNJ (Mira, Basile, Anouk, Ines), tous de rôle `vendor`, ajoutés à `game/data-buildings.js` sans toucher une seule ligne de code — la preuve d’acceptation de 0.2/0.5. Chaque position a été validée par accessibilité réelle (`GardenConstruction.approach`/`walkable`, pas seulement une heuristique de distance) : les maisons diagonalement proches doivent respecter l’emprise réelle des cercles de mur (diagonale d’angle à angle, pas seulement l’écart axial), et rester à l’écart des couloirs de bord de zone et des pots de départ d’une partie neuve. Nouveau bump `landscape: 6` dans `garden-state-migrate.js`, même patron que le bump 5 (réutilise `relocate()`) : un objet piégé dans une des quatre nouvelles maisons est déplacé à la cellule libre la plus proche sans perdre sa plante ; une sauvegarde jamais concernée reste bit à bit identique. 72 tests de règles passent (2 nouveaux dans `garden-construction.cjs`, couvrant la migration 6 et son cas neutre). Suite Chromium : nouvelle fixture dédiée `garden-village-wave1.cjs` (générique sur l’id du PNJ) qui rejoint chacun des quatre par `dispatch("go")` — un clic écran réel peut accrocher un arbre du paysage sur le même rayon de caméra avant le PNJ, un défaut préexistant déjà contourné ailleurs, pas propre à cette vague — sélectionne le visiteur, ouvre sa porte et vérifie que sa boutique liste exactement ses articles, exactement le même dispatch générique que Noé — capture `/tmp/garden-village-wave1.png`. La pagination de la carte (`hud-panel-rows.js`) déborde maintenant sur une deuxième page une fois les sept visiteurs listés ; `garden-play-touch.cjs` a été ajusté pour parcourir les pages au lieu de supposer que toute ressource tient sur la première. Charge maximale : 126 appels de dessin (contre 117) et 1 171 100 triangles pour les quatre maisons supplémentaires, toujours environ 60,1 images/s le jour et 59,9 la nuit — le budget de 60 images/s reste tenu.
- **Épic 1.2 — boutique généralisée** : Noé ne vend plus un unique pot codé en dur (`c.type === "buy"` restreint à `item !== "pot"`, prix `D.balance.potPrice` global) — `role: "vendor"` porte maintenant `roleData.wares: [{item, cost}, ...]` pour les cinq vendeurs, prix en feuilles propre à chaque article. Nouvelle commande générique `buy` (nouveau `garden-state-cmd-d.js`, cinquième segment alphabétique) : le coût est relu depuis la donnée du vendeur, jamais depuis la commande, pour qu’une commande forgée ne puisse pas acheter gratuitement. Nouveau panneau `shop` générique dans `game/hud-panel.js`, qui réutilise `game/hud-panel-rows.js` (pagination, bouton par ligne) sans code neuf : chaque ligne montre le prix, se désactive si les feuilles manquent. L’ancien bouton « Acheter · 8 feuilles » codé en dur dans la fiche détail de l’inventaire a été supprimé (`hud-panel-inventory.js`), remplacé par cette boutique par visiteur. 78 tests de règles passent (6 nouveaux dans `garden-shop.cjs` : schéma des articles, achat réussi, fonds insuffisants refusés atomiquement, article/vendeur inconnu refusé, vendeuse multi-articles, objet acheté ensuite posable normalement) ; le test de rôle de Noé est mis à jour pour le nouveau panneau (`garden-roles.cjs`). Suite Chromium : `garden-houses.cjs` exerce un achat réel chez Noé (paiement exact, pot ajouté à l’inventaire) ; `garden-village-wave1.cjs` vérifie en plus le refus des deux articles de Mira à fonds insuffisants et un achat réussi chez Basile. Aucune régression de rendu : mêmes 126 appels de dessin / 1 171 100 triangles / ~60 images/s (la boutique est un panneau HUD, pas un changement de scène 3D).
- **Épic 2.1 — moteur de quêtes et panneau générique** : nouveau `game/quests.js`, `evaluateObjective(objective, s, entry) → {progress, complete}` pur, cinq types (`deliver`, `networkState`, `networkSustained`, `discoverSpecies`, `reputation`) tels que décrits dans le plan. `networkState`/`networkSustained` dérivent l’activité du graphe existant (`GardenIrrigation.activity(s, id) > 0`, sans dupliquer la topologie) ; le minuteur `sustained` (`entry.progress.sustainedSince`) est basé sur l’horloge de simulation `s.elapsed`, jamais l’horloge murale — vérifié par un cycle complet accepter → progresser → sauvegarder/recharger en plein minuteur → rattrapage hors-ligne → terminer, qui prouve que le minuteur survit intact. Nouvelle commande `quest` (`action: "accept"|"complete"`, nouveau `garden-state-cmd-e.js`, sixième segment alphabétique) : la reconstitution de récompense (feuilles, plans, réputation) réutilise `economy.js`/le patron déjà en place de `progression.js`. Nouveau panneau `quest` générique dans `game/hud-panel.js`, même patron de lignes que la boutique. Catalogue `data-quests.js` : deux quêtes de départ (« Première récolte » → « Un goutteur actif, trente secondes », chaînées par `requires`) qui prouvent le moteur et le chaînage sans construire toute l’échelle de 6 à 10 quêtes prévue en 2.2 (tâche de contenu séparée) — aucun visiteur ne porte encore le rôle `quest-giver` (`npcId` reste une étiquette de métadonnée, même posture que 0.5), donc la commande et le panneau sont vérifiés directement (`GardenState.command()`, navigateur réel) plutôt qu’en marchant jusqu’à un PNJ. 88 tests de règles passent (10 nouveaux dans `garden-quests.cjs` — dont un qui exerce directement le chemin de récompense « plans » via une quête de test injectée/retirée à la volée, preuve de l’Épic 2.3 sans attendre le contenu de 2.2 — et 1 remplacé dans `garden-quests-schema.cjs` pour refléter le catalogue non-vide). Suite Chromium et test visuel/perf inchangés (126 appels de dessin / 1 171 100 triangles / ~60 images/s) ; vérification manuelle en navigateur réel (accepter → terminer « Première récolte », feuilles créditées, aucune erreur console).
- **Épic 3.1 — planteur automatique** : nouveau `D.recipes.autoPlanter` (`sow: true`, `capacity: 6`, `range: 3`, débloqué comme la pompe/le collecteur à la zone 2), l’inverse exact du collecteur — son `e.buffer` contient des graines (chargées par la nouvelle commande générique `load`, symétrique de `withdraw`) au lieu d’accumuler une production, et une nouvelle primitive `tickSow(e, s)` dans `game/automation.js` les plante une à une dans un pot vide à portée dès qu’il y en a un, au lieu de les stocker. La plantation elle-même réutilise l’application exacte de la commande manuelle « plant » : la construction de `e.plant` a été extraite en `sow(e, species, source)` dans `garden-state-util.js`, appelée à l’identique par la commande et par le tick — aucune duplication. Chargement au champ : même geste que l’arrosage/la mine (outil « graine » en main, `E` en s’approchant), aucun nouveau panneau. `withdraw` et la validation de sauvegarde du buffer, jusque-là câblés en dur sur `"collector"`/`24`, sont généralisés à tout `recipe.buffer || recipe.sow` avec sa propre `capacity` — le vrai test de flexibilité de 0.3 que le plan demandait explicitement, sans corriger la primitive ni dupliquer de branche. Un bug réel de vidage multi-graines par tick (le compteur restait figé à sa valeur de départ au lieu d’être relu en direct sur `e.buffer`) a été détecté et corrigé grâce au test d’intégration à deux graines. 94 tests de règles passent (6 nouveaux dans `garden-autoplanter.cjs`) ; nouveau fixture Chromium `garden-autoplanter-browser.cjs` (pose, charge une graine en marchant dessus avec l’outil en main, plante automatiquement au tick suivant, aucune erreur console) — capture `/tmp/garden-autoplanter-planted.png`. Correction d’ordre de chargement des scripts découverte au passage : `game/automation.js` référençait `GardenIrrigation` avant que ce global ne soit réellement posé (il n’est écrit qu’à la fin de la chaîne `irrigation.js` → `irrigation-sim.js` → `irrigation-status.js`, pas dans `irrigation.js` lui-même) ; `automation.js` est désormais chargé après ces trois fichiers dans `index.html`. Suite Chromium complète et test visuel/perf inchangés (126 appels de dessin / 1 171 100 triangles / ~60 images/s — le planteur n’est pas posé par défaut, aucun changement de charge mesurée).
- **Épic 3.2 — collecteur tier 2** : nouveau `D.recipes.collectorT2` (mêmes `buffer: true`, capacité 48 et portée 6 contre 24/4 pour le collecteur de base, débloqué à la rocaille aux côtés du composteur), zéro nouveau mécanisme comme demandé — `tickBuffer` s’applique sans modification. La commande `withdraw`, la validation de la réserve d’une sauvegarde et l’affichage (`garden-context.js`, `game/hud-inspect.js`) étaient jusqu’ici câblés en dur sur `e.type === "collector"` avec la capacité `24` recopiée à quatre endroits ; ils lisent désormais `D.recipes[e.type]?.buffer`/`.capacity`/`.name` génériquement, donc `collectorT2` (et toute future variante) n’ajoute aucune branche dans ces fichiers. Le modèle 3D et l’animation de l’orbe (jusque-là aussi un `e.type === "collector"` isolé dans `render-frame.js`) réutilisent la même géométrie à une échelle 1,25× plutôt qu’un second mécanisme de rendu. 98 tests de règles passent (4 nouveaux dans `garden-collector-t2.cjs`, dont un qui vérifie que le rejet de validation utilise bien la capacité propre de `collectorT2`, pas le `24` du collecteur de base) ; nouveau fixture Chromium `garden-collector-t2-browser.cjs` qui vérifie que la ligne de statut au contact (`E`) nomme « Collecteur amélioré » et sa capacité de 48, la preuve que la généralisation de la couche UI fonctionne réellement, pas seulement les données — capture `/tmp/garden-collector-t2.png`. Suite Chromium complète et test visuel/perf inchangés (126 appels de dessin / 1 171 100 triangles / ~60 images/s — non posé par défaut).
- **Épic 3.3 — serre (aura de croissance)** : nouvelle primitive générique `D.recipes[type].aura = {range}` + `tickAura(e, s)` dans `game/automation.js`, troisième forme après `job`/`buffer`/`sow` — une « aura de portée passive » plutôt qu’un minuteur ou une réserve. Elle ne crée aucun second mécanisme de croissance : chaque plante à portée reçoit exactement le même champ `plant.boostUntil` que l’engrais du composteur pose déjà (Épic 0.4), rafraîchi juste après l’instant courant à chaque tick tant qu’elle reste à portée, et qui retombe naturellement dès qu’elle en sort — aucune branche neuve dans la boucle de croissance de `garden-state.js`, qui lit toujours le même `boostUntil > s.elapsed`. `D.recipes.greenhouse` (`aura: {range: 2.5}`, débloquée à la rocaille) est purement passive : sans buffer ni job, elle retombe automatiquement sur la fiche « F · Déplacer » générique, aucun nouveau code de contexte. Nouveau matériau de verre translucide (`transparent: true, opacity: 0.4`, mis en cache sur l’instance de vue comme la lanterne) pour un modèle simple (montants de bois, panneau vitré, toit), sans nouvelle géométrie. 103 tests de règles passent (5 nouveaux dans `garden-greenhouse.cjs`, dont un qui compare la croissance réelle d’une plante à portée contre une identique hors de portée dans la même simulation, et un qui confirme qu’une sauvegarde avec `boostUntil` posé par une serre passe la validation existante sans modification, preuve que le schéma de 0.4 est vraiment réutilisé) ; nouveau fixture Chromium `garden-greenhouse-browser.cjs` (pose, plante à portée, un tick réel, `boostUntil` posé, aucune erreur console) — capture `/tmp/garden-greenhouse-placed.png`. Suite Chromium complète et test visuel/perf inchangés (126 appels de dessin / 1 171 100 triangles / ~60 images/s — non posée par défaut).
- **Épic 3.4 — distributeur de graines (optionnel, réalisé)** : quatrième primitive, `D.recipes[type].dispense = {species, every, range}` + `tickDispense(e, s)`, symétrique du collecteur — au lieu d’accumuler depuis des plantes prêtes, il fabrique une graine tous les `every` ticks (via `s.elapsed % every === 0`, sans nouveau champ persistant de minuteur) jusqu’à sa propre capacité, puis distribue une graine à la fois à tout planteur automatique à portée qui a de la place. Même forme `e.buffer` que le collecteur/planteur, juste déplacée entre deux entités au lieu de rester dans une seule. Les quatre points déjà généralisés en 3.1/3.2 (création d’entité, commande `withdraw`, validation de sauvegarde, fiche de contact) reconnaissent maintenant `recipe.dispense` au même titre que `recipe.buffer`/`recipe.sow` — un distributeur se récupère, se valide et s’affiche sans aucune branche supplémentaire. `D.recipes.seedDispenser` débloqué à la rocaille aux côtés des trois autres. 109 tests de règles passent (6 nouveaux dans `garden-seed-dispenser.cjs`, dont un cycle complet pose → fabrication → distribution → plantation automatique à travers `GardenState.command()`/`step()` réels) ; nouveau fixture Chromium `garden-seed-dispenser-browser.cjs` (pose des deux, tick réel jusqu’à distribution, aucune erreur console) — capture `/tmp/garden-seed-dispenser-placed.png`. Suite Chromium complète et test visuel/perf inchangés (126 appels de dessin / 1 171 100 triangles / ~60 images/s — non posé par défaut). Avec cette tâche, les quatre automatisations recommandées de l’Épic 3 (composteur, planteur, collecteur tier 2, serre) plus le distributeur optionnel sont toutes livrées ; l’Épic 3 est complet.
- **Épic 4.1 — `zoneAt` remplacé par un vrai test polygonal** : chaque zone porte maintenant un `polygon` (contour organique), en plus de son `bounds` (rectangle d’origine, inchangé — toujours utilisé tel quel pour la taille du sol plat jusqu’au relief réel de 4.2). `game/geometry.js` gagne `pointInPolygon` (ray-casting) et `distanceToPolygonEdge` (distance minimale à un segment du contour), et `zoneAt` teste désormais le polygone au lieu du rectangle AABB. Stratégie de réduction de risque suivie à la lettre : chaque polygone est un **sur-ensemble strict** de son rectangle — seuls les deux bords qui touchent le bord extérieur de la carte entière bulgent vers l’extérieur (un simple octogone, coins exacts, milieux de bord poussés de 3 unités) ; les bords partagés entre deux zones voisines restent parfaitement droits et alignés, donc aucun chevauchement possible entre zones adjacentes — vérifié point par point sur toute la frontière interne (x = −16, z = −5). Propriété de sur-ensemble vérifiée exhaustivement : tout point à l’intérieur de l’ancien rectangle résout toujours vers la même zone qu’avant, donc **aucun bump `landscape` n’est nécessaire** — rien ne peut devenir invalide puisque rien ne rétrécit (contrairement aux maisons de l’Épic 1, qui ajoutaient un obstacle physique capable de piéger une entité existante). Dans `construction.js`, l’ancienne approximation par quatre coins AABB de `placement()` est remplacée par un vrai test géométrique : `distanceToPolygonEdge(zone.polygon, e.x, e.z) < r` — si le disque de rayon `r` autour du centre reste plus loin de chaque bord du contour que `r`, l’empreinte entière est prouvée à l’intérieur d’une seule zone, quelle que soit sa forme ; l’ancienne boucle de vérification séparée sur quatre coins devient inutile et est supprimée. Effet réel et vérifié : un objet peut maintenant être posé jusqu’à environ z = 21 au nord de la pépinière (contre z < 19 avant, rejeté net à l’ancienne frontière du rectangle). 115 tests de règles passent (`garden-geometry.cjs` entièrement réécrit — l’ancien test de rectangle inclusif/exclusif sur les bords n’a plus de sens une fois les zones organiques, remplacé par sept tests sur la propriété de sur-ensemble, les frontières partagées, le non-chevauchement des bulges, et `distanceToPolygonEdge`/`pointInPolygon` isolément ; 2 nouveaux dans `garden-construction.cjs` pour la pose dans un bulge et son rejet au-delà). Suite Chromium complète et test visuel/perf inchangés au pixel/à l’image près (126 appels de dessin / 1 171 100 triangles / ~60 images/s — changement de logique de collision uniquement, aucun rendu neuf ; le sol plat et sa forme rectangulaire restent le rendu visuel jusqu’à 4.2). 4.2 (relief réel) et 4.3 (rivière) restent délibérément une session séparée, comme le préconise le plan pour l’Épic le plus risqué après 0.4.
- **Épic 4.2 + 4.3 + corrections village, en réponse directe à un retour utilisateur** : après la première passe de l’Épic 4, l’utilisateur a signalé cinq problèmes réels et vérifiables en jouant — toits de maison à l’envers, maisons dispersées sans former un vrai village (Anouk isolée à l’opposé du reste, parfois près de l’inaccessibilité), maisons en planches de bois plutôt qu’en pierre, terrain toujours plat malgré 4.1, rivière toujours droite. Chacun a été vérifié en navigateur avant correction, pas supposé.
  - **Toit inversé (bug réel, pas juste esthétique)** : `render-houses.js`, `panel.rotation.x = -side * angle` avait le signe inversé — la crête du toit tombait au point le plus bas (hauteur du mur) et les avant-toits montaient à la hauteur du faîtage, un vrai « toit en creux ». Vérifié avec `three.min.js` en Node, corrigé en `side * angle` (crête haute, avant-toits bas des deux côtés) ; capture avant/après.
  - **Village recomposé** : Basile et Anouk (auparavant à `(-9.5,3)` et `(3,15)`, éloignés du reste du quartier) déplacés en `(-9.5,20)` et `(-3.5,20)` — une troisième « rue » au nord de Mira/Ines, dans le nouveau bulge organique de l’Épic 4.1, formant un vrai village à trois rangées avec Léa/Noé/Iris. Accessibilité et absence de collision revérifiées de zéro (`C.approach` sur les 7 visiteurs et toutes les ressources de la zone 0 : aucun échec).
  - **Maisons en pierre** : les murs (bois, `this.mat.wood`) deviennent `this.mat.stone`, avec un solin sombre à la base, des chaînages d’angle en pierre à deux tons empilés sur trois niveaux et des moellons apparents sur chaque façade (studs, deux tons alternés) — tout par la même géométrie/matériau partagés (aucun nouveau matériau par maison), donc toujours 0 appel de dessin supplémentaire (vérifié : batching par géométrie+matériau confirmé, 3 104 triangles de plus pour les 7 maisons, aucun changement de draw calls).
  - **Relief réel (4.2)** : `game/terrain.js` calcule maintenant de vraies bosses (huit collines à chute quartique bornée, jamais du bruit simplex — empreinte finie et prouvable), amplitude jusqu’à 2,6 unités (calibrée après avoir constaté qu’en dessous de ~1,5 unité une colline reste visuellement plate sous l’éclairage ambiant très marqué de ce jeu — vérifié en forçant une colline à hauteur 4 pour confirmer que le pipeline de rendu marchait, puis redescendu à des valeurs plus douces mais réellement visibles). Deux collines dans la zone 0, positionnées et dimensionnées par un script de distance réel contre toutes les ressources/visiteurs/maisons/pots de départ (jamais à l’œil) pour rester exactement à 0 sur chaque coordonnée testée — la rivière fixe (bande est, x ≥ 4) a été découverte comme un piège lors de la première tentative (colline placée dessous par erreur, invisible sous l’eau) et corrigée. `screenPoint()` (render.js) additionne désormais `Terrain.terrainHeight` à son décalage au lieu de supposer un sol plat — sinon toute la suite de clics existante (des dizaines de tests) se serait mise à cliquer au mauvais endroit dès qu’un objet reposerait sur une bosse. Le clic-au-sol (`pick()`, render-flow.js) raffine désormais le plan de coupe itérativement contre le relief réel au lieu d’un plan unique — mesuré à 7 passes nécessaires sur la colline la plus pentue (~40° à mi-pente) pour converger sous le centimètre (4 passes laissaient une erreur de visée réelle de 0,8 unité, un vrai problème pour le joueur) ; porté à 8 passes.
  - **Sol des zones suivant le vrai contour organique (4.1 visuel)** : `buildZones()` construit désormais un maillage sur grille (pas 0,75) découpé par `pointInPolygon` sur `z.polygon` au lieu d’un `BoxGeometry` plat sur `z.bounds` — la carte a maintenant une silhouette visiblement moins carrée, pas seulement en collision. Chaque sommet échantillonne `terrainHeight`, donc le sol montre vraiment les collines ci-dessus. `z.bounds` reste inchangé (taille du texte de zone, calcul d’aire des tests existants).
  - **Rivière sinueuse (4.3)** : nouveau `game/river.js`, `riverX(z) = 5,8 + 2,2·sin(2π(z−4)/40)` — une seule courbe, épinglée exactement sur l’ancienne position droite à z = 4 (le ponton, l’étiquette « La rivière » et le point d’interaction « remplir l’arrosoir » restent donc inchangés au pixel près) mais serpentant réellement ailleurs. `distanceToRiver` (recherche échantillonnée locale, ±3 unités) remplace la règle en dur `pompe.x ≥ 2,5` par `distanceToRiver(x, z) ≤ 2,5` — vérifié rétrocompatible avec toutes les positions de pompe déjà utilisées par la suite de tests. Rendu : `riverRibbon()` (même technique de bande que le sol) remplace les deux `BoxGeometry` droits (eau + berge), et les rochers de rive suivent désormais `riverX(z) + décalage` au lieu d’un x fixe.
  - 124 tests de règles passent (6 nouveaux dans `garden-terrain-seam.cjs` réécrit, 6 nouveaux dans `garden-river.cjs`) ; suite Chromium complète toujours verte, y compris `garden-shadows.cjs` (le mécanisme d’ombre reste fonctionnel, seuil `darkened > 100` toujours largement dépassé). Perf revalidée comme l’exige explicitement 4.4 : 126 appels de dessin (inchangé), 1 183 856 triangles (contre 1 171 100 avant cette tâche, +1,1 % pour huit collines, quatre sols de zone subdivisés, un ruban de rivière et le détail en pierre de sept maisons), ~60,1 images/s le jour et ~59,9 la nuit — budget tenu.
  - **Porte surdimensionnée (retour immédiat après capture d’écran)** : une deuxième capture de l’utilisateur a montré la porte comme un immense aplat de la couleur d’accent du visiteur, couvrant presque toute la façade avant. La cause n’était pas nouvelle : la vantail de porte faisait déjà `door.w − 0,06` de large et `WALL_H − 0,1` de haut (soit ~95 % de la hauteur du mur et ~60 % de sa largeur) avant cette session, simplement invisible sur les anciens murs en planches de bois de même ton — la texture de pierre l’a rendue flagrante par contraste. La largeur/hauteur du passage réellement traversable (le vide dans les cercles muraux, `houseWalls()` dans `data-buildings.js`) reste inchangée à dessein, pour ne pas retoucher la tolérance de dérive diagonale déjà éprouvée. Corrigé : un vantail bien plus petit (1,3 × 1,65, bois `this.mat.bark`) avec un panneau encastré plus sombre, une petite poignée à la couleur d’accent (le seul endroit restant où cette couleur sert), et deux jambages en pierre foncée qui comblent l’embrasure entre le vantail et le mur — proportions vérifiées en navigateur, porte fermée ET ouverte (le pivot d’ouverture existant s’applique tel quel). Aucun changement de collision : entièrement vérifié en navigateur (fermée, ouverte, avec le panneau d’interaction de Noé/Iris) et par la suite complète (124 tests de règles, suite Chromium, `garden-shadows.cjs`) — tout reste vert. Perf : 128 appels de dessin (+2, un nouveau ton de bois pour le panneau encastré, partagé par les sept maisons donc un seul appel de dessin de plus, pas sept), 1 184 192 triangles, ~60 images/s — négligeable.
- **Toit qui traverse les murs, maisons toutes identiques, aucun PNJ « juste habitant » (retour utilisateur après vérification Playwright MCP)** : le retour précédent avait corrigé la porte, mais l’utilisateur a immédiatement signalé un deuxième défaut sur les mêmes captures — le toit visiblement « à travers » la pierre — plus deux demandes de contenu (variété des maisons, pancarte pour les vendeurs, des PNJ qui n’ont rien à vendre).
  - **Pignon qui ne suit pas la pente du toit (bug géométrique réel, pas cosmétique)** : les murs pignons (`x = ±w2`, `render-houses.js`) étaient un simple pavé de pierre plein, `roofHeight` de haut sur toute la profondeur de la maison — alors que le toit ne culmine à `roofHeight` qu’au faîtage (`z = 0`) et redescend à `WALL_H` aux deux avant-toits (`z = ±d2`). Le panneau de toit, incliné, traversait donc ce pavé plein sur presque toute sa pente. Corrigé avec une vraie forme de pignon : rectangle `WALL_H` (comme les murs avant/arrière) surmonté d’un triangle qui suit exactement la pente, construit à la main en `BufferGeometry` (`gableGeometry()`, 6 sommets, 6 triangles) — le sens de chaque triangle a été vérifié par produit vectoriel avant d’écrire le code (pas à l’essai-erreur) pour que chaque normale calculée pointe déjà vers l’extérieur. Vérifié en navigateur réel sous plusieurs angles (vue de face et de profil) : le pignon suit maintenant la ligne du toit sans aucun chevauchement visible.
  - **Variété des maisons** : `roofColor` et `roofHeight` deviennent des champs par maison dans `data-buildings.js` (palette de bruns/verts, hauteurs 2,55–2,9) au lieu d’une seule constante partagée par les sept — le village ne lit plus comme un même bâtiment recopié sept fois. Trois maisons (Noé, Mira, Ines) reçoivent en plus une cheminée en pierre (`b.chimney`), plaquée contre le mur pignon extérieur plutôt que traversant le toit, pour ne réutiliser aucune géométrie de pente et éviter tout nouveau risque de chevauchement.
  - **Pancarte pour les vendeurs** : les maisons dont `role === "vendor"` reçoivent un petit poteau + planche dans la cour près de la porte (couleur d’accent du visiteur), pour les distinguer d’un coup d’œil des habitants sans boutique — les rôles `trader`/`botanist`/`resident` n’en portent pas.
  - **Deux habitants sans utilité de jeu** : nouveau rôle `resident` dans `game/data-roles.js` — `E` affiche juste une ligne d’ambiance (`roleData.line`) et ne fait rien (`command: null`, le même mécanisme générique que la mine en recharge ou le planteur auto plein, pas une nouvelle branche). Hugo (« lecture ») et Zoé (« sieste ») rejoignent ainsi le village sans boutique ni quête. Leurs positions n’ont **pas** été choisies par simple distance : une première tentative à `(-7,5, 16)`, pourtant à 4,47 unités du bâtiment le plus proche par le centre, s’est révélée bloquer le paillasson de Basile et le test de placement dans le bulge de l’Épic 4.1 une fois passée dans le vrai moteur de collision — les cercles muraux d’une maison s’étendent au-delà de son centre, et un chiffre de distance seul ne suffit pas. Position finale (`-12,5, 14` pour Zoé, `3, 13,5` pour Hugo) validée par un script qui rejoue littéralement les mêmes assertions que `tests/garden-construction.cjs` (paillasson/seuil/tolérance de dérive de chaque maison, test du bulge, les deux points de réapparition du joueur dans `render.js`) avant d’écrire la position dans les données.
  - 125 tests de règles passent (1 nouveau dans `garden-roles.cjs` pour le rôle `resident`) ; suite Chromium complète toujours verte, zéro erreur console. Vérifié en navigateur réel (Playwright, capture d’ensemble du village et gros plans sur le pignon, la cheminée et la pancarte) plutôt que supposé correct après lecture du code.
- **« On ne voit jamais l’intérieur à cause du toit » / « On ne voit pas le relief parce que pas d’ombre » (deux retours utilisateur consécutifs, même session)** :
  - **Toit qui masque l’intérieur** : même porte ouverte, un toit fermé au-dessus de toute l’empreinte de la maison reste dans le champ de vision de la caméra isométrique fixe (élévation ~41°), quel que soit le côté vers lequel la porte fait face — donc l’intérieur (sol, visiteur, lampe) n’était en pratique jamais visible. Les deux pans de toit, la planche de faîtage et la cheminée (le cas échéant) sont maintenant collectés par maison dans `this.roofs` (`render-houses.js`) et cachés (`mesh.visible = false`) dès que le joueur est à portée de la porte (même seuil de 1,8 unité que l’ouverture de la porte, réutilisé tel quel dans `updateDoors`) — révélés à nouveau en s’éloignant. Gratuit : `updateBatches()` (`render-light.js`) vérifie déjà `.visible` sur toute la chaîne de parents à chaque image pour construire les lots d’`InstancedMesh`, donc masquer un maillage n’exige aucune reconstruction de lot, juste un changement d’état lu la frame suivante.
  - **Relief invisible malgré l’Épic 4.2** : `ground.castShadow` était à `false` (`render-world.js`) — le sol ne pouvait pas s’auto-ombrer, donc le versant éloigné d’une colline ne recevait jamais l’ombre portée du versant proche. Passé à `true`. Mesuré avant de conclure que c’était suffisant : à hauteur inchangée (2,0 pour la colline principale de la zone 0), l’auto-ombrage seul ne suffisait toujours pas à rendre le relief visible sous l’éclairage ambiant très marqué de ce jeu — vérifié par comparaison d’écran à h=2,0/3,0/3,6/4,2 sur la même colline, même angle de caméra : 2,0 lit complètement plat, 3,6 et plus lit comme un vrai monticule arrondi. Toutes les hauteurs de `game/terrain.js` relevées d’environ ×1,8 (plafond documenté 2,6 → 4,7, mesuré par balayage réel de la grille, pas supposé additif). Aucun rayon changé, donc aucune des clairances déjà vérifiées (maisons/visiteurs/ressources de la zone 0, exactement à 0) n’a besoin d’être revérifiée — seule l’amplitude a changé.
  - **Régression réelle découverte par ce changement de hauteur, pas supposée** : la suite Chromium a révélé un vrai échec (`garden-play.cjs`, pose d’un pot près de la colline de la zone 0) après la hausse d’amplitude. Cause : `render-flow.js`'s `pick()` reprojetait sur un plan plat, sautait tout droit à la hauteur du point obtenu, et répétait — avec une colline assez haute, la toute première reprojection (plan à y=0) pouvait sauter par-dessus toute la colline et atterrir sur un point plat éloigné qui validait par coïncidence son propre critère de convergence (« la hauteur réelle de ce point correspond au plan qui a servi à le trouver ») sans jamais avoir échantillonné la colline. Remplacé par une bissection du rayon entre y=`Terrain.MAX_HEIGHT` (nouvelle constante, prouvablement au-dessus de toute colline possible) et y=0 (le sol plat) — une bissection ne peut pas sauter par-dessus une bosse puisqu’elle ne fait que resserrer l’intervalle. Précision mesurée sur le point exact qui faisait échouer le test : de ~3,3 unités d’erreur (le mauvais point) à 4,6×10⁻⁷ unité. `this.plane` (render.js) supprimé, devenu inutile.
  - 125 tests de règles passent (plafond d’amplitude du test relevé à 4,7, mesuré) ; suite Chromium complète repassée entièrement au vert après la correction de `pick()`, y compris `garden-shadows.cjs` (l’auto-ombrage du sol ne casse pas la détection d’occlusion des lanternes) et `garden-play.cjs` (le test qui avait révélé la régression). Vérifié en navigateur réel : capture avant/après sur la colline de la zone 0, capture toit ouvert/fermé en s’approchant/s’éloignant d’Iris.
- **Relief lisible en journée — ombre cuite orientée soleil (retour utilisateur : « le terrain n’affiche pas d’ombres, on ne voit pas les collines »)** : diagnostic complet avant de toucher au code — `.diag-garden.cjs` (sunset/noon sur la colline de la zone 0) + `.diag-probe.cjs` (comparaison de pixels) ont d’abord prouvé que le pipeline fonctionnait (ombre directionnelle 2 048 px active : la désactiver modifie 6,5 % de l’écran, la restaurer remet la différence à 0) ; la cause était géométrique : les collines sont des dômes convexes, leur auto-ombre retombe sur le versant **lointain**, caché derrière la crête jamais côté caméra — aucune shadow map ne peut donc faire lire un dôme convexe. Correction : `reBakeTerrain(sunDir, weight)` dans `render-world.js` module chaque sommet du sol par `(0,42 + 0,9·(n·s)²)` — une réponse au carré qui exagère le Lambert physique déjà appliqué par la lumière directionnelle (versant au soleil plus clair, versant opposé plus sombre), sans jamais le contre-doubler ; le poids est `min(1, intensitéSoleil/2,6)`, donc 0 la nuit (seul l’ombrage de forme persiste) et 1 au zénith. Recuit toutes les 2 s de jeu depuis `render-light.js` (le soleil avance de 0,3°/s, l’étape est imperceptible) : quelques milliers de sommets + un upload de couleur, coût négligeable, zéro appel de dessin, zéro changement de géométrie ni de collision. Vérifié en navigateur : à midi la colline de la zone 0 lit comme un vrai monticule (versant au sud assombri, crête claire), le relief reste lisible au crépuscule par l’ombrage de forme seul, la zone 1 y gagne de la profondeur entre les arbres ; 125 tests de règles au vert.
